---
phase: 25-llm-first-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md
autonomous: true
requirements: [D-02, D-04, D-05, D-06, D-07]

must_haves:
  truths:
    - "Skill file exists at /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md on server"
    - "Skill YAML frontmatter declares name=social-verifier, title=Instagram Doctor Verifier"
    - "Skill accepts list of doctors (name + specialization + clinic) as input"
    - "Skill implements all 5 passes with correct key assignments: Pass 1→008, Pass 2→009, Pass 3→010, Pass 4→011, Pass 5→012"
    - "Skill outputs verified social media accounts with pass-markers (which pass found each doctor)"
    - "Skill references /root/.hermes/keys/apify_keys.json for key rotation"
    - "Skill is self-contained — Hermes can invoke it independently without loading full presale-pipeline SKILL.md"
  artifacts:
    - path: "/root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md"
      provides: "Standalone 5-pass Instagram doctor verifier skill extracted from monolithic SKILL.md"
      min_lines: 150
      contains: "Pass 1"
      contains: "Pass 2"
      contains: "Pass 3"
      contains: "Pass 4"
      contains: "Pass 5"
      contains: "RESIDENTIAL"
  key_links:
    - from: "social-verifier/SKILL.md (key rotation section)"
      to: "/root/.hermes/keys/apify_keys.json"
      via: "key rotation instructions referencing apify_keys.json path"
    - from: "social-verifier/SKILL.md (fallback section)"
      to: "presale-pipeline/SKILL.md"
      via: "explicit fallback instruction: if verification fails, load full presale-pipeline SKILL.md"
    - from: "social-verifier/SKILL.md (Pass 1-5)"
      to: "Apify Profile Scraper (apify/instagram-profile-scraper)"
      via: "Apify actor API with RESIDENTIAL proxy"
---

<objective>
Extract the 5-pass Instagram doctor verification system (lines 306-397 of SKILL.md v2.55.0) into a standalone skill file at `/root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md`.

Purpose: First tool extracted from the monolithic SKILL.md per D-02. Proves the extraction pattern scales to other components. The new skill is independently invocable by Hermes.

Output: A self-contained SKILL.md file (est. 200-250 lines) with YAML frontmatter, input/output contracts, the complete 5-pass algorithm, key rotation instructions, error handling, and fallback reference to the original SKILL.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
### Source files (on server — ssh root@138.16.224.188)

**Primary source (lines to extract):**
- `/root/.hermes/skills/software-development/presale-pipeline/SKILL.md` — lines 306-397 containing the 5-pass block:
  - L306-310: Problem statement & general principle (single list, retroactive passes)
  - L312-326: Pass 1 — Extraction + Apify batch (key 008, RESIDENTIAL)
  - L328-336: Pass 2 — Google cross-reference for unfound (key 009, RESIDENTIAL)
  - L338-348: Pass 3 — Broad search + all platforms (key 010, RESIDENTIAL)
  - L350-364: Pass 4 — Cross-contamination (key 011, RESIDENTIAL)
  - L366-373: Pass 5 — Telegram final sweep + master table (key 012)
  - L375-383: Iron rules 1-7 (key-per-pass, don't stop after pass 1, retroactive passes, log each pass, mark facts in KP, fallback when keys exhausted, VK+TG every pass)
  - L385-392: Bitrix SPA expert extraction algorithm
  - L394-396: Grades — 5★ >10K, 4★ 1-10K, 3★ <1K, 2★ private, 1★ not found, 0★ clinic without IG

**Supporting references (read for context, not extracted):**
- `/root/.hermes/skills/software-development/presale-pipeline/references/doctor-social-audit-workflow.md` — grade system, sources, Instagram fallback
- `/root/.hermes/skills/software-development/presale-pipeline/references/apify-instagram-reel-scraper.md` — RESIDENTIAL proxy requirement, key rotation algorithm, correct payload format
- `/root/.hermes/skills/software-development/presale-pipeline/references/bitrix-spa-expert-extraction.md` — Bitrix extraction workflow

**Related rules in SKILL.md to reference (NOT to extract):**
- Rule 16 (L676-683): Parallel multi-agent search within each pass (4 threads per pass)
- Rule 21 (L709-721): 5-pass mandatory — the most important lesson from retrospective

**Key infrastructure:**
- `/root/.hermes/keys/apify_keys.json` — 12 Apify keys, priority 008→009→010→011→012 for passes
- `/root/bin/tg-mtproto.py` — Telegram MTProto script (resolve, search, messages)
- Instagram tools B1-B4 from SKILL.md L93-107 (Profile Scraper, Reel Scraper, AssemblyAI, Post Scraper)

### Format reference
The existing SKILL.md frontmatter format:
```yaml
---
name: presale-pipeline
title: Presale Pipeline — Медицинские клиники
description: ...
metadata:
  version: 2.56.0
  author: AIM
  depends_on: [...]
---
```
The new skill follows this exact YAML/metadata structure.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create social-verifier skill directory and SKILL.md</name>
  <files>/root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md</files>

  <read_first>
  - /root/.hermes/skills/software-development/presale-pipeline/SKILL.md (lines 306-397 — the 5-pass block to extract)
  - /root/.hermes/skills/software-development/presale-pipeline/references/apify-instagram-reel-scraper.md (key rotation algorithm and RESIDENTIAL proxy payload format)
  - /root/.hermes/skills/software-development/presale-pipeline/references/doctor-social-audit-workflow.md (grades and fallback patterns)
  - /root/.hermes/skills/software-development/presale-pipeline/references/bitrix-spa-expert-extraction.md (Bitrix extraction)
  </read_first>

  <action>
  SSH to root@138.16.224.188. Create directory `/root/.hermes/skills/software-development/presale-pipeline/social-verifier/`.

  Write `/root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md` with this structure:

  **Section 1 — YAML frontmatter:**
  ```yaml
  ---
  name: social-verifier
  title: Instagram Doctor Verifier
  description: 5-pass social media verification for medical doctors — finds Instagram, Telegram, VK accounts with pass-markers indicating which pass discovered each doctor
  metadata:
    version: 1.0.0
    author: AIM
    extracted_from: presale-pipeline v2.55.0
    depends_on:
      - Apify Profile Scraper (apify/instagram-profile-scraper)
      - Apify keys at /root/.hermes/keys/apify_keys.json
      - Telegram MTProto (/root/bin/tg-mtproto.py)
      - Hermes tools: web_search, browser_console, browser_navigate, web_extract
    approach: "llm-first — Hermes invokes this skill when doctor social verification is needed"
  ---
  ```

  **Section 2 — When to use:**
  Explain that Hermes calls this skill when:
  - Client asks about doctors' social media presence
  - Presale pipeline reaches Phase 2 (Doctor Social Audit)
  - Competitor doctor analysis is needed
  - Standalone doctor verification is requested
  Fallback: if this skill fails (all keys exhausted, network issues), load `skill_view(name='presale-pipeline')` and use Phase 2 from the full pipeline.

  **Section 3 — Input specification:**
  The skill receives a list of doctors. Each doctor object:
  - `name` (required): Full name (Имя Фамилия)
  - `specialization` (required): Medical specialization (косметолог, остеопат, дерматолог, etc.)
  - `clinic` (required): Clinic name
  - `clinic_domain` (optional): Clinic website domain for SPA extraction
  - `known_instagram` (optional): Known Instagram username if available
  - `known_telegram` (optional): Known Telegram @handle if available

  **Section 4 — Output specification:**
  After 5 passes, the skill produces a master table with columns:
  | Врач | Клиника | IG (username, подп.) | TG | VK | Другие площадки | Статус (найден в проходе N / не найден) |
  Plus:
  - Grade for each found doctor (5★ to 0★)
  - Summary count: found vs not found
  - Pass distribution: which pass found how many
  - Format note: "Проверено 5 проходами — не обнаружен (возможно, под другим именем или не ведёт соцсети)" for unfound doctors

  **Section 5 — The 5-pass algorithm (extracted verbatim from SKILL.md L306-397):**

  Preserve the EXACT logic, step numbering, tool names, and key assignments from the original. Do NOT change any pass logic per D-05.

  Include:
  - Problem statement (L306-310): Single list principle, retroactive passes
  - Pass 1 (L312-326): Extraction + Apify batch, key 008, RESIDENTIAL
  - Pass 2 (L328-336): Google cross-reference, key 009, RESIDENTIAL
  - Pass 3 (L338-348): Broad search all platforms, key 010, RESIDENTIAL
  - Pass 4 (L350-364): Cross-contamination (IG/TG clinic, browser_console, web_search), key 011, RESIDENTIAL — with key insight about finding doctors not on website
  - Pass 5 (L366-373): Telegram final sweep + master table formation, key 012
  - Iron rules 1-7 (L375-383): key-per-pass, don't stop after pass 1, retroactive passes, log each pass, mark facts, fallback when keys exhausted, VK+TG every pass
  - Bitrix SPA algorithm (L385-392): extraction when /experts/ returns 404
  - Grades (L394-396): 5★ to 0★ system

  **Section 6 — Key rotation (self-contained instructions):**

  Write a compact key rotation section that:
  - Points to `/root/.hermes/keys/apify_keys.json` for the key list (12 keys)
  - Documents the pass-key mapping: Pass 1→008, Pass 2→009, Pass 3→010, Pass 4→011, Pass 5→012
  - States rotation conditions: "error", "insufficient", "limit", "exceeded", "disabled", "unauthorized", "not-enough-usage", HTTP 4xx/5xx, empty dataset []
  - States the RESIDENTIAL proxy requirement with correct payload format from apify-instagram-reel-scraper.md:
    ```json
    {"usernames": ["user"], "resultsLimit": 20, "proxy": {"useApifyProxy": true, "apifyProxyGroups": ["RESIDENTIAL"]}}
    ```
  - States: if ALL 12 keys exhausted → switch to browser + Google only, mark results as "limited — keys exhausted"

  **Section 7 — Parallel search within passes (from rule 16, L676-683):**

  Include the 4-thread parallel search pattern within each pass:
  - Thread 1 — Google IG: `web_search("site:instagram.com \"{Name}\" \"{Clinic}\"")`
  - Thread 2 — Apify Search Scraper (key 006)
  - Thread 3 — Telegram: `tg-mtproto.py search "{Name Surname}"`
  - Thread 4 — Broad: `web_search("\"{Name}\" {specialization} Moscow")`
  Batch doctors in groups of 10 when >10.

  **Section 8 — Instant fallback protocol:**
  If this skill fails:
  1. Log what failed (which pass, which key, what error)
  2. Load `skill_view(name='presale-pipeline')` — the original SKILL.md v2.55.0
  3. Execute Phase 2 from the full pipeline as fallback
  4. Note: "social-verifier failed at Pass N — continuing with presale-pipeline fallback"

  **DO NOT include in this skill:**
  - CSS, animations, HTML generation
  - Financial data collection (EGRUL, Rusprofile)
  - Competitor discovery/scoring
  - Content analysis, content plans
  - GEO checking, Schema.org
  - MAX presence checking
  - Any presale pipeline phases other than Phase 2 (Doctor Social Audit)
  - The "Critical Rules" section (rules 1-22) — those apply to the full pipeline
  </action>

  <acceptance_criteria>
  - File `/root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md` exists on server
  - `grep -c '^---$' /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md` returns 2 (valid YAML frontmatter)
  - `grep -c 'name: social-verifier' /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md` returns 1
  - `grep -c 'Pass 1' /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md` returns >= 1
  - `grep -c 'Pass 2' /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md` returns >= 1
  - `grep -c 'Pass 3' /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md` returns >= 1
  - `grep -c 'Pass 4' /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md` returns >= 1
  - `grep -c 'Pass 5' /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md` returns >= 1
  - `grep -c 'RESIDENTIAL' /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md` returns >= 1
  - `grep -c 'apify_keys.json' /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md` returns >= 1
  - `grep -c 'skill_view.*presale-pipeline' /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md` returns >= 1 (fallback reference exists)
  - File does NOT contain: 'water ripple', 'CSS', 'HTML-КП', 'GEO', 'ФАЗА 1', 'ФАЗА 3', 'ФАЗА 4', 'ФАЗА 5', 'ФАЗА 6', 'ФАЗА 7', 'MAX', 'Контур.Фокус', 'Rusprofile'
  - `wc -l /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md` returns >= 150
  </acceptance_criteria>

  <verify>
    <automated>
      ssh root@138.16.224.188 "bash -c '
        FILE=/root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md
        echo \"=== File exists ===\" && test -f \"\$FILE\" && echo OK || echo MISSING
        echo \"=== YAML frontmatter ===\" && test \"\$(grep -c \"^---$\" \"\$FILE\")\" -eq 2 && echo OK || echo \"BAD (need exactly 2)\"
        echo \"=== Name ===\" && grep -c \"name: social-verifier\" \"\$FILE\"
        echo \"=== Passes ===\" && for p in \"Pass 1\" \"Pass 2\" \"Pass 3\" \"Pass 4\" \"Pass 5\"; do echo -n \"\$p: \"; grep -c \"\$p\" \"\$FILE\"; done
        echo \"=== RESIDENTIAL ===\" && grep -c \"RESIDENTIAL\" \"\$FILE\"
        echo \"=== apify_keys.json ===\" && grep -c \"apify_keys.json\" \"\$FILE\"
        echo \"=== Fallback ===\" && grep -c \"skill_view.*presale-pipeline\" \"\$FILE\"
        echo \"=== Line count ===\" && wc -l < \"\$FILE\"
        echo \"=== Forbidden terms ===\" && for term in \"water ripple\" \"CSS\" \"HTML-КП\" \"GEO\" \"ФАЗА 1\" \"ФАЗА 3\" \"Контур.Фокус\"; do echo -n \"\$term: \"; grep -c \"\$term\" \"\$FILE\"; done
      '"
    </automated>
  </verify>

  <done>
  Social-verifier SKILL.md exists on server at correct path. Valid YAML frontmatter with name=social-verifier. All 5 passes documented. RESIDENTIAL proxy and apify_keys.json referenced. Fallback to full SKILL.md documented. No non-doctor-verification content leaked (no CSS, HTML-KP, GEO, financials). Line count >= 150.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate skill completeness against benchmark data</name>
  <files>/root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md</files>

  <read_first>
  - /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md (newly created)
  - /root/.hermes/backups/2026-06-06_v2.55.0_snapshot/ampermy-competitors.md (etalon benchmark)
  - /root/.hermes/backups/2026-06-06_v2.55.0_snapshot/SKILL.md (original v2.55.0 — compare pass structure)
  </read_first>

  <action>
  Validate that the new skill is complete and correct by comparing against the original:

  1. **Pass-by-pass comparison:** Extract the 5-pass text from both the original SKILL.md (L306-397) and the new social-verifier/SKILL.md. For each pass:
     - Confirm the same step count and logic flow
     - Confirm the same key assignment (008/009/010/011/012)
     - Confirm the same tool names (Apify Profile Scraper, web_search, browser_console, tg-mtproto.py)
     - Confirm the same proxy requirement (RESIDENTIAL)

  2. **Iron rules check:** Verify all 7 iron rules from the original (L375-383) are present:
     - Rule 1: Key per pass
     - Rule 2: Don't stop after pass 1
     - Rule 3: Retroactive passes for newly found doctors
     - Rule 4: Log each pass
     - Rule 5: Mark facts in output
     - Rule 6: Fallback to browser+Google when keys exhausted
     - Rule 7: VK+TG every pass, not just IG

  3. **Grade system check:** Verify grades 5★ through 0★ match the original exactly:
     - 5★: >10K subscribers
     - 4★: 1K-10K subscribers
     - 3★: <1K subscribers
     - 2★: Private account
     - 1★: Not found on Instagram
     - 0★: Clinic has no Instagram

  4. **Input/Output validation:**
     - Input spec clearly lists required fields: name, specialization, clinic
     - Output spec includes master table format with all columns
     - Pass-marker convention is clear (which pass found each doctor)

  5. **Ampermy benchmark readiness:** Read the etalon competitors file. Extract the doctor list. Confirm that following the new skill's instructions would produce a verifiable result comparable to the original SKILL.md's output. Document the expected verification workflow: load doctors → execute passes → compare count.

  6. **Cross-contamination check:** Verify that no non-doctor-verification content leaked from the original SKILL.md (run the forbidden terms check from Task 1 output).
  </action>

  <acceptance_criteria>
  - All 5 passes in new skill match original SKILL.md L306-397 in logic and tool names
  - All 7 iron rules present in new skill
  - Grade system (5★-0★) matches original exactly
  - Input spec declares name, specialization, clinic as required fields
  - Output spec includes master table with columns: Врач | Клиника | IG | TG | VK | Другие | Статус
  - No forbidden terms found (from Task 1 criteria list)
  - Ampermy etalon doctor list identified and documented as benchmark data
  </acceptance_criteria>

  <verify>
    <automated>
      ssh root@138.16.224.188 "bash -c '
        NEW=/root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md
        OLD=/root/.hermes/backups/2026-06-06_v2.55.0_snapshot/SKILL.md
        echo \"=== Iron rules check ===\"
        for rule in \"новый ключ Apify\" \"Не останавливаться после прохода 1\" \"ретроспективно\" \"Фиксировать каждый проход\" \"маркировку\" \"browser.*Google\" \"VK и Telegram\"; do
          echo -n \"\$rule: \"; grep -ci \"\$rule\" \"\$NEW\"
        done
        echo \"=== Grade system ===\"
        for grade in \">10\" \"1-10K\" \"<1K\" \"Приватный\" \"Не найден\" \"клиника без IG\"; do
          echo -n \"\$grade: \"; grep -ci \"\$grade\" \"\$NEW\"
        done
        echo \"=== Input fields ===\"
        for field in \"name\" \"specialization\" \"clinic\"; do
          echo -n \"\$field: \"; grep -ci \"\$field\" \"\$NEW\"
        done
        echo \"=== Output columns ===\"
        for col in \"Врач\" \"Клиника\" \"IG\" \"TG\" \"VK\" \"Другие\" \"Статус\"; do
          echo -n \"\$col: \"; grep -ci \"\$col\" \"\$NEW\"
        done
      '"
    </automated>
  </verify>

  <done>
  Pass-by-pass logic verified against original v2.55.0. All 7 iron rules present. Grade system matches. Input/output contracts complete. Ampermy benchmark data identified. Skill is ready for benchmark testing against etalon.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill file → Apify API | API tokens from apify_keys.json sent to api.apify.com |
| Skill file → Telegram MTProto | tg-mtproto.py uses Telethon session from .env |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure | social-verifier/SKILL.md | mitigate | Skill file references key paths but does NOT contain actual API tokens. Tokens remain in apify_keys.json (0600 permissions) and .env. |
| T-25-02 | Denial of Service | Apify key exhaustion | accept | If all 12 keys exhausted, skill falls back to browser+Google search. Fallback to full presale-pipeline SKILL.md if complete failure. |
| T-25-SC | Tampering | Apify keys rotation | mitigate | Keys loaded from /root/.hermes/keys/apify_keys.json (root-only read). Skill uses existing rotation mechanism — no new key handling code. |
</threat_model>

<verification>
1. `ssh root@138.16.224.188 "test -f /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md && echo EXISTS || echo MISSING"`
2. `ssh root@138.16.224.188 "grep -c 'name: social-verifier' /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md"` → 1
3. All 5 passes present (Pass 1 through Pass 5)
4. RESIDENTIAL proxy requirement documented
5. Fallback to presale-pipeline SKILL.md documented
6. No non-doctor-verification content leaked
7. Iron rules 1-7 all present
8. Grade system 5★-0★ matches original
</verification>

<success_criteria>
Skill file is self-contained and ready for Hermes to invoke independently. The 5-pass system is faithfully reproduced without logic changes (per D-05). Key rotation uses existing infrastructure (per D-06). Fallback to full SKILL.md is clearly documented (per D-03). Input/output contracts are explicit. The skill can be loaded via `skill_view(name='social-verifier')` by Hermes.
</success_criteria>

<output>
Create `.planning/phases/25-llm-first-orchestration/25-01-SUMMARY.md` when done.
</output>
