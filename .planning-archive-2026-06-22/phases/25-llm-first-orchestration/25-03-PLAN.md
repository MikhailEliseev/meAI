---
phase: 25-llm-first-orchestration
plan: 03
type: execute
wave: 3
depends_on: ["25-01", "25-02"]
files_modified:
  - /root/.hermes/skills/software-development/presale-pipeline/tech-auditor/SKILL.md
  - /root/.hermes/skills/software-development/presale-pipeline/content-analyzer/SKILL.md
  - /root/.hermes/skills/software-development/presale-pipeline/SKILL.md
autonomous: true
requirements: [GAP-01, GAP-02]

must_haves:
  truths:
    - "tech-auditor SKILL.md exists on server with 8-parameter audit contract"
    - "content-analyzer SKILL.md exists on server with all-10-experts contract"
    - "SKILL.md v2.56.0 routing table references both new tools"
    - "Phase 4 content-analysis block delegates to content-analyzer skill"
  artifacts:
    - path: "/root/.hermes/skills/software-development/presale-pipeline/tech-auditor/SKILL.md"
      provides: "Technical website audit skill — 8 params (speed, broken links, meta, h1, alt, sitemap, SSL, mobile)"
      min_lines: 150
    - path: "/root/.hermes/skills/software-development/presale-pipeline/content-analyzer/SKILL.md"
      provides: "Content analysis for ALL 10 experts — formats, engagement, viral posts with numbers"
      min_lines: 200
  key_links:
    - from: "/root/.hermes/skills/software-development/presale-pipeline/SKILL.md"
      to: "tech-auditor/SKILL.md"
      via: "routing table entry + Phase reference"
      pattern: "tech-auditor.*tech-auditor/SKILL.md"
    - from: "/root/.hermes/skills/software-development/presale-pipeline/SKILL.md"
      to: "content-analyzer/SKILL.md"
      via: "routing table entry + Phase 4 delegation"
      pattern: "content-analyzer.*content-analyzer/SKILL.md"
---

<objective>
Create two new presale-pipeline skills to close HIGH-priority gaps vs the ampermy etalon:

1. **tech-auditor** — 8-parameter technical website audit (Lighthouse speed, broken links, meta tags, h1, alt text, sitemap, SSL/HTTPS, mobile responsiveness). Input: URL. Output: table with fix priorities.

2. **content-analyzer** — Content analysis for ALL 10 experts (not just TOP-2). For each expert: winning formats, engagement metrics, viral posts with concrete numbers. Input: verified expert list with social media handles. Output: per-expert analysis cards with metrics.

Purpose: Close gaps 1 and 2 identified in the ampermy etalon comparison. These are the highest-impact gaps — technical audit is a 2x argument multiplier for clients with problematic sites (like Ampermy on 1C-Bitrix), and full content analysis reveals patterns invisible when only TOP-2 experts are analyzed.
Output: 2 new SKILL.md files (tech-auditor, content-analyzer) + updated main SKILL.md routing table
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

All work is server-side on 138.16.224.188. Deploy via base64 over SSH (backtick escaping). Tools are Hermes skills — markdown files in the presale-pipeline directory. Each tool is a standalone SKILL.md with YAML frontmatter, input/output contracts, algorithm, and fallback to the main SKILL.md.
</execution_context>

<context>
@.planning/phases/25-llm-first-orchestration/25-03-CONTEXT.md
@.planning/phases/25-llm-first-orchestration/25-01-SUMMARY.md
@.planning/phases/25-llm-first-orchestration/25-02-SUMMARY.md

<interfaces>
<!-- Patterns from existing extracted skills. All tools follow this contract. -->

From social-verifier/SKILL.md (the first extracted tool — reference pattern):
```
YAML frontmatter:
---
name: social-verifier
title: Instagram Doctor Verifier
description: 5-pass social media verification for medical doctors
metadata:
  version: 1.0.0
  author: AIM
  extracted_from: presale-pipeline v2.55.0
  depends_on:
    - Apify Profile Scraper
    - Apify keys at /root/.hermes/keys/apify_keys.json
    - Telegram MTProto (/root/bin/tg-mtproto.py)
    - Hermes tools: web_search, browser_console, browser_navigate, web_extract
  approach: "llm-first — Hermes invokes this skill when doctor social verification is needed"
---

# Title

## When to Use
## Input Specification
## Output Specification
## Algorithm
## Fallback Protocol
```

From SKILL.md v2.56.0 routing table (lines 7-11):
```
  routing:
    social-verifier: "Phase 2 Doctor Social Audit -> social-verifier/SKILL.md"
    html-kp-generator: "Phase 6 HTML-KP -> html-kp-generator/SKILL.md"
    financial-fetcher: "Phase 1 Financial Data -> financial-fetcher/SKILL.md"
    competitor-scorer: "Phase 3 Competitors -> competitor-scorer/SKILL.md"
```

Server paths:
- Skills directory: /root/.hermes/skills/software-development/presale-pipeline/
- Apify keys: /root/.hermes/keys/apify_keys.json
- References: /root/.hermes/skills/software-development/presale-pipeline/references/
- Etalon: /root/work/presale-ampermy.html (23KB)
</interfaces>

<reference_materials>
<!-- Already on server. Read via SSH if needed during implementation. -->

| File | Lines | What It Provides |
|------|-------|-----------------|
| `references/technical-audit-case-study.md` | 129 | erasmile.ru multi-page audit — crawl pattern, 20+ URL templates, findings format |
| `references/content-analysis-workflow.md` | 49 | Content analysis workflow — tools, algorithm, card format, 4 winning formats |
| `references/etalon-ampermy-checklist.md` | 151 | Ampermy etalon checklist — what the final output should contain |
| `references/geo-check-methodology.md` | 119 | GEO visibility check — 5 params, report format, human-language output |
</reference_materials>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tech-auditor SKILL.md</name>
  <files>/root/.hermes/skills/software-development/presale-pipeline/tech-auditor/SKILL.md</files>
  <read_first>
    - /root/.hermes/skills/software-development/presale-pipeline/references/technical-audit-case-study.md (eramile.ru crawl pattern, findings format)
    - /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md (reference for YAML frontmatter format, section structure, fallback pattern)
    - /root/.hermes/skills/software-development/presale-pipeline/SKILL.md lines 25-45 (PRE-FLIGHT CHECKLIST + TOOL LIST — to understand what existing tools are available)
  </read_first>
  <acceptance_criteria>
    1. File exists at /root/.hermes/skills/software-development/presale-pipeline/tech-auditor/SKILL.md
    2. YAML frontmatter valid: name=tech-auditor, version=1.0.0, extracted_from=presale-pipeline v2.56.0
    3. depends_on lists: Hermes tools (web_search, web_extract, web_fetch, browser_console, browser_navigate)
    4. All 8 audit parameters defined with exact tools and methods:
       - (1) Lighthouse/PageSpeed — browser_console run Lighthouse via Chrome DevTools Protocol OR web_extract on PageSpeed Insights
       - (2) Broken links — crawl 15-20 key pages (list from medical clinic URL template), record HTTP status
       - (3) Meta tags — web_extract home page, check title (50-60 chars), meta description (140-160 chars), viewport meta
       - (4) H1 tags — web_extract home + 5 key pages, check exactly 1 H1 per page, no duplicates
       - (5) Alt text — web_extract home + 3 service pages, count images with/without alt, flag missing
       - (6) Sitemap — web_extract /sitemap.xml AND /robots.txt, check presence + lastmod dates
       - (7) SSL/HTTPS — browser_console check certificate validity, redirect HTTP→HTTPS
       - (8) Mobile responsiveness — browser_console viewport 375x812 check overflow, touch targets, font sizes
    5. Input section specifies: `url` (required, with protocol), `competitor_urls` (optional array for comparison)
    6. Output section specifies: per-parameter table with status (✅/🔴/🟡), finding, priority (HIGH/MEDIUM/LOW), fix description
    7. Algorithm section: Step 1 Home page scan → Step 2 Multi-page crawl → Step 3 Performance check → Step 4 Format findings
    8. "🔴 ТЕХНИЧЕСКИЕ ПРОБЛЕМЫ (срочно)" output format matched from case study
    9. Fallback section: skill_view(name='presale-pipeline') with Phase 0 pre-flight
    10. Line count >= 150
  </acceptance_criteria>
  <action>
    This task creates a brand new skill file on the server. Per D-01, tech-auditor is a standalone tool — it does NOT modify any existing file.

    Create the directory, then write the SKILL.md using Write tool over SSH with base64 encoding.

    **CRITICAL — File creation pattern (same as 25-01):**
    1. Build the markdown content in memory
    2. Base64-encode it: `echo "$CONTENT" | base64 | tr -d '\n'`
    3. Create directory on server: `ssh root@138.16.224.188 "mkdir -p /root/.hermes/skills/software-development/presale-pipeline/tech-auditor"`
    4. Write via SSH: `ssh root@138.16.224.188 "echo '$B64' | base64 -d > /root/.hermes/skills/software-development/presale-pipeline/tech-auditor/SKILL.md"`
    5. Verify: `ssh root@138.16.224.188 "wc -l /root/.hermes/skills/software-development/presale-pipeline/tech-auditor/SKILL.md"`

    **SKILL.md structure (follow social-verifier pattern):**

    ```
    ---
    name: tech-auditor
    title: Technical Website Auditor
    description: 8-parameter technical website audit — Lighthouse speed, broken links, meta tags, h1, alt text, sitemap, SSL, mobile responsiveness. For presale pipeline Phase 0 pre-flight and standalone site health checks.
    metadata:
      version: 1.0.0
      author: AIM
      extracted_from: presale-pipeline v2.56.0
      depends_on:
        - Hermes tools: web_search, web_extract, web_fetch, browser_console, browser_navigate
      approach: "llm-first — Hermes invokes this skill to audit any medical website's technical health"
    ---

    # Technical Website Auditor — 8-Parameter Site Health Check
    ## When to Use
    ## Input Specification
    ## Output Specification
    ## The 8-Parameter Audit
    ### Parameter 1: Lighthouse / PageSpeed
    ### Parameter 2: Broken Links (Multi-page Crawl)
    ### Parameter 3: Meta Tags
    ### Parameter 4: H1 Tags
    ### Parameter 5: Alt Text
    ### Parameter 6: Sitemap + robots.txt
    ### Parameter 7: SSL / HTTPS
    ### Parameter 8: Mobile Responsiveness
    ## Algorithm
    ## Output Format
    ## Fallback Protocol
    ```

    **Parameter 1 — Lighthouse/PageSpeed details:**
    Use web_fetch to `https://pagespeed.web.dev/report?url={URL_ENCODED}` or use browser_console to run `await fetch('https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=' + encodeURIComponent(url))` — extract performance score, FCP, LCP, TBT, CLS. Score <50 = 🔴, 50-89 = 🟡, 90+ = ✅.

    **Parameter 2 — Broken links details:**
    The erasmile case study defines 20+ URL templates for medical clinics. Use that pattern. Check each URL with web_extract or web_fetch. Record HTTP status. Any 404/500 = 🔴. 301/302 = 🟡 (check redirect chain). List all broken URLs explicitly.

    **Parameters 3-5 — Meta/H1/Alt details:**
    web_extract the home page + 5 key service pages. For meta: extract `<title>`, `<meta name="description">`. Check length ranges. For H1: count `<h1>` per page — must be exactly 1. For alt: count `<img>` with/without `alt` attribute. Flag pages where >20% images lack alt.

    **Parameter 6 — Sitemap details:**
    web_extract `/robots.txt` first — find Sitemap: directive. Then web_extract `/sitemap.xml`. Check: file exists, contains URLs, has `<lastmod>` dates, no 404.

    **Parameter 7 — SSL details:**
    browser_console: check `location.protocol === 'https:'`. Verify no mixed content warnings. Check certificate validity via web_fetch (will error on invalid cert).

    **Parameter 8 — Mobile details:**
    browser_console with viewport 375x812. Check: horizontal overflow, touch targets >= 48px, font-size >= 16px. Flag any horizontal scroll.

    **Output format — MUST match case study pattern:**
    ```
    🔴 ТЕХНИЧЕСКИЕ ПРОБЛЕМЫ (срочно)
    • {finding} — {impact} — {fix}

    🟡 РЕКОМЕНДАЦИИ (средний приоритет)
    • {finding} — {fix}

    Итого: X критических проблем, Y рекомендаций
    ```

    **Fallback:** skill_view(name='presale-pipeline') → Phase 0 Pre-flight checklist.
  </action>
  <verify>
    <automated>
      ssh root@138.16.224.188 "
        FILE=/root/.hermes/skills/software-development/presale-pipeline/tech-auditor/SKILL.md
        echo '=== EXISTS ===' && test -f \$FILE && echo OK || echo FAIL
        echo '=== YAML ===' && head -1 \$FILE | grep -c '^---$'
        echo '=== NAME ===' && grep -c 'name: tech-auditor' \$FILE
        echo '=== VERSION ===' && grep -c 'version: 1.0.0' \$FILE
        echo '=== PARAMS ===' && grep -c 'Parameter [1-8]' \$FILE
        echo '=== LIGHTHOUSE ===' && grep -c 'PageSpeed\|Lighthouse\|pagespeed' \$FILE
        echo '=== BROKEN LINKS ===' && grep -c '404\|HTTP status\|broken.link\|broken' \$FILE
        echo '=== META ===' && grep -c 'meta.*tag\|meta.*description\|<title>' \$FILE
        echo '=== H1 ===' && grep -c '<h1>\|H1' \$FILE
        echo '=== ALT ===' && grep -c 'alt=\|alt text\|alt.*attribute' \$FILE
        echo '=== SITEMAP ===' && grep -c 'sitemap\|robots.txt' \$FILE
        echo '=== SSL ===' && grep -c 'SSL\|HTTPS\|https\|certificate' \$FILE
        echo '=== MOBILE ===' && grep -c 'mobile\|viewport\|375\|responsive' \$FILE
        echo '=== FALLBACK ===' && grep -c 'skill_view.*presale-pipeline\|fallback' \$FILE
        echo '=== LINES ===' && wc -l \$FILE
        echo '=== TECH PROBLEMS FORMAT ===' && grep -c 'ТЕХНИЧЕСКИЕ ПРОБЛЕМЫ' \$FILE
      "
    </automated>
  </verify>
  <done>
    tech-auditor SKILL.md exists at /root/.hermes/skills/software-development/presale-pipeline/tech-auditor/SKILL.md. All 8 parameters defined with concrete tools and methods. YAML frontmatter valid. Fallback to presale-pipeline configured. Line count >= 150.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create content-analyzer SKILL.md</name>
  <files>/root/.hermes/skills/software-development/presale-pipeline/content-analyzer/SKILL.md</files>
  <read_first>
    - /root/.hermes/skills/software-development/presale-pipeline/references/content-analysis-workflow.md (49 lines — algorithm, card format, 4 winning formats)
    - /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md (input format — the content-analyzer receives verified social data from social-verifier)
    - /root/.hermes/skills/software-development/presale-pipeline/SKILL.md lines 343-386 (Phase 4 Content Strategy — media mentions, doctor content analysis, content plan, Telegram MTProto)
    - /root/.hermes/skills/software-development/presale-pipeline/references/etalon-ampermy-checklist.md (to ensure output matches etalon quality)
  </read_first>
  <acceptance_criteria>
    1. File exists at /root/.hermes/skills/software-development/presale-pipeline/content-analyzer/SKILL.md
    2. YAML frontmatter valid: name=content-analyzer, version=1.0.0, extracted_from=presale-pipeline v2.56.0
    3. depends_on lists: social-verifier (doctor social data), Apify Profile Scraper, Hermes tools (web_search, web_extract, web_fetch, browser_console)
    4. Input section: array of experts, each with {name, specialization, clinic, ig_handle, tg_handle, vk_handle, verified_socials} from social-verifier output
    5. Output section specifies per-expert card format: name + clinic, themes, formats, signature move, top post (with numbers), insight
    6. Card format with border-left: IG 3px solid var(--text) for client, #999 for competitors
    7. Section "4 формата-победителя" with methodology: (1) шоу/сериал, (2) интрига + вовлечение, (3) авторская методика, (4) образовательная экспертиза
    8. Section "Белое поле" — what no competitor does (determined per client)
    9. Explicit instruction: analyze ALL experts in input list, not just TOP-2. Every expert with verified social media gets a card.
    10. Key rule: if Apify blocks on an expert → Google fallback (web_search site:instagram.com) rather than skip
    11. Telegram MTProto integration: t.me/channel messages 20 for TG channel analysis
    12. Fallback section: skill_view(name='presale-pipeline') with Phase 4 content analysis
    13. Line count >= 200
  </acceptance_criteria>
  <action>
    This task creates the content-analyzer skill — per D-02, it must analyze ALL 10 experts, not just TOP-2. This is the biggest gap vs ampermy etalon: the current pipeline only deep-analyzes 2 experts, while ampermy shows full competitive landscape.

    **File creation pattern (same as Task 1):**
    1. Build content in memory
    2. Base64-encode: `echo "$CONTENT" | base64 | tr -d '\n'`
    3. Create directory: `ssh root@138.16.224.188 "mkdir -p /root/.hermes/skills/software-development/presale-pipeline/content-analyzer"`
    4. Write: `ssh root@138.16.224.188 "echo '$B64' | base64 -d > /root/.hermes/skills/software-development/presale-pipeline/content-analyzer/SKILL.md"`

    **SKILL.md structure:**

    ```
    ---
    name: content-analyzer
    title: Expert Content Analyzer
    description: Deep content analysis for ALL medical experts — winning formats, engagement metrics, viral posts with concrete numbers. Analyzes Instagram and Telegram content per expert. Input from social-verifier. Outputs per-expert cards + aggregate format insights + white-space analysis.
    metadata:
      version: 1.0.0
      author: AIM
      extracted_from: presale-pipeline v2.56.0 (ФАЗА 4 — Content Strategy)
      depends_on:
        - social-verifier (verified doctor social media data)
        - Apify Profile Scraper (apify/instagram-profile-scraper, RESIDENTIAL proxy)
        - Apify keys at /root/.hermes/keys/apify_keys.json
        - Telegram MTProto (/root/bin/tg-mtproto.py)
        - Hermes tools: web_search, web_extract, web_fetch
      approach: "llm-first — Hermes invokes this skill to analyze content strategies of all experts in the competitive landscape"
    ---

    # Expert Content Analyzer — Full Competitive Content Landscape
    ## When to Use
    ## CRITICAL: Analyze ALL Experts (not just TOP-2)
    ## Input Specification
    ## Output Specification
    ## Content Analysis Algorithm
    ### Step 1: Batch Instagram Scrape
    ### Step 2: Telegram Analysis
    ### Step 3: Per-Expert Deep Dive
    ### Step 4: Format Classification
    ### Step 5: 4 Winning Formats Extraction
    ### Step 6: White Space Identification
    ## Per-Expert Card Format
    ## 4 Winning Formats Framework
    ## White Space Analysis
    ## Google Fallback (when Apify blocks)
    ## Telegram MTProto Integration
    ## Fallback Protocol
    ```

    **Key content to embed — from content-analysis-workflow.md:**

    The "4 формата-победителя" framework (plastic surgery Moscow example):
    1. **Шоу/сериал** — serialized content, seasons, high production (e.g., Егорова "Новая молодость" 2 сезона, 204K ❤️)
    2. **Интрига + вовлечение** — provocative hooks, audience participation (e.g., Свиридов "отменил операцию — угадайте почему")
    3. **Авторская методика** — branded technique, unique signature (e.g., Кузин #kuzincustommade)
    4. **Образовательная экспертиза** — deep educational content (e.g., Круглик школа ментопластики)

    **Per-expert card format (from reference):**
    ```
    Имя Фамилия (Клиника) — N.K IG + N.K TG
    Темы: {topic1}, {topic2}, ...
    Формат: {format1}, {format2}, ...
    Фишка: {signature move}
    Топ-пост: "{title}" — N ❤️, K просмотров
    Insight: {strategic insight}
    ```

    **ALL experts requirement (core D-02 implementation):**
    Add explicit iron rule at the top: "⚠️ АНАЛИЗИРОВАТЬ ВСЕХ ЭКСПЕРТОВ. Не останавливаться после TOP-2. Каждый эксперт с верифицированными соцсетями получает карточку анализа. Это НЕ опционально — это разница между поверхностным и эталонным пресейлом."

    **Google fallback rule:** If Apify Profile Scraper returns error or empty for an expert → immediately fall back to `web_search("site:instagram.com \"{name}\" \"{specialization}\"")` and extract follower count, post count, recent content snippets from search snippets. Never write "не найден" without attempting Google fallback.

    **Telegram MTProto:** For each expert with TG handle → `python3 /root/bin/tg-mtproto.py messages @{channel} 20` → analyze: post themes, view counts, reaction patterns. TG may outperform IG (e.g., Ковынцев: IG 12.9K vs TG 21.6K).

    **Grouping in output:**
    - Client experts: border-left: 3px solid var(--text)
    - Competitor experts: border-left: 3px solid #999
    - Sub-headers separating groups

    **Fallback:** skill_view(name='presale-pipeline') → Phase 4 Content Strategy + Doctor Content Analysis.
  </action>
  <verify>
    <automated>
      ssh root@138.16.224.188 "
        FILE=/root/.hermes/skills/software-development/presale-pipeline/content-analyzer/SKILL.md
        echo '=== EXISTS ===' && test -f \$FILE && echo OK || echo FAIL
        echo '=== YAML ===' && head -1 \$FILE | grep -c '^---$'
        echo '=== NAME ===' && grep -c 'name: content-analyzer' \$FILE
        echo '=== VERSION ===' && grep -c 'version: 1.0.0' \$FILE
        echo '=== ALL EXPERTS ===' && grep -c 'ВСЕХ.*эксперт\|ALL.*expert\|всех эксперт\|каждый эксперт' \$FILE
        echo '=== NOT ONLY TOP2 ===' && grep -c 'TOP-2\|TOP.2\|топ-2\|первых двух' \$FILE && echo '(expected 0 or 1 with context)'
        echo '=== 4 FORMATS ===' && grep -c 'шоу\|сериал\|интрига\|вовлечение\|авторская.*методик\|образователь\|4 формата' \$FILE
        echo '=== CARD FORMAT ===' && grep -c 'Имя Фамилия\|Темы:\|Фишка:\|Топ-пост:\|Insight:' \$FILE
        echo '=== WHITE SPACE ===' && grep -c 'белое поле\|white.space\|никто не дела' \$FILE
        echo '=== GOOGLE FALLBACK ===' && grep -c 'Google.*fallback\|site:instagram.com\|web_search.*instagram' \$FILE
        echo '=== TELEGRAM ===' && grep -c 'tg-mtproto\|mtproto.py\|Telegram.*MTProto' \$FILE
        echo '=== APIFY REF ===' && grep -c 'apify_keys.json\|Apify.*Profile\|instagram-profile-scraper' \$FILE
        echo '=== SOCIAL-VERIFIER REF ===' && grep -c 'social-verifier' \$FILE
        echo '=== FALLBACK ===' && grep -c 'skill_view.*presale-pipeline' \$FILE
        echo '=== BORDER-LEFT ===' && grep -c 'border-left' \$FILE
        echo '=== LINES ===' && wc -l \$FILE
      "
    </automated>
  </verify>
  <done>
    content-analyzer SKILL.md exists at /root/.hermes/skills/software-development/presale-pipeline/content-analyzer/SKILL.md. ALL-experts requirement explicitly stated. 4 winning formats framework embedded. Per-expert card format defined. Google fallback when Apify blocks. Telegram MTProto integration. Fallback to presale-pipeline Phase 4. Line count >= 200.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update SKILL.md v2.56.0 routing table and Phase 4 references</name>
  <files>/root/.hermes/skills/software-development/presale-pipeline/SKILL.md</files>
  <read_first>
    - /root/.hermes/skills/software-development/presale-pipeline/SKILL.md lines 1-15 (YAML frontmatter + routing table — lines 7-11)
    - /root/.hermes/skills/software-development/presale-pipeline/SKILL.md lines 343-386 (ФАЗА 4 — Content Strategy + Doctor Content Analysis)
    - /root/.hermes/skills/software-development/presale-pipeline/SKILL.md lines 25-45 (PRE-FLIGHT CHECKLIST — to add tech-auditor reference)
  </read_first>
  <acceptance_criteria>
    1. Two new routing table entries added: tech-auditor and content-analyzer
    2. Routing table format matches existing entries: `tool-name: "description -> tool-name/SKILL.md"`
    3. ФАЗА 4 section updated: content analysis block references content-analyzer skill (delegation, same pattern as Phase 2 → social-verifier)
    4. PRE-FLIGHT CHECKLIST updated: add tech-auditor as a pre-presale step
    5. Version bumped: 2.56.0 → 2.57.0
    6. metadata.extracted list (if exists) updated to include tech-auditor and content-analyzer extraction dates
    7. No loss of existing content — only additions, no deletions
    8. YAML fences (---) remain valid — 2 fences, frontmatter intact
  </acceptance_criteria>
  <action>
    Per D-05 and D-06, update the main SKILL.md to route to the new tools and ensure quality matches ampermy etalon.

    **This task modifies the main SKILL.md on the server. Before changes, create a snapshot backup:**

    ```bash
    ssh root@138.16.224.188 "cp /root/.hermes/skills/software-development/presale-pipeline/SKILL.md /root/.hermes/skills/software-development/presale-pipeline/SKILL.md.bak-250603"
    ```

    **Change 1 — Routing table (append 2 entries after line 11):**
    The current routing table ends with competitor-scorer. Add:

    ```yaml
        tech-auditor: "Technical Website Audit -> tech-auditor/SKILL.md"
        content-analyzer: "Phase 4 Content Analysis -> content-analyzer/SKILL.md"
    ```

    **Change 2 — Version bump (metadata section):**
    Change `version: 2.56.0` to `version: 2.57.0`. Optionally add `extracted:` metadata:
    ```yaml
      extracted:
        - social-verifier (v2.56.0, 2026-06-06)
        - tech-auditor (v2.57.0, 2026-06-06)
        - content-analyzer (v2.57.0, 2026-06-06)
    ```

    **Change 3 — PRE-FLIGHT CHECKLIST (around line 25):**
    Add as first checklist item: `[ ] Технический аудит сайта запущен — tech-auditor skill (8 параметров: скорость, битые ссылки, meta, h1, alt, sitemap, SSL, mobile)`. Place BEFORE the existing items to establish it as the first pre-flight step.

    **Change 4 — ФАЗА 4 (around line 343):**
    After the Phase 4 header and before the detailed algorithm, add a delegation block similar to Phase 2's social-verifier reference:

    ```
    **⚠️ Делегирование:** Контент-анализ экспертов делегирован в `content-analyzer/SKILL.md`.
    Загрузить через: `skill_view(name='content-analyzer')`.
    Передать: список экспертов с верифицированными соцсетями (из social-verifier).
    Content-analyzer анализирует ВСЕХ экспертов (не только TOP-2), возвращает карточки анализа + 4 формата-победителя + белое поле.
    
    Ниже — оригинальный алгоритм (использовать если content-analyzer недоступен):
    ```

    **Change 5 — Add tech-auditor reference in ФАЗА 0 (around line 287):**
    After the Phase 0 header, add: `Перед началом сбора данных запустить технический аудит сайта: \`skill_view(name='tech-auditor')\`. Результат покажет технические проблемы, которые станут аргументом в КП.`

    **Deployment method:**
    Since we're modifying an existing file line-by-line via SSH, use sed or a Python one-liner:
    1. Read the current file
    2. Apply each change with targeted sed commands
    3. Verify YAML fences still valid (2 × `---`)
  </action>
  <verify>
    <automated>
      ssh root@138.16.224.188 "
        FILE=/root/.hermes/skills/software-development/presale-pipeline/SKILL.md
        echo '=== BACKUP EXISTS ===' && test -f /root/.hermes/skills/software-development/presale-pipeline/SKILL.md.bak-250603 && echo OK || echo FAIL
        echo '=== VERSION ===' && grep 'version: 2.57.0' \$FILE | head -1
        echo '=== TECH-AUDITOR ROUTE ===' && grep -c 'tech-auditor.*tech-auditor/SKILL.md' \$FILE
        echo '=== CONTENT-ANALYZER ROUTE ===' && grep -c 'content-analyzer.*content-analyzer/SKILL.md' \$FILE
        echo '=== PHASE 4 DELEGATION ===' && grep -c 'content-analyzer' \$FILE
        echo '=== TECH-AUDITOR IN PREFLIGHT ===' && grep -c 'технический.*аудит\|tech-auditor' \$FILE
        echo '=== YAML FENCES ===' && grep -c '^---$' \$FILE
        echo '=== PHASES PRESENT ===' && grep -c 'ФАЗА [0-7]' \$FILE
        echo '=== TOTAL LINES ===' && wc -l \$FILE
        echo '=== ROUTING TABLE ===' && sed -n '/^  routing:/,/^  [a-z]/p' \$FILE | head -20
      "
    </automated>
  </verify>
  <done>
    SKILL.md v2.57.0 has routing table entries for tech-auditor and content-analyzer. Phase 4 delegates content analysis to content-analyzer skill. PRE-FLIGHT CHECKLIST includes tech-auditor as first step. Phase 0 references tech-auditor. Backup created. Version bumped. 2 YAML fences valid. No existing content lost.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner → Server (SSH) | SKILL.md content deployed via base64 over SSH. No user data involved — only skill definition files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | SSH deployment | mitigate | Files written to server via authenticated SSH. Base64 encoding prevents injection. Verify file integrity via grep checks after deploy. |
| T-25-02 | Information Disclosure | SKILL.md content | accept | Skill files contain only tool definitions and algorithms — no secrets, keys, or PII. Apify key references use path to keys file, not actual keys. |
| T-25-03 | Elevation of Privilege | skill_view fallback | accept | Fallback loads existing presale-pipeline skill — no privilege change. Same Hermes context. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan — all work is markdown files deployed over SSH. |
</threat_model>

<verification>
### End-to-End Verification

After all tasks complete, run the comprehensive check:

```bash
ssh root@138.16.224.188 '
  echo "=== PHASE 25-03 VERIFICATION ==="
  echo ""
  
  echo "--- tech-auditor ---"
  F1=/root/.hermes/skills/software-development/presale-pipeline/tech-auditor/SKILL.md
  test -f $F1 && echo "EXISTS: OK ($(wc -l < $F1) lines)" || echo "EXISTS: FAIL"
  for p in lighthouse pagespeed broken.link meta.tag h1 alt sitemap SSL mobile; do
    grep -qi "$p" $F1 && echo "  PARAM $p: OK" || echo "  PARAM $p: MISSING"
  done
  grep -q "skill_view.*presale-pipeline" $F1 && echo "FALLBACK: OK" || echo "FALLBACK: MISSING"
  
  echo ""
  echo "--- content-analyzer ---"
  F2=/root/.hermes/skills/software-development/presale-pipeline/content-analyzer/SKILL.md
  test -f $F2 && echo "EXISTS: OK ($(wc -l < $F2) lines)" || echo "EXISTS: FAIL"
  grep -qi "всех.*эксперт\|ALL.*expert\|каждый эксперт" $F2 && echo "ALL EXPERTS: OK" || echo "ALL EXPERTS: MISSING"
  grep -qi "4 формата\|четыре формата" $F2 && echo "4 FORMATS: OK" || echo "4 FORMATS: MISSING"
  grep -q "белое поле\|white.space" $F2 && echo "WHITE SPACE: OK" || echo "WHITE SPACE: MISSING"
  grep -q "skill_view.*presale-pipeline" $F2 && echo "FALLBACK: OK" || echo "FALLBACK: MISSING"
  
  echo ""
  echo "--- SKILL.md v2.57.0 ---"
  F3=/root/.hermes/skills/software-development/presale-pipeline/SKILL.md
  grep -q "version: 2.57.0" $F3 && echo "VERSION 2.57.0: OK" || echo "VERSION 2.57.0: FAIL"
  grep -q "tech-auditor.*tech-auditor/SKILL.md" $F3 && echo "TECH-AUDITOR ROUTE: OK" || echo "TECH-AUDITOR ROUTE: MISSING"
  grep -q "content-analyzer.*content-analyzer/SKILL.md" $F3 && echo "CONTENT-ANALYZER ROUTE: OK" || echo "CONTENT-ANALYZER ROUTE: MISSING"
  grep -q "content-analyzer" $F3 && echo "PHASE 4 DELEGATION: OK" || echo "PHASE 4 DELEGATION: MISSING"
  YF=$(grep -c "^---$" $F3)
  [ "$YF" = "2" ] && echo "YAML FENCES: OK (2)" || echo "YAML FENCES: FAIL ($YF)"
  test -f /root/.hermes/skills/software-development/presale-pipeline/SKILL.md.bak-250603 && echo "BACKUP: OK" || echo "BACKUP: MISSING"
  
  echo ""
  echo "=== VERIFICATION COMPLETE ==="
'
```
</verification>

<success_criteria>
All three verification blocks pass:
1. tech-auditor/SKILL.md exists, >=150 lines, 8 parameters defined, fallback configured
2. content-analyzer/SKILL.md exists, >=200 lines, ALL-experts rule, 4 formats, white space, fallback configured
3. SKILL.md v2.57.0: routing table has both tools, Phase 4 delegates to content-analyzer, pre-flight references tech-auditor, backup created, 2 YAML fences valid
</success_criteria>

<output>
Create `.planning/phases/25-llm-first-orchestration/25-03-SUMMARY.md` when done
</output>
