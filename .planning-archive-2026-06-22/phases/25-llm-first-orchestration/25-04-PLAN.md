---
phase: 25-llm-first-orchestration
plan: 04
type: execute
wave: 4
depends_on: ["25-01", "25-02", "25-03"]
files_modified:
  - /root/.hermes/skills/software-development/presale-pipeline/competitor-scorer/SKILL.md
  - /root/.hermes/skills/software-development/presale-pipeline/reel-scraper/SKILL.md
  - /root/.hermes/skills/software-development/presale-pipeline/SKILL.md
autonomous: true
requirements: [GAP-03, GAP-04]

must_haves:
  truths:
    - "competitor-scorer SKILL.md includes viral post search methodology for competitors"
    - "reel-scraper SKILL.md exists with Apify Instagram Reel Scraper integration — links only, no visual analysis"
    - "SKILL.md v2.57.0 routing table updated with reel-scraper entry"
    - "reel-scraper explicitly excludes ffmpeg, AssemblyAI, and visual content type detection"
  artifacts:
    - path: "/root/.hermes/skills/software-development/presale-pipeline/competitor-scorer/SKILL.md"
      provides: "Competitor scoring with viral post search — what competitors post that goes viral, how to adapt for client"
      contains: "Viral Post Search"
    - path: "/root/.hermes/skills/software-development/presale-pipeline/reel-scraper/SKILL.md"
      provides: "Instagram Reel URL collection via Apify — links with target=_blank, no visual analysis"
      min_lines: 150
  key_links:
    - from: "/root/.hermes/skills/software-development/presale-pipeline/competitor-scorer/SKILL.md"
      to: "Apify Profile Scraper + web_search"
      via: "viral post search algorithm"
      pattern: "viral.*post\|Viral Post Search"
    - from: "/root/.hermes/skills/software-development/presale-pipeline/reel-scraper/SKILL.md"
      to: "Apify Instagram Reel Scraper (apify/instagram-reel-scraper)"
      via: "Apify API with RESIDENTIAL proxy and key rotation"
      pattern: "instagram-reel-scraper"
</must_haves>
---

<objective>
Close the two MEDIUM-priority gaps vs the ampermy etalon:

1. **Competitor viral themes** (D-03) — Extend competitor-scorer with viral post search. For competitors like Wellcure and Nekrasova: what content goes viral on their channels, what themes resonate, how to adapt for the client. This turns competitor analysis from "they exist" into "here's what works for them that you can do better."

2. **reel-scraper skill** (D-04) — Instagram Reel Scraper via Apify. Collect reel URLs for experts (especially Alifer with 135K audience). Links with target=_blank. EXPLICITLY NO visual analysis — no ffmpeg frame extraction, no AssemblyAI transcription, no content-type detection (talking head / montage / slideshow). Visual analysis is deferred to post-contract phase.

Purpose: These are the "competitive intelligence" layer that differentiates ampermy-quality presale from basic. Viral themes show the client you understand their market dynamics. Reel links with engagement data demonstrate concrete competitor monitoring capability.
Output: Extended competitor-scorer SKILL.md (v1.1.0) + new reel-scraper SKILL.md + updated main SKILL.md routing
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

All work is server-side on 138.16.224.188. Deploy via base64 over SSH (backtick escaping). This plan extends an existing skill (competitor-scorer) and creates a new one (reel-scraper). Preexisting Apify keys in /root/.hermes/keys/apify_keys.json. Reference material in /root/.hermes/skills/software-development/presale-pipeline/references/.
</execution_context>

<context>
@.planning/phases/25-llm-first-orchestration/25-03-CONTEXT.md
@.planning/phases/25-llm-first-orchestration/25-01-SUMMARY.md
@.planning/phases/25-llm-first-orchestration/25-02-SUMMARY.md

<interfaces>
<!-- Patterns from existing extracted skills. All tools follow this contract. -->

competitor-scorer/SKILL.md current structure (v1.0.0, to be extended):
```
YAML frontmatter: name=competitor-scorer, extracted_from=presale-pipeline v2.55.0 (ФАЗА 3)
Sections: When to Use, Input Specification, Output Specification, Analysis Algorithm
  Step 1 — Financial Comparison
  Step 2 — Doctor Social Audit (per competitor)
  Step 3 — Yandex.Maps Comparison
  Step 4 — VK Presence Check
  Step 5 — Digital Presence Map
Scoring Weights: Financial(0-10), Social(0-10), Doctors(0-10), Digital(0-10), Content(0-10)
Fallback Protocol: skill_view(name='presale-pipeline') → Phase 3
```

Existing SKILL.md routing table:
```yaml
  routing:
    social-verifier: "Phase 2 Doctor Social Audit -> social-verifier/SKILL.md"
    html-kp-generator: "Phase 6 HTML-KP -> html-kp-generator/SKILL.md"
    financial-fetcher: "Phase 1 Financial Data -> financial-fetcher/SKILL.md"
    competitor-scorer: "Phase 3 Competitors -> competitor-scorer/SKILL.md"
```

Server paths:
- Skills: /root/.hermes/skills/software-development/presale-pipeline/
- Apify keys: /root/.hermes/keys/apify_keys.json
- Reference: /root/.hermes/skills/software-development/presale-pipeline/references/apify-instagram-reel-scraper.md (218 lines — RESIDENTIAL proxy, key rotation, actor params)
</interfaces>

<reference_materials>
| File | Lines | What It Provides |
|------|-------|-----------------|
| `competitor-scorer/SKILL.md` (v1.0.0) | ~140 | Current skill — to be extended with Step 6 |
| `references/apify-instagram-reel-scraper.md` | 218 | Reel Scraper actor params, RESIDENTIAL proxy, key rotation algorithm, dataset fields |
| `references/content-analysis-workflow.md` | 49 | Content analysis workflow — helps define viral post detection patterns |
| `references/wellness-spa-center-research.md` | 112 | Competitor research context — Wellcure, Nekrasova, Respace patterns |
</reference_materials>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend competitor-scorer SKILL.md with viral post search</name>
  <files>/root/.hermes/skills/software-development/presale-pipeline/competitor-scorer/SKILL.md</files>
  <read_first>
    - /root/.hermes/skills/software-development/presale-pipeline/competitor-scorer/SKILL.md (full file — v1.0.0, ~140 lines, to be extended)
    - /root/.hermes/skills/software-development/presale-pipeline/references/content-analysis-workflow.md (49 lines — content card format, 4 winning formats)
    - /root/.hermes/skills/software-development/presale-pipeline/SKILL.md lines 343-386 (ФАЗА 4 media mentions + competitor content patterns from main pipeline)
  </read_first>
  <acceptance_criteria>
    1. competitor-scorer SKILL.md version bumped from 1.0.0 → 1.1.0
    2. New section "Step 6 — Viral Post Search" added after Step 5 (Digital Presence Map)
    3. Viral post methodology defines: (a) scrape top-20 posts per competitor via Apify Profile Scraper (RESIDENTIAL proxy), (b) identify posts with engagement > 2x competitor average, (c) extract viral theme, format, and hook, (d) produce adaptation suggestion for client
    4. Output section extended: new component "Viral Themes per Competitor" with per-competitor table (viral post title, theme, engagement numbers, format, adaptation for client)
    5. Scoring weights Content dimension updated: now includes "viral post frequency" as sub-criterion
    6. Integration note: viral post search uses same Apify batch as social-verifier to avoid redundant API calls
    7. Wellcure, Nekrasova mentioned as example competitors in methodology
    8. "Как адаптировать под {client}" pattern defined — for each viral theme: what the competitor did + how the client can do it better
    9. Fallback section updated (no changes needed — existing fallback works)
    10. No existing content deleted — only additions between Step 5 and Scoring Weights sections
    11. YAML frontmatter fences (2 × ---) remain valid
  </acceptance_criteria>
  <action>
    Per D-03, extend the competitor-scorer skill with viral post search. This is the key competitive intelligence feature that ampermy etalon has and current pipeline lacks. Competitors like Wellcure and Nekrasova have identifiable viral patterns — we need to detect and adapt them.

    **File modification pattern:**
    1. Read current competitor-scorer/SKILL.md via SSH
    2. Build the new section content
    3. Insert between Step 5 (Digital Presence Map) and Scoring Weights sections — find the insertion point by locating `## Scoring Weights`
    4. Write updated file via base64+SSH (same deploy pattern as 25-03)

    **Insertion point:** After the Digital Presence Map section (Step 5), before the Scoring Weights section. Locate via:
    ```bash
    ssh root@138.16.224.188 "grep -n 'Scoring Weights\|Digital Presence Map' /root/.hermes/skills/software-development/presale-pipeline/competitor-scorer/SKILL.md"
    ```

    **New Section Content:**

    ```
    ### Step 6 — Viral Post Search (per competitor)

    **⚠️ ИЩЕМ ВИРАЛЬНЫЕ ПОСТЫ КОНКУРЕНТОВ.** Это ключевая разница между "вот их конкуренты" и "вот что у них работает, и как вам это повторить лучше."

    **Цель:** Для каждого конкурента найти 2-3 виральных поста (вовлечение > 2x среднего по аккаунту), определить тему, формат и хук, предложить адаптацию для клиента.

    **Алгоритм:**
    1. **Собрать посты:** Apify Profile Scraper (B1) — `resultsLimit: 20`, RESIDENTIAL proxy, ротация ключей. Batch до 5 usernames.
    2. **Вычислить среднее вовлечение:** (лайки + комментарии) / количество постов для каждого аккаунта.
    3. **Найти виральные:** посты с вовлечением > 2x среднего. Если ни одного — снизить порог до 1.5x.
    4. **Для каждого вирального поста определить:**
       - **Тема:** о чём пост (процедура, результат, эксперт, закулисье, тренд)
       - **Формат:** Reel / карусель / фото / текст
       - **Хук:** что зацепило (эмоция, интрига, цифры, до/после, конфликт)
       - **Числа:** лайки, комментарии, просмотры, сохранения
    5. **Сформулировать адаптацию:** для каждой виральной темы — как клиент может сделать лучше.

    **Формат вывода (на каждого конкурента):**

    ```
    🔥 ВИРАЛЬНЫЕ ТЕМЫ — {Competitor Name}

    Пост 1: "{title}" — N ❤️ | K 💬 | M 👀
    Тема: {topic} | Формат: {format}
    Хук: {hook description}
    → Адаптация для {client}: {how to adapt and improve}

    Пост 2: ...
    ```

    **Интеграция с social-verifier:**
    Виральные посты ищутся в том же вызове Apify Profile Scraper, который используется для social-verifier. Не делать повторных вызовов для одних и тех же аккаунтов — брать данные из результатов social-verifier. Если social-verifier ещё не запущен — запустить его сначала.

    **Пример (Wellcure, @wellcure.float):**
    Анализ профиля → среднее вовлечение 45 ❤️. Пост "3 вещи, которые убивают вашу спину" — 187 ❤️ (4x среднего). Тема: боль в спине + бытовые привычки. Хук: страх + узнаваемость. → Адаптация для клиента: снять Reel "Что портит вашу осанку в офисе" с главврачом.

    **Что НЕ делаем:**
    - Не пишем "контент не найден" без попыток Google fallback
    - Не скрапим только 1-2 конкурентов — ВСЕ конкуренты
    - Не пропускаем Reels — они чаще всего виральные
    ```

    **Version bump:** Change `version: 1.0.0` to `version: 1.1.0` in YAML frontmatter.

    **Content scoring update:** In Scoring Weights section, update the Content dimension row to include:
    ```
    | Content (0-10) | Posting frequency, format diversity (Reels/carousel/text), viral post frequency |
    ```
  </action>
  <verify>
    <automated>
      ssh root@138.16.224.188 "
        FILE=/root/.hermes/skills/software-development/presale-pipeline/competitor-scorer/SKILL.md
        echo '=== VERSION ===' && grep 'version: 1.1.0' \$FILE
        echo '=== STEP 6 EXISTS ===' && grep -c 'Step 6.*Viral\|Step 6.*viral\|Viral Post Search' \$FILE
        echo '=== VIRAL METHODOLOGY ===' && grep -c 'виральн\|viral' \$FILE
        echo '=== ADAPTATION PATTERN ===' && grep -c 'Адаптация для\|adaptation for\|адаптировать' \$FILE
        echo '=== WELLCURE REF ===' && grep -c 'Wellcure\|wellcure' \$FILE
        echo '=== NEKRASOVA REF ===' && grep -c 'Nekrasova\|nekrasova\|Некрасов' \$FILE
        echo '=== APIFY BATCH NOTE ===' && grep -c 'social-verifier\|batch.*5.*username' \$FILE
        echo '=== SCORING UPDATE ===' && grep -c 'viral post frequency' \$FILE
        echo '=== YAML FENCES ===' && grep -c '^---$' \$FILE
        echo '=== ORIGINAL SECTIONS ==='
        echo '  Financial Comparison:' && grep -c 'Financial Comparison' \$FILE
        echo '  Doctor Social Audit:' && grep -c 'Doctor Social Audit' \$FILE
        echo '  Yandex.Maps:' && grep -c 'Yandex.Maps Comparison' \$FILE
        echo '  VK Presence:' && grep -c 'VK Presence Check' \$FILE
        echo '  Digital Presence:' && grep -c 'Digital Presence Map' \$FILE
        echo '  Scoring Weights:' && grep -c 'Scoring Weights' \$FILE
        echo '  Fallback:' && grep -c 'skill_view.*presale-pipeline' \$FILE
        echo '=== TOTAL LINES ===' && wc -l \$FILE
      "
    </automated>
  </verify>
  <done>
    competitor-scorer SKILL.md v1.1.0 includes Step 6 — Viral Post Search with methodology, Wellcure/Nekrasova example, adaptation pattern, and integration note with social-verifier. Content scoring updated. All original sections preserved. YAML fences valid.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create reel-scraper SKILL.md</name>
  <files>/root/.hermes/skills/software-development/presale-pipeline/reel-scraper/SKILL.md</files>
  <read_first>
    - /root/.hermes/skills/software-development/presale-pipeline/references/apify-instagram-reel-scraper.md (218 lines — FULL reference: RESIDENTIAL proxy config, key rotation algorithm, actor params, dataset fields, limits)
    - /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md (reference for YAML format + Apify integration pattern)
    - /root/.hermes/skills/software-development/presale-pipeline/SKILL.md lines 88-112 (Инструментарий A-N — to understand where reel-scraper fits in the tool ecosystem)
  </read_first>
  <acceptance_criteria>
    1. File exists at /root/.hermes/skills/software-development/presale-pipeline/reel-scraper/SKILL.md
    2. YAML frontmatter valid: name=reel-scraper, version=1.0.0, extracted_from=presale-pipeline v2.56.0
    3. depends_on lists: Apify Instagram Reel Scraper (apify/instagram-reel-scraper), Apify keys at /root/.hermes/keys/apify_keys.json, RESIDENTIAL proxy
    4. Input section: `usernames` (array of Instagram handles, max 5 per batch), `results_limit` (default 20)
    5. Output section: per-username list of reels with {shortCode, url, timestamp, likesCount, commentsCount, playsCount, caption, videoDuration} — links formatted as `<a href="https://instagram.com/reel/{shortCode}" target="_blank">`
    6. **EXPLICIT EXCLUSION:** section "What This Skill Does NOT Do" listing: ffmpeg frame extraction, AssemblyAI transcription, visual content type detection (talking head / montage / slideshow), quality assessment, CDN video download
    7. RESIDENTIAL proxy config included verbatim: `{"useApifyProxy": true, "apifyProxyGroups": ["RESIDENTIAL"]}`
    8. Key rotation algorithm (from apify-instagram-reel-scraper.md) — iterate through keys from apify_keys.json, try each, return first successful dataset
    9. Actor: `apify/instagram-reel-scraper` (NOT instagram-scraper), parameter: `username: ["nick"]` (array, NOT directUrls)
    10. Key priority order: account-008 → account-009 → account-010 → remaining keys
    11. Fallback: skill_view(name='presale-pipeline') → Phase 2 Doctor Social Audit (which includes Instagram scraping)
    12. Line count >= 150
  </acceptance_criteria>
  <action>
    Per D-04, create the reel-scraper skill. This is a focused tool — collect reel URLs ONLY, no visual analysis. The full analysis (ffmpeg + AssemblyAI) is deferred to post-contract per the CONTEXT.md decisions.

    **CRITICAL — What NOT to include (D-04 enforcement):**
    This skill MUST NOT reference: ffmpeg, AssemblyAI, frame extraction, scene detection, talking head detection, montage detection, slideshow detection, video quality assessment, CDN video download via proxy, audio extraction. The reel-reference.md has all of this — but we only take the scraping part. Visual analysis is post-contract.

    **File creation pattern:**
    1. Build content in memory
    2. Base64-encode: `echo "$CONTENT" | base64 | tr -d '\n'`
    3. Create directory: `ssh root@138.16.224.188 "mkdir -p /root/.hermes/skills/software-development/presale-pipeline/reel-scraper"`
    4. Write: `ssh root@138.16.224.188 "echo '$B64' | base64 -d > /root/.hermes/skills/software-development/presale-pipeline/reel-scraper/SKILL.md"`

    **SKILL.md structure:**

    ```
    ---
    name: reel-scraper
    title: Instagram Reel Scraper
    description: Collects Instagram Reel URLs and engagement metrics via Apify Instagram Reel Scraper. For presale expert analysis — links with target=_blank. NO visual analysis (ffmpeg/AssemblyAI deferred to post-contract phase).
    metadata:
      version: 1.0.0
      author: AIM
      extracted_from: presale-pipeline v2.56.0 (Инструментарий B2 — Reel Scraper)
      depends_on:
        - Apify Instagram Reel Scraper (apify/instagram-reel-scraper)
        - Apify keys at /root/.hermes/keys/apify_keys.json
        - RESIDENTIAL proxy (required — DATACENTER returns error)
      approach: "llm-first — Hermes invokes this skill to collect Reel links for experts and competitors"
    ---

    # Instagram Reel Scraper — Collect Reel URLs (No Visual Analysis)

    ## When to Use
    ## ⚠️ What This Skill Does NOT Do (Visual Analysis = Post-Contract)
    ## Input Specification
    ## Output Specification
    ## Apify Actor Configuration
    ### RESIDENTIAL Proxy (REQUIRED)
    ### Key Rotation Algorithm
    ### Key Priority Order
    ## Scraping Algorithm
    ### Step 1: Load and Validate Keys
    ### Step 2: Run Reel Scraper per Username
    ### Step 3: Parse and Validate Dataset
    ### Step 4: Format Output as HTML Links
    ### Step 5: Handle Errors with Key Rotation
    ## Batch Processing
    ## Output Format (HTML-ready)
    ## Rate Limits and Economy
    ## When NOT to Use This Skill
    ## Fallback Protocol
    ```

    **Key content from apify-instagram-reel-scraper.md (scraping-only parts):**

    RESIDENTIAL proxy config (MUST include):
    ```json
    {
      "username": ["handle"],
      "resultsLimit": 20,
      "proxy": {
        "useApifyProxy": true,
        "apifyProxyGroups": ["RESIDENTIAL"]
      }
    }
    ```

    Key rotation algorithm (Python pattern for execute_code):
    ```python
    import json, requests, time
    keys = json.load(open("/root/.hermes/keys/apify_keys.json"))
    for key_obj in keys['keys']:
        token = key_obj['token']
        label = key_obj['label']
        r = requests.post(
            "https://api.apify.com/v2/acts/apify~instagram-reel-scraper/runs",
            headers={"Authorization": f"Bearer {token}", "Content-Type": "application/json"},
            json={"username": [username], "resultsLimit": limit, "proxy": {"useApifyProxy": True, "apifyProxyGroups": ["RESIDENTIAL"]}}
        )
        if r.status_code != 201:
            continue
        run_id = r.json()['data']['id']
        # Poll for completion (max 120s)
        for _ in range(24):
            time.sleep(5)
            status_r = requests.get(f"https://api.apify.com/v2/acts/apify~instagram-reel-scraper/runs/{run_id}", headers={"Authorization": f"Bearer {token}"})
            if status_r.json()['data']['status'] == 'SUCCEEDED':
                break
        # Get dataset
        dataset_id = r.json()['data']['defaultDatasetId']
        data = requests.get(f"https://api.apify.com/v2/datasets/{dataset_id}/items", headers={"Authorization": f"Bearer {token}"}).json()
        if isinstance(data, list) and len(data) > 0 and isinstance(data[0], dict):
            return data  # SUCCESS
        # else: try next key
    return None  # All keys exhausted
    ```

    Key priority: account-008 (proven working with RESIDENTIAL) → account-009 → account-010 → remaining.

    **Output format — HTML-ready links:**
    ```
    @{username} — {N} Reels collected ({actor_time}s)
    | # | Reel | Date | ❤️ | 💬 | 👀 | Duration |
    |---|------|------|-----|-----|-----|----------|
    | 1 | <a href="https://instagram.com/reel/{shortCode}" target="_blank">View</a> | {timestamp} | {likesCount} | {commentsCount} | {playsCount} | {videoDuration}s |
    ```

    **Explicit exclusion section — "What This Skill Does NOT Do":**
    ```
    ## ⚠️ What This Skill Does NOT Do (Visual Analysis = Post-Contract)

    This skill collects Reel URLs and engagement metrics ONLY. The following are EXPLICITLY EXCLUDED and deferred to the post-contract marketing strategy phase:

    - ❌ ffmpeg frame extraction
    - ❌ Scene detection (shot changes, montage analysis)
    - ❌ AssemblyAI transcription
    - ❌ Visual content type classification (talking head / montage / slideshow)
    - ❌ Video quality assessment (professional / smartphone / screen recording)
    - ❌ CDN video download via proxy
    - ❌ Audio track analysis
    - ❌ Subtitle/overlay extraction

    The Reel URLs and engagement data collected here provide the INPUT for visual analysis in the post-contract phase. The full analysis pipeline is documented in `references/apify-instagram-reel-scraper.md` (visual analysis section) and will be invoked by the marketing strategist skill after contract signing.
    ```

    **Batch processing:**
    - 1 username at a time for reliability (batch of 5+ can return "error" on some)
    - Skip usernames with no verified Instagram (use social-verifier output to filter)
    - If a username returns empty/error across all keys → mark as "Reels not accessible (private/restricted)" — do NOT retry infinitely
    - Save the working key label for next invocation

    **Alifer (135K audience) priority note:**
    For ampermy etalon, Alifer is the highest-priority Reel target due to 135K Instagram audience. Process their handle first. The Reel data collection is the most time-consuming part (~80s for 50 Reels), so start early.

    **Fallback:** skill_view(name='presale-pipeline') → Инструментарий B2 (Reel Scraper section) with the original instructions.
  </action>
  <verify>
    <automated>
      ssh root@138.16.224.188 "
        FILE=/root/.hermes/skills/software-development/presale-pipeline/reel-scraper/SKILL.md
        echo '=== EXISTS ===' && test -f \$FILE && echo OK || echo FAIL
        echo '=== YAML ===' && head -1 \$FILE | grep -c '^---$'
        echo '=== NAME ===' && grep -c 'name: reel-scraper' \$FILE
        echo '=== VERSION ===' && grep -c 'version: 1.0.0' \$FILE
        echo '=== ACTOR ===' && grep -c 'instagram-reel-scraper' \$FILE
        echo '=== RESIDENTIAL ===' && grep -c 'RESIDENTIAL' \$FILE
        echo '=== KEY ROTATION ===' && grep -c 'key.*rotation\|ротаци.*ключ\|apify_keys.json' \$FILE
        echo '=== OUTPUT FORMAT ===' && grep -c 'target.*_blank\|shortCode\|View.*href' \$FILE
        echo '=== ENGAGEMENT FIELDS ===' && grep -c 'likesCount\|commentsCount\|playsCount' \$FILE
        echo '=== NO FFMPEG ===' && grep -c 'ffmpeg' \$FILE && echo '(expected 0 — in exclusion list only)'
        echo '=== NO ASSEMBLYAI ===' && grep -c 'AssemblyAI' \$FILE && echo '(expected 0 or 1 — in exclusion list only)'
        echo '=== NO VISUAL TYPE ===' && grep -c 'talking.head\|montage\|slideshow\|visual.*content.*type' \$FILE && echo '(expected 0 or 1 — in exclusion list only)'
        echo '=== EXCLUDES SECTION ===' && grep -c 'What This Skill Does NOT Do\|НЕ.*дела\|НЕ ДЕЛА' \$FILE
        echo '=== FALLBACK ===' && grep -c 'skill_view.*presale-pipeline' \$FILE
        echo '=== BATCH NOTE ===' && grep -c 'batch\|1 username' \$FILE
        echo '=== KEY PRIORITY ===' && grep -c 'account-008\|account-009' \$FILE
        echo '=== LINES ===' && wc -l \$FILE
      "
    </automated>
  </verify>
  <done>
    reel-scraper SKILL.md exists at /root/.hermes/skills/software-development/presale-pipeline/reel-scraper/SKILL.md. Apify Instagram Reel Scraper configured with RESIDENTIAL proxy and key rotation. Output format: HTML links with target=_blank. ffmpeg, AssemblyAI, and visual content type detection EXPLICITLY excluded. Fallback to presale-pipeline Phase 2. Line count >= 150.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update SKILL.md v2.57.0 routing table with reel-scraper and competitor-scorer v1.1.0 reference</name>
  <files>/root/.hermes/skills/software-development/presale-pipeline/SKILL.md</files>
  <read_first>
    - /root/.hermes/skills/software-development/presale-pipeline/SKILL.md lines 1-15 (YAML frontmatter + routing table)
    - /root/.hermes/skills/software-development/presale-pipeline/SKILL.md lines 40-115 (Инструментарий A-N — to add reel-scraper reference in B2)
    - /root/.hermes/skills/software-development/presale-pipeline/SKILL.md lines 326-342 (ФАЗА 3 — Competitors section, to reference competitor-scorer v1.1.0)
  </read_first>
  <acceptance_criteria>
    1. Routing table: reel-scraper entry added: `reel-scraper: "Instagram Reel Scraping -> reel-scraper/SKILL.md"`
    2. Инструментарий B2 (Reel Scraper) updated: add delegation note "→ reel-scraper/SKILL.md" to direct to the new skill
    3. competitor-scorer routing entry updated: description now mentions "with viral post search"
    4. ФАЗА 3 competitor section: add reference to competitor-scorer v1.1.0 viral post search capability
    5. Version bumped: 2.57.0 → 2.57.1 (patch bump — minor routing additions)
    6. YAML fences (2 × ---) remain valid
    7. No existing content lost
  </acceptance_criteria>
  <action>
    Update the main SKILL.md to route to the new and extended tools. This is a lighter update than 25-03 Task 3 since most routing is already in place from 25-03.

    **Before changes, create a backup of the current v2.57.0:**
    ```bash
    ssh root@138.16.224.188 "cp /root/.hermes/skills/software-development/presale-pipeline/SKILL.md /root/.hermes/skills/software-development/presale-pipeline/SKILL.md.bak-250603-v2570"
    ```

    **Change 1 — Routing table (append after content-analyzer entry):**
    ```yaml
        reel-scraper: "Instagram Reel Scraping -> reel-scraper/SKILL.md"
    ```

    **Change 2 — Update competitor-scorer routing description:**
    Change `"Phase 3 Competitors -> competitor-scorer/SKILL.md"` to `"Phase 3 Competitors (with viral post search) -> competitor-scorer/SKILL.md"`

    **Change 3 — Инструментарий B2 (Reel Scraper, around line 103):**
    After the B2 description line, add: `⚠️ Делегировано в reel-scraper/SKILL.md. Загрузить через skill_view(name='reel-scraper'). Для визуального анализа (ffmpeg/AssemblyAI) — постконтрактная фаза.`

    **Change 4 — ФАЗА 3 competitor section (around line 326):**
    After the competitor-scorer delegation reference, add a note about viral post search:
    `competitor-scorer v1.1.0 включает поиск виральных постов конкурентов (Step 6): что виральное у Wellcure, Некрасовой и др., как адаптировать под клиента.`

    **Change 5 — Version bump:** `version: 2.57.0` → `version: 2.57.1`

    **Deployment:** Same pattern — sed-based targeted edits via SSH, verify YAML fences intact.
  </action>
  <verify>
    <automated>
      ssh root@138.16.224.188 "
        FILE=/root/.hermes/skills/software-development/presale-pipeline/SKILL.md
        echo '=== VERSION ===' && grep 'version: 2.57.1' \$FILE | head -1
        echo '=== REEL-SCRAPER ROUTE ===' && grep -c 'reel-scraper.*reel-scraper/SKILL.md' \$FILE
        echo '=== COMPETITOR-SCORER VIRAL ===' && grep -c 'viral.*post\|viral\|виральн' \$FILE
        echo '=== B2 DELEGATION ===' && grep -c 'reel-scraper.*SKILL.md\|reel-scraper/SKILL' \$FILE
        echo '=== PHASE 3 VIRAL NOTE ===' && grep -c 'competitor-scorer.*1.1.0\|competitor-scorer v1.1' \$FILE
        echo '=== YAML FENCES ===' && grep -c '^---$' \$FILE
        echo '=== ALL PHASES ===' && grep -c 'ФАЗА [0-7]' \$FILE
        echo '=== ROUTING TABLE ===' && sed -n '/^  routing:/,/^  [a-z]/p' \$FILE | head -20
        echo '=== BACKUP ===' && test -f /root/.hermes/skills/software-development/presale-pipeline/SKILL.md.bak-250603-v2570 && echo OK || echo FAIL
      "
    </automated>
  </verify>
  <done>
    SKILL.md v2.57.1 has reel-scraper routing entry, competitor-scorer updated with viral post search reference, B2 delegation to reel-scraper, Phase 3 viral note, backup created. Version bumped. YAML fences valid. No existing content lost.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner → Server (SSH) | SKILL.md content deployed via base64 over SSH. No user data — skill definitions only. |
| reel-scraper → Apify API | Reel Scraper calls Apify API with bearer tokens from apify_keys.json. API keys are server-side only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Information Disclosure | reel-scraper Apify tokens | accept | Apify tokens stored server-side in /root/.hermes/keys/apify_keys.json. Skill files reference paths, not actual tokens. SSH-only access to server. |
| T-25-05 | Denial of Service | Apify key exhaustion | mitigate | Key rotation algorithm with 12 keys. Each key tried sequentially. If all exhausted — graceful fallback to presale-pipeline, not infinite retry. |
| T-25-06 | Tampering | competitor-scorer update | mitigate | Backup created before modification. Version bumped (1.0.0→1.1.0). YAML fences validated after edit. Existing sections preserved — only additions. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan — markdown file edits over SSH only. |
</threat_model>

<verification>
### End-to-End Verification

After all tasks complete, run the comprehensive check:

```bash
ssh root@138.16.224.188 '
  echo "=== PHASE 25-04 VERIFICATION ==="
  echo ""
  
  echo "--- competitor-scorer v1.1.0 ---"
  F1=/root/.hermes/skills/software-development/presale-pipeline/competitor-scorer/SKILL.md
  grep -q "version: 1.1.0" $F1 && echo "VERSION 1.1.0: OK" || echo "VERSION: FAIL"
  grep -q "Step 6" $F1 && echo "STEP 6 EXISTS: OK" || echo "STEP 6: MISSING"
  grep -qi "viral\|виральн" $F1 && echo "VIRAL METHODOLOGY: OK" || echo "VIRAL: MISSING"
  grep -qi "адаптац\|adaptation" $F1 && echo "ADAPTATION PATTERN: OK" || echo "ADAPTATION: MISSING"
  grep -q "skill_view.*presale-pipeline" $F1 && echo "FALLBACK: OK" || echo "FALLBACK: MISSING"
  ORIG=$(grep -cE "Step [1-5]|Financial Comparison|Doctor Social|Yandex.Maps|Digital Presence|Scoring Weight" $F1)
  [ "$ORIG" -ge 5 ] && echo "ORIGINAL SECTIONS: OK ($ORIG)" || echo "ORIGINAL SECTIONS: MISSING ($ORIG)"
  YF=$(grep -c "^---$" $F1)
  [ "$YF" = "2" ] && echo "YAML FENCES: OK (2)" || echo "YAML FENCES: FAIL ($YF)"
  
  echo ""
  echo "--- reel-scraper ---"
  F2=/root/.hermes/skills/software-development/presale-pipeline/reel-scraper/SKILL.md
  test -f $F2 && echo "EXISTS: OK ($(wc -l < $F2) lines)" || echo "EXISTS: FAIL"
  grep -q "instagram-reel-scraper" $F2 && echo "ACTOR: OK" || echo "ACTOR: MISSING"
  grep -q "RESIDENTIAL" $F2 && echo "RESIDENTIAL PROXY: OK" || echo "RESIDENTIAL: MISSING"
  grep -qi "target.*_blank\|shortCode" $F2 && echo "OUTPUT FORMAT: OK" || echo "OUTPUT FORMAT: MISSING"
  
  # Exclusion checks (should be in exclusion section only, not in algorithm)
  FFMPEG=$(grep -c "ffmpeg" $F2)
  AAI=$(grep -c "AssemblyAI" $F2)
  echo "FFMPEG refs: $FFMPEG (should be 0 or in exclusion list)"
  echo "AssemblyAI refs: $AAI (should be 0 or in exclusion list)"
  grep -qi "НЕ.*дела\|does not do\|НЕ ДЕЛА" $F2 && echo "EXCLUSION SECTION: OK" || echo "EXCLUSION SECTION: MISSING"
  
  grep -q "skill_view.*presale-pipeline" $F2 && echo "FALLBACK: OK" || echo "FALLBACK: MISSING"
  
  echo ""
  echo "--- SKILL.md v2.57.1 ---"
  F3=/root/.hermes/skills/software-development/presale-pipeline/SKILL.md
  grep -q "version: 2.57.1" $F3 && echo "VERSION 2.57.1: OK" || echo "VERSION: FAIL"
  grep -q "reel-scraper.*reel-scraper/SKILL.md" $F3 && echo "REEL-SCRAPER ROUTE: OK" || echo "REEL-SCRAPER ROUTE: MISSING"
  grep -qi "viral.*post\|виральн" $F3 && echo "VIRAL NOTE: OK" || echo "VIRAL NOTE: MISSING"
  YF=$(grep -c "^---$" $F3)
  [ "$YF" = "2" ] && echo "YAML FENCES: OK (2)" || echo "YAML FENCES: FAIL ($YF)"
  test -f /root/.hermes/skills/software-development/presale-pipeline/SKILL.md.bak-250603-v2570 && echo "BACKUP: OK" || echo "BACKUP: MISSING"
  
  echo ""
  echo "=== VERIFICATION COMPLETE ==="
'
```
</verification>

<success_criteria>
All verification blocks pass:
1. competitor-scorer v1.1.0: Step 6 Viral Post Search added, version bumped, Wellcure/Nekrasova examples, adaptation pattern defined, original sections intact
2. reel-scraper/SKILL.md exists, >=150 lines, Apify actor configured with RESIDENTIAL proxy + key rotation, output format with target=_blank links, ffmpeg/AssemblyAI/visual-type EXPLICITLY excluded
3. SKILL.md v2.57.1: routing table has all tools (social-verifier, html-kp-generator, financial-fetcher, competitor-scorer, tech-auditor, content-analyzer, reel-scraper), Инструментарий B2 delegates to reel-scraper, Phase 3 references viral post search, backup created
</success_criteria>

<output>
Create `.planning/phases/25-llm-first-orchestration/25-04-SUMMARY.md` when done
</output>
