---
phase: 25-llm-first-orchestration
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - /root/.hermes/skills/software-development/presale-pipeline/SKILL.md
autonomous: true
requirements: [D-01, D-03, D-08]

must_haves:
  truths:
    - "Working SKILL.md no longer contains the 5-pass algorithm (lines 306-397 extracted to social-verifier)"
    - "Working SKILL.md contains a reference to `skill_view(name='social-verifier')` at the former 5-pass location"
    - "All other phases (1-7) in working SKILL.md remain intact and functional"
    - "Backup at /root/.hermes/backups/2026-06-06_v2.55.0_snapshot/SKILL.md is untouched (per D-03)"
    - "Working SKILL.md still loads as a valid skill and can execute the full presale pipeline as fallback"
  artifacts:
    - path: "/root/.hermes/skills/software-development/presale-pipeline/SKILL.md"
      provides: "Updated presale pipeline SKILL.md with 5-pass block replaced by social-verifier reference"
      max_lines: 680
      contains: "social-verifier"
      contains: "skill_view"
      not_contains: "Pass 1"
      not_contains: "Pass 2"
  key_links:
    - from: "presale-pipeline/SKILL.md (Phase 2 section)"
      to: "social-verifier/SKILL.md"
      via: "skill_view(name='social-verifier') reference replacing lines 306-397"
    - from: "presale-pipeline/SKILL.md (Rule 21, L709-721)"
      to: "5-pass concept"
      via: "rule text preserved but updated to reference social-verifier"
    - from: "presale-pipeline/SKILL.md (Rule 16, L676-683)"
      to: "parallel search pattern"
      via: "rule text preserved (parallel search is a general pattern used across phases)"
---

<objective>
Remove the extracted 5-pass Instagram doctor verification block (lines 306-397) from the working presale-pipeline SKILL.md and replace it with a reference to the new standalone social-verifier skill. The backup at `/root/.hermes/backups/2026-06-06_v2.55.0_snapshot/` is never touched.

Purpose: Keep the working SKILL.md as lean fallback. Hermes calls social-verifier independently for doctor verification. If social-verifier fails, SKILL.md Phase 2 delegates to it via `skill_view`.

Output: Cleaned SKILL.md (est. 680 lines, down from 767) with Phase 2 reduced to a delegation block that instructs Hermes to use the social-verifier tool.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
### Key files

**Working SKILL.md to modify:**
- `/root/.hermes/skills/software-development/presale-pipeline/SKILL.md` (767 lines)
  - Lines 306-397: The 5-pass block to REMOVE (now extracted to social-verifier/SKILL.md)
  - Lines 306-310: Problem statement & general principle
  - Lines 312-326: Pass 1
  - Lines 328-336: Pass 2
  - Lines 338-348: Pass 3
  - Lines 350-364: Pass 4
  - Lines 366-373: Pass 5
  - Lines 375-383: 7 iron rules of passes
  - Lines 385-392: Bitrix SPA expert extraction algorithm
  - Lines 394-396: Grade system (5★ through 0★)

**Lines to KEEP (NOT touched by this plan):**
- Lines 1-305: Phases before doctor verification (Phase 1, etc.)
- Lines 306-397: REPLACED with delegation block
- Lines 398-767: Phases after doctor verification (Phases 3-7, rules 1-22)

**Backup (NEVER TOUCHED, per D-03):**
- `/root/.hermes/backups/2026-06-06_v2.55.0_snapshot/SKILL.md` — pristine v2.55.0

**New skill already extracted (by plan 25-01):**
- `/root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md`

### Architecture decisions
- D-01: Incremental extraction — tools extracted one by one, working in parallel with SKILL.md
- D-03: SKILL.md v2.55.0 NOT TOUCHED — это про БЭКАП. Рабочий SKILL.md МОЖНО менять, бэкап — НЕЛЬЗЯ.
- D-08: LLM-First orchestration — после извлечения инструментов Hermes получает их как отдельные tools

### What the replacement must achieve
- When Hermes reaches Phase 2 in the presale pipeline, SKILL.md says: "Use social-verifier tool"
- The delegation block is short (10-15 lines), not a re-implementation
- The original 5-pass content is NOT duplicated — it lives in social-verifier/SKILL.md
- Rules 16 and 21 are updated to reference social-verifier instead of inline pass descriptions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace 5-pass block with social-verifier delegation</name>
  <files>/root/.hermes/skills/software-development/presale-pipeline/SKILL.md</files>

  <read_first>
  - /root/.hermes/skills/software-development/presale-pipeline/SKILL.md (lines 300-400 — the exact block to replace)
  - /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md (the new skill — to confirm it exists and understand its contract)
  - /root/.hermes/backups/2026-06-06_v2.55.0_snapshot/SKILL.md (original — verify backup is pristine, never modify it)
  </read_first>

  <action>
  SSH to root@138.16.224.188.

  **Step 1 — Verify backup integrity (per D-03):**
  ```bash
  diff /root/.hermes/skills/software-development/presale-pipeline/SKILL.md /root/.hermes/backups/2026-06-06_v2.55.0_snapshot/SKILL.md
  ```
  If backup differs from working copy, note the differences but proceed. The backup is the canonical v2.55.0 snapshot.

  **Step 2 — Verify social-verifier exists:**
  ```bash
  test -f /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md && echo "EXISTS" || echo "MISSING — STOP, run plan 25-01 first"
  ```
  Do NOT proceed if social-verifier/SKILL.md does not exist. This plan depends on 25-01.

  **Step 3 — Create the delegation block:**
  Replace lines 306-397 (the entire 5-pass block) with this delegation text:

  ```
  ---
  
  **ФАЗА 2: АУДИТ СОЦСЕТЕЙ ВРАЧЕЙ**
  
  > **LLM-First:** Hermes вызывает инструмент `social-verifier` для проверки соцсетей врачей.
  
  **Инструкция для LLM:**
  1. Загрузи skill `social-verifier`: `skill_view(name='social-verifier')`
  2. Передай список врачей (имя, специализация, клиника) на вход инструменту
  3. Инструмент выполнит 5-pass проверку и вернёт мастер-таблицу с верифицированными соцсетями
  4. Pass-маркеры покажут, в каком проходе найден каждый врач
  5. Оценки (5★–0★) проставлены автоматически
  
  **Fallback:** Если `social-verifier` недоступен или завершился с ошибкой:
  - Загрузи полный SKILL.md v2.55.0: `skill_view(name='presale-pipeline')`
  - Выполни Фазу 2 из оригинального пайплайна
  - Отметь результат: "social-verifier failed — used presale-pipeline fallback"
  
  **Результат:** Мастер-таблица врачей с верифицированными соцсетями (IG, TG, VK, другие площадки), pass-маркерами и оценками.
  
  ---
  ```

  **Step 4 — Trim any blank lines left from removal:**
  Ensure exactly one blank line before and after the delegation block. No orphaned blank lines from the removed 5-pass content.

  **Step 5 — Verify the replacement boundaries:**
  - The content BEFORE line 306 (the end of Phase 1 or previous section) must remain intact
  - The delegation block replaces lines 306-397 exactly
  - The content AFTER line 397 (Phases 3-7) must remain intact
  - The replacement must NOT shift line numbers for the post-397 content in a way that breaks section references
  </action>

  <acceptance_criteria>
  - `grep -c 'social-verifier' /root/.hermes/skills/software-development/presale-pipeline/SKILL.md` returns >= 3 (mentioned in delegation block + fallback)
  - `grep -c 'skill_view' /root/.hermes/skills/software-development/presale-pipeline/SKILL.md` returns >= 2 (delegation + fallback)
  - `grep -c 'Pass 1' /root/.hermes/skills/software-development/presale-pipeline/SKILL.md` returns 0 (5-pass block fully removed)
  - `grep -c 'Pass 2' /root/.hermes/skills/software-development/presale-pipeline/SKILL.md` returns 0
  - `grep -c 'Pass 3' /root/.hermes/skills/software-development/presale-pipeline/SKILL.md` returns 0
  - `grep -c 'Pass 4' /root/.hermes/skills/software-development/presale-pipeline/SKILL.md` returns 0
  - `grep -c 'Pass 5' /root/.hermes/skills/software-development/presale-pipeline/SKILL.md` returns 0
  - `wc -l /root/.hermes/skills/software-development/presale-pipeline/SKILL.md` returns <= 690 (down from 767, roughly 80-90 lines removed)
  - Backup file `/root/.hermes/backups/2026-06-06_v2.55.0_snapshot/SKILL.md` is UNCHANGED (verified via diff)
  </acceptance_criteria>

  <verify>
    <automated>
      ssh root@138.16.224.188 "bash -c '
        FILE=/root/.hermes/skills/software-development/presale-pipeline/SKILL.md
        BACKUP=/root/.hermes/backups/2026-06-06_v2.55.0_snapshot/SKILL.md
        echo \"=== Backup exists and unchanged ===\"
        test -f \"\$BACKUP\" && wc -l < \"\$BACKUP\"
        echo \"=== social-verifier references ===\"
        grep -c \"social-verifier\" \"\$FILE\"
        echo \"=== skill_view references ===\"
        grep -c \"skill_view\" \"\$FILE\"
        echo \"=== 5-pass block removed ===\"
        for pass in \"Pass 1\" \"Pass 2\" \"Pass 3\" \"Pass 4\" \"Pass 5\"; do
          echo -n \"\$pass: \"; grep -c \"\$pass\" \"\$FILE\"
        done
        echo \"=== New line count ===\"
        wc -l < \"\$FILE\"
        echo \"=== Delegation block present ===\"
        grep -c \"ФАЗА 2\" \"\$FILE\"
      '"
    </automated>
  </verify>

  <done>
  Working SKILL.md no longer contains the 5-pass inline algorithm. Phase 2 delegates to social-verifier via skill_view. Backup is untouched. File is ~680 lines (down from 767). All other phases intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify SKILL.md integrity after extraction</name>
  <files>/root/.hermes/skills/software-development/presale-pipeline/SKILL.md</files>

  <read_first>
  - /root/.hermes/skills/software-development/presale-pipeline/SKILL.md (modified — post-cleanup)
  - /root/.hermes/backups/2026-06-06_v2.55.0_snapshot/SKILL.md (original — for comparison)
  </read_first>

  <action>
  SSH to root@138.16.224.188.

  **1. Phase integrity check:**
  Verify every phase from the original is still present in the working SKILL.md. Each phase has a `**ФАЗА N:**` marker:
  ```bash
  for phase in "ФАЗА 1" "ФАЗА 2" "ФАЗА 3" "ФАЗА 4" "ФАЗА 5" "ФАЗА 6" "ФАЗА 7"; do
    echo -n "$phase: "; grep -c "$phase" /root/.hermes/skills/software-development/presale-pipeline/SKILL.md
  done
  ```
  Expected: each returns >= 1. Phase 2 may return 2 (the marker + fallback reference) — that's acceptable.

  **2. Critical rules check:**
  Verify rules 1-22 from the original are preserved (rules 16 and 21 may be updated):
  ```bash
  grep -c "Правило [0-9]" /root/.hermes/skills/software-development/presale-pipeline/SKILL.md
  ```
  Expected: >= 18 (rules may be renumbered or consolidated but most must survive)

  **3. Key infrastructure references:**
  Verify critical references are intact:
  ```bash
  for ref in "apify_keys.json" "tg-mtproto.py" "web_search" "browser_console"; do
    echo -n "$ref: "; grep -c "$ref" /root/.hermes/skills/software-development/presale-pipeline/SKILL.md
  done
  ```
  These may have lower counts than the original (since the 5-pass section was a heavy user of these tools), but must NOT be zero — other phases use them too.

  **4. YAML frontmatter check:**
  Verify the YAML frontmatter is still valid:
  ```bash
  grep -c '^---$' /root/.hermes/skills/software-development/presale-pipeline/SKILL.md
  ```
  Expected: 2 (valid frontmatter)

  **5. Version bump:**
  Update the version in the YAML frontmatter metadata from `2.55.0` to `2.56.0` to reflect the extraction. Add an `extracted` field:
  ```yaml
  metadata:
    version: 2.56.0
    extracted:
      - social-verifier: "Phase 2 Doctor Social Audit → /root/.hermes/skills/software-development/presale-pipeline/social-verifier/SKILL.md"
  ```

  **6. No orphaned references:**
  The cleaned SKILL.md must not reference content that no longer exists:
  - `grep -c 'RESIDENTIAL'` may return 0 or low count (was mainly in 5-pass block). Other phases using Apify should still reference it.
  - Check that the 5★ grade references in other phases (Phase 6 HTML-КП may reference grades) still make sense without the inline definition. Add a brief note if needed: "Оценки (5★–0★) определены в social-verifier."
  </action>

  <acceptance_criteria>
  - All 7 phases (ФАЗА 1 through ФАЗА 7) still present in working SKILL.md
  - Rules 1-22 mostly preserved (>= 18 rule references remain)
  - Key infrastructure references (apify_keys.json, tg-mtproto.py, web_search, browser_console) still present
  - YAML frontmatter valid (2 `---` fences)
  - Version bumped to 2.56.0 with `extracted` metadata field
  - No broken cross-references to removed content
  - Working SKILL.md loads as valid skill (can be opened and parsed by Hermes)
  </acceptance_criteria>

  <verify>
    <automated>
      ssh root@138.16.224.188 "bash -c '
        FILE=/root/.hermes/skills/software-development/presale-pipeline/SKILL.md
        echo \"=== Phase integrity ===\"
        for phase in \"ФАЗА 1\" \"ФАЗА 2\" \"ФАЗА 3\" \"ФАЗА 4\" \"ФАЗА 5\" \"ФАЗА 6\" \"ФАЗА 7\"; do
          echo -n \"\$phase: \"; grep -c \"\$phase\" \"\$FILE\"
        done
        echo \"=== Rule count ===\"
        echo -n \"Rules referenced: \"; grep -c \"Правило [0-9]\" \"\$FILE\"
        echo \"=== Critical references ===\"
        for ref in \"apify_keys.json\" \"tg-mtproto.py\" \"web_search\" \"browser_console\"; do
          echo -n \"\$ref: \"; grep -c \"\$ref\" \"\$FILE\"
        done
        echo \"=== YAML frontmatter ===\"
        test \"\$(grep -c \"^---$\" \"\$FILE\")\" -eq 2 && echo \"VALID\" || echo \"BROKEN\"
        echo \"=== Version ===\"
        grep \"version\" \"\$FILE\"
        echo \"=== extracted metadata ===\"
        grep -A2 \"extracted\" \"\$FILE\"
      '"
    </automated>
  </verify>

  <done>
  Working SKILL.md is valid and complete. All 7 phases present. Rules preserved. Version bumped to 2.56.0. Cleaned file is ~680 lines — stripped of doctor verification details but fully functional as fallback for all pipeline phases. Hermes can load it via skill_view(name='presale-pipeline') for any phase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md (working copy) → social-verifier | Delegation boundary — Phase 2 delegates to external skill, failure must not cascade |
| SKILL.md (working copy) → backup | Backup is read-only — modification of working copy must not corrupt backup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02a | Denial of Service | SKILL.md (working) | mitigate | Social-verifier failure triggers explicit fallback to original SKILL.md Phase 2 logic. The delegation block includes a fallback path: if social-verifier unavailable, Hermes loads full presale-pipeline SKILL.md. No cascading failure. |
| T-25-02b | Tampering | SKILL.md (working) | mitigate | Diff against backup verifies only Phase 2 content changed. All other phases preserved. Automated grep checks confirm no content outside lines 306-397 was modified. |
| T-25-02c | Information Disclosure | Backup exposure | mitigate | Backup at /root/.hermes/backups/2026-06-06_v2.55.0_snapshot/ is NEVER touched by this plan. Read-only access only. No risk of backup corruption from extraction cleanup. |
</threat_model>

<verification>
1. `ssh root@138.16.224.188 "test -f /root/.hermes/skills/software-development/presale-pipeline/SKILL.md && wc -l < /root/.hermes/skills/software-development/presale-pipeline/SKILL.md"` → <= 690
2. `ssh root@138.16.224.188 "grep -c 'Pass 1\|Pass 2\|Pass 3\|Pass 4\|Pass 5' /root/.hermes/skills/software-development/presale-pipeline/SKILL.md"` → 0 (5-pass block fully removed)
3. `ssh root@138.16.224.188 "grep -c 'social-verifier' /root/.hermes/skills/software-development/presale-pipeline/SKILL.md"` → >= 3
4. All 7 phases still present (ФАЗА 1 through ФАЗА 7)
5. YAML frontmatter valid (2 fences)
6. Backup untouched
7. Version bumped to 2.56.0
8. Rules 1-22 preserved (>= 18 references)
</verification>

<success_criteria>
Working SKILL.md is cleaned of the extracted 5-pass content. Phase 2 delegates to social-verifier via skill_view with explicit fallback. The file is ~680 lines (down from 767), all other phases and rules intact, and still functions as a complete fallback when Hermes loads it via skill_view(name='presale-pipeline'). The backup (v2.55.0 snapshot) is untouched per D-03. Hermes can now call social-verifier independently for doctor verification without loading the full pipeline.
</success_criteria>

<output>
Create `.planning/phases/25-llm-first-orchestration/25-02-SUMMARY.md` when done.
</output>
