---
phase: 29-hermes-multi-tenant-pm
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - AIM/hermes/skills/project-management/SKILL.md
  - AIM/hermes/skills/presale-pipeline/SKILL.md
  - AIM/hermes/tests/test_pm_integration.py
autonomous: true
requirements:
  - PM-05
  - PM-06

must_haves:
  truths:
    - "presale-pipeline Phase 0 вызывает project-management для создания/проверки проекта (PM-05)"
    - "Данные presale сохраняются в project_dir/data.json, HTML-КП в deliverables/ (PM-05)"
    - "PM-скилл может искать по Second Brain через search-kb и сохранять через ingest-clinic.py (PM-06)"
    - "После /new → pm-detect-context → загрузка context.json → восстановление pending_tasks (PM-06)"
    - "Клиентские данные (врачи, финансы, конкуренты) дублируются в shared/ при завершении presale (PM-05)"
    - "Интеграционные тесты покрывают полный цикл: create → bind → context → checkpoint → detect → restore (PM-05, PM-06)"
  artifacts:
    - path: "AIM/hermes/skills/project-management/SKILL.md"
      provides: "Дополненный SKILL.md с детальными инструкциями по интеграциям"
      min_lines: 280
      contains: "presale-pipeline integration"
    - path: "AIM/hermes/skills/presale-pipeline/SKILL.md"
      provides: "Модифицированный presale-pipeline — вызывает project-management на Phase 0 для создания/проверки проекта"
      contains: "project-management"
    - path: "AIM/hermes/tests/test_pm_integration.py"
      provides: "Интеграционные тесты полного цикла PM-скилла"
      min_lines: 100
      contains: "test_full_lifecycle"
  key_links:
    - from: "presale-pipeline/SKILL.md (Phase 0)"
      to: "project-management/SKILL.md"
      via: "skill_view(name='project-management') на Phase 0"
      pattern: "skill_view.*project-management"
    - from: "project-management/SKILL.md (presale hook)"
      to: "pm-create-project.py"
      via: "вызов при старте presale для нового клиента"
      pattern: "pm-create-project\\.py"
    - from: "project-management/SKILL.md (Second Brain)"
      to: "ingest-clinic.py"
      via: "сохранение клиентских данных в shared/"
      pattern: "ingest-clinic\\.py"
    - from: "project-management/SKILL.md (context-preservation)"
      to: "pm-detect-context.py"
      via: "восстановление контекста после /new"
      pattern: "pm-detect-context\\.py.*new"
    - from: "test_pm_integration.py"
      to: "pm-create-project.py"
      via: "интеграционный тест полного цикла"
      pattern: "pm-create-project"
---

<objective>
Интегрировать PM-скилл с presale-pipeline (Phase 0 создание/проверка проекта, data.json, deliverables), Second Brain (поиск search-kb, сохранение ingest-clinic.py) и context-preservation (восстановление контекста после /new). Добавить интеграционные тесты полного цикла.

Purpose: PM-скилл становится частью экосистемы Hermes: presale автоматически создаёт проекты, данные сохраняются в изолированные директории, Second Brain доступен для поиска, контекст восстанавливается после перезапуска сессии. Без этих интеграций PM-скилл — изолированный инструмент; с ними — фундамент multi-tenant архитектуры.

Output:
- Дополненный SKILL.md с секциями интеграций (presale-pipeline, Second Brain, context-preservation)
- Модифицированный presale-pipeline/SKILL.md с вызовом project-management на Phase 0
- Интеграционные тесты (test_pm_integration.py) — полный цикл от создания проекта до восстановления контекста
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md — Phase 29 success criteria #5 (presale integration), #6 (Second Brain integration), #7 (context-preservation integration)
@.planning/phases/AIM-29-hermes-multi-tenant-pm/29-CONTEXT.md — D-17 (presale-pipeline), D-18 (Second Brain), D-19 (context-preservation) — LOCKED
@.planning/phases/AIM-29-hermes-multi-tenant-pm/pm-skill-fts.md — секции 6 (Интеграция с presale-pipeline), 4.4 (Second Brain), 7.5 (Компактизация), 9 (Фаза 2)
@.planning/phases/AIM-29-hermes-multi-tenant-pm/multi-tenant-research.md — 5-уровневая архитектура

<interfaces>
<!-- Контракты, созданные в Plan 29-01 и используемые в этом плане -->

PM-скрипты (созданы в 29-01, пути развёртывания):
```bash
/root/.hermes/skills/project-management/scripts/pm-registry.py     # add-chat, get, list-client, list-all, update, remove, move-project
/root/.hermes/skills/project-management/scripts/pm-create-project.py # создание структуры проекта
/root/.hermes/skills/project-management/scripts/pm-context.py        # set, get, save, restore, add-note, add-task, done-task
/root/.hermes/skills/project-management/scripts/pm-detect-context.py # определение проекта по chat_id
```

Структура проекта (создаётся pm-create-project.py):
```
/root/projects/{client_slug}/
├── shared/                          # doctors.json, financials.json, competitors.json, site-meta.json
└── {project_slug}/
    ├── .project-meta.json           # метаданные проекта (slug, client_slug, status, phases_completed, current_phase, team)
    ├── context.json                 # текущий контекст (last_topic, pending_tasks, key_findings)
    ├── data.json                    # данные presale (заполняется presale-pipeline)
    ├── notes/
    ├── files/ (reels/, transcripts/, reports/, checkpoints/)
    ├── skills/
    ├── knowledge/
    └── deliverables/                # HTML-КП и другие результаты
```

Текущий presale-pipeline SKILL.md — Phase sequence (для модификации):
- Phase 0: Deep Research (автономный) → skill_view(name='deep-research-phase-0')
- Phase 1: Init (Flash) → skill_view(name='tech-auditor') + skill_view(name='financial-fetcher')
- Phase 2: Collect → skill_view(name='social-verifier') + skill_view(name='competitor-scorer') + skill_view(name='reel-scraper')
- Phase 3: Analyze + Hard Gate → skill_view(name='doctor-content-analysis')
- Phase 4: HTML (Pro) → skill_view(name='html-kp-generator')

Second Brain доступ (per D-18):
```bash
search-kb "{query}"                                              # глобальный поиск
search-kb --path /root/projects/{client_slug}/knowledge/ "{query}"  # поиск по проекту
python3 /root/.knowledge/scripts/ingest-clinic.py {client_slug} "{name}"  # сохранение
```

Контекст из прошлой фазы (Phase 28 — Deep Research Phase 0) — паттерн модификации presale-pipeline:
- Phase 28 добавил Phase 0 (Deep Research) ПЕРЕД существующими фазами
- Модификация была в presale-pipeline/SKILL.md: добавлен `skill_view(name='deep-research-phase-0')` в Phase 0
- Аналогично: 29-02 добавляет `skill_view(name='project-management')` в Phase 0 presale-pipeline для создания/проверки проекта
</interfaces>

## Решения из CONTEXT.md (ЗАБЛОКИРОВАНЫ)

- **D-17**: presale-pipeline: Phase 0 создаёт/проверяет проект, данные сохраняются в project_dir/data.json, HTML-КП в deliverables/.
- **D-18**: Second Brain: поиск через search-kb, сохранение через ingest-clinic.py. Клиентские данные дублируются в shared/.
- **D-19**: context-preservation: после /new → pm-detect-context → загрузка context.json, восстановление pending_tasks.
- **D-03**: 5-уровневая архитектура изоляции (без fork ядра).
- **D-04**: Клиент может иметь несколько проектов. Данные клиента в shared/ доступны всем проектам клиента.
- **D-16**: Инициализация сессии: chat_id → registry → загрузка data.json + .project-meta.json → подгрузка skills → проверка pending_tasks.

## Deferred (НЕ включать в план)
- Telegram gateway hook (pre_gateway_dispatch) — Фаза 3 (Production)
- Multi-tenant memory isolation — ждёт GitHub PR #34352
- Dashboard/статистика проектов — отдельная фича
</context>

<tasks>

<task type="auto">
  <name>Task 1: Интегрировать PM-скилл с presale-pipeline — Phase 0 создание/проверка проекта, data.json, deliverables</name>
  <files>AIM/hermes/skills/presale-pipeline/SKILL.md, AIM/hermes/skills/project-management/SKILL.md</files>
  <read_first>
Обязательное чтение перед реализацией:
- /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/skills/presale-pipeline/SKILL.md — полный текущий текст (все 188 строк), особое внимание на Phase 0, Phase sequence и модель routing
- /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/skills/project-management/SKILL.md — созданный в 29-01 SKILL.md (секция «Интеграция с presale-pipeline» будет дополнена)
- /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/AIM-29-hermes-multi-tenant-pm/pm-skill-fts.md — секция 6 (Интеграция с presale-pipeline)
  </read_first>
  <action>
Модифицировать два SKILL.md для реализации D-17 (presale-pipeline → project-management):

### 1. Модификация presale-pipeline/SKILL.md

В файл `AIM/hermes/skills/presale-pipeline/SKILL.md`:

**A. В YAML frontmatter — добавить project-management в depends_on:**
В секцию `depends_on.hermes_skills` добавить строку:
```yaml
    project-management: ">=1.0"
```

**B. В Phase 0 (Deep Research) — добавить Шаг 0 (создание/проверка проекта) ПЕРЕД вызовом deep-research-phase-0:**

Добавить перед `skill_view(name='deep-research-phase-0')` новый шаг:

```
### Phase 0: Project Setup + Deep Research (автономный)

**Шаг 0 — Создать/проверить проект (НЕ показывать клиенту):**
1. skill_view(name='project-management') — загрузить PM-скилл
2. Определить client_slug из URL клиники (извлечь домен: atlas-esthetics.ru → atlas-esthetics)
3. Определить project_slug = "presale-{client_slug}-{год}"
4. Вызвать:
   - pm-detect-context.py --chat-id {telegram_chat_id} — проверить, привязан ли чат
   - Если found=false → pm-create-project.py --client-slug={client_slug} --client-name="{название}" --project-slug={project_slug} --project-name="Presale {название} {год}"
   - pm-registry.py add-chat --chat-id={telegram_chat_id} --client-slug={client_slug} --client-name="{название}" --project-slug={project_slug} --project-name="Presale {название} {год}"
5. Установить HERMES_PROJECT_DIR = /root/projects/{client_slug}/{project_slug}/
6. **НЕ сообщать клиенту о создании проекта — это внутренняя операция**

**Шаг 1 — Deep Research:**
skill_view(name='deep-research-phase-0')
```

**C. В Phase 4 (HTML) — указать сохранение в deliverables/:**
В секции html-kp-generator добавить инструкцию:
```
Сохранить HTML-КП в {HERMES_PROJECT_DIR}/deliverables/index.html
```

**D. После завершения presale — сохранить data.json и дублировать в shared/:**
Добавить новый шаг после Phase 4:
```
### Phase 5: Save & Handoff

1. Сохранить финальный data.json в {HERMES_PROJECT_DIR}/data.json
2. Дублировать ключевые данные в shared/:
   - Врачи → {HERMES_PROJECT_DIR}/../shared/doctors.json
   - Финансы → {HERMES_PROJECT_DIR}/../shared/financials.json
   - Конкуренты → {HERMES_PROJECT_DIR}/../shared/competitors.json
   - Мета сайта → {HERMES_PROJECT_DIR}/../shared/site-meta.json
3. pm-context.py save --chat-id={telegram_chat_id} — создать чекпоинт контекста
4. Сообщить клиенту: КП готов, проект сохранён
```

### 2. Дополнение project-management/SKILL.md

В файл `AIM/hermes/skills/project-management/SKILL.md` (секция «Интеграция с presale-pipeline») добавить детальные инструкции:

**A. Сценарий: presale запущен для нового клиента**
```
1. presale-pipeline Phase 0 вызывает project-management
2. pm-detect-context.py проверяет привязку чата
3. Если проект не существует → pm-create-project.py создаёт структуру
4. pm-registry.py add-chat привязывает чат
5. Все последующие фазы presale пишут в {HERMES_PROJECT_DIR}/
6. После завершения presale:
   - data.json сохраняется в project_dir
   - HTML-КП → deliverables/index.html
   - Ключевые данные → shared/
   - Контекст → context.json (через pm-context.py save)
```

**B. Сценарий: presale для существующего клиента (повторный заход)**
```
1. pm-detect-context.py находит существующий проект
2. Загружается data.json + контекст
3. Если presale уже был завершён → предложить новый проект или обновить существующий
4. Новый проект: pm-create-project.py с --project-slug="presale-{client}-{год}-v2"
```

**C. Важно для LLM:**
- НИКОГДА не сообщать клиенту о создании проекта/директорий — это инфраструктурная операция (Iron Rule 9 из presale-pipeline)
- Имена директорий: client_slug и project_slug только латиницей, snake_case
- При конфликте project_slug → автоинкремент (presale-atlas-2026-v2, presale-atlas-2026-v3)
  </action>
  <verify>
    <automated>grep -c "project-management" AIM/hermes/skills/presale-pipeline/SKILL.md && grep -c "pm-create-project" AIM/hermes/skills/presale-pipeline/SKILL.md && grep -c "pm-detect-context" AIM/hermes/skills/presale-pipeline/SKILL.md && grep -c "deliverables" AIM/hermes/skills/presale-pipeline/SKILL.md && grep -c "shared/" AIM/hermes/skills/presale-pipeline/SKILL.md && grep -c "presale-pipeline integration" AIM/hermes/skills/project-management/SKILL.md && grep -c "data.json" AIM/hermes/skills/project-management/SKILL.md</automated>
  </verify>
  <done>
presale-pipeline/SKILL.md: YAML depends_on содержит project-management ">=1.0". Phase 0 начинается с Шага 0 (создание/проверка проекта через project-management). Phase 5 (Save & Handoff) сохраняет data.json, дублирует в shared/, создаёт чекпоинт. Phase 4 сохраняет HTML в deliverables/. project-management/SKILL.md: секция «presale-pipeline integration» содержит оба сценария (новый клиент, повторный заход), правило о невидимости инфраструктурных операций для клиента.
  </done>
</task>

<task type="auto">
  <name>Task 2: Интегрировать PM-скилл с Second Brain и context-preservation</name>
  <files>AIM/hermes/skills/project-management/SKILL.md</files>
  <read_first>
Обязательное чтение перед реализацией:
- /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/skills/project-management/SKILL.md — текущий текст (результат 29-01 Task 3 + 29-02 Task 1)
- /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/AIM-29-hermes-multi-tenant-pm/pm-skill-fts.md — секции 4.4 (Second Brain), 7.5 (Компактизация/потеря контекста)
  </read_first>
  <action>
Дополнить project-management/SKILL.md детальными инструкциями по двум интеграциям per D-18 (Second Brain) и D-19 (context-preservation):

### 1. Second Brain интеграция (D-18, секция 4.4 ФТЗ)

Заменить текущую секцию «Интеграция со Second Brain» на расширенную версию:

**A. Поиск по Second Brain:**
```
## Second Brain Integration

PM-скилл имеет доступ к Второму Мозгу (коллективному знанию всех проектов):

### Поиск
- Глобальный поиск: `search-kb "{query}"` — ищет по всей базе знаний
- Поиск по проекту: `search-kb --path /root/projects/{client_slug}/knowledge/ "{query}"` — только в knowledge/ текущего проекта
- Поиск по клиенту: `search-kb --path /root/projects/{client_slug}/ "{query}"` — по всем проектам клиента

### Использование в сценариях:
- Клиент спрашивает «какие конкуренты у похожих клиник?» → search-kb "competitors.json" + фильтр по специализации
- Нужен пример КП для клиники похожего профиля → search-kb --path /root/projects/ "index.html"
- Поиск лучших практик → search-kb --path /root/.knowledge/ "best practice"
```

**B. Сохранение в Second Brain:**
```
### Сохранение знаний
После значимых находок в проекте — сохранить в Second Brain для будущих проектов:

1. Ключевые находки → ingest:
   python3 /root/.knowledge/scripts/ingest-clinic.py {client_slug} "{client_name}" \
     --specialization="{специализация}" \
     --revenue={оборот} \
     --doctors-count={количество врачей}

2. Данные клиента в shared/ автоматически индексируются Second Brain:
   - doctors.json, financials.json, competitors.json — доступны для глобального поиска
   - knowledge/findings.md — ключевые инсайты проекта

3. Правило: сохранять ТОЛЬКО обезличенные агрегированные данные
   (не сохранять персональные данные клиентов в глобальный Second Brain)
```

### 2. Context Preservation интеграция (D-19, секция 7.5 ФТЗ)

Заменить текущую секцию «Context preservation» на расширенную версию:

**A. Автоматическое сохранение контекста:**
```
## Context Preservation

### После каждого значимого действия
После любого изменения в проекте (задача выполнена, нота добавлена, фаза завершена) выполнить:

pm-context.py save --chat-id={telegram_chat_id}

Это создаёт чекпоинт context.json в files/checkpoints/{timestamp}.json.

Структура чекпоинта:
- files/checkpoints/2026-06-07T14:30:00.json — полная копия context.json на момент сохранения
- Хранить последние 10 чекпоинтов (старые автоматически удалять при save)

### Восстановление после /new (компактизация контекста)
Когда Hermes перезапускает сессию (пользователь написал /new или контекст закочился):

1. Определить chat_id из метаданных входящего сообщения
2. pm-detect-context.py --chat-id={telegram_chat_id} — найти проект
3. Если found=true:
   - Загрузить context.json:
     - Восстановить last_topic — напомнить, на чём остановились
     - Показать pending_tasks — что ещё не сделано
     - Показать key_findings — ключевые инсайты
   - Загрузить .project-meta.json:
     - Текущая фаза: current_phase
     - Завершённые фазы: phases_completed
4. Ответить клиенту: «Продолжаем работу над проектом {project_name}. Текущая фаза: {current_phase}. Незавершённых задач: {len(pending_tasks)}.»
5. НЕ перезапускать прерванные операции — только восстановить контекст и ждать команды

### Восстановление из чекпоинта
Если context.json повреждён или потерян:
1. pm-context.py restore --chat-id={telegram_chat_id} --checkpoint="{timestamp}"
2. Выбрать последний валидный чекпоинт (по дате)
3. Сообщить: «Контекст восстановлен из чекпоинта {timestamp}»
```

**B. Правила безопасности контекста:**
```
### Context Security Rules
- context.json НИКОГДА не содержит персональных данных клиентов (имя, телефон, email)
- Персональные данные хранятся в зашифрованном виде через Lead Dossier (ФЗ-152)
- key_findings содержит только бизнес-инсайты (не ПДн)
- При обнаружении ПДн в context.json → удалить, записать в encrypted_lead_dossier
```
  </action>
  <verify>
    <automated>grep -c "search-kb" AIM/hermes/skills/project-management/SKILL.md && grep -c "ingest-clinic" AIM/hermes/skills/project-management/SKILL.md && grep -c "context preservation" AIM/hermes/skills/project-management/SKILL.md && grep -c "pm-detect-context" AIM/hermes/skills/project-management/SKILL.md && grep -c "pm-context.py restore" AIM/hermes/skills/project-management/SKILL.md && grep -c "checkpoints" AIM/hermes/skills/project-management/SKILL.md && grep -c "ФЗ-152" AIM/hermes/skills/project-management/SKILL.md</automated>
  </verify>
  <done>
project-management/SKILL.md: секция Second Brain содержит инструкции по search-kb (глобальный, по проекту, по клиенту) и ingest-clinic.py. Секция context-preservation содержит автоматическое сохранение (после каждого действия), восстановление после /new (пошаговый алгоритм), восстановление из чекпоинта. Правила безопасности контекста (нет ПДн в context.json).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Интеграционные тесты — полный цикл PM-скилла</name>
  <files>AIM/hermes/tests/test_pm_integration.py</files>
  <read_first>
Обязательное чтение перед реализацией:
- /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/tests/test_pm_scripts.py — unit-тесты из 29-01 (для понимания фикстур и хелперов)
- /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/tests/test_presale_flow.py — эталонный стиль тестов Hermes (pytest, Path-фикстуры, строковые ассерты)
- /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/skills/project-management/scripts/pm-registry.py — созданный скрипт (для понимания CLI-интерфейса)
- /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/skills/project-management/scripts/pm-create-project.py — созданный скрипт
- /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/skills/project-management/scripts/pm-context.py — созданный скрипт
- /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/skills/project-management/scripts/pm-detect-context.py — созданный скрипт
  </read_first>
  <behavior>
    - Test 1 (full lifecycle — create → bind → context → checkpoint → detect → restore): создать проект, привязать чат, установить контекст, сохранить чекпоинт, определить проект по chat_id, восстановить из чекпоинта — все шаги успешны
    - Test 2 (project isolation): создать два проекта с разными chat_id, установить разный контекст в каждом → get для каждого chat_id возвращает правильный контекст (нет утечки между проектами)
    - Test 3 (shared/ доступ): создать клиента с двумя проектами, shared/ создана один раз, оба проекта видят shared/
    - Test 4 (presale data.json handoff): создать проект, записать data.json в project_dir в формате presale → data.json читается корректно при get контекста
    - Test 5 (context restore after /new simulation): создать проект, установить контекст с pending_tasks, сохранить чекпоинт → симулировать /new (загрузить context.json заново) → pending_tasks восстановлены
    - Test 6 (registry concurrency): два параллельных потока делают add-chat с разными chat_id → оба успешны, registry не повреждён
    - Test 7 (move-project preserves data): создать проект с контекстом, переместить к новому клиенту → контекст сохранён, client_slug обновлён
    - Test 8 (checkpoint rotation): создать 15 чекпоинтов → старые 5 удалены (осталось 10 последних)
    - Test 9 (error recovery — missing registry): удалить projects-registry.json → любая команда pm-registry создаёт новый пустой registry
    - Test 10 (error recovery — corrupted context): записать битый JSON в context.json → pm-context.py get сообщает об ошибке, предлагает restore
  </behavior>
  <action>
Создать `AIM/hermes/tests/test_pm_integration.py` — интеграционные тесты полного цикла PM-скилла.

### Тестовое окружение
- Использовать `tmp_path` фикстуру pytest для изоляции файловой системы
- Установить переменную окружения `HERMES_HOME` в tmp_path или использовать monkeypatch для переопределения путей в скриптах
- **Важно:** скрипты используют абсолютные пути `/root/.hermes/` и `/root/projects/`. Для тестов создать временную структуру через `tmp_path` и либо:
  - Патчить глобальные переменные путей в скриптах через monkeypatch
  - Или запускать скрипты через subprocess с временным `HOME=/tmp/...`
- **Рекомендуемый подход:** запускать скрипты через `subprocess.run([sys.executable, script_path, ...], env={**os.environ, 'HERMES_HOME': str(tmp_path)})` и модифицировать скрипты в 29-01 для поддержки `HERMES_HOME` env var (с дефолтом `/root`)

### Фикстуры
```python
@pytest.fixture
def temp_hermes_home(tmp_path):
    """Создать временную структуру Hermes home с registry."""
    hermes_home = tmp_path / "root"
    registry_dir = hermes_home / ".hermes"
    registry_dir.mkdir(parents=True)
    registry_path = registry_dir / "projects-registry.json"
    registry_path.write_text('{"version": 1, "projects": {}, "clients": {}}')
    projects_dir = hermes_home / "projects"
    projects_dir.mkdir(parents=True)
    return hermes_home

@pytest.fixture
def scripts_dir():
    """Путь к директории со скриптами PM."""
    return Path(__file__).resolve().parent.parent / "skills" / "project-management" / "scripts"
```

### Тесты (10 штук — см. behaviour)

**test_full_lifecycle:** создаёт проект через pm-create-project → привязывает чат через pm-registry add-chat → устанавливает контекст через pm-context set → сохраняет чекпоинт через pm-context save → определяет проект через pm-detect-context → восстанавливает контекст из чекпоинта через pm-context restore → проверяет, что все значения корректны.

**test_project_isolation:** создаёт два проекта (chat_id=-1001 и chat_id=-1002) → в каждом свой контекст → проверяет, что get для -1001 не возвращает данные -1002 и наоборот.

**test_shared_access:** создаёт клиента (atlas-esthetics) с двумя проектами → shared/ создана один раз → оба проекта могут читать shared/.

**test_presale_data_handoff:** создаёт проект → записывает data.json в project_dir (симулируя presale) → проверяет, что data.json читается.

**test_context_restore_after_new:** создаёт проект с pending_tasks: [{"id": "t1", "description": "Сбор врачей", "status": "pending"}] → сохраняет чекпоинт → симулирует /new (загружает context.json) → pending_tasks содержат t1.

**test_registry_concurrency:** через threading.Thread запускает 5 параллельных add-chat → проверяет, что все 5 успешны и registry не повреждён.

**test_move_project_preserves_data:** создаёт проект с контекстом → перемещает к новому клиенту → контекст сохранён, client_slug обновлён.

**test_checkpoint_rotation:** создаёт 15 чекпоинтов → проверяет, что осталось 10 (последних), старые 5 удалены.

**test_missing_registry_recovery:** удаляет registry-файл → вызывает pm-registry get → файл создан с дефолтной структурой.

**test_corrupted_context_recovery:** записывает битый JSON в context.json → pm-context get → сообщение об ошибке, exit code 1.

### Структура тестового файла
```python
"""Интеграционные тесты PM-скилла — полный цикл от создания проекта до восстановления контекста."""

import json
import os
import subprocess
import sys
import threading
from pathlib import Path

import pytest

# ... фикстуры ...

class TestFullLifecycle:
    """Тесты полного цикла PM."""

class TestProjectIsolation:
    """Тесты изоляции проектов."""

class TestIntegration:
    """Тесты интеграций (presale, shared, concurrency)."""

class TestErrorRecovery:
    """Тесты восстановления после ошибок."""
```
  </action>
  <verify>
    <automated>python3 -m pytest AIM/hermes/tests/test_pm_integration.py -x -v</automated>
  </verify>
  <done>
10 интеграционных тестов проходят. Полный цикл (create → bind → context → checkpoint → detect → restore) покрыт. Изоляция проектов верифицирована (нет утечек между chat_id). Восстановление после ошибок (missing registry, corrupted context) работает. Concurrency-тест подтверждает flock-безопасность.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| presale-pipeline → project-management | presale-pipeline (LLM) вызывает project-management для создания проектов. Должен передавать корректный client_slug (извлечённый из URL). |
| project-management → shared/ | Данные из presale дублируются в shared/. Только агрегированные не-ПДн данные. |
| /new → context.json | Восстановление контекста после компактизации. context.json не должен содержать ПДн. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-08 | Tampering | presale-pipeline/SKILL.md (client_slug extraction) | mitigate | client_slug извлекается из URL клиники через домен (не из пользовательского ввода). Валидация: только [a-z0-9-]+. |
| T-29-09 | Information Disclosure | shared/ (data duplication) | mitigate | В shared/ сохраняются только агрегированные данные (doctors.json без контактов, financials.json — публичные данные ФНС, competitors.json — публичные данные). Персональные данные НЕ дублируются. |
| T-29-10 | Information Disclosure | context.json (ПДн утечка) | mitigate | SKILL.md содержит правило: при обнаружении ПДн в context.json → удалить, записать в encrypted_lead_dossier. |
| T-29-11 | Elevation of Privilege | /project bind, /project create (в presale-контексте) | mitigate | В presale-контексте bind/create выполняются только при инициализации нового клиента. После привязки — повторный bind отклоняется. |
| T-29-12 | Denial of Service | checkpoint rotation | mitigate | Максимум 10 чекпоинтов. Старые удаляются автоматически при save. Предотвращает исчерпание диска. |

Все угрозы имеют disposition mitigate. Правила безопасности контекста и shared/ включены в SKILL.md.
</threat_model>

<verification>
## Проверка плана

### Автоматизированная
1. `python3 -m pytest AIM/hermes/tests/test_pm_integration.py -x -v` — все 10 интеграционных тестов проходят
2. `grep -c "project-management" AIM/hermes/skills/presale-pipeline/SKILL.md` — >= 1 (presale-pipeline вызывает project-management)
3. `grep -c "pm-create-project" AIM/hermes/skills/presale-pipeline/SKILL.md` — >= 1 (создание проекта в presale flow)
4. `grep -c "deliverables" AIM/hermes/skills/presale-pipeline/SKILL.md` — >= 1 (сохранение HTML-КП в deliverables/)
5. `grep -c "search-kb" AIM/hermes/skills/project-management/SKILL.md` — >= 1 (Second Brain поиск)
6. `grep -c "ingest-clinic" AIM/hermes/skills/project-management/SKILL.md` — >= 1 (Second Brain сохранение)
7. `grep -c "pm-detect-context.*new\|/new.*pm-detect-context" AIM/hermes/skills/project-management/SKILL.md` — >= 1 (context-preservation после /new)
8. `grep -c "pm-context.py restore" AIM/hermes/skills/project-management/SKILL.md` — >= 1 (восстановление из чекпоинта)
9. `grep -c "ФЗ-152\|ПДн\|персональные данные" AIM/hermes/skills/project-management/SKILL.md` — >= 1 (правила безопасности контекста)
10. `grep -c "shared/" AIM/hermes/skills/presale-pipeline/SKILL.md` — >= 1 (дублирование в shared/)
</verification>

<success_criteria>
После выполнения плана:
- [ ] presale-pipeline Phase 0 начинается с Шага 0: создание/проверка проекта через project-management
- [ ] presale-pipeline Phase 5 (Save & Handoff) сохраняет data.json, дублирует в shared/, создаёт чекпоинт
- [ ] HTML-КП сохраняется в deliverables/index.html
- [ ] PM-скилл содержит детальные инструкции по search-kb (глобальный, по проекту, по клиенту)
- [ ] PM-скилл содержит детальные инструкции по ingest-clinic.py (сохранение знаний)
- [ ] PM-скилл содержит алгоритм восстановления контекста после /new (5 шагов)
- [ ] PM-скилл содержит правила безопасности контекста (нет ПДн в context.json)
- [ ] PM-скилл содержит сценарии presale integration (новый клиент, повторный заход)
- [ ] 10 интеграционных тестов проходят, покрывая полный цикл
</success_criteria>

<output>
Создать `.planning/phases/AIM-29-hermes-multi-tenant-pm/29-02-SUMMARY.md` при завершении
</output>
