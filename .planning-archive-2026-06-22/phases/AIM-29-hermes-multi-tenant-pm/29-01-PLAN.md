---
phase: 29-hermes-multi-tenant-pm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AIM/hermes/skills/project-management/scripts/pm-registry.py
  - AIM/hermes/skills/project-management/scripts/pm-create-project.py
  - AIM/hermes/skills/project-management/scripts/pm-context.py
  - AIM/hermes/skills/project-management/scripts/pm-detect-context.py
  - AIM/hermes/tests/test_pm_scripts.py
  - AIM/hermes/skills/project-management/SKILL.md
  - AIM/hermes/skills/project-management/templates/projects-registry.json
  - AIM/hermes/skills/project-management/templates/project-meta.json
  - AIM/hermes/skills/project-management/templates/context.json
  - AIM/hermes/skills/project-management/research/multi-tenant-research.md
autonomous: true
requirements:
  - PM-01
  - PM-02
  - PM-03
  - PM-04

must_haves:
  truths:
    - "4 CLI-скрипта (pm-registry.py, pm-create-project.py, pm-context.py, pm-detect-context.py) находятся в кодовой базе AIM/hermes/skills/project-management/scripts/ (PM-01)"
    - "PM SKILL.md загружается как skill первого уровня с YAML frontmatter (name: project-management) и skill_view(name='project-management') (PM-02)"
    - "pm-registry.py управляет projects-registry.json с flock-блокировкой при записи, автосозданием при старте и поддержкой команд add-chat/get/list-client/list-all/update/remove/move-project (PM-03)"
    - "pm-create-project.py создаёт полную структуру /root/projects/{client_slug}/{project_slug}/ со всеми поддиректориями, .project-meta.json, context.json и регистрацией в clients секции (PM-04)"
    - "pm-context.py поддерживает set/get/save(checkpoint)/restore/add-note/add-task/done-task с чекпоинтами в files/checkpoints/{timestamp}.json (PM-01)"
    - "pm-detect-context.py по chat_id возвращает JSON {found, client_slug, project_slug, project_dir} через запрос к pm-registry.py get (PM-01)"
    - "Все скрипты обрабатывают ошибки: понятное сообщение в stderr, exit code 1, файл создаётся если не существует (D-20, D-21)"
  artifacts:
    - path: "AIM/hermes/skills/project-management/scripts/pm-registry.py"
      provides: "CLI для управления реестром проектов — add-chat, get, list-client, list-all, update, remove, move-project"
      min_lines: 150
      contains: "flock"
    - path: "AIM/hermes/skills/project-management/scripts/pm-create-project.py"
      provides: "CLI для создания структуры проекта на диске и регистрации в registry"
      min_lines: 100
      contains: "argparse"
    - path: "AIM/hermes/skills/project-management/scripts/pm-context.py"
      provides: "CLI для управления контекстом: set, get, save(checkpoint), restore, add-note, add-task, done-task"
      min_lines: 130
      contains: "argparse"
    - path: "AIM/hermes/skills/project-management/scripts/pm-detect-context.py"
      provides: "CLI для определения проекта по chat_id: возвращает JSON с client_slug, project_slug, project_dir"
      min_lines: 60
      contains: "argparse"
    - path: "AIM/hermes/tests/test_pm_scripts.py"
      provides: "Unit-тесты на все 4 CLI-скрипта"
      min_lines: 80
      contains: "test_pm_registry"
    - path: "AIM/hermes/skills/project-management/SKILL.md"
      provides: "LLM-инструкция для PM-скилла: инициализация сессии, slash-команды, изоляция, интеграции"
      min_lines: 200
      contains: "skill_view"
    - path: "AIM/hermes/skills/project-management/templates/projects-registry.json"
      provides: "Шаблон реестра проектов с автосозданием при первом запуске"
      contains: "version"
    - path: "AIM/hermes/skills/project-management/research/multi-tenant-research.md"
      provides: "Исследование multi-tenant архитектуры в репозитории (почему не fork, 5 уровней изоляции)"
      contains: "GitHub Issue #34352"
  key_links:
    - from: "pm-detect-context.py"
      to: "pm-registry.py get"
      via: "subprocess.run(['python3', 'pm-registry.py', 'get', '--chat-id', chat_id])"
      pattern: "pm-registry\\.py.*get"
    - from: "pm-create-project.py"
      to: "pm-registry.py"
      via: "регистрация проекта в clients секции registry"
      pattern: "clients.*append|pm-registry.*add-chat"
    - from: "pm-context.py"
      to: ".project-meta.json"
      via: "чтение для определения client_slug по chat_id"
      pattern: "project-meta\\.json"
    - from: "SKILL.md (инициализация)"
      to: "pm-detect-context.py"
      via: "вызов для определения проекта при входящем сообщении"
      pattern: "pm-detect-context\\.py.*chat.id"
    - from: "SKILL.md (project create)"
      to: "pm-create-project.py"
      via: "/project create → вызов pm-create-project.py"
      pattern: "pm-create-project\\.py"
---

<objective>
Создать фундамент PM-скилла (Project Management) в кодовой базе meAI: 4 CLI-скрипта, SKILL.md для Hermes, JSON-шаблоны и исследование multi-tenant архитектуры. Скрипты адаптируются из готового пакета на сервере (/root/work/pm-skill-package/) в структуру AIM/hermes/skills/project-management/.

Purpose: Один инстанс Hermes обслуживает множество изолированных Telegram-групп. chat_id → client_slug → project_slug. Реестр проектов с flock-блокировками, автоматическое создание структуры папок, управление контекстом, определение проекта по chat_id.

Output (все файлы в meAI репозитории):
- 4 Python CLI-скрипта в AIM/hermes/skills/project-management/scripts/
- PM SKILL.md (LLM-инструкция первого уровня) в AIM/hermes/skills/project-management/
- 3 JSON-шаблона в AIM/hermes/skills/project-management/templates/
- Исследование multi-tenant архитектуры в research/
- Unit-тесты в AIM/hermes/tests/test_pm_scripts.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md — Phase 29 goal, 9 success criteria, 2 planned plans
@.planning/phases/AIM-29-hermes-multi-tenant-pm/29-CONTEXT.md — D-01..D-21 user decisions (LOCKED)
@.planning/phases/AIM-29-hermes-multi-tenant-pm/pm-skill-fts.md — Полное ФТЗ (21KB): архитектура, компоненты, интеграции, граничные случаи
@.planning/phases/AIM-29-hermes-multi-tenant-pm/multi-tenant-research.md — Исследование: GitHub #34352, почему не fork, 5-уровневая архитектура

<interfaces>
<!-- Ключевые контракты из ФТЗ для executor'а -->

Формат projects-registry.json (D-05, D-07):
```json
{
  "version": 1,
  "projects": {
    "-1001234567890": {
      "client_slug": "atlas-esthetics",
      "client_name": "Atlas Esthetics",
      "project_slug": "presale-atlas-2026",
      "project_name": "Presale Atlas 2026",
      "created_at": "2026-06-07T12:00:00Z",
      "last_active": "2026-06-07T14:00:00Z",
      "status": "active",
      "members": [{"user_id": 322367335, "role": "manager", "name": "Михаил Елисеев"}],
      "skills": ["presale-pipeline", "second-brain"],
      "workdir": "/root/projects/atlas-esthetics/presale-atlas-2026/"
    }
  },
  "clients": {
    "atlas-esthetics": {"name": "Atlas Esthetics", "projects": ["presale-atlas-2026"]}
  }
}
```

Формат .project-meta.json (D-08):
```json
{
  "slug": "presale-atlas-2026", "name": "Presale Atlas 2026",
  "client_slug": "atlas-esthetics", "client_name": "Atlas Esthetics",
  "created_at": "2026-06-07T12:00:00Z", "status": "active",
  "phases_completed": [], "current_phase": null,
  "telegram_chat_id": "-1001234567890",
  "team": [{"user_id": 322367335, "role": "manager"}]
}
```

Формат context.json (D-12):
```json
{
  "last_active": "2026-06-07T14:30:00Z",
  "last_topic": "",
  "last_data_refs": {},
  "pending_tasks": [],
  "key_findings": []
}
```

Структура директорий проекта (D-08, D-09):
```
/root/projects/{client_slug}/
├── shared/                        # doctors.json, financials.json, competitors.json, site-meta.json
└── {project_slug}/
    ├── .project-meta.json
    ├── context.json
    ├── data.json
    ├── notes/
    ├── files/ (reels/, transcripts/, reports/, checkpoints/)
    ├── skills/
    ├── knowledge/
    └── deliverables/
```

CLI-интерфейс pm-registry.py (D-10):
```bash
pm-registry.py add-chat --chat-id="X" --client-slug="Y" --client-name="Z" --project-slug="W" --project-name="T"
pm-registry.py get --chat-id="X"           # JSON вывод {found, client_slug, project_slug, ...}
pm-registry.py list-client --client-slug="X" # JSON список проектов клиента
pm-registry.py list-all                     # JSON список всех проектов
pm-registry.py update --chat-id="X" --status="paused"
pm-registry.py remove --chat-id="X"
pm-registry.py move-project --project-slug="X" --new-client-slug="Y"
```

CLI-интерфейс pm-create-project.py (D-11):
```bash
pm-create-project.py --client-slug="X" --client-name="Y" --project-slug="Z" --project-name="W"
```

CLI-интерфейс pm-context.py (D-12):
```bash
pm-context.py set --chat-id="X" --key="Y" --value="Z"
pm-context.py get --chat-id="X"
pm-context.py save --chat-id="X"            # создаёт чекпоинт
pm-context.py restore --chat-id="X" --checkpoint="DATE"
pm-context.py add-note --chat-id="X" --text="..."
pm-context.py add-task --chat-id="X" --text="..."
pm-context.py done-task --chat-id="X" --task-id="ID"
```

CLI-интерфейс pm-detect-context.py (D-13):
```bash
pm-detect-context.py --chat-id="X"
# Вывод: {"found": true, "client_slug": "...", "project_slug": "...", "project_dir": "...", "client_name": "..."}
```

Существующая структура skills в Hermes (паттерн для SKILL.md):
- AIM/hermes/skills/presale-pipeline/SKILL.md — YAML frontmatter с name, title, description, version, depends_on; загрузка через skill_view(name='...')
- AIM/hermes/skills/deep-research-phase-0/SKILL.md — аналогичный паттерн
</interfaces>

## Решения из CONTEXT.md (ЗАБЛОКИРОВАНЫ — не пересматривать)

- **D-01**: Один бот, много чатов. Никакой супергруппы.
- **D-02**: chat_id = primary key. По chat_id → client_slug + project_slug через registry.
- **D-05**: projects-registry.json в /root/.hermes/ с маппингом chat_id → проект.
- **D-06**: flock-блокировка при записи в registry.
- **D-07**: Автосоздание registry при старте с `{"version": 1, "projects": {}, "clients": {}}`.
- **D-08**: Структура /root/projects/{client_slug}/shared/ + /{project_slug}/.
- **D-20**: Python 3.10+, argparse, JSON I/O. Все пути абсолютные (от /).
- **D-21**: Валидация входных данных перед записью. Linux (сервер AIM).

## Claude's Discretion
- Размещение скриптов: в `AIM/hermes/skills/project-management/scripts/` (следуя паттерну skills с поддиректориями)
- Интеграция в Hermes: через `skill_view(name='project-management')` (паттерн Phase 26)
- Тесты: pytest в `AIM/hermes/tests/test_pm_scripts.py`

## Deferred (НЕ включать в план)
- Telegram gateway hook (pre_gateway_dispatch) — Фаза 3 (Production), не сейчас
- Multi-tenant memory isolation — ждёт GitHub PR #34352
- Dashboard/статистика проектов — отдельная фича
- Автоматическое логирование активности — Фаза 3 (Production)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать pm-registry.py + pm-create-project.py — управление реестром и проектами</name>
  <files>AIM/hermes/skills/project-management/scripts/pm-registry.py, AIM/hermes/skills/project-management/scripts/pm-create-project.py</files>
  <read_first>
Обязательное чтение перед реализацией:
- /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/AIM-29-hermes-multi-tenant-pm/pm-skill-fts.md — секции 2 (Project Registry), 3 (Структура проекта), 5.1, 5.3
- /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/skills/presale-pipeline/SKILL.md — формат YAML frontmatter skills
  </read_first>
  <behavior>
    - Test 1 (Registry auto-create): registry-файл не существует → pm-registry.py get --chat-id="X" создаёт файл с `{"version": 1, "projects": {}, "clients": {}}`, возвращает `{"found": false}`
    - Test 2 (add-chat): pm-registry.py add-chat с валидными параметрами → запись в projects и clients секции, возврат `{"status": "ok"}`
    - Test 3 (add-chat duplicate): повторный add-chat с тем же chat_id → возврат ошибки, exit code 1
    - Test 4 (get found): после add-chat → get возвращает полную информацию о проекте
    - Test 5 (get not found): get с несуществующим chat_id → `{"found": false}`
    - Test 6 (flock-safety): два параллельных процесса add-chat с разными chat_id → оба успешны, данные не повреждены
    - Test 7 (list-client): list-client --client-slug="X" → JSON-массив проектов клиента
    - Test 8 (list-all): list-all → JSON-массив всех проектов
    - Test 9 (update status): update --chat-id="X" --status="paused" → статус обновлён
    - Test 10 (remove): remove --chat-id="X" → проект удалён из projects, но clients секция сохраняет проект в списке
    - Test 11 (move-project): move-project --project-slug="X" --new-client-slug="Y" → проект перемещён в нового клиента
    - Test 12 (pm-create-project creates dirs): pm-create-project.py → созданы все директории (shared/, project_slug/, notes/, files/, files/reels/, files/transcripts/, files/reports/, files/checkpoints/, skills/, knowledge/, deliverables/)
    - Test 13 (pm-create-project creates files): pm-create-project.py → созданы .project-meta.json и context.json с корректной структурой
    - Test 14 (pm-create-project registries client): pm-create-project.py → client_slug добавлен в clients секцию registry
    - Test 15 (pm-create-project idempotent): повторный вызов с теми же параметрами → не ломает существующие файлы, обновляет только если структура не полная
    - Test 16 (validation): add-chat без --chat-id → exit code 1, сообщение в stderr
  </behavior>
  <action>
Создать два Python CLI-скрипта по спецификациям ФТЗ (секции 5.1, 5.3) с адаптацией под кодовую базу meAI:

### pm-registry.py (секция 5.1 ФТЗ)
- Путь к registry: `/root/.hermes/projects-registry.json` (per D-05, D-07)
- Python 3.10+, только stdlib (argparse, json, fcntl/msvcrt, os, sys, datetime)
- flock-блокировка при КАЖДОЙ записи (per D-06): `fcntl.flock(fd, fcntl.LOCK_EX)`, таймаут 5 секунд
- Автосоздание файла при старте любой команды (per D-07): если файла нет → создать `{"version": 1, "projects": {}, "clients": {}}`
- Команды: add-chat, get, list-client, list-all, update, remove, move-project
- add-chat: проверяет, что chat_id уникален (не дублируется в projects); валидирует все обязательные поля (--chat-id, --client-slug, --client-name, --project-slug, --project-name); добавляет запись в projects и clients
- get: возвращает JSON с ключами found (bool), client_slug, project_slug, project_name, client_name, status, workdir, members, skills
- list-client: принимает --client-slug, возвращает JSON-массив проектов клиента
- list-all: возвращает JSON-массив всех проектов из секции projects
- update: принимает --chat-id и любые поля для обновления (--status, --project-name, --client-name); обновляет last_active на текущее время
- remove: удаляет запись из projects по chat_id; НЕ удаляет данные на диске; оставляет запись в clients секции
- move-project: меняет client_slug для всех записей с project_slug в projects и переносит запись между clients
- Вывод: JSON в stdout (машиночитаемый), человекочитаемый summary после JSON при --verbose
- Обработка ошибок: все ошибки → stderr + exit code 1

### pm-create-project.py (секция 5.3 ФТЗ)
- Python 3.10+, только stdlib (argparse, json, os, pathlib, datetime)
- Аргументы: --client-slug (обязательный), --client-name (обязательный), --project-slug (обязательный), --project-name (обязательный)
- Создаёт структуру директорий per D-08, D-09:
  1. `/root/projects/{client_slug}/shared/` (если не существует)
  2. `/root/projects/{client_slug}/{project_slug}/` + все поддиректории: notes/, files/, files/reels/, files/transcripts/, files/reports/, files/checkpoints/, skills/, knowledge/, deliverables/
- Создаёт `.project-meta.json` с полями: slug, name, client_slug, client_name, created_at (ISO-8601), status ("active"), phases_completed ([]), current_phase (null), telegram_chat_id (null), team ([])
- Создаёт пустой `context.json` с полями: last_active (null), last_topic (""), last_data_refs ({}), pending_tasks ([]), key_findings ([])
- Регистрирует клиента в clients секции registry: если client_slug нет в clients → добавляет с пустым projects списком; затем добавляет project_slug в clients[client_slug].projects
- Идемпотентность: если директория уже существует → проверяет наличие всех поддиректорий, создаёт недостающие; если .project-meta.json или context.json существуют → НЕ перезаписывает
- Вывод: JSON с created_paths (список созданных директорий), project_dir (полный путь)
- Обработка ошибок: ошибка создания директорий → stderr + exit code 1
  </action>
  <verify>
    <automated>python3 -m pytest AIM/hermes/tests/test_pm_scripts.py -x -v -k "registry or create_project"</automated>
  </verify>
  <done>
pm-registry.py: все 7 команд работают (add-chat, get, list-client, list-all, update, remove, move-project). flock-блокировка при записи. Автосоздание registry. Валидация входных данных. pm-create-project.py: создаёт полную структуру директорий, .project-meta.json, context.json, регистрирует клиента в clients секции. Идемпотентен. Все тесты из behaviour проходят.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать pm-context.py + pm-detect-context.py — управление контекстом и детекция проекта</name>
  <files>AIM/hermes/skills/project-management/scripts/pm-context.py, AIM/hermes/skills/project-management/scripts/pm-detect-context.py</files>
  <read_first>
Обязательное чтение перед реализацией:
- /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/AIM-29-hermes-multi-tenant-pm/pm-skill-fts.md — секции 4.1 (Инициализация), 4.3 (Slash-команды), 5.2, 5.4
- /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/skills/project-management/scripts/pm-registry.py — только что созданный скрипт (зависимость для pm-detect-context)
  </read_first>
  <behavior>
    - Test 1 (context set new key): pm-context.py set --chat-id="X" --key="last_topic" --value="Аудит сайта" → записывает в context.json
    - Test 2 (context get): pm-context.py get --chat-id="X" → возвращает полный context.json
    - Test 3 (context save checkpoint): pm-context.py save --chat-id="X" → создаёт files/checkpoints/{timestamp}.json с копией context.json
    - Test 4 (context restore): pm-context.py restore --chat-id="X" --checkpoint="2026-06-07T14:30:00Z" → восстанавливает context.json из чекпоинта
    - Test 5 (context add-note): pm-context.py add-note --chat-id="X" --text="Важная заметка" → добавляет запись в notes/findings.md с timestamp
    - Test 6 (context add-task): pm-context.py add-task --chat-id="X" --text="Собрать конкурентов" → добавляет задачу в pending_tasks с id="t1", статусом "pending"
    - Test 7 (context done-task): pm-context.py done-task --chat-id="X" --task-id="t1" → меняет статус на "completed"
    - Test 8 (detect context found): pm-detect-context.py --chat-id="X" (после add-chat) → `{"found": true, "client_slug": "...", "project_slug": "...", "project_dir": "...", "client_name": "..."}`
    - Test 9 (detect context not found): pm-detect-context.py --chat-id="UNKNOWN" → `{"found": false}`, exit code 0
    - Test 10 (context NoProjectError): pm-context.py get --chat-id="NONEXISTENT" → сообщение об ошибке, exit code 1
    - Test 11 (detect context sets env vars): pm-detect-context.py --chat-id="X" --export-env → выводит export HERMES_PROJECT_CHAT_ID=... строки для shell
  </behavior>
  <action>
Создать два Python CLI-скрипта по спецификациям ФТЗ (секции 5.2, 5.4):

### pm-context.py (секция 5.2 ФТЗ)
- Python 3.10+, только stdlib (argparse, json, os, shutil, datetime, pathlib)
- Как определяет проект: читает `.project-meta.json` для маппинга chat_id → client_slug + project_slug. Ищет по всем директориям в `/root/projects/` (обходит `Path(/root/projects/).iterdir()`).
- Команды: set, get, save, restore, add-note, add-task, done-task
- set: --chat-id, --key, --value → обновляет context.json[key] = value, обновляет last_active
- get: --chat-id → возвращает полный context.json
- save (checkpoint): --chat-id → копирует context.json в `files/checkpoints/{timestamp}.json` (timestamp = ISO-8601 без двоеточий для файловой совместимости)
- restore: --chat-id, --checkpoint → копирует файл из checkpoints/{checkpoint}.json обратно в context.json
- add-note: --chat-id, --text → добавляет запись в `notes/findings.md` с форматом `- [timestamp] text`
- add-task: --chat-id, --text → добавляет задачу в context.json pending_tasks с автоинкрементным id (t1, t2, ...), статус "pending"
- done-task: --chat-id, --task-id → находит задачу по id в pending_tasks, меняет статус на "completed", добавляет completed_at timestamp
- Все команды обновляют last_active в context.json на текущее время
- Обработка ошибок: проект не найден → stderr "Проект не привязан к чату {chat_id}" + exit code 1

### pm-detect-context.py (секция 5.4 ФТЗ)
- Python 3.10+, только stdlib (argparse, json, subprocess, os)
- Аргументы: --chat-id (обязательный), --export-env (флаг, опциональный)
- Вызывает `python3 /root/.hermes/skills/project-management/scripts/pm-registry.py get --chat-id {chat_id}` через subprocess
- Парсит JSON-ответ registry
- Если found=false → возвращает `{"found": false}`, exit code 0
- Если found=true → возвращает JSON: `{"found": true, "client_slug": "...", "project_slug": "...", "project_dir": "...", "client_name": "..."}`
- При --export-env: дополнительно выводит shell-команды для установки переменных:
  ```
  export HERMES_PROJECT_CHAT_ID="{chat_id}"
  export HERMES_CLIENT_SLUG="{client_slug}"
  export HERMES_PROJECT_SLUG="{project_slug}"
  export HERMES_PROJECT_DIR="{workdir}"
  ```
- Обработка ошибок: registry не отвечает (subprocess error) → stderr "Registry недоступен" + exit code 2
  </action>
  <verify>
    <automated>python3 -m pytest AIM/hermes/tests/test_pm_scripts.py -x -v -k "context or detect"</automated>
  </verify>
  <done>
pm-context.py: все 7 команд (set, get, save, restore, add-note, add-task, done-task) работают. Чекпоинты в files/checkpoints/. pm-detect-context.py: определяет проект по chat_id через registry, возвращает JSON, опционально генерирует shell export. Все тесты из behaviour проходят.
  </done>
</task>

<task type="auto">
  <name>Task 3: Создать SKILL.md, JSON-шаблоны и перенести research</name>
  <files>AIM/hermes/skills/project-management/SKILL.md, AIM/hermes/skills/project-management/templates/projects-registry.json, AIM/hermes/skills/project-management/templates/project-meta.json, AIM/hermes/skills/project-management/templates/context.json, AIM/hermes/skills/project-management/research/multi-tenant-research.md</files>
  <read_first>
Обязательное чтение перед реализацией:
- /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/skills/presale-pipeline/SKILL.md — полный текст (эталонный формат YAML frontmatter и структуры скилла)
- /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/skills/deep-research-phase-0/SKILL.md — ещё один пример формата скилла
- /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/AIM-29-hermes-multi-tenant-pm/pm-skill-fts.md — секции 4 (PM Skill логика), 5.5 (SKILL.md), 9 (порядок реализации)
- /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/AIM-29-hermes-multi-tenant-pm/multi-tenant-research.md — полный текст исследования (будет скопирован в research/)
  </read_first>
  <action>
Создать SKILL.md, три JSON-шаблона и перенести исследование multi-tenant архитектуры.

### SKILL.md структура
Создать `AIM/hermes/skills/project-management/SKILL.md` — LLM-инструкцию первого уровня, следуя формату presale-pipeline/SKILL.md и deep-research-phase-0/SKILL.md:

**YAML frontmatter** (per D-14, D-15):
```yaml
---
name: project-management
title: Multi-Tenant Project Management for AIM
description: >
  Управление проектами в изолированных Telegram-чатах. Один инстанс Hermes → множество клиентов.
  chat_id → client_slug → project_slug. Полная изоляция контекста, общие данные клиента через shared/.
  Интеграция с presale-pipeline, Second Brain и context-preservation.
version: 1.0.0
depends_on:
  hermes_skills:
    presale-pipeline: ">=3.0"
    second-brain: ">=1.0"
    context-preservation: ">=1.0"
---
```

**Тело SKILL.md** (не менее 200 строк) должно содержать:

1. **Инициализация сессии (entry point)** — per D-16, секция 4.1 ФТЗ:
   - При получении сообщения в Telegram-группе: извлечь chat_id из metadata.telegram_chat_id
   - Вызвать `pm-detect-context.py --chat-id {chat_id}` для определения проекта
   - Если found=false → ответ: «Этот чат не привязан к проекту. Используйте /project create или обратитесь к @mikhaileliseev»
   - Если found=true → загрузить data.json + .project-meta.json + context.json
   - Подгрузить привязанные skills из поля skills
   - Проверить pending_tasks в context.json → напомнить о незавершённых
   - Установить переменные: HERMES_PROJECT_CHAT_ID, HERMES_CLIENT_SLUG, HERMES_PROJECT_SLUG, HERMES_PROJECT_DIR

2. **Slash-команды** — per D-15, секция 4.3 ФТЗ:
   - `/project status` — текущее состояние: фаза, задачи, дата последней активности (читать из .project-meta.json и context.json)
   - `/project summary` — краткая сводка проекта для клиента (last_topic, pending_tasks, key_findings)
   - `/project note <текст>` — вызов `pm-context.py add-note --chat-id {chat_id} --text "{текст}"`
   - `/project doctor <фамилия>` — поиск в shared/doctors.json
   - `/project skill <name>` — привязать скилл к проекту: добавить в поле skills .project-meta.json
   - `/project add-task <текст>` — вызов `pm-context.py add-task --chat-id {chat_id} --text "{текст}"`
   - `/project done <task_id>` — вызов `pm-context.py done-task --chat-id {chat_id} --task-id {task_id}`
   - `/project log` — показать последние действия из context.json (last_topic + pending_tasks)
   - `/project bind <client_slug> <project_slug>` — вызов `pm-registry.py add-chat` (ТОЛЬКО для admin user_id=322367335)
   - `/project create <slug> <name>` — вызов `pm-create-project.py` (ТОЛЬКО для admin)

3. **Изоляция данных** — per D-03, D-04:
   - Каждый проект работает только в своей директории `/root/projects/{client_slug}/{project_slug}/`
   - Общие данные клиента доступны через `shared/` (read-only для проекта)
   - При поиске файлов или данных — сначала в project_dir, затем в shared/
   - Никогда не пересекать данные между разными client_slug

4. **Интеграция с presale-pipeline** — per D-17, секция 6 ФТЗ:
   - При запуске presale: проверить registry → создать проект если нет → привязать чат
   - Phase 0 presale-pipeline создаёт data.json в project_dir
   - HTML-КП сохраняется в deliverables/

5. **Интеграция со Second Brain** — per D-18, секция 4.4 ФТЗ:
   - Поиск: `search-kb "{query}"` — глобальный
   - Поиск по проекту: `search-kb --path /root/projects/{client_slug}/knowledge/ "{query}"`
   - Сохранение: `python3 /root/.knowledge/scripts/ingest-clinic.py {client_slug} "{name}"`

6. **Context preservation** — per D-19, секция 7.5 ФТЗ:
   - После каждого значимого действия → обновить context.json (last_active, last_topic)
   - После /new → загрузить context.json, восстановить pending_tasks
   - Периодически создавать чекпоинты через `pm-context.py save`

7. **Граничные случаи** — per секция 7 ФТЗ:
   - Чат без проекта → сообщение с инструкцией
   - Один клиент в нескольких чатах → shared/ доступен из всех чатов
   - Компактизация контекста → context.json на диске всегда актуальный
   - Смена модели → все данные на диске, не в памяти модели

8. **Правила безопасности**:
   - Команды /project bind и /project create доступны ТОЛЬКО пользователю с user_id=322367335
   - Все операции записи — через CLI-скрипты (не напрямую в файлы)
   - При неопределённом проекте — не выполнять действия с данными

### JSON-шаблоны
Создать три шаблона в `AIM/hermes/skills/project-management/templates/`:

- **projects-registry.json**: `{"version": 1, "projects": {}, "clients": {}}` — структура per D-07
- **project-meta.json**: полный шаблон `.project-meta.json` per секция 3 ФТЗ (все поля с example-значениями в комментариях)
- **context.json**: полный шаблон `context.json` per секция 3 ФТЗ

### Исследование multi-tenant
Скопировать `multi-tenant-research.md` из `.planning/phases/AIM-29-hermes-multi-tenant-pm/multi-tenant-research.md` в `AIM/hermes/skills/project-management/research/multi-tenant-research.md`. Добавить в начало файла YAML frontmatter с метаданными:
```yaml
---
title: Исследование multi-tenant архитектуры для Hermes Agent
date: 2026-06-07
status: accepted
decision: Гибридная 5-уровневая архитектура без fork ядра
references:
  - https://github.com/NousResearch/hermes-agent/issues/34352
---
```
  </action>
  <verify>
    <automated>grep -c "skill_view" AIM/hermes/skills/project-management/SKILL.md && grep -c "project.status" AIM/hermes/skills/project-management/SKILL.md && grep -c "pm-detect-context.py" AIM/hermes/skills/project-management/SKILL.md && python3 -c "import json; json.load(open('AIM/hermes/skills/project-management/templates/projects-registry.json')); json.load(open('AIM/hermes/skills/project-management/templates/project-meta.json')); json.load(open('AIM/hermes/skills/project-management/templates/context.json')); print('All JSON valid')"</automated>
  </verify>
  <done>
SKILL.md: YAML frontmatter с name=project-management, 8 секций (>200 строк), все slash-команды задокументированы, правила изоляции, интеграции с presale-pipeline/Second Brain/context-preservation, граничные случаи. Три JSON-шаблона валидны. Исследование multi-tenant перенесено в research/ с YAML frontmatter.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat_id → registry lookup | Входящий chat_id из Telegram metadata не должен позволять доступ к чужим проектам. Registry — единственный источник истины. |
| SKILL.md → CLI scripts | LLM вызывает CLI-скрипты через subprocess. Аргументы должны валидироваться внутри скриптов. |
| shared/ доступ | Проект не должен модифицировать shared/ директорию клиента — только читать. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Spoofing | pm-detect-context.py (chat_id) | mitigate | chat_id извлекается из Telegram gateway metadata (не из текста сообщения). Верификация через registry lookup. |
| T-29-02 | Tampering | projects-registry.json | mitigate | flock-блокировка (LOCK_EX) при КАЖДОЙ записи. Таймаут 5 секунд. Атомарная запись через tempfile + rename. |
| T-29-03 | Information Disclosure | pm-detect-context.py (cross-project) | mitigate | chat_id однозначно маппится на один проект. При found=false — возврат без данных проекта. |
| T-29-04 | Elevation of Privilege | SKILL.md (/project bind, /project create) | mitigate | Проверка user_id=322367335 (admin) перед выполнением команд bind/create. CLI-скрипты также проверяют --user-id флаг. |
| T-29-05 | Denial of Service | pm-registry.py (flock contention) | mitigate | Таймаут flock 5 секунд. При таймауте — exit code 3 с сообщением «Registry занят, попробуйте позже». |
| T-29-06 | Tampering | context.json (injection via set) | mitigate | Валидация --key на допустимые ключи (только поля из шаблона context.json). Валидация --value на максимальную длину (10KB). |
| T-29-07 | Information Disclosure | shared/ (cross-client read) | mitigate | pm-context.py проверяет client_slug из .project-meta.json. При add-note/add-task — запись ТОЛЬКО в project_dir (не в shared/). |

Все угрозы имеют disposition mitigate. Атомарная запись и flock-блокировки включены в реализацию.
</threat_model>

<verification>
## Проверка плана

### Автоматизированная
1. `python3 -m pytest AIM/hermes/tests/test_pm_scripts.py -x -v` — все тесты проходят (16+ тестов для registry/create + 11+ для context/detect)
2. `grep -c "flock" AIM/hermes/skills/project-management/scripts/pm-registry.py` — flock используется
3. `python3 -c "import json; json.load(open('AIM/hermes/skills/project-management/templates/projects-registry.json')); json.load(open('AIM/hermes/skills/project-management/templates/project-meta.json')); json.load(open('AIM/hermes/skills/project-management/templates/context.json')); print('OK')"` — все JSON валидны
4. `grep -c "skill_view" AIM/hermes/skills/project-management/SKILL.md` — > 0 (SKILL.md содержит инструкции по загрузке других скиллов)

### Ручная (human checkpoint)
- Проверить SKILL.md на соответствие ФТЗ: все 8 секций присутствуют, slash-команды задокументированы
</verification>

<success_criteria>
После выполнения плана:
- [ ] 4 CLI-скрипта лежат в AIM/hermes/skills/project-management/scripts/ и запускаются с python3
- [ ] pm-registry.py: add-chat, get, list-client, list-all, update, remove, move-project работают с flock
- [ ] pm-create-project.py: создаёт полную структуру директорий + .project-meta.json + context.json
- [ ] pm-context.py: set, get, save, restore, add-note, add-task, done-task работают
- [ ] pm-detect-context.py: определяет проект по chat_id, возвращает JSON
- [ ] SKILL.md: YAML frontmatter, 8 секций, >200 строк, загружается через skill_view(name='project-management')
- [ ] Три JSON-шаблона валидны и соответствуют структуре из ФТЗ
- [ ] Исследование multi-tenant перенесено в research/ с YAML frontmatter
- [ ] Unit-тесты (test_pm_scripts.py) содержат минимум 27 тестов, все проходят
</success_criteria>

<output>
Создать `.planning/phases/AIM-29-hermes-multi-tenant-pm/29-01-SUMMARY.md` при завершении
</output>
