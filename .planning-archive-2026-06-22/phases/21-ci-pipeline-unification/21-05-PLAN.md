---
phase: 21-ci-pipeline-unification
plan: 05
type: execute
wave: 2
depends_on: [21-03, 21-04]
files_modified:
  - AIM/tests/subagents/test_ci_pipeline_integration.py
  - AIM/src/aim/subagents/competitive_intel/orchestrator/ci_orchestrator.py
autonomous: true
gap_closure: true
requirements: [SC8]

must_haves:
  truths:
    - "All 49 CI integration tests pass (0 failures, 0 errors)"
    - "All gap observations from VERIFICATION.md are resolved"
    - "No test is skipped or xfailed — every test either passes or is updated to match new architecture"
  artifacts:
    - path: "AIM/tests/subagents/test_ci_pipeline_integration.py"
      provides: "Passing test suite"
  key_links:
    - from: "test methods"
      to: "CIOrchestrator methods"
      via: "analyzer fixture → CIOrchestrator instance → method calls"
      pattern: "analyzer._extract_tactics_from_matrix|analyzer._extract_swot_from_matrix"
    - from: "test imports"
      to: "ci_marketing_analysis module"
      via: "CiAnalysisResult, _tactic_impact_effort, SwotQuadrant, StealWorthyTactic"
      pattern: "from aim.services.ci_marketing_analysis import"
---

<objective>
Run the full CI integration test suite after Plan 21-03 (methods + proxy + serialization) and Plan 21-04 (SSE alias) fixes are applied, identify any remaining failures, and fix them to achieve 49/49 passing tests.

Purpose: Close gap SC8 — 24 tests currently fail. Plans 21-03 and 21-04 fix the root causes (missing methods, broken serialization, wrong function behavior, missing proxy), but edge cases may remain. This plan verifies everything passes and fixes any stragglers.

Output: Test suite at 49/49 passing, zero failures, zero errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-ci-pipeline-unification/21-VERIFICATION.md
@.planning/phases/21-ci-pipeline-unification/21-03-PLAN.md
@.planning/phases/21-ci-pipeline-unification/21-04-PLAN.md

After Plan 21-03 completes:
- CIOrchestrator has _extract_tactics_from_matrix, _extract_swot_from_matrix, _top_rec_from_matrix, _generate_analysis_summary
- CiMarketingAnalyzer is a thin proxy delegating to CIOrchestrator
- _tactic_impact_effort returns correct (impact, effort) tuples per test expectations
- AuditTask has to_dict() and from_dict() methods

After Plan 21-04 completes:
- /api/competitors/analyze/stream uses the shared CIOrchestrator singleton
- _run_quick_ci_analysis standalone function is removed
- /api/competitors/analyze endpoint uses shared orchestrator

Expected test class status after Plan 21-03:
| Test Class | Tests | Expected After 21-03 |
|-----------|-------|---------------------|
| TestWowEstimator | 5 | 5 pass ✅ |
| TestTacticClassifier | 3 | 3 pass (fixed _tactic_impact_effort) |
| TestTacticsExtraction | 6 | 6 pass (methods now on CIOrchestrator) |
| TestSwotExtraction | 4 | 4 pass (methods now on CIOrchestrator) |
| TestTopRecommendation | 3 | 3 pass (methods now on CIOrchestrator) |
| TestAnalysisSummary | 5 | 5 pass (methods now on CIOrchestrator) |
| TestCiAnalysisResult | 5 | 5 pass ✅ (CiAnalysisResult unchanged) |
| TestAuditTaskPersistence | 5 | 5 pass (to_dict/from_dict added) |
| TestCIOrchestratorStructure | 2 | 2 pass ✅ (stubs already removed) |
| TestModelsConsistency | 2 | 2 pass ✅ |
| TestUnifiedArchitecture | 9 | 9 pass ✅ |
| **Total** | **49** | **49 pass** |

Possible leftover failures and their fixes:
- TestTacticsExtraction failure on `test_weak_seo_tactic` or `test_no_pricing_tactic`: Adjust tactic detection logic to match test fixture data exactly
- TestSwotExtraction quadrant limits: Verify max 5 per quadrant is enforced
- TestAnalysisSummary section detection: Verify exact section headers match test expectations
- TestAuditTaskPersistence test_file_persist_and_load: Verify file I/O works in temp directory
- test_orchestrator_event_bus_injection: Verify scout agent is accessible and event_bus matches
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and analyze all failures</name>
  <files>AIM/tests/subagents/test_ci_pipeline_integration.py</files>
  <read_first>
AIM/tests/subagents/test_ci_pipeline_integration.py (full file — understand all test expectations)
  </read_first>
  <action>
Run the full test suite and analyze EVERY failure:

```bash
cd /Users/mikhaileliseev/Desktop/Dev/meAI && ./venv/bin/python -m pytest AIM/tests/subagents/test_ci_pipeline_integration.py -v 2>&1
```

Parse the output. For each failure:
1. Identify the exact assertion that failed and the actual vs expected values
2. Determine whether it's a logic bug in the new code (fix the source) or a test that needs updating (update the test)
3. If a test needs updating because the architecture changed legitimately, update it with a comment explaining why

**Priority rules:**
- Tests that test business logic (tactics, SWOT, recommendations, summary) → fix the source code to match test expectations. The tests define correct behavior.
- Tests that test removed code paths (_delegate_to_agent, _execute_single_agent, etc.) → these should already pass (CR-02 fixed in 21-01/21-02)
- Tests that test the new architecture (EventBus injection, _run_quick_analysis routing) → fix source if minor, update test if the architecture intent is truly different

**Known potential issues and fixes:**

1. `test_high_impact_keywords`: Fixed by Plan 21-03 Task 2. Verify.
2. `test_extracts_tactics_from_matrix`: Fixed by Plan 21-03 Task 1. Verify.
3. `test_weak_seo_tactic`: May fail if SEO exploit detection logic doesn't match the fixture data. The fixture has competitor with seo.score=45 (<60). Ensure the method detects this.
4. `test_no_pricing_tactic`: Test expects a pricing transparency tactic. The fixture has competitor with pricing_visible=False. Ensure the method detects this.
5. `test_swot_max_5`: Ensure each quadrant is capped at 5. Use slicing: `quadrant[:5]`.
6. `test_empty_matrix_defaults`: Ensure empty matrix returns defaults including "локальный рынок" in strengths.
7. `test_substantive_output`: Recommendation for 2-competitor matrix must be >40 chars. The format includes the competitor name in **bold**, SEO score, and descriptive text — verify it's long enough.
8. `test_has_all_sections`: Uses exact string "Обзор конкурентной среды" — ensure this exact text appears in the summary output.
9. `test_wow_section_skipped_when_empty`: Checks "Прогноз по пациентам" is NOT in output when wow={}. Verify the check is: `if not wow or not wow.get("patients_per_month"):`
10. `test_tactics_section_skipped_when_empty`: Checks "Что можно внедрить" is NOT in output when tactics=[]. Verify: `if not tactics:`
11. `test_to_dict`, `test_from_dict`, `test_roundtrip`: Fixed by Plan 21-03 Task 2. Verify.
12. `test_file_persist_and_load`: Uses json.dumps/loads with AuditTask serialization. Verify.
13. `test_ttl_expired_cleanup`: Uses AuditTask serialization. Verify.
14. `test_running_task_not_expired`: Uses AuditTask serialization. Verify.
15. `test_orchestrator_event_bus_injection`: Creates CIOrchestrator, calls `o._get_agent("ci-scout")`, asserts `scout.event_bus is eb`. If scout agent import fails, this returns None → AttributeError. May need to skip if scout agent module is unavailable in CI.
</action>
<verify>
  <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && ./venv/bin/python -m pytest AIM/tests/subagents/test_ci_pipeline_integration.py -v 2>&1 | tail -80</automated>
</verify>
<done>Full test suite run. Failures analyzed and root causes identified. List of remaining failures documented.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all remaining test failures to achieve 49/49 passing</name>
  <files>
AIM/tests/subagents/test_ci_pipeline_integration.py
AIM/src/aim/subagents/competitive_intel/orchestrator/ci_orchestrator.py
  </files>
  <read_first>
AIM/tests/subagents/test_ci_pipeline_integration.py (re-read after Task 1 analysis — focus on the specific failing tests)
AIM/src/aim/subagents/competitive_intel/orchestrator/ci_orchestrator.py (the four methods added in Plan 21-03 — check for logic issues)
  </read_first>
  <action>
Fix every remaining failure identified in Task 1. Apply fixes iteratively:

**For source code bugs (fix ci_orchestrator.py):**
- If tactic detection misses expected tactics → adjust the detection logic to match test fixture data patterns
- If SWOT quadrant > 5 items → add slicing
- If SWOT empty matrix defaults wrong → fix default strings
- If recommendation too short → add more descriptive text
- If summary missing sections → fix section headers and conditional logic
- If summary includes sections when it shouldn't → fix conditional checks

**For test expectation issues (fix test file):**
- Only update a test if the new architecture legitimately changes behavior AND the change is intentional
- Add a comment explaining why the test was updated
- Example: `# Updated for Phase 21: method moved from CiMarketingAnalyzer to CIOrchestrator`

**Fix-then-verify cycle:**
After each source fix:
```bash
./venv/bin/python -m pytest AIM/tests/subagents/test_ci_pipeline_integration.py -v 2>&1 | grep -E "PASSED|FAILED|ERROR|test_"
```

Stop when: `49 passed, 0 failed, 0 errors`.

**Specific fixes to apply (if tests still fail after Plan 21-03):**

1. `test_weak_seo_tactic` — if failing: Add SEO exploit detection to _extract_tactics_from_matrix:
   - Check if any competitor has `seo.score < 60` 
   - Create tactic: `StealWorthyTactic(source_competitor=weakest_seo['name'], tactic_description=f"SEO-оптимизация — обойти {weakest_seo['name']} в поиске", why_it_works=f"У {weakest_seo['name']} слабое SEO ({seo_score}/100)", expected_impact="High", estimated_effort="Medium")`

2. `test_no_pricing_tactic` — if failing: Add pricing transparency detection:
   - Check if any competitor has `website.pricing_visible == False` and client has `"цены"` in their website features
   - Create tactic: `StealWorthyTactic(source_competitor=no_pricing['name'], tactic_description="Прозрачные цены на сайте", why_it_works=f"{no_pricing['name']} не показывает цены, клиенты ищут прозрачность", expected_impact="High", estimated_effort="Low")`

3. `test_substantive_output` — if failing: Increase recommendation verbosity:
   - Current: f"Главная возможность — обойти **{name}** по SEO: у них {score}/100, исправьте ошибки которые мы нашли на их сайте, и вы выше."
   - Extended: add more context about why this matters and what specific action to take.

4. `test_has_all_sections` — if a section header is missing: Verify exact header strings:
   - "Обзор конкурентной среды" (not "## Обзор...")
   - "## По конкурентам"
   - "## SWOT-анализ"
   - "## Что можно внедрить"
   - "## Прогноз по пациентам"
   - "## Главная рекомендация"

5. `test_orchestrator_event_bus_injection` — if scout agent cannot be imported:
   - Check if the CIScoutAgent module exists and loads correctly
   - If import fails in test environment, wrap in try/except or mark as skip if module truly unavailable
</action>
<verify>
  <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && ./venv/bin/python -m pytest AIM/tests/subagents/test_ci_pipeline_integration.py -v 2>&1 | grep -E "passed|failed|error|==="</automated>
</verify>
<done>All 49 CI integration tests pass with 0 failures and 0 errors. Test output shows: "49 passed in X.XXs".</done>
</task>

<task type="auto">
  <name>Task 3: Final gap verification — confirm all VERIFICATION.md gaps are closed</name>
  <files>AIM/tests/subagents/test_ci_pipeline_integration.py</files>
  <read_first>
.planning/phases/21-ci-pipeline-unification/21-VERIFICATION.md (re-read the gaps section — verify each gap is now addressable)
  </read_first>
  <action>
Run the final comprehensive verification:

```bash
cd /Users/mikhaileliseev/Desktop/Dev/meAI && ./venv/bin/python -m pytest AIM/tests/subagents/test_ci_pipeline_integration.py -v 2>&1
```

Confirm output shows exactly: `49 passed in X.XXs`

Then verify each gap from VERIFICATION.md is closed:

**SC4 (CiMarketingAnalyzer proxy):** Verify proxy:
```bash
cd /Users/mikhaileliseev/Desktop/Dev/meAI && ./venv/bin/python -c "
from aim.services.ci_marketing_analysis import CiMarketingAnalyzer
import inspect
src = inspect.getsource(CiMarketingAnalyzer.analyze)
assert '_run_quick_analysis' in src or 'execute_ci_analysis' in src, 'analyze() does not delegate to CIOrchestrator'
print('SC4 CLOSED: CiMarketingAnalyzer is a proxy')
"
```

**SC7 (/analyze/stream alias):** Verify alias:
```bash
cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -c "_get_orchestrator" AIM/src/aim/api/competitors.py
```
Should return > 0.

**SC8 (49 tests pass):** Already confirmed by Task 2.

**H1 (Pipeline duplication):** Verify old path is gone:
```bash
cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -c "_delegate_to_agent\|_execute_single_agent\|_execute_phase_stub" AIM/src/aim/subagents/competitive_intel/orchestrator/ci_orchestrator.py
```
Should return 0.

**SC3 (EventBus delegation):** Verify:
```bash
cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -c "execute_ci_analysis" AIM/src/aim/subagents/competitive_intel/orchestrator/ci_orchestrator.py
```
Should return > 1 (both the method definition and the call from execute_task).

Run all checks sequentially and report the final gap closure status.
  </action>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && ./venv/bin/python -m pytest AIM/tests/subagents/test_ci_pipeline_integration.py -v 2>&1 | grep -E "passed|failed|error"</automated>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && ./venv/bin/python -c "
# SC4: CiMarketingAnalyzer proxy
from aim.services.ci_marketing_analysis import CiMarketingAnalyzer
import inspect
src = inspect.getsource(CiMarketingAnalyzer.analyze)
assert '_run_quick_analysis' in src or 'execute_ci_analysis' in src, 'SC4 NOT CLOSED: CiMarketingAnalyzer.analyze() does not delegate'
print('SC4 CLOSED: CiMarketingAnalyzer is a thin proxy')

# H1: No pipeline duplication
import subprocess
result = subprocess.run(['grep', '-c', r'_delegate_to_agent|_execute_single_agent|_execute_phase_stub',
    'AIM/src/aim/subagents/competitive_intel/orchestrator/ci_orchestrator.py'],
    capture_output=True, text=True)
stub_count = int(result.stdout.strip()) if result.stdout.strip().isdigit() else 0
assert stub_count == 0, f'H1 NOT CLOSED: {stub_count} stub methods remain'
print('H1 CLOSED: No legacy stub methods in CIOrchestrator')

# SC7: Stream alias
result2 = subprocess.run(['grep', '-c', '_get_orchestrator',
    'AIM/src/aim/api/competitors.py'],
    capture_output=True, text=True)
alias_count = int(result2.stdout.strip()) if result2.stdout.strip().isdigit() else 0
assert alias_count > 0, 'SC7 NOT CLOSED: Stream endpoint does not use _get_orchestrator'
print('SC7 CLOSED: Stream endpoint uses shared orchestrator singleton')

# SC3: execute_ci_analysis is the single execution path
result3 = subprocess.run(['grep', '-c', 'execute_ci_analysis',
    'AIM/src/aim/subagents/competitive_intel/orchestrator/ci_orchestrator.py'],
    capture_output=True, text=True)
exec_count = int(result3.stdout.strip()) if result3.stdout.strip().isdigit() else 0
assert exec_count >= 2, f'SC3 NOT CLOSED: execute_ci_analysis appears only {exec_count} times (expected >=2)'
print('SC3 CLOSED: execute_ci_analysis is the single execution path')

print('ALL GAPS CLOSED')
"</automated>
  </verify>
  <done>All VERIFICATION.md gaps confirmed closed: SC4 (proxy), SC7 (alias), SC8 (49/49 tests), H1 (no duplication), SC3 (EventBus-only path). 0 gaps remaining.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test assertions | Test code validates production code behavior |
| File I/O in AuditTask tests | Temp file reads/writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-16 | Tampering | Test file modifications | accept | Tests are internal; changes are intentional and documented |
</threat_model>

<verification>
## Verification

### Automated
1. `./venv/bin/python -m pytest AIM/tests/subagents/test_ci_pipeline_integration.py -v` — MUST show 49 passed, 0 failed
2. `grep -c "_delegate_to_agent\|_execute_single_agent\|_execute_phase_stub" AIM/src/aim/subagents/competitive_intel/orchestrator/ci_orchestrator.py` — MUST return 0
3. `grep -c "_get_orchestrator" AIM/src/aim/api/competitors.py` — MUST return > 0
4. `./venv/bin/python -c "from aim.services.ci_marketing_analysis import CiMarketingAnalyzer; import inspect; src = inspect.getsource(CiMarketingAnalyzer.analyze); assert '_run_quick_analysis' in src or 'execute_ci_analysis' in src"` — MUST pass

### Final success check
All 6 VERIFICATION.md gaps (SC2, SC3, SC4, SC7, SC8, H1) are closed. Only deferred items remain unaddressed.
</verification>

<success_criteria>
1. `pytest AIM/tests/subagents/test_ci_pipeline_integration.py -v` shows: **49 passed in X.XXs**
2. Zero test failures, zero test errors, zero test skips
3. CiMarketingAnalyzer.analyze() delegates to CIOrchestrator._run_quick_analysis() (proxy pattern)
4. No legacy stub methods (_delegate_to_agent, _execute_single_agent, _execute_phase_stub) on CIOrchestrator
5. /api/competitors/analyze/stream imports and uses _get_orchestrator from api.seo
6. All 6 VERIFICATION.md gaps confirmed closed
</success_criteria>

<output>
Create `.planning/phases/21-ci-pipeline-unification/21-05-SUMMARY.md` when done
</output>
