---
phase: 32-perplexity-pipeline
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - AIM/hermes/app/tools/quick_overview.py
  - AIM/hermes/app/tools/run_background_pipeline.py
autonomous: true
requirements: [PP-03, PP-04]

must_haves:
  truths:
    - "quick_overview tool calls Perplexity API and returns structured JSON with 6 sections"
    - "quick_overview has 15-second timeout and handles errors gracefully"
    - "run_background_pipeline sequentially executes all scout tools and saves results to session archive"
    - "run_background_pipeline calls generate_html_report after all tools complete"
    - "run_background_pipeline calls sell_presentation (Perplexity) as final step of Phase 3"
    - "Both tools register in aim-operations toolset with correct schemas"
  artifacts:
    - path: "AIM/hermes/app/tools/quick_overview.py"
      provides: "Perplexity-powered quick clinic overview (Phase 1)"
      min_lines: 100
    - path: "AIM/hermes/app/tools/run_background_pipeline.py"
      provides: "Background orchestration of all scout tools + Phase 3 presentation (Phases 2+3)"
      min_lines: 200
  key_links:
    - from: "quick_overview.py"
      to: "prompts/quick_overview.txt"
      via: "file read for Perplexity system+user prompt"
      pattern: "Path(__file__).parent.parent / \"prompts\" / \"quick_overview.txt\""
    - from: "run_background_pipeline.py"
      to: "prompts/sell_presentation.txt"
      via: "file read for Perplexity Phase 3 prompt"
      pattern: "Path(__file__).parent.parent / \"prompts\" / \"sell_presentation.txt\""
    - from: "run_background_pipeline.py"
      to: "session_archive.save_tool_output"
      via: "import and call after each tool"
      pattern: "from .session_archive import save_tool_output"
    - from: "run_background_pipeline.py"
      to: "generate_html_report module"
      via: "import and call at pipeline end"
      pattern: "from .generate_html_report import"
---

<objective>
Create two new Hermes tools: **quick_overview** (Perplexity-powered instant clinic analysis for Phase 1) and **run_background_pipeline** (orchestrates all scout tools in background for Phase 2, then generates sell presentation for Phase 3).

Purpose: quick_overview provides the 5-second "wow hook" that keeps leads engaged. run_background_pipeline does deep analysis while the client is away, producing a complete session archive and a Perplexity-generated sell presentation.
Output: Two registered Hermes tools (quick_overview, run_background_pipeline) in the aim-operations toolset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phase-32-perplexity-pipeline/32-01-PLAN.md
@AIM/hermes/app/tools/run_prescan.py
@AIM/hermes/app/tools/session_archive.py
@AIM/hermes/app/tools/run_full_scout.py
@AIM/hermes/app/tools/__init__.py
@docs/superpowers/specs/2026-06-17-3phase-pipeline-perplexity-design.md

<interfaces>
<!-- Key imports and patterns from existing tools that both new tools MUST follow -->

From tools/registry (hermes-agent package):
```python
from tools.registry import registry

registry.register(
    name="tool_name",           # unique name
    toolset="aim-operations",   # toolset for registration
    schema={...},               # OpenAI function calling schema
    handler=handle_function,    # async handler function
    check_fn=lambda: True,      # availability check
    is_async=True,              # async handler
    description="...",          # short description
    emoji="...",                # single emoji
)
```

From tools/session_archive.py:
```python
from .session_archive import save_tool_output, ensure_session_dir, upsert_metadata, load_all_data
```

Existing tool handler pattern (from run_prescan.py):
```python
async def handle_run_prescan(url=None, **kwargs) -> str:
    # Normalize args (hermes-agent passes whole dict as first param)
    if isinstance(url, dict):
        url = url.get("url", "")
    # ... implementation ...
    return json.dumps(result, ensure_ascii=False, indent=2)
```

Existing scout tool imports (from run_full_scout.py):
```python
from .run_prescan import handle_run_prescan
from .find_competitors import handle_find_competitors
from .run_ci_analysis import handle_run_ci_analysis
from .run_seo_audit import handle_run_seo_audit
from .run_content_analysis import handle_run_content_analysis
from .run_content_gaps import handle_run_content_gaps
from .run_review_platforms import handle_run_review_platforms
from .run_doctor_dossiers import handle_run_doctor_dossiers
from .run_pagespeed import handle_run_pagespeed
from .run_ads_intelligence import handle_run_ads_intelligence
from .run_ads_report import handle_run_ads_report
from .find_company_financials import handle_find_company_financials
from .run_instagram_content import handle_run_instagram_content
```

OpenAI SDK usage for Perplexity (base pattern):
```python
from openai import AsyncOpenAI

client = AsyncOpenAI(
    api_key=os.getenv("PERPLEXITY_API_KEY"),
    base_url="https://api.perplexity.ai",
)

response = await client.chat.completions.create(
    model="sonar-pro",
    messages=[
        {"role": "system", "content": system_prompt},
        {"role": "user", "content": user_prompt},
    ],
    timeout=15.0,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create quick_overview.py — Perplexity-powered instant clinic overview</name>
  <files>AIM/hermes/app/tools/quick_overview.py</files>
  <read_first>
AIM/hermes/app/tools/run_prescan.py — reference for tool registration pattern, arg normalization (dict unwrapping), httpx error handling, and push_tool_progress usage.
AIM/hermes/app/tools/session_archive.py — reference for save_tool_output function signature.
docs/superpowers/specs/2026-06-17-3phase-pipeline-perplexity-design.md — lines 75-106, sections "quick_overview (новый инструмент Hermes)" and "Промт Perplexity #1 — quick_overview".
  </read_first>
  <action>
Create `AIM/hermes/app/tools/quick_overview.py` — a Hermes tool that calls Perplexity API for instant clinic overview. Follow the exact patterns from run_prescan.py (arg normalization, error handling, progress pushing, json.dumps return).

**Structure:**

1. **Imports:** `json`, `logging`, `os`, `httpx`, `from pathlib import Path`, `from tools.registry import registry`, `from openai import AsyncOpenAI`.

2. **Constants:**
   - `REQUEST_TIMEOUT = 15.0` (seconds, Perplexity quick call)
   - `PERPLEXITY_MODEL = "sonar-pro"`
   - `PERPLEXITY_BASE_URL = "https://api.perplexity.ai"`

3. **Prompt loading helper** `_load_quick_overview_prompt() -> tuple[str, str]`:
   - Read `AIM/hermes/app/prompts/quick_overview.txt` using `Path(__file__).parent.parent / "prompts" / "quick_overview.txt"`
   - Split on `\n---\n` into system_prompt and user_prompt
   - Return `(system_prompt.strip(), user_prompt.strip())`

4. **Handler** `async def handle_quick_overview(url=None, **kwargs) -> str`:
   - **Arg normalization** (same pattern as run_prescan.py): if `url` is a dict, extract `url = url.get("url", "")`. If url is empty/None, return `json.dumps({"error": "url is required"})`.
   - **URL normalization:** Add `https://` prefix if URL doesn't start with `http://` or `https://`.
   - **API key check:** Read `PERPLEXITY_API_KEY` from env. If missing, return error JSON: `{"error": "PERPLEXITY_API_KEY not configured"}`.
   - **Progress push:** Call `from app.main import push_tool_progress` and push: `push_tool_progress("quick-overview", "⚡ Perplexity: собираю информацию о клинике…")`.
   - **Load prompt:** Call `_load_quick_overview_prompt()` to get system + user prompts, format user prompt with `.format(url=url)`.
   - **Perplexity call:** Create `AsyncOpenAI(api_key=..., base_url=PERPLEXITY_BASE_URL)`, call `client.chat.completions.create(model=PERPLEXITY_MODEL, messages=[...], timeout=REQUEST_TIMEOUT)`.
   - **Parse response:** Extract `response.choices[0].message.content`. Parse it as JSON if Perplexity returns JSON, otherwise wrap raw text.
   - **Error handling:**
     - `openai.APIConnectionError` → `json.dumps({"error": "Cannot reach Perplexity API", "detail": str(e)})`
     - `openai.RateLimitError` → `json.dumps({"error": "Perplexity rate limit exceeded", "detail": str(e)})`
     - `openai.APIStatusError` with 401/403 → `json.dumps({"error": "Perplexity API key invalid", "detail": str(e)})`
     - `httpx.TimeoutException` → `json.dumps({"error": "Perplexity timed out after 15s", "detail": str(e)})`
     - Generic `Exception` → `json.dumps({"error": "Unexpected error in quick_overview", "detail": str(e)})`
   - **Progress push on success:** `push_tool_progress("quick-overview", "✓ Обзор клиники готов — Perplexity собрал ключевые данные")`.
   - **Return:** `json.dumps(result, ensure_ascii=False, indent=2)` where result is a dict with keys: `url`, `model`, `sections` (the structured data from Perplexity), `raw_text` (fallback if JSON parsing fails).

5. **Registry registration** at module level (same pattern as run_prescan.py line 481-517):
   - `name="quick_overview"`
   - `toolset="aim-operations"`
   - schema with function name `quick_overview`, description in Russian explaining it's a Perplexity-powered instant clinic analysis, parameters: `url` (string, required, description: "URL сайта клиники")
   - `handler=handle_quick_overview`
   - `check_fn=lambda: bool(os.getenv("PERPLEXITY_API_KEY"))` — only available when key is configured
   - `is_async=True`
   - `description="Мгновенный обзор клиники через Perplexity (бизнес, врачи, конкуренты, соцсети, сайт, зацепка). 5-15 секунд."`
   - `emoji="⚡"`
  </action>
  <verify>
    <automated>python3 -c "
import ast, sys
path = '/Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/app/tools/quick_overview.py'
with open(path) as f:
    tree = ast.parse(f.read())
functions = {n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)}
assert 'handle_quick_overview' in functions, 'Missing handle_quick_overview'
# Check key constants and patterns
content = open(path).read()
assert 'PERPLEXITY_BASE_URL' in content, 'Missing PERPLEXITY_BASE_URL'
assert 'sonar-pro' in content, 'Missing sonar-pro model'
assert 'registry.register' in content, 'Missing registry.register call'
assert 'PERPLEXITY_API_KEY' in content, 'Missing PERPLEXITY_API_KEY check'
assert 'quick_overview.txt' in content, 'Missing prompt file reference'
assert 'push_tool_progress' in content, 'Missing push_tool_progress'
assert '15' in content or '15.0' in content, 'Missing 15s timeout'
print('OK: quick_overview.py structure valid')
"</automated>
  </verify>
  <acceptance_criteria>
    - `AIM/hermes/app/tools/quick_overview.py` exists
    - Contains `handle_quick_overview` async function with arg normalization (dict unwrapping)
    - Uses `AsyncOpenAI` with `base_url="https://api.perplexity.ai"` and `model="sonar-pro"`
    - Reads prompt from `prompts/quick_overview.txt` via `Path(__file__).parent.parent / "prompts" / "quick_overview.txt"`
    - Has 15-second httpx timeout
    - Handles `PERPLEXITY_API_KEY` missing with clear error
    - Handles Perplexity API errors: connection, rate limit, auth (401/403), timeout
    - Registers in aim-operations toolset via `registry.register()`
    - `check_fn` returns False when `PERPLEXITY_API_KEY` env var is not set
    - Calls `push_tool_progress` at start and on success
    - Returns JSON string via `json.dumps(..., ensure_ascii=False, indent=2)`
  </acceptance_criteria>
  <done>quick_overview tool ready — calls Perplexity for instant clinic overview, returns structured data in 5-15 seconds.</done>
</task>

<task type="auto">
  <name>Task 2: Create run_background_pipeline.py — background scout orchestration + Phase 3 sell presentation</name>
  <files>AIM/hermes/app/tools/run_background_pipeline.py</files>
  <read_first>
AIM/hermes/app/tools/run_full_scout.py — reference for sequential tool execution pattern, _run_phase helper, session archive saving, and generate_html_report integration. Lines 1-100 cover the pattern.
AIM/hermes/app/tools/session_archive.py — reference for save_tool_output, ensure_session_dir, upsert_metadata, load_all_data. Full file.
AIM/hermes/app/tools/__init__.py — reference for which tools are importable and registered.
docs/superpowers/specs/2026-06-17-3phase-pipeline-perplexity-design.md — lines 102-121, section "run_background_pipeline (новый инструмент Hermes)" and lines 122-126, section "sell_presentation (промт, не инструмент)".
  </read_first>
  <action>
Create `AIM/hermes/app/tools/run_background_pipeline.py` — a Hermes tool that sequentially runs all scout tools in background (Phase 2), saves results to session archive, generates HTML report, and calls Perplexity for the sell presentation (Phase 3).

**Structure:**

1. **Imports:** `asyncio`, `json`, `logging`, `os`, `time`, `from datetime import datetime, timezone`, `from pathlib import Path`, `from tools.registry import registry`, `from openai import AsyncOpenAI`, plus all scout tool handlers (see <interfaces> above for the full list from run_full_scout.py).

2. **Constants:**
   - `REQUEST_TIMEOUT = 3600.0` (1 hour for full pipeline)
   - `PERPLEXITY_MODEL = "sonar-pro"`
   - `PERPLEXITY_BASE_URL = "https://api.perplexity.ai"`

3. **Pipeline phase definitions** `PIPELINE_PHASES` — list of dicts, each with:
   - `name` (str): human-readable phase name for progress messages
   - `handler` (callable): the async handler function imported from the tool module
   - `kwargs_builder` (callable): function `(url, company_name, city, instagram) -> dict` that builds kwargs for the handler
   - `archive_key` (str): key for `save_tool_output(session_hash, key, data)`

   Phases in execution order:
   1. `find_competitors` — handler: `handle_find_competitors`, kwargs: `{"url": url}`. archive_key: `"competitors"`
   2. `run_ci_analysis` — handler: `handle_run_ci_analysis`, kwargs: `{"url": url, "competitors": ...}` (pass competitors from previous phase). archive_key: `"ci_analysis"`
   3. `run_seo_audit` — handler: `handle_run_seo_audit`, kwargs: `{"url": url}`. archive_key: `"seo_audit"`
   4. `run_content_analysis` — handler: `handle_run_content_analysis`, kwargs: `{"url": url}`. archive_key: `"content_analysis"`
   5. `run_content_gaps` — handler: `handle_run_content_gaps`, kwargs: `{"url": url}`. archive_key: `"content_gaps"`
   6. `run_review_platforms` — handler: `handle_run_review_platforms`, kwargs: `{"url": url, "company_name": company_name, "city": city}`. archive_key: `"review_platforms"`
   7. `run_doctor_dossiers` — handler: `handle_run_doctor_dossiers`, kwargs: `{"url": url, "company_name": company_name}`. archive_key: `"doctor_dossiers"`
   8. `run_pagespeed` — handler: `handle_run_pagespeed`, kwargs: `{"url": url}`. archive_key: `"pagespeed"`
   9. `run_ads_intelligence` — handler: `handle_run_ads_intelligence`, kwargs: `{"url": url, "company_name": company_name}`. archive_key: `"ads_intelligence"`
   10. `run_ads_report` — handler: `handle_run_ads_report`, kwargs: `{"url": url}`. archive_key: `"ads_report"`
   11. `find_company_financials` — handler: `handle_find_company_financials`, kwargs: `{"url": url, "company_name": company_name}`. archive_key: `"financials"`
   12. `run_instagram_content` — handler: `handle_run_instagram_content`, kwargs: `{"handle": instagram}` (if instagram handle known). archive_key: `"instagram_content"`

4. **Phase execution helper** `async def _run_phase(name, handler, kwargs, session_hash, archive_key) -> dict | None`:
   - Same pattern as run_full_scout.py `_run_phase` (lines 31-50)
   - Push progress via `push_tool_progress("bg-pipeline", f"🔄 {name}…")`
   - Call handler, measure elapsed time
   - Parse result (JSON string → dict)
   - If result has no error: `save_tool_output(session_hash, archive_key, result)`
   - Push result: `push_tool_progress("bg-pipeline", f"✓ {name} ({elapsed:.0f}s)")` or `⚠ {name}: {error}`
   - Return parsed result dict (for downstream phases that need it, e.g., ci_analysis needs competitors)

5. **Perplexity sell_presentation helper** `async def _generate_sell_presentation(session_hash: str) -> str`:
   - Load all session data via `load_all_data(session_hash)` from session_archive
   - Read `prompts/sell_presentation.txt`, split on `---` into system + user prompt
   - Extract `clinic_name`, `city`, `specialization` from prescan data in session archive
   - Format user prompt with `.format(clinic_name=..., city=..., specialization=..., all_data=json.dumps(all_data, ensure_ascii=False, indent=2))`
   - Call Perplexity via `AsyncOpenAI` with model `sonar-pro`, timeout 120s
   - Save result as `sell_presentation.json` in session archive
   - Return the presentation text

6. **Handler** `async def handle_run_background_pipeline(session_hash=None, url=None, company_name="", city="", specialization="", instagram="", **kwargs) -> str`:
   - **Arg normalization:** if `session_hash` is a dict, extract all fields
   - **Validate:** `session_hash` is required, return error if missing
   - **Ensure session dir:** `ensure_session_dir(session_hash)`
   - **Save metadata:** `upsert_metadata(session_hash, url=url, started_at=datetime.now(timezone.utc).isoformat(), pipeline="3-phase-perplexity")`
   - **Push start progress:** `push_tool_progress("bg-pipeline", "🚀 Запускаю фоновый анализ (12 инструментов, ~1 час)…")`
   - **Sequential execution:** Loop through `PIPELINE_PHASES`, calling `_run_phase` for each. Pass previous phase results where needed (ci_analysis needs competitors data). Handle per-phase errors gracefully — a failure in one phase does not abort the pipeline.
   - **Generate HTML report:** Import and call `handle_generate_html_report(session_hash=session_hash)`. Save result.
   - **Generate sell presentation:** Call `_generate_sell_presentation(session_hash)` — this is Phase 3.
   - **Push completion progress:** `push_tool_progress("bg-pipeline", "✅ Фоновый анализ завершён! Все данные сохранены, HTML-отчёт и презентация готовы.")`
   - **Update metadata:** `upsert_metadata(session_hash, completed_at=datetime.now(timezone.utc).isoformat(), phases_completed=N, phases_failed=M)`
   - **Return:** JSON with summary: `{"success": True, "session_hash": session_hash, "phases_completed": N, "phases_failed": M, "phases": [...], "html_report_url": "...", "sell_presentation_saved": True}`

7. **Registry registration** at module level:
   - `name="run_background_pipeline"`
   - `toolset="aim-operations"`
   - schema parameters: `session_hash` (string, required), `url` (string, required), `company_name` (string), `city` (string), `specialization` (string), `instagram` (string)
   - `handler=handle_run_background_pipeline`
   - `check_fn=lambda: True` (always available — falls back gracefully if some tools unavailable)
   - `is_async=True`
   - `description="Фоновый анализ клиники (12 инструментов, ~1 час) + HTML-отчёт + продающая презентация. Вызывать после collect_contact."`
   - `emoji="🔄"`
  </action>
  <verify>
    <automated>python3 -c "
import ast, sys
path = '/Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/app/tools/run_background_pipeline.py'
with open(path) as f:
    tree = ast.parse(f.read())
functions = {n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)}
assert 'handle_run_background_pipeline' in functions, 'Missing handle_run_background_pipeline'
assert '_generate_sell_presentation' in functions, 'Missing _generate_sell_presentation'
assert '_run_phase' in functions, 'Missing _run_phase'
content = open(path).read()
assert 'registry.register' in content, 'Missing registry.register call'
assert '3600' in content, 'Missing 3600s timeout'
assert 'sell_presentation.txt' in content, 'Missing sell_presentation prompt reference'
assert 'save_tool_output' in content, 'Missing save_tool_output call'
assert 'load_all_data' in content, 'Missing load_all_data call'
assert 'ensure_session_dir' in content, 'Missing ensure_session_dir call'
assert 'upsert_metadata' in content, 'Missing upsert_metadata call'
assert 'handle_generate_html_report' in content, 'Missing handle_generate_html_report call'
assert 'handle_find_competitors' in content, 'Missing find_competitors handler'
assert 'handle_run_ci_analysis' in content, 'Missing run_ci_analysis handler'
assert 'handle_run_seo_audit' in content, 'Missing run_seo_audit handler'
assert 'PERPLEXITY_API_KEY' in content, 'Missing PERPLEXITY_API_KEY check'
assert 'sonar-pro' in content, 'Missing sonar-pro model'
print('OK: run_background_pipeline.py structure valid')
"</automated>
  </verify>
  <acceptance_criteria>
    - `AIM/hermes/app/tools/run_background_pipeline.py` exists
    - Contains `handle_run_background_pipeline` async function with arg normalization
    - Defines pipeline phases as a list of dicts with name, handler, kwargs_builder, archive_key
    - Includes all 12 scout tools: find_competitors, run_ci_analysis, run_seo_audit, run_content_analysis, run_content_gaps, run_review_platforms, run_doctor_dossiers, run_pagespeed, run_ads_intelligence, run_ads_report, find_company_financials, run_instagram_content
    - Has `_run_phase` helper following run_full_scout.py pattern
    - Saves each phase result via `save_tool_output(session_hash, archive_key, result)`
    - Calls `handle_generate_html_report` after all phases
    - Has `_generate_sell_presentation` function that loads all session data, reads sell_presentation.txt prompt, calls Perplexity API, saves result
    - Uses `AsyncOpenAI` with base_url `https://api.perplexity.ai` and model `sonar-pro`
    - Has 3600-second (1 hour) total timeout
    - Per-phase errors do not abort the pipeline (graceful degradation)
    - Updates metadata.json with started_at, completed_at, phases_completed, phases_failed
    - Registers in aim-operations toolset via `registry.register()`
    - Returns JSON with success status, counts, and phase summaries
  </acceptance_criteria>
  <done>run_background_pipeline tool ready — runs all 12 scout tools in background, generates HTML report, and creates Perplexity sell presentation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hermes → Perplexity API | External LLM service — prompt text and session data sent over HTTPS |
| Hermes → AIM API (app:8000) | Internal Docker network — tool handlers call AIM API endpoints |
| Perplexity response → session archive | Perplexity-generated content saved to disk — sanitize before file write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06 | Spoofing | quick_overview Perplexity call | mitigate | Validate PERPLEXITY_API_KEY at handler init; handle 401/403/401 errors explicitly |
| T-32-07 | Tampering | Perplexity JSON response parsing | mitigate | Try/except on json.loads of Perplexity content; fall back to raw_text key on parse failure |
| T-32-08 | Information Disclosure | sell_presentation — session data sent to Perplexity | mitigate | Send only aggregated tool outputs (no raw PII beyond what tools already expose); all session data already sanitized by scout tools |
| T-32-09 | Denial of Service | run_background_pipeline 1-hour timeout | mitigate | 3600s timeout; per-phase try/except ensures one failing tool doesn't kill pipeline; asyncio.gather not used (sequential only) |
| T-32-10 | Elevation of Privilege | tool handler imports | accept | All handlers are internal imports within the same container; no privilege boundary crossed |
| T-32-11 | Tampering | URL injection in Perplexity prompt | mitigate | URL validated before insertion into prompt; prompt uses `.format(url=url)` with already-sanitized value |
</threat_model>

<verification>
After all tasks complete:
1. `quick_overview.py` passes structure validation (handler function, registry call, Perplexity integration)
2. `run_background_pipeline.py` passes structure validation (handler, _run_phase, _generate_sell_presentation, all 12 tools referenced, registry call)
3. Both files import from correct paths and follow existing tool patterns
</verification>

<success_criteria>
- quick_overview tool calls Perplexity API with 15s timeout, returns structured overview
- run_background_pipeline tool orchestrates all 12 scout tools sequentially, saves to session archive
- run_background_pipeline generates HTML report and Perplexity sell presentation as final steps
- Both tools register in aim-operations toolset with correct schemas
- Error handling is defensive: missing keys, API failures, timeouts all produce structured error JSON
</success_criteria>

<output>
Create `.planning/phases/32-perplexity-pipeline/32-02-SUMMARY.md` when done
</output>
