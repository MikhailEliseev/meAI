---
phase: 13-landing-marketing
plan: 02
type: execute
wave: 2
depends_on: ["13-04"]
files_modified:
  - AIM/src/aim/subagents/ads/yandex_direct_client.py
  - AIM/src/aim/subagents/ads/ad_copy_generator.py
  - AIM/tests/unit/test_yandex_direct_stats.py
  - AIM/tests/unit/test_ad_copy_compliance.py
autonomous: true
requirements:
  - MKTG-01

must_haves:
  truths:
    - "get_campaign_stats() returns real data from Yandex Direct Reports API, not hardcoded values"
    - "TSV report response is parsed correctly into CampaignStats dataclass instances"
    - "Ad copy generator enforces ФЗ-38 medical advertising compliance: mandatory disclaimer, age restriction, license number, prohibited claims"
    - "All existing tests still pass after modifications"
    - "API-retrieved Yandex Direct campaign data is synced to the Campaign database table via sync_campaigns_to_db() so the attribution pipeline has data to query"
  artifacts:
    - path: "AIM/src/aim/subagents/ads/yandex_direct_client.py"
      provides: "Real Yandex Direct campaign statistics via Reports API"
      contains: "async def get_campaign_stats("
      min_lines: 500
    - path: "AIM/src/aim/subagents/ads/ad_copy_generator.py"
      provides: "ФЗ-38 medical advertising compliance checks"
      contains: "ФЗ_38_DISCLAIMER"
      min_lines: 670
    - path: "AIM/tests/unit/test_yandex_direct_stats.py"
      provides: "Tests for TSV parsing and real stats retrieval"
      contains: "def test_real_tsv_parsing"
    - path: "AIM/src/aim/subagents/ads/yandex_direct_client.py"
      provides: "Campaign DB sync method writes API data to Campaign model"
      contains: "async def sync_campaigns_to_db"
    - path: "AIM/tests/unit/test_ad_copy_compliance.py"
      provides: "Tests for ФЗ-38 compliance checks"
      contains: "def test_medical_disclaimer_required"
  key_links:
    - from: "yandex_direct_client.py::get_campaign_stats"
      to: "Yandex Direct Reports API (POST /reports)"
      via: "httpx.AsyncClient with TSV response parsing"
      pattern: "csv\\.DictReader.*delimiter='\\\\t'"
    - from: "ad_copy_generator.py::_check_compliance"
      to: "ФЗ-38 mandatory disclaimer text"
      via: "injection into ad copy"
      pattern: "ИМЕЮТСЯ ПРОТИВОПОКАЗАНИЯ"
    - from: "yandex_direct_client.py::sync_campaigns_to_db"
      to: "campaign_models.py::Campaign (SQLAlchemy)"
      via: "async DB session write"
      pattern: "db\\.add\\(Campaign"
---

<objective>
Fix the Yandex Direct MOCK stats (replace hardcoded CampaignStats with real TSV report parsing) and add ФЗ-38 medical advertising compliance checks to the AdCopyGenerator.

Purpose: Every downstream system (ROI calculator, dashboard, budget optimizer) depends on real campaign stats. Without this fix, all marketing analytics produce fake data. ФЗ-38 compliance is legally required for medical advertising in Russia.

Output: Real Yandex Direct stats via Reports API + ФЗ-38 compliant ad copy generation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AIM/src/aim/subagents/ads/yandex_direct_client.py
@AIM/src/aim/subagents/ads/ad_copy_generator.py
@AIM/tests/conftest.py
@.planning/phases/13-landing-marketing/13-RESEARCH.md

<interfaces>
<!-- Key types from yandex_direct_client.py the executor needs -->
From yandex_direct_client.py:
```python
@dataclass
class CampaignStats:
    campaign_id: int
    impressions: int
    clicks: int
    cost: float
    conversions: int
    ctr: float
    cpc: float
    cpa: float
    date: str

class YandexDirectAPIClient:
    def __init__(self, token: str | None = None):
        self.token = token
        self.base_url = "https://api.direct.yandex.com/json/v5"
        self.timeout = httpx.Timeout(30.0)
        self.logger = structlog.get_logger()
    
    # MOCK method to fix (lines 180-264):
    async def get_campaign_stats(self, campaign_ids: list[int], date_from: str, date_to: str) -> list[CampaignStats]:
        # CURRENTLY returns hardcoded CampaignStats for EACH campaign_id
```

From ad_copy_generator.py:
```python
@dataclass
class ComplianceCheck:
    platform: str          # "yandex", "google"
    is_compliant: bool
    violations: list[str]
    warnings: list[str]

class AdCopyGenerator:
    def __init__(self):
        self.forbidden_words = ["лучший", "самый", "номер 1", "100%", "бесплатно", "даром"]
    
    def _check_compliance(self, headline: str, description: str, platform: str) -> ComplianceCheck:
        # Checks forbidden words, length limits, caps, exclamation marks
```

RESEARCH.md specifies:
- Yandex Reports API: POST /reports with async generation (201→202→200), TSV response
- ФЗ-38 disclaimers: "ИМЕЮТСЯ ПРОТИВОПОКАЗАНИЯ, НЕОБХОДИМА КОНСУЛЬТАЦИЯ СПЕЦИАЛИСТА", age 18+, license number, ЕРИР token
- Python `csv` module with `dialect='excel-tab'` for TSV parsing
- Prohibited medical claims: guaranteed results, "100% cure", superlatives
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace MOCK stats with real Yandex Direct Reports API TSV parsing</name>
  <files>AIM/src/aim/subagents/ads/yandex_direct_client.py</files>
  <read_first>
    - AIM/src/aim/subagents/ads/yandex_direct_client.py (current state: MOCK on lines 244-257)
    - .planning/phases/13-landing-marketing/13-RESEARCH.md (lines 554-642: real TSV parsing example)
  </read_first>
  <behavior>
    - Test 1: `get_campaign_stats([123], "2026-05-01", "2026-05-20")` with mocked httpx response (TSV string) returns correctly parsed CampaignStats with campaign_id=123, impressions=5678, clicks=234, cost=12345.67, conversions=12, ctr=4.12, cpc=52.76, cpa=1028.81, date="2026-05-20"
    - Test 2: `get_campaign_stats([999], "2026-01-01", "2026-01-31")` with empty TSV returns empty list []
    - Test 3: Multiple campaign_ids in request produce stats for ALL campaigns in TSV response
  </behavior>
  <action>
Replace the MOCK stats block in `get_campaign_stats()` (lines 241-257 of yandex_direct_client.py) with real Yandex Direct Reports API implementation:

1. **Keep the CampaignStats dataclass unchanged.** Keep the existing method signature unchanged.

2. **Replace the MOCK for-loop (lines 242-257)** with real TSV report generation + parsing:

```python
import csv
import io
import asyncio

# Build Reports API request (replacing the current payload on lines 210-232)
payload = {
    "params": {
        "SelectionCriteria": {
            "DateFrom": date_from,
            "DateTo": date_to,
            "Filter": [{
                "Field": "CampaignId",
                "Operator": "IN",
                "Values": [str(cid) for cid in campaign_ids],
            }],
        },
        "FieldNames": [
            "Date", "CampaignId", "CampaignName",
            "Impressions", "Clicks", "Cost",
            "Conversions", "Ctr", "AvgCpc", "AvgCpa",
        ],
        "ReportName": f"Campaign_Stats_{date_from}_{date_to}",
        "ReportType": "CAMPAIGN_PERFORMANCE_REPORT",
        "DateRangeType": "CUSTOM_DATE",
        "Format": "TSV",
        "IncludeVAT": "YES",
    }
}

# Headers for async report generation
headers = {
    "Authorization": f"Bearer {self.token}",
    "Accept-Language": "ru",
    "processingMode": "auto",
    "skipReportHeader": "true",
    "skipColumnHeader": "true",
    "skipReportSummary": "true",
}

# POST to /reports
response = await client.post(
    f"{self.base_url}/reports",
    json=payload,
    headers=headers,
)

# Handle async report generation (HTTP 201/202 → poll with retryIn header)
while response.status_code in (201, 202):
    retry_in = int(response.headers.get("retryIn", 5))
    await asyncio.sleep(retry_in)
    response = await client.get(
        f"{self.base_url}/reports",
        headers=headers,
    )

response.raise_for_status()

# Parse TSV response using csv module
tsv_data = response.text
reader = csv.DictReader(
    io.StringIO(tsv_data),
    delimiter='\t',
)

stats = []
for row in reader:
    # Yandex returns costs in micros (1/1,000,000 of currency unit)
    cost_micros = float(row.get("Cost", "0"))
    cpc_micros = float(row.get("AvgCpc", "0"))
    cpa_micros = float(row.get("AvgCpa", "0"))

    stats.append(CampaignStats(
        campaign_id=int(row["CampaignId"]),
        impressions=int(row.get("Impressions", "0")),
        clicks=int(row.get("Clicks", "0")),
        cost=round(cost_micros / 1_000_000, 2),  # micros → RUB
        conversions=int(row.get("Conversions", "0")),
        ctr=round(float(row.get("Ctr", "0")), 2),
        cpc=round(cpc_micros / 1_000_000, 2) if cpc_micros else 0.0,
        cpa=round(cpa_micros / 1_000_000, 2) if cpa_micros else 0.0,
        date=row.get("Date", date_to),
    ))

return stats
```

3. **Add `import csv`, `import io`, `import asyncio`** at the top of the file (asyncio may already be imported — check first, don't duplicate).

4. **Do NOT modify:** CampaignStats dataclass, CampaignInfo dataclass, BudgetRecommendation dataclass, __init__, get_campaigns(), optimize_budgets(), create_campaign(), or main().

5. **Do NOT remove the async with httpx block** — the new code replaces ONLY the MOCK for-loop inside the existing async with block.
</action>
  <verify>
    <automated>pytest AIM/tests/unit/test_yandex_direct_stats.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - yandex_direct_client.py no longer contains the string "Mock stats for now"
    - yandex_direct_client.py contains `csv.DictReader` with `delimiter='\t'`
    - yandex_direct_client.py contains `Cost.*1_000_000` (micros to RUB conversion)
    - yandex_direct_client.py contains `processingMode.*auto` header
    - yandex_direct_client.py contains `while response.status_code in (201, 202):` for async polling
    - grep -c "Mock stats" yandex_direct_client.py returns 0
  </acceptance_criteria>
  <done>
get_campaign_stats() calls Yandex Reports API, parses real TSV data, returns actual per-campaign stats. No hardcoded values remain. All existing tests pass.
</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ФЗ-38 medical advertising compliance to AdCopyGenerator</name>
  <files>AIM/src/aim/subagents/ads/ad_copy_generator.py</files>
  <read_first>
    - AIM/src/aim/subagents/ads/ad_copy_generator.py (current ComplianceCheck + _check_compliance method)
    - .planning/phases/13-landing-marketing/13-RESEARCH.md (lines 504-519: Pitfall 2 — ФЗ-38 requirements)
  </read_first>
  <behavior>
    - Test 1: Ad copy with medical keyword "имплантация зубов" but WITHOUT disclaimer text → ComplianceCheck.violations contains "ФЗ-38: отсутствует обязательное предупреждение"
    - Test 2: Ad copy WITH disclaimer "ИМЕЮТСЯ ПРОТИВОПОКАЗАНИЯ, НЕОБХОДИМА КОНСУЛЬТАЦИЯ СПЕЦИАЛИСТА" → is_compliant=True
    - Test 3: Ad copy containing prohibited claim "гарантированный результат" → violations contains "запрещённое утверждение эффективности"
    - Test 4: Non-medical ad copy (e.g., "маркетинговое агентство") without disclaimer → is_compliant=True, NO medical violations
    - Test 5: Medical ad with age restriction "18+" → no age violation (18+ is correct for medical)
  </behavior>
  <action>
Add ФЗ-38 "О рекламе" medical advertising compliance to the existing `_check_compliance()` method in ad_copy_generator.py:

1. **Add ФЗ-38 constants at class level** (inside `__init__`, after existing `self.forbidden_words`):

```python
# ФЗ-38 "О рекламе" — mandatory medical advertising compliance
# Source: Федеральный закон от 13.03.2006 N 38-ФЗ, статья 24
self.FZ38_MANDATORY_DISCLAIMER = "ИМЕЮТСЯ ПРОТИВОПОКАЗАНИЯ, НЕОБХОДИМА КОНСУЛЬТАЦИЯ СПЕЦИАЛИСТА"

# Medical service keywords — trigger ФЗ-38 compliance checks
self.MEDICAL_KEYWORDS = [
    "имплант", "имплантат", "стоматолог", "ортодонт",
    "хирург", "операци", "лечени", "диагност", "клиник",
    "врач", "медицин", "пациент", "здоров", "болезн",
    "терап", "протез", "реабилитац", "анализ",
]

# Prohibited efficacy claims (ФЗ-38 ст.24 ч.7-8)
self.PROHIBITED_MEDICAL_CLAIMS = [
    "гарантированный результат",
    "100% излечение",
    "полное выздоровление",
    "лучшее лечение",
    "самый эффективный",
    "без побочных эффектов",
    "безопасно на 100%",
    "излечивает навсегда",
]
```

2. **Extend `_check_compliance()` method** — add after existing forbidden_words check (before the length check), inside the method body after `violations = []` and `warnings = []`:

```python
# Check if this is a medical ad (triggers ФЗ-38 rules)
text_lower = f"{headline} {description}".lower()
is_medical_ad = any(kw in text_lower for kw in self.MEDICAL_KEYWORDS)

if is_medical_ad:
    # ФЗ-38 ст.24 ч.1: Mandatory disclaimer
    if self.FZ38_MANDATORY_DISCLAIMER.lower() not in text_lower:
        violations.append(
            "ФЗ-38: отсутствует обязательное предупреждение "
            "'ИМЕЮТСЯ ПРОТИВОПОКАЗАНИЯ, НЕОБХОДИМА КОНСУЛЬТАЦИЯ СПЕЦИАЛИСТА'"
        )
    
    # ФЗ-38 ст.24 ч.7: Prohibited efficacy claims
    for claim in self.PROHIBITED_MEDICAL_CLAIMS:
        if claim in text_lower:
            violations.append(
                f"ФЗ-38: запрещённое утверждение эффективности — '{claim}'"
            )
```

3. **Add age restriction check** — after the prohibited claims check:

```python
    # ФЗ-38 ст.24: Age restriction check (medical ads require 18+)
    age_patterns = ["0+", "6+", "12+", "16+"]
    found_age = False
    for pattern in age_patterns:
        if pattern in f"{headline} {description}":
            found_age = True
            warnings.append(
                f"ФЗ-38: медицинская реклама должна иметь маркировку 18+, "
                f"не {pattern}"
            )
            break
    if not found_age and "18+" not in f"{headline} {description}":
        warnings.append(
            "ФЗ-38: рекомендуется указать возрастную маркировку 18+ "
            "для рекламы медицинских услуг"
        )
```

4. **Add ЕРИР token check warning** — after age restriction check:

```python
    # ЕРИР маркировка (Федеральный закон от 02.07.2021 N 347-ФЗ)
    # All internet ads must carry ЕРИР token
    if "erir" not in text_lower and "ерир" not in text_lower:
        warnings.append(
            "ЕРИР: интернет-реклама должна содержать маркировку "
            "(токен ЕРИР / erir_token)"
        )
```

5. **Fix the duplicated `generate_hashtags` functions** at the bottom of the file (lines 523-659): these are 3 identical copies of the same function. Delete lines 565-659 (the 2nd and 3rd copies), keeping only the first copy (lines 517-563). After removal, verify the remaining function body is complete and syntactically valid.

6. **Do NOT modify:** Existing `forbidden_words` check, length limit checks, capitalization/exclamation warnings, `generate()`, `_generate_headlines()`, `_generate_descriptions()`, `_generate_ctas()`, `_create_variants()`, or `main()`.
</action>
  <verify>
    <automated>pytest AIM/tests/unit/test_ad_copy_compliance.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - ad_copy_generator.py contains `FZ38_MANDATORY_DISCLAIMER` constant with exact text `ИМЕЮТСЯ ПРОТИВОПОКАЗАНИЯ, НЕОБХОДИМА КОНСУЛЬТАЦИЯ СПЕЦИАЛИСТА`
    - ad_copy_generator.py contains `MEDICAL_KEYWORDS` list with at least 10 medical terms
    - ad_copy_generator.py contains `PROHIBITED_MEDICAL_CLAIMS` list
    - _check_compliance() checks for is_medical_ad before applying ФЗ-38 rules
    - grep -c "def generate_hashtags" ad_copy_generator.py returns 1 (exactly one copy, not 3)
    - ad_copy_generator.py no longer contains duplicate function definitions after line 563
  </acceptance_criteria>
  <done>
AdCopyGenerator enforces ФЗ-38: medical ads require disclaimer text, prohibited claims blocked, age restriction warnings, ЕРИР token warnings. Duplicate function definitions removed. Existing ad copy generation still works.
</done>
</task>

<task type="auto">
  <name>Task 3: Write tests for real stats parsing and ФЗ-38 compliance</name>
  <files>AIM/tests/unit/test_yandex_direct_stats.py, AIM/tests/unit/test_ad_copy_compliance.py</files>
  <read_first>
    - AIM/tests/conftest.py (test fixtures and patterns)
    - AIM/src/aim/subagents/ads/yandex_direct_client.py (CampaignStats dataclass shape)
    - AIM/src/aim/subagents/ads/ad_copy_generator.py (ComplianceCheck dataclass shape)
  </read_first>
  <action>
Create two test files using pytest patterns from existing tests in `AIM/tests/unit/`:

**File 1: `AIM/tests/unit/test_yandex_direct_stats.py`**

```python
"""Tests for Yandex Direct real stats (TSV parsing)."""
import csv
import io
import pytest
from unittest.mock import AsyncMock, patch, MagicMock
from aim.subagents.ads.yandex_direct_client import YandexDirectAPIClient, CampaignStats


SAMPLE_TSV = (
    "CampaignId\tDate\tImpressions\tClicks\tCost\tConversions\tCtr\tAvgCpc\tAvgCpa\r\n"
    "123\t2026-05-20\t5678\t234\t12345670\t12\t4.12\t52760000\t1028810000\r\n"
    "456\t2026-05-20\t3200\t89\t8900000\t3\t2.78\t100000000\t2966666667\r\n"
)

EMPTY_TSV = ""


def make_mock_response(status_codes, body):
    """Helper to build mock httpx response with status_code attribute."""
    class MockResponse:
        def __init__(self, status, body):
            self.status_code = status
            self.text = body
            self.headers = {}
        def raise_for_status(self):
            if self.status_code >= 400:
                raise Exception(f"HTTP {self.status_code}")
    return MockResponse(status_codes, body)


@pytest.mark.asyncio
async def test_real_tsv_parsing():
    """TSV response with real data parses into correct CampaignStats."""
    client = YandexDirectAPIClient(token="test_token")

    with patch("httpx.AsyncClient") as mock_client_cls:
        mock_client = AsyncMock()
        mock_client_cls.return_value.__aenter__.return_value = mock_client
        mock_client.post.return_value = make_mock_response(200, SAMPLE_TSV)

        stats = await client.get_campaign_stats(
            campaign_ids=[123, 456],
            date_from="2026-05-01",
            date_to="2026-05-20",
        )

    assert len(stats) == 2

    # Campaign 123
    assert stats[0].campaign_id == 123
    assert stats[0].impressions == 5678
    assert stats[0].clicks == 234
    assert stats[0].cost == pytest.approx(12.35, rel=0.01)  # 12345670 / 1e6
    assert stats[0].conversions == 12
    assert stats[0].ctr == pytest.approx(4.12, rel=0.01)
    assert stats[0].cpc == pytest.approx(52.76, rel=0.01)
    assert stats[0].cpa == pytest.approx(1028.81, rel=0.01)
    assert stats[0].date == "2026-05-20"

    # Campaign 456
    assert stats[1].campaign_id == 456
    assert stats[1].impressions == 3200
    assert stats[1].clicks == 89


@pytest.mark.asyncio
async def test_empty_tsv_returns_empty_list():
    """Empty TSV response returns empty stats list."""
    client = YandexDirectAPIClient(token="test_token")

    with patch("httpx.AsyncClient") as mock_client_cls:
        mock_client = AsyncMock()
        mock_client_cls.return_value.__aenter__.return_value = mock_client
        mock_client.post.return_value = make_mock_response(200, EMPTY_TSV)

        stats = await client.get_campaign_stats(
            campaign_ids=[999],
            date_from="2026-01-01",
            date_to="2026-01-31",
        )

    assert stats == []


@pytest.mark.asyncio
async def test_async_report_polling():
    """HTTP 202 then 200 response — polls correctly and parses final TSV."""
    client = YandexDirectAPIClient(token="test_token")

    with patch("httpx.AsyncClient") as mock_client_cls:
        mock_client = AsyncMock()
        mock_client_cls.return_value.__aenter__.return_value = mock_client

        # First response: 202 (still generating)
        resp_202 = make_mock_response(202, "")
        resp_202.headers = {"retryIn": "1"}

        # Second response: 200 with data
        resp_200 = make_mock_response(200, SAMPLE_TSV)

        mock_client.post.return_value = resp_202
        mock_client.get.return_value = resp_200

        stats = await client.get_campaign_stats(
            campaign_ids=[123],
            date_from="2026-05-01",
            date_to="2026-05-20",
        )

    assert len(stats) == 2  # SAMPLE_TSV has 2 rows
    mock_client.get.assert_called_once()  # Polled once
```

**File 2: `AIM/tests/unit/test_ad_copy_compliance.py`**

```python
"""Tests for ФЗ-38 medical advertising compliance."""
import pytest
from aim.subagents.ads.ad_copy_generator import AdCopyGenerator, ComplianceCheck


@pytest.fixture
def generator():
    return AdCopyGenerator()


def test_medical_disclaimer_required(generator):
    """Medical ad without disclaimer → violation."""
    result = generator._check_compliance(
        headline="Имплантация зубов Nobel Biocare",
        description="Качественная установка имплантов. Запишитесь сегодня.",
        platform="yandex",
    )
    assert not result.is_compliant
    assert any("ФЗ-38" in v and "отсутствует" in v for v in result.violations), \
        f"Expected ФЗ-38 disclaimer violation, got: {result.violations}"


def test_medical_disclaimer_present_passes(generator):
    """Medical ad WITH disclaimer → compliant."""
    result = generator._check_compliance(
        headline="Имплантация зубов Nobel Biocare",
        description=(
            "Качественная установка имплантов. "
            "ИМЕЮТСЯ ПРОТИВОПОКАЗАНИЯ, НЕОБХОДИМА КОНСУЛЬТАЦИЯ СПЕЦИАЛИСТА. "
            "Запишитесь сегодня."
        ),
        platform="yandex",
    )
    assert result.is_compliant


def test_non_medical_ad_no_disclaimer_needed(generator):
    """Non-medical ad without disclaimer → still compliant."""
    result = generator._check_compliance(
        headline="Маркетинговое агентство AIM",
        description="Продвижение медицинских клиник. Узнайте больше.",
        platform="yandex",
    )
    # May have other violations (forbidden words etc) but no ФЗ-38 violation
    fz38_violations = [v for v in result.violations if "ФЗ-38" in v]
    assert len(fz38_violations) == 0


def test_prohibited_efficacy_claim(generator):
    """Medical ad with 'гарантированный результат' → violation."""
    result = generator._check_compliance(
        headline="Лечение зубов — гарантированный результат",
        description=(
            "Профессиональная стоматология. "
            "ИМЕЮТСЯ ПРОТИВОПОКАЗАНИЯ, НЕОБХОДИМА КОНСУЛЬТАЦИЯ СПЕЦИАЛИСТА."
        ),
        platform="yandex",
    )
    assert any("запрещённое утверждение" in v for v in result.violations), \
        f"Expected prohibited claim violation, got: {result.violations}"


def test_age_restriction_warning_16plus(generator):
    """Medical ad with 16+ → warning (should be 18+)."""
    result = generator._check_compliance(
        headline="Стоматологическая клиника 16+",
        description=(
            "Лечение зубов. "
            "ИМЕЮТСЯ ПРОТИВОПОКАЗАНИЯ, НЕОБХОДИМА КОНСУЛЬТАЦИЯ СПЕЦИАЛИСТА."
        ),
        platform="yandex",
    )
    assert any("18+" in w for w in result.warnings), \
        f"Expected 18+ age warning, got: {result.warnings}"


def test_age_restriction_18plus_ok(generator):
    """Medical ad with 18+ → no age violation warning."""
    result = generator._check_compliance(
        headline="Стоматологическая клиника 18+",
        description=(
            "Лечение зубов. "
            "ИМЕЮТСЯ ПРОТИВОПОКАЗАНИЯ, НЕОБХОДИМА КОНСУЛЬТАЦИЯ СПЕЦИАЛИСТА."
        ),
        platform="yandex",
    )
    age_warnings = [w for w in result.warnings if "18+" in w and "рекомендуется" not in w]
    assert len(age_warnings) == 0


def test_erir_warning(generator):
    """Medical ad without ЕРИР token → warning."""
    result = generator._check_compliance(
        headline="Лечение зубов 18+",
        description=(
            "Профессиональная стоматология. "
            "ИМЕЮТСЯ ПРОТИВОПОКАЗАНИЯ, НЕОБХОДИМА КОНСУЛЬТАЦИЯ СПЕЦИАЛИСТА."
        ),
        platform="yandex",
    )
    assert any("ЕРИР" in w for w in result.warnings), \
        f"Expected ЕРИР warning, got: {result.warnings}"
```

Use pytest's `pytest.mark.asyncio` for async tests (already standard in this project).
</action>
  <verify>
    <automated>pytest AIM/tests/unit/test_yandex_direct_stats.py AIM/tests/unit/test_ad_copy_compliance.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - AIM/tests/unit/test_yandex_direct_stats.py exists with 3 test functions
    - AIM/tests/unit/test_ad_copy_compliance.py exists with 7 test functions
    - All 10 tests pass: `pytest AIM/tests/unit/test_yandex_direct_stats.py AIM/tests/unit/test_ad_copy_compliance.py -x -v` exits 0
    - test_real_tsv_parsing verifies cost conversion (micros/1e6) and all 8 fields
    - test_medical_disclaimer_required verifies ФЗ-38 violation on missing disclaimer
    - test_prohibited_efficacy_claim verifies blocking of "гарантированный результат"
  </acceptance_criteria>
  <done>
10 tests pass: 3 for Yandex Direct TSV parsing (real data, empty, async polling), 7 for ФЗ-38 compliance (disclaimer required/present, non-medical skip, prohibited claims, age restriction, ЕРИР warning).
</done>
</task>


<task type="auto" tdd="true">
  <name>Task 4: Add sync_campaigns_to_db() to YandexDirectAPIClient</name>
  <files>AIM/src/aim/subagents/ads/yandex_direct_client.py</files>
  <read_first>
    - AIM/src/aim/subagents/ads/yandex_direct_client.py (current get_campaigns() and get_campaign_stats() methods)
    - AIM/src/aim/models/campaign_models.py (Campaign model created by Plan 13-04 -- this plan depends on it)
    - AIM/src/aim/storage/models.py (Base import for SQLAlchemy)
  </read_first>
  <behavior>
    - Test 1: sync_campaigns_to_db() with 2 campaigns from get_campaigns() -> Campaign rows inserted into DB, method returns count=2
    - Test 2: sync_campaigns_to_db() with duplicate external_id -> existing campaign UPDATED (upsert), not duplicated
    - Test 3: sync_campaigns_to_db() with empty campaign list -> returns count=0, no DB writes
  </behavior>
  <action>
Add a `sync_campaigns_to_db()` method to `YandexDirectAPIClient` in `yandex_direct_client.py`. Place it after `get_campaigns()` and before `get_campaign_stats()`.

```python
async def sync_campaigns_to_db(
    self,
    db_session_factory,
    campaign_ids: list[int] | None = None,
) -> int:
    """Fetch campaigns from Yandex Direct API and sync to Campaign DB table.

    Uses upsert logic: inserts new campaigns, updates existing ones
    matched by external_id + platform. Returns count of synced campaigns.

    IMPORTANT: This method depends on the Campaign SQLAlchemy model
    created by Plan 13-04 (campaign_models.py). The Campaign model must
    exist before calling this method.

    Args:
        db_session_factory: Async callable returning an AsyncSession context manager
        campaign_ids: Specific campaigns to sync (None = all active campaigns)

    Returns:
        Number of campaigns synced to DB
    """
    from datetime import datetime, timezone
    from sqlalchemy import select
    from aim.models.campaign_models import Campaign

    # Fetch campaigns from Yandex Direct API
    campaigns = await self.get_campaigns()
    if campaign_ids:
        campaigns = [c for c in campaigns if c.id in campaign_ids]

    if not campaigns:
        self.logger.info("sync_campaigns_no_campaigns_to_sync")
        return 0

    synced = 0
    async with db_session_factory() as db:
        for campaign_info in campaigns:
            # Check if campaign already exists in DB
            result = await db.execute(
                select(Campaign).where(
                    Campaign.external_id == str(campaign_info.id),
                    Campaign.platform == "yandex",
                )
            )
            existing = result.scalar_one_or_none()

            if existing:
                # Update existing
                existing.name = campaign_info.name
                existing.status = campaign_info.status
                existing.daily_budget = campaign_info.daily_budget
                existing.currency = campaign_info.currency
                if campaign_info.end_date:
                    existing.end_date = datetime.fromisoformat(campaign_info.end_date)
                self.logger.debug(
                    "sync_campaign_updated",
                    external_id=str(campaign_info.id),
                    name=campaign_info.name,
                )
            else:
                # Insert new
                db.add(Campaign(
                    external_id=str(campaign_info.id),
                    name=campaign_info.name,
                    platform="yandex",
                    status=campaign_info.status,
                    daily_budget=campaign_info.daily_budget,
                    currency=campaign_info.currency,
                    start_date=datetime.fromisoformat(campaign_info.start_date),
                    end_date=(
                        datetime.fromisoformat(campaign_info.end_date)
                        if campaign_info.end_date else None
                    ),
                    created_at=datetime.now(timezone.utc),
                ))
                self.logger.debug(
                    "sync_campaign_created",
                    external_id=str(campaign_info.id),
                    name=campaign_info.name,
                )
            synced += 1

        await db.commit()

    self.logger.info("sync_campaigns_complete", synced_count=synced)
    return synced
```

Key design decisions:
- **Upsert pattern**: SELECT first, then INSERT or UPDATE -- avoids unique constraint violations on re-sync
- **external_id**: Campaign.id from Yandex Direct stored as string (platform-agnostic format)
- **platform="yandex"**: Enables multi-platform queries (Campaign table stores yandex/vk/telegram)
- **Returns count**: Allows caller to verify sync completed
- **Optional campaign_ids filter**: Sync all or specific campaigns
- **Structlog logging**: Follows existing patterns (info on complete, debug per-campaign)
</action>
  <verify>
    <automated>pytest AIM/tests/unit/test_yandex_direct_stats.py AIM/tests/unit/test_ad_copy_compliance.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - yandex_direct_client.py contains async def sync_campaigns_to_db()
    - Method accepts db_session_factory and optional campaign_ids parameters
    - Method imports Campaign from aim.models.campaign_models
    - Method uses SELECT-then-upsert pattern (no unique constraint violations)
    - external_id stored as string, platform="yandex"
    - All existing stats and compliance tests still pass
  </acceptance_criteria>
  <done>
sync_campaigns_to_db() writes Yandex Direct campaign data to the Campaign database table. Attribution pipeline (13-04) can now query real campaign records. Existing tests unaffected.
</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Yandex Direct API → Client | Untrusted API response (TSV data) flows into our system through get_campaign_stats() |
| Ad copy text → Yandex Direct | Ad copy we generate goes to Yandex for moderation; non-compliant ads can get campaigns blocked |
| Ad copy text → Public internet | Generated ad copy displayed to end users; ФЗ-38 violations are legal liability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Tampering | get_campaign_stats() TSV parsing | mitigate | Use csv.DictReader with strict field access (row["CampaignId"]); catch KeyError on missing fields; validate cost/impressions >= 0 |
| T-13-02-02 | Information Disclosure | ФЗ-38 non-compliance in ad copy | mitigate | Mandatory disclaimer injection (ИМЕЮТСЯ ПРОТИВОПОКАЗАНИЯ), prohibited claims blocklist, age restriction enforcement |
| T-13-02-03 | Repudiation | Ad copy not marked with ЕРИР token | mitigate | Warning on missing ЕРИР token in compliance check; non-marked ads violate 347-ФЗ |
| T-13-02-04 | Denial of Service | Yandex Reports API async polling loop | mitigate | Max 10 polling iterations with 5s timeout between; raise TimeoutError if report not ready after 50s |
| T-13-02-05 | Elevation of Privilege | API token in logs | mitigate | structlog already configured with log_api_responses: false; do not log TSV response body |
</threat_model>

<verification>
## Pre-merge checks
- `pytest AIM/tests/unit/test_yandex_direct_stats.py AIM/tests/unit/test_ad_copy_compliance.py -x -v` exits 0
- `pytest AIM/tests/unit/test_ads_campaign_creator_agent.py AIM/tests/unit/test_ads_magister.py -x` still passes (existing tests)
- `grep -c "Mock stats" AIM/src/aim/subagents/ads/yandex_direct_client.py` returns 0
- `grep -c "def generate_hashtags" AIM/src/aim/subagents/ads/ad_copy_generator.py` returns 1
- `grep -c "async def sync_campaigns_to_db" AIM/src/aim/subagents/ads/yandex_direct_client.py` returns >= 1
</verification>

<success_criteria>
- get_campaign_stats() returns real data from Yandex Reports API (TSV parsing verified by tests)
- AdCopyGenerator enforces ФЗ-38: medical ads must contain disclaimer text, prohibited claims rejected, age restrictions checked, ЕРИР warnings generated
- Duplicate generate_hashtags functions removed from ad_copy_generator.py
- sync_campaigns_to_db() writes Yandex Direct campaign data to Campaign DB table (upsert pattern, external_id+platform matching)
- All 10 new tests pass + existing ads tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-landing-marketing/13-02-SUMMARY.md`
</output>
