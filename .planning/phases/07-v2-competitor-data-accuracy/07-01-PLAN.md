---
plan_id: "07-01"
wave: 1
depends_on: []
files_modified:
  - AIM/src/aim/services/brand_resolver.py
  - AIM/src/aim/services/competitor_matcher_v2.py
  - AIM/src/aim/services/lib/perplexity_client.py
  - AIM/src/aim/services/lib/instagram_enricher.py
  - AIM/src/aim/api/competitors.py
  - AIM/docker-compose.yml
requirements: [COMP-01, COMP-02, COMP-03, COMP-04]
autonomous: true
---

# Plan 07-01: V2 Competitor Pipeline — 4 доработки точности данных

**Goal:** Доработать v2 пайплайн до полной эталонной таблицы конкурентов (5 колонок: Конкурент, Выручка, Тренд, Хирургов, Instagram) на тесте IPHK.

## must_haves

**truths:**
- IPHK: `effective_revenue` берётся из ФНС (bo.nalog), не из оценки `estimated_revenue`
- IPHK: топ-5 конкурентов имеют `surgeons_count` ≠ None (≥3 из 5)
- IPHK: топ-5 конкурентов имеют `instagram_followers` ≠ None (≥3 из 5)
- «Медиал на Ленинском проспекте» нормализуется до «Медиал» перед резолвом brand→INN
- Время выполнения v2 pipeline остаётся ≤30с (Instagram enrichment только топ-5)

---

## Task 1: Нормализация брендов (COMP-04)

<action>
Добавить функцию `normalize_brand_name(brand: str) -> str` в `brand_resolver.py`. Функция убирает гео-привязки из названий брендов через regex-паттерны:

Паттерны для удаления (в порядке применения):
1. `r'\s+на\s+[\w\s-]*(?:проспект[е]?|улиц[ае]?|переулок[е]?|шоссе|проезд[е]?|набережной|площади)$'` — «на Ленинском проспекте»
2. `r'\s+в\s+[\w\s-]*(?:переулке|проезде|тупике)$'` — «в Орловском переулке»
3. `r'\s+(?:метро|m)\s+[\w-]+$'` — «метро Таганская»
4. `r',?\s*(?:г\.|город)\s+[\w\s]+$'` — «г. Москва»
5. `r'\s+№\s*\d+'` — «№ 5»

Применить `normalize_brand_name` в `resolve_brands_batch` (brand_resolver.py) ПЕРЕД вызовом `resolve_brand_to_inn` для каждого бренда. Также применить в `CompetitorMatcherV2._discover_via_perplexity` и `_discover_via_searxng` — к каждому бренду в списке перед merge.

Сохранить оригинальное название в поле `brand_name_original` на ResolvedBrand для отладки.
</action>

<read_first>
- `AIM/src/aim/services/brand_resolver.py` — текущий резолвер, структура ResolvedBrand
- `AIM/src/aim/services/competitor_matcher_v2.py` — где вызываются discovery каналы
</read_first>

<acceptance_criteria>
- `normalize_brand_name("Медиал на Ленинском проспекте")` возвращает `"Медиал"`
- `normalize_brand_name("ЕМС в Орловском переулке")` возвращает `"ЕМС"`
- `normalize_brand_name("ОН Клиник на Таганке")` возвращает `"ОН Клиник"`
- `normalize_brand_name("Клазко")` возвращает `"Клазко"` (без изменений)
- `resolve_brands_batch` вызывает normalize перед резолвом каждого бренда
- Логи показывают: `brand_normalized: "Медиал на Ленинском проспекте" → "Медиал"`
</acceptance_criteria>

<verify>
```bash
ssh aim "docker exec aim-app python3 -c \"
from src.aim.services.brand_resolver import normalize_brand_name
assert normalize_brand_name('Медиал на Ленинском проспекте') == 'Медиал'
assert normalize_brand_name('ЕМС в Орловском переулке') == 'ЕМС'
assert normalize_brand_name('Клазко') == 'Клазко'
print('normalize_brand_name: ALL TESTS PASSED')
\""
```
</verify>

---

## Task 2: Резолв ИНН клиента (COMP-01)

<action>
Доработать этап 0 в `competitor_matcher_v2.py` — метод `find_competitors`, блок после `extract_client_profile`:

1. Если `client_inn` пустой И есть `company_name`:
   - Вызвать `resolve_brand_to_inn(company_name, city, nalog=self.nalog)` (уже существует)
   - Если найден → `client_inn = resolved_client.inn`

2. Если `client_inn` всё ещё пустой (brand_resolver не нашёл):
   - Perplexity запрос через `perplexity_chat`: «Найди ИНН клиники {company_name}, {city}. Верни ТОЛЬКО число или null.»
   - Валидация: `len(inn) in (10, 12)` и `inn.isdigit()`
   - Если Perplexity дал ИНН → bo.nalog `search(inn)` подтверждает существование → берём

3. После резолва ИНН → `_get_revenue_by_inn(client_inn)` уже работает (берёт из ФНС)

4. Явный лог: `logger.info("Client INN resolution: source=%s inn=%s revenue=%s", source, inn, revenue)`

Добавить новый метод `_resolve_client_inn(self, company_name, city, url) -> tuple[str, str]` возвращающий `(inn, source)` где source ∈ {"site_scrape", "bo_nalog", "perplexity", "failed"}.
</action>

<read_first>
- `AIM/src/aim/services/competitor_matcher_v2.py` — этап 0 (строки ~155-190), метод `_get_revenue_by_inn`
- `AIM/src/aim/services/brand_resolver.py` — `resolve_brand_to_inn`
- `AIM/src/aim/services/lib/perplexity_client.py` — `perplexity_chat`
- `AIM/src/aim/services/service_extractor.py` — `_extract_inn` (что уже скрапит)
</read_first>

<acceptance_criteria>
- IPHK: лог показывает `Client INN resolution: source=bo_nalog inn=<10 цифр> revenue=<миллиарды>`
- `effective_revenue` ≠ `estimated_revenue` (оценка 80М) — берётся из ФНС
- Если ФНС не нашёл → fallback на оценку с логом `source=failed, using estimate`
- Коридор 0.3×–3× работает с реальной выручкой (сортировка по близости, не по размеру)
</acceptance_criteria>

<verify>
```bash
# Тест: выручка клиента должна быть из ФНС, не оценка
ssh aim "docker exec aim-app curl -s -X POST http://localhost:8000/api/competitors/find \
  -H 'Content-Type: application/json' \
  -d '{\"url\": \"https://iphk.ru\", \"count\": 5, \"strategy\": \"v2\"}'" | \
  python3 -c "import sys,json; d=json.load(sys.stdin); comps=d['competitors']; revs=[c['revenue_year'] for c in comps if c['revenue_year']]; print(f'Top revenue: {max(revs)/1e9:.1f}B — should be comparable to client (4.3B), not tiny')"
```
</verify>

---

## Task 3: Instagram enrichment (COMP-02)

<action>
Создать новый файл `AIM/src/aim/services/lib/instagram_enricher.py`:

1. Функция `find_instagram_handle(website: str, brand_name: str) -> str | None`:
   - Скрапинг сайта (httpx GET, timeout 10s) → BeautifulSoup → поиск `<a href*="instagram.com/">`
   - Извлечь handle из URL (убрать instagram.com/, /, ?)
   - Если нет на сайте → вернуть None

2. Функция `get_instagram_followers(handle: str) -> int | None`:
   - Apify actor `apify~instagram-profile-scraper` (как в hermes-v2 `run_instagram_content.py`)
   - Загрузить ключи из `/opt/data/apify_keys.json` (через apify_key_pool.py)
   - Start run → poll (24×5s=120s max) → dataset item 0 → `profile.get("followersCount")`
   - Возвращает число подписчиков или None

3. Функция `enrich_instagram_batch(competitors: list[CompetitorMatch], max_count: int = 5) -> None`:
   - Для топ-N конкурентов (по count, default 5):
     - `find_instagram_handle(website, brand_name)` → если handle найден
     - `get_instagram_followers(handle)` → followers
     - Заполнить `profile.social_links["instagram"] = handle` и сохранить followers в CompetitorMatch
   - Параллельно через asyncio.gather (Semaphore 3 — не спамить Apify)

В `competitor_matcher_v2.py`, после сортировки и среза `result = filtered[:count]`:
```python
# Instagram enrichment для финального топ-N
await enrich_instagram_batch(result, max_count=count)
```

Добавить `instagram_followers` и `instagram_handle` поля в CompanyProfile model (`rusprofile/models.py`) — или хранить в `social_links` dict.

В `_enrich_one` добавить сохранение followers в match_reason: `f"Instagram: {followers} подписчиков"`.
</action>

<read_first>
- `AIM/hermes-v2/app/tools/run_instagram_content.py` — Apify IG scraper (паттерн для копирования)
- `AIM/hermes-v2/app/lib/apify_client.py` — Apify client с key rotation
- `AIM/src/aim/services/apify_key_pool.py` — пул ключей на сервере
- `AIM/src/aim/services/rusprofile/models.py` — CompanyProfile (где хранить social_links)
- `AIM/src/aim/api/competitors.py` — CompetitorJson (instagram_followers, instagram_handle поля уже добавлены)
</read_first>

<acceptance_criteria>
- Файл `instagram_enricher.py` существует и содержит `find_instagram_handle`, `get_instagram_followers`, `enrich_instagram_batch`
- `find_instagram_handle("https://iphk.ru", "IPHK")` возвращает строку (handle) или None (не падает)
- IPHK тест: ≥3 из 5 конкурентов имеют `instagram_followers` ≠ None
- Если Apify ключи недоступны → лог warning, Instagram пропускается, pipeline не падает
- enrich_instagram_batch не блокирует дольше 30с (timeout на весь batch)
</acceptance_criteria>

<verify>
```bash
ssh aim "docker exec aim-app curl -s -X POST http://localhost:8000/api/competitors/find \
  -H 'Content-Type: application/json' \
  -d '{\"url\": \"https://iphk.ru\", \"count\": 5, \"strategy\": \"v2\"}'" | \
  python3 -c "import sys,json; d=json.load(sys.stdin); comps=d['competitors']; ig=[c for c in comps if c.get('instagram_followers')]; print(f'Instagram filled: {len(ig)}/{len(comps)} competitors')"
```
</verify>

---

## Task 4: Число хирургов — дообогащение (COMP-03)

<action>
В `competitor_matcher_v2.py`, метод `_enrich_all` или новый метод `_enrich_surgeons_batch`:

1. После enrichment financials, для конкурентов где `surgeons_count is None`:
   - Perplexity запрос: «Сколько врачей/хирургов работает в клинике "{brand_name}" ({city})? Верни ТОЛЬКО целое число или null.»
   - Парсинг ответа: `int(response.strip())` с обработкой ошибок
   - Сохранить в `profile.employee_count` (используется как surgeons_count в _competitor_to_json)

2. Ограничение: только для топ-N (count) финальных конкурентов — не для всех разрешённых

3. Параллельно через asyncio.gather (Semaphore 3 — Perplexity rate limit)

Добавить метод:
```python
async def _enrich_surgeons_batch(
    self, competitors: list[CompetitorMatch], city: str, max_count: int = 5
) -> None:
    """Дообогащение числа хирургов через Perplexity для топ-N."""
```

Вызов: после `enriched = self._enrich_all(valid, perplexity_brands)` и до сортировки, ИЛИ после среза `result = filtered[:count]` (второй вариант быстрее — меньше запросов).
</action>

<read_first>
- `AIM/src/aim/services/competitor_matcher_v2.py` — `_enrich_all`, `_enrich_one` (где хранится employee_count)
- `AIM/src/aim/services/lib/perplexity_client.py` — `perplexity_chat`
- `AIM/src/aim/api/competitors.py` — `_competitor_to_json` (surgeons_count = p.employee_count)
</read_first>

<acceptance_criteria>
- IPHK тест: ≥3 из 5 конкурентов имеют `surgeons_count` ≠ None
- Если Perplexity не дал число → `surgeons_count = None` без ошибки (graceful)
- Время: дообогащение топ-5 добавляет ≤10с к pipeline
- Perplexity запрос содержит бренд и город (не голый «сколько врачей?»)
</acceptance_criteria>

<verify>
```bash
ssh aim "docker exec aim-app curl -s -X POST http://localhost:8000/api/competitors/find \
  -H 'Content-Type: application/json' \
  -d '{\"url\": \"https://iphk.ru\", \"count\": 5, \"strategy\": \"v2\"}'" | \
  python3 -c "import sys,json; d=json.load(sys.stdin); comps=d['competitors']; surg=[c for c in comps if c.get('surgeons_count')]; print(f'Surgeons filled: {len(surg)}/{len(comps)} competitors')"
```
</verify>

---

## Task 5: Интеграция + end-to-end тест

<action>
Интегрировать все 4 доработки в единый pipeline в `competitor_matcher_v2.py`:

1. Этап 0: `_resolve_client_inn` (Task 2) → реальная выручка
2. Этап 1: `normalize_brand_name` в discovery (Task 1)
3. Этап 2: нормализованные бренды → `resolve_brands_batch`
4. Этап 3: `_enrich_all` + коридор + сортировка
5. НОВЫЙ Этап 3.5 (после среза top-N):
   - `_enrich_surgeons_batch(result, city, count)` (Task 4)
   - `enrich_instagram_batch(result, count)` (Task 3)

Обновить лог:
```python
logger.info(
    "CompetitorMatcherV2 done: url=%s competitors=%d elapsed=%.1fs "
    "client_revenue=%s surgeons_filled=%d instagram_filled=%d",
    url, len(result), elapsed,
    f"{effective_revenue:,}" if effective_revenue else "None",
    sum(1 for r in result if r.profile.employee_count),
    sum(1 for r in result if r.profile.social_links.get("instagram")),
)
```

Полный end-to-end тест на IPHK:
```bash
ssh aim "docker exec aim-app curl -s -X POST http://localhost:8000/api/competitors/find \
  -H 'Content-Type: application/json' \
  -d '{\"url\": \"https://iphk.ru\", \"count\": 5, \"strategy\": \"v2\"}' --max-time 60"
```
</action>

<read_first>
- `AIM/src/aim/services/competitor_matcher_v2.py` — полная структура pipeline
- `AIM/src/aim/api/competitors.py` — _competitor_to_json (как поля маппятся в JSON)
</read_first>

<acceptance_criteria>
- IPHK: полная таблица 5 конкурентов со всеми колонками:
  - Конкурент (brand_name) ✅
  - Выручка (revenue_year) — все 5 заполнены
  - Тренд (revenue_trend) — все 5 заполнены
  - Хирургов (surgeons_count) — ≥3 из 5 заполнены
  - Instagram (instagram_followers) — ≥3 из 5 заполнены
- Время выполнения ≤30с (с Instagram enrichment)
- strategy=v1 (Google Maps) всё ещё работает как fallback
- Лог показывает: `surgeons_filled=4 instagram_filled=3` (или лучше)
</acceptance_criteria>

<verify>
```bash
# Полный end-to-end тест
ssh aim "docker exec aim-app curl -s -X POST http://localhost:8000/api/competitors/find \
  -H 'Content-Type: application/json' \
  -d '{\"url\": \"https://iphk.ru\", \"count\": 5, \"strategy\": \"v2\"}' --max-time 60" | \
  python3 -c "
import sys, json
d = json.loads(sys.stdin.read())
comps = d['competitors']
print(f'{\"#\":<3} {\"Бренд\":<35} {\"Выручка\":>12} {\"Тренд\":<10} {\"Хир\":>5} {\"IG\":>8}')
print('-' * 80)
for i, c in enumerate(comps, 1):
    brand = (c.get('brand_name') or c.get('legal_name') or '?')[:33]
    rev = c.get('revenue_year', 0) or 0
    rev_str = f'{rev/1e9:.1f}B' if rev >= 1e9 else f'{rev/1e6:.0f}M' if rev else '-'
    trend = c.get('revenue_trend') or '-'[:10]
    surg = c.get('surgeons_count') or '-'
    ig = c.get('instagram_followers')
    ig_str = f'{ig//1000}K' if ig else '-'
    print(f'{i:<3} {brand:<35} {rev_str:>12} {trend:<10} {surg:>5} {ig_str:>8}')
filled = sum(1 for c in comps if c.get('revenue_year') and c.get('surgeons_count') and c.get('instagram_followers'))
print(f'\nFully filled rows: {filled}/{len(comps)}')
"
```
</verify>

---

## Artifacts this phase produces

- `normalize_brand_name()` — функция в brand_resolver.py
- `_resolve_client_inn()` — метод в CompetitorMatcherV2
- `instagram_enricher.py` — новый модуль (find_instagram_handle, get_instagram_followers, enrich_instagram_batch)
- `_enrich_surgeons_batch()` — метод в CompetitorMatcherV2
- Instagram enrichment интегрирован в CompetitorMatcherV2 pipeline

## Deployment

```bash
# Деплой изменений
cd AIM
rsync -avz src/aim/services/brand_resolver.py aim:/opt/aim/AIM/src/aim/services/
rsync -avz src/aim/services/competitor_matcher_v2.py aim:/opt/aim/AIM/src/aim/services/
rsync -avz src/aim/services/lib/ aim:/opt/aim/AIM/src/aim/services/lib/

# Пересборка образа
ssh aim "cd /opt/aim/AIM && docker compose build app && docker compose up -d app"
```

## Rollback

strategy=v1 мгновенно включает старый Google Maps пайплайн (без изменений).
