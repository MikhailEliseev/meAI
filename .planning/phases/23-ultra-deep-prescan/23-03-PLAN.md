---
phase: 23-ultra-deep-prescan
plan: 03
type: execute
wave: 3
depends_on:
  - 23-02
files_modified:
  - AIM/hermes/app/tools/run_prescan.py
  - AIM/hermes/skills/aim/SOUL.md
  - AIM/hermes/app/agent_wrapper.py
autonomous: false
requirements:
  - SC-23-04
  - SC-23-07

must_haves:
  truths:
    - "run_prescan tool shows progress messages after each stage ('Смотрю финансы...', 'Анализирую лицензии...')"
    - "Hermes receives stage_1 summary after ~25s and can immediately narrate financial data to client"
    - "Hermes shows '5 агентов анализируют ваш сайт' language in chat to build theatrical WOW-effect"
    - "SOUL.md describes the 3-stage prescan flow in Шаг 2"
    - "PRESALE prompt has explicit rules: tell client about each stage as it completes, don't wait for all 3"
    - "Staged prescan endpoint is called instead of old prescan endpoint (but fallback to old if staged unavailable)"
  artifacts:
    - path: "AIM/hermes/app/tools/run_prescan.py"
      provides: "Updated Hermes tool with stage progress streaming"
      min_lines: 250
      contains: "handle_run_prescan"
    - path: "AIM/hermes/skills/aim/SOUL.md"
      provides: "Updated SOUL with staged prescan flow description"
      min_lines: 650
    - path: "AIM/hermes/app/agent_wrapper.py"
      provides: "Updated PRESALE prompt with staged flow rules"
      contains: "_presale_prompt"
  key_links:
    - from: "run_prescan.py"
      to: "/api/presale/prescan-staged"
      via: "httpx POST"
      pattern: "api/presale/prescan-staged"
    - from: "run_prescan.py"
      to: "push_tool_progress"
      via: "import from app.main"
      pattern: "push_tool_progress"
    - from: "SOUL.md:Шаг 2"
      to: "run_prescan tool"
      via: "Hermes tool selection"
      pattern: "run_prescan"
---

<objective>
Update the Hermes `run_prescan` tool to call the new staged endpoint, stream intermediate results as Telegram progress messages, and present results conversationally after each stage. Update SOUL.md and the PRESALE system prompt to teach DeepSeek the new staged flow with explicit anti-hallucination rules.

Purpose: The 3-stage pipeline is invisible to the end user unless Hermes narrates it. Hermes bridges the technical pipeline and the human client — showing progress, delivering WOW-moments after each stage, and making the "theatre of 5 agents" feel real.
Output: Updated `run_prescan.py`, updated `SOUL.md`, updated `_presale_prompt()`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ultra-deep-prescan/CONTEXT.md
@AIM/hermes/app/tools/run_prescan.py
@AIM/hermes/app/agent_wrapper.py
@AIM/hermes/skills/aim/SOUL.md

<interfaces>
<!-- Extracted from codebase. Do not explore — use these directly. -->

Existing run_prescan handler (lines 31-162 of run_prescan.py):
```python
async def handle_run_prescan(url=None, **kwargs) -> str:
    # Calls POST http://app:8000/api/presale/prescan
    # Uses push_tool_progress("prescan", "...")
    # Returns JSON summary
```

push_tool_progress signature (from Hermes main.py line 63):
```python
def push_tool_progress(stage: str, message: str, competitor: str = "") -> None:
    # Thread-safe — can be called from any thread
    # Events flow to SSE → Next.js → Telegram
```

registry.register pattern (from find_company_financials.py lines 143-179):
```python
registry.register(
    name="run_prescan",
    toolset="aim-operations",
    schema={...},
    handler=handle_run_prescan,
    check_fn=lambda: True,
    is_async=True,
    description="...",
    emoji="🔎",
)
```

_presale_prompt() — line 120-197 of agent_wrapper.py. Must be extended, not replaced.

SOUL.md structure — 623 lines, key sections:
- Шаг 2 (line 62): "Получил URL → вызываю run_prescan(url)"
- Tools catalog (line 261): run_prescan description
- PROHIBITIONS for PRESALE (line 207)

Staged endpoint contract (from 23-02):
```python
POST /api/presale/prescan-staged
Request:  {"url": "...", "force_refresh": false}
Response: {
    "success": true,
    "url": "...",
    "elapsed_seconds": 85.3,
    "stages": [
        {"stage": 1, "name": "Финансовый хук", "summary": {...}, "timestamp": 25.1},
        {"stage": 2, "name": "Под капотом", "summary": {...}, "timestamp": 55.4},
        {"stage": 3, "name": "Рынок", "summary": {...}, "timestamp": 85.3},
    ],
    "profile_data": {"stage_1": {...}, "stage_2": {...}, "stage_3": {...}},
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Hermes run_prescan tool for staged flow with progress streaming</name>
  <files>AIM/hermes/app/tools/run_prescan.py</files>
  <action>
Rewrite `handle_run_prescan` to call the new staged endpoint and stream progress messages as each stage completes. Preserve backward compatibility: if the staged endpoint returns 404 (not deployed yet), fall back to the old `/api/presale/prescan` endpoint.

**New flow:**

1. **Call staged endpoint:** `POST http://app:8000/api/presale/prescan-staged` with `{"url": url, "force_refresh": false}`
2. **Read the response:** If HTTP 200, parse the `stages` array and `profile_data`
3. **For each completed stage**, push a Telegram progress message that builds the "theatre" narrative

**Progress messages by stage (use `push_tool_progress`):**

Stage 1 completing (at ~25s):
```
push_tool_progress("prescan", "🔍 Этап 1/3 — Финансовый хук: нашли юрлицо, оборот {revenue}, прибыль {profit}")
```

Stage 2 completing (at ~55s):
```
push_tool_progress("prescan", "⚙️ Этап 2/3 — Под капотом: проверили лицензии ({count} шт.), SEO, отзывы ({rating}), соцсети")
```

Stage 3 completing (at ~85s):
```
push_tool_progress("prescan", "🌐 Этап 3/3 — Рынок: карты Яндекс/Google, конкуренты рядом, аудит контента")
```

After all 3 stages complete, push a celebratory message:
```
push_tool_progress("prescan", "✅ Разведка завершена! 5 агентов проанализировали: сайт, финансы, лицензии, SEO, отзывы, соцсети, карты, конкурентов и контент")
```

**The tool return value (JSON string)** should include BOTH the stage-by-stage summaries (for Hermes to narrate progressively) AND a merged final summary (for fallback if Hermes only reads the final result):

```python
# Compact JSON for LLM consumption
return json.dumps({
    "url": url,
    "cached": data.get("cached", False),
    "elapsed_seconds": data.get("elapsed_seconds", 0),
    # Stage summaries — Hermes narrates these conversationally
    "stage_1_financials": {
        "revenue": ..., "profit": ..., "legal_name": ..., "years_on_market": ...,
        "specialization": ..., "city": ...,
    },
    "stage_2_under_the_hood": {
        "licenses_count": len(stage_2.get("licenses", [])),
        "founders": stage_2.get("founders", []),
        "seo_score": ..., "seo_deep_issues": [...],
        "rating": ..., "reviews_count": ...,
        "social_links": ...,
        "has_sitemap": ..., "has_structured_data": ...,
    },
    "stage_3_market": {
        "yandex_maps": ..., "google_maps": ...,
        "nearby_competitors": [...],
        "content_audit": ...,
        "revenue_trend": ...,
    },
    # Denormalized fast-access fields (backward compat with old summary format)
    "specialization": ..., "city": ..., "inn": ...,
    "revenue_year": ..., "profit_year": ...,
    "seo_score": ..., "seo_issues": [...],
    "rating": ..., "reviews_count": ...,
    "social_links": ...,
    "web_speed": ...,  # Pre-computed from stage_2 seo_deep
    "errors": data.get("errors", []),
}, ensure_ascii=False, indent=2)
```

**Fallback logic** (if staged endpoint not available):
```python
if response.status_code == 404:
    logger.info("Staged prescan not available — falling back to legacy prescan")
    # Call old endpoint and use old result formatting
    response = await client.post(f"{AIM_API_BASE}/api/presale/prescan", ...)
    # ... existing old flow ...
```

**Increase timeout** to 150 seconds (up from 120) since the 3-stage pipeline takes up to 90+ seconds.

**Registry update:** Update the tool's `schema` description to mention the new staged flow:
- "3-stage intelligence pipeline (financials → deep analysis → market)"
- "Progress messages streamed as each stage completes"
- "60-90 seconds total, results shown progressively"

Do NOT change `name`, `toolset`, or `check_fn`. Keep `emoji="🔎"`.
  </action>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && python -c "
import ast, inspect
# Check the updated handler handles the staged response format
# Syntax check
with open('AIM/hermes/app/tools/run_prescan.py') as f:
    tree = ast.parse(f.read())
# Verify handle_run_prescan function exists
funcs = [n for n in ast.walk(tree) if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))]
names = [f.name for f in funcs]
assert 'handle_run_prescan' in names, 'handle_run_prescan missing'
# Verify api/presale/prescan-staged referenced
source = open('AIM/hermes/app/tools/run_prescan.py').read()
assert 'prescan-staged' in source, 'staged endpoint not referenced'
assert 'push_tool_progress' in source, 'push_tool_progress not used'
print('OK: run_prescan tool structure valid')
"</automated>
  </verify>
  <done>run_prescan calls staged endpoint; pushes progress per stage; returns merged summary; falls back to old endpoint; registry description updated</done>
</task>

<task type="auto">
  <name>Task 2: Update SOUL.md — staged prescan flow in Шаг 2</name>
  <files>AIM/hermes/skills/aim/SOUL.md</files>
  <action>
Update `AIM/hermes/skills/aim/SOUL.md` — specifically the sections describing the prescan flow. DeepSeek reads SOUL.md as its identity, so the flow descriptions must be explicit and leave no room for improvisation.

**Changes to make (targeted edits, not full rewrite):**

### Section: Шаг 2 — Параллельная разведка (prescan) (around line 62)

Replace the existing Шаг 2 content. The old content describes "5 потоков параллельно за 60-90 секунд." Replace with:

```markdown
#### Шаг 2 — Трёхстадийная разведка (ultra-prescan)

Получил URL → вызываю `run_prescan(url)`. Это МОЩНЫЙ трёхстадийный сбор: 5 агентов работают последовательно, и я показываю результаты ПОСЛЕ КАЖДОЙ СТАДИИ.

**Как работает ultra-prescan (3 стадии):**

**Стадия 1 — «Финансовый хук» (20-30 сек):**
- DaData: юрлицо, ИНН, ОГРН, дата регистрации, юрадрес
- ГИР БО (nalog.gov.ru): оборот, прибыль, тренд выручки по годам
- Структура сайта: специализация, город, услуги, врачи
- **Сразу показываю клиенту:** «Оборот X млн, прибыль Y млн, на рынке Z лет»

**Стадия 2 — «Под капотом» (40-60 сек):**
- Росздравнадзор: медицинские лицензии (номера, сроки, услуги)
- DaData: учредители, гендиректор
- Глубокий SEO: sitemap, structured data (schema.org), мета-теги, скорость
- Отзывы: Яндекс.Карты, 2ГИС, ПроДокторов
- Соцсети: VK, Telegram (ссылки, подписчики)
- **Показываю клиенту:** «Лицензий X, учредители Y, SEO-проблем Z, отзывов N»

**Стадия 3 — «Рынок» (60-90+ сек):**
- Яндекс/Google Maps: карточка, рейтинг, координаты
- Конкуренты рядом: поиск по гео, топ-5 по рейтингу
- Многолетние тренды выручки (год к году)
- Аудит контента: количество страниц, thin content, блог
- **Показываю клиенту:** «В радиусе X км — Y конкурентов, тренд выручки Z»

**КРИТИЧЕСКОЕ ПРАВИЛО:** Я показываю результаты ПОСЛЕ КАЖДОЙ СТАДИИ. Не жду окончания всех трёх. Клиент видит ценность сразу: через 20-30 секунд он уже знает свои финансы, через 60 секунд — всё про лицензии и SEO, через 90 секунд — полную картину рынка.

**Если `run_prescan` вернул `cached: true`** — данные уже есть в базе. Не перезапускаю разведку. Сразу показываю сохранённые результаты.

**Если staged-версия недоступна (старая версия):** использую старый `run_prescan` (5 потоков, 60-90 сек) + `run_seo_audit(url)` для глубины.
```

### Section: Инструменты — run_prescan (around line 261)

Update the run_prescan tool entry:

```markdown
**run_prescan** — трёхстадийная разведка сайта клиента: финансы → лицензии/SEO/отзывы → рынок/конкуренты/контент
- Что делает: Стадия 1 (20-30 сек) — финансы и юрлицо. Стадия 2 (40-60 сек) — лицензии, учредители, SEO, отзывы, соцсети. Стадия 3 (60-90 сек) — карты, конкуренты, тренды, контент.
- Результат: полный профиль компании (profile_data со всеми тремя стадиями)
- Возвращает: отдельные summaries для каждой стадии + общий profile_data
- Когда: Шаг 2 PRESALE — сразу после получения URL. НИКОГДА не вызывай parallel с другими инструментами.
- После вызова: ПОСЛЕ КАЖДОЙ СТАДИИ показываю клиенту что нашёл. Не жду все 3.
- **cached: true** → показываю данные из кэша мгновенно
```

### Section: PROHIBITIONS for PRESALE (around line 207)

Add these rules (if not already present):

```markdown
- ❌ Ждать окончания всех 3 стадий прежде чем показать хоть что-то клиенту. Показываю после КАЖДОЙ стадии.
- ❌ Пропускать стадию 1 (финансы) и начинать со стадии 2. Порядок: финансы → под капотом → рынок. Всегда.
- ❌ Вызывать find_competitors до того как все 3 стадии prescan завершены и показаны клиенту. Prescan сначала полностью — потом конкуренты.
```

### Anti-hallucination additions for DeepSeek

Add a critical note in the prescan flow description:

```markdown
**АНТИГАЛЛЮЦИНАЦИЯ для prescan:**
- Каждую цифру называю ТОЛЬКО из результата run_prescan. НЕ прикидываю, НЕ округляю.
- Если в stage_1_financials нет revenue — НЕ придумываю «~50 млн». Говорю: «финансовые данные не найдены».
- web_speed — готовое поле, использую его КАК ЕСТЬ (например «1.1 сек — хорошая скорость»). НЕ конвертирую сам.
- количество лицензий — ТОЛЬКО из stage_2_under_the_hood.licenses_count.
- nearby_competitors — ТОЛЬКО из stage_3_market. НЕ придумываю «рядом 5 конкурентов».
```
  </action>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && python -c "
import re
soul = open('AIM/hermes/skills/aim/SOUL.md').read()
# 1. Must mention 3 stages
assert 'Стадия 1' in soul or 'Stage 1' in soul, 'Stage 1 not mentioned'
assert 'Стадия 2' in soul or 'Stage 2' in soul, 'Stage 2 not mentioned'
assert 'Стадия 3' in soul or 'Stage 3' in soul, 'Stage 3 not mentioned'
# 2. Must mention Росздравнадзор
assert 'Росздравнадзор' in soul or 'roszdravnadzor' in soul.lower(), 'Roszdravnadzor not mentioned'
# 3. Must mention cached flow
assert 'cached' in soul, 'Cache behavior not described'
# 4. Must mention anti-hallucination rules
assert 'АНТИГАЛЛЮЦИНАЦИЯ' in soul, 'Anti-hallucination rules missing'
# 5. Must mention progressive display (after each stage)
assert 'ПОСЛЕ КАЖДОЙ СТАДИИ' in soul or 'после каждой стадии' in soul.lower(), 'Progressive display not enforced'
print('OK: SOUL.md staged prescan rules verified')
print(f'   SOUL.md size: {len(soul)} chars')
"</automated>
  </verify>
  <done>SOUL.md describes 3-stage flow; Шаг 2 updated with stage details; anti-hallucination rules added; progressive display rules enforced; cached flow documented</done>
</task>

<task type="auto">
  <name>Task 3: Update PRESALE system prompt with staged flow rules</name>
  <files>AIM/hermes/app/agent_wrapper.py</files>
  <action>
Update the `_presale_prompt()` function in `AIM/hermes/app/agent_wrapper.py` (lines 120-197) to add explicit rules for the staged prescan flow. The prompt must be updated in-place — do not rewrite the entire function.

**Add after the "ПРАВИЛО ПЕРВОГО СООБЩЕНИЯ" section (around line 134):**

```
### 🎭 ТРЁХСТАДИЙНАЯ РАЗВЕДКА (КАК ТЫ РАБОТАЕШЬ)

Когда ты вызываешь run_prescan — он проходит 3 стадии. Ты получаешь результат каждой стадии ОТДЕЛЬНО. Вот как ты должен себя вести:

**После Стадии 1 (финансовый хук, ~25 сек):**
Ты получаешь данные: оборот, прибыль, юрлицо, специализация, город. СРАЗУ расскажи клиенту что видишь. Не жди остальных стадий. Формат: «Ого, смотрите что уже видно: клиника "[legal_name]" в [city], специализация — [specialization]. Оборот [revenue_year] ₽, прибыль [profit_year] ₽, на рынке [years_on_market] лет. Сейчас копаю глубже — смотрю лицензии, SEO, отзывы…»

**После Стадии 2 (под капотом, ~55 сек):**
Ты получаешь: лицензии, учредители, глубокий SEO, отзывы, соцсети. Продолжи рассказ: «Вот что ещё нашёл: [licenses_count] медицинских лицензий, учредители — [founders]. SEO-аудит показал [seo_score]/100 — [2-3 главные проблемы из seo_deep_issues]. Отзывов [reviews_count], рейтинг [rating]. Соцсети: [перечисли]. Сейчас финальный рывок — анализирую рынок и конкурентов…»

**После Стадии 3 (рынок, ~85 сек):**
Ты получаешь: карты, конкуренты, тренды, контент. Заверши историю: «И финальный штрих: на Яндекс.Картах рейтинг [yandex_maps.rating]. Рядом в радиусе 5 км — [count] конкурентов со средним рейтингом [avg]. Контент-аудит: [total_pages_estimated] страниц, из них [thin_content_pages] — слишком тонкие. Тренд выручки — [revenue_trend].»

**ПРАВИЛА РАССКАЗА:**
1. Рассказываешь ИСТОРИЮ, которая строится от стадии к стадии. Как детектив: сначала финансы → потом лицензии и SEO → потом конкуренты и рынок.
2. В начале разведки скажи что-то вроде: «Запускаю 5 агентов разведки — смотрят сайт, финансы, лицензии, SEO и отзывы. Первый этап через 20-30 секунд…»
3. НИКОГДА не молчи все 90 секунд. После каждой стадии — живой комментарий.
4. 5 агентов — это театр. Ты можешь говорить «мои агенты нашли», «разведка показывает», «аналитический отдел докладывает». Это добавляет WOW-эффект.
5. Если run_prescan вернул `cached: true` — данные уже есть. Сразу рассказывай всё, не говори «запускаю разведку».

### ПРАВИЛО: НЕ ЖДИ ВСЕ СТАДИИ (КРИТИЧЕСКИ)

Ты получаешь stage_1_financials, stage_2_under_the_hood, stage_3_market в ОДНОМ ответе от run_prescan. НО рассказываешь их клиенту ПОСЛЕДОВАТЕЛЬНО — в трёх сообщениях подряд. Это создаёт эффект живого расследования. Клиент видит как картина строится слой за слоем.
```

**Update the "Как рассказывать данные" section (around line 147) — add:**

After the run_prescan bullet:
```
- **run_prescan (staged)** — инструмент возвращает 3 блока: stage_1_financials, stage_2_under_the_hood, stage_3_market. Расскажи клиенту историю в 3 сообщениях: сначала финансы (впечатляющие цифры!), потом SEO/лицензии/отзывы (глубина!), потом рынок (масштаб!). Не пытайся уместить всё в одно сообщение — это убивает эффект. Дай клиенту переварить каждый слой.
```

**Important constraints:**
- Do NOT change the core structure of `_presale_prompt()` — the mode headers, tool descriptions, formatting rules, and anti-hallucination rules must remain
- The "ПРАВИЛО ПЕРВОГО ХОДА" section must stay — run_prescan is still called FIRST, alone
- The "Формат финального отчёта" section must stay
- Only ADD staged-specific rules, don't remove existing ones
- Target: +40-60 lines of new prompt content
  </action>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && python -c "
prompt_text = open('AIM/hermes/app/agent_wrapper.py').read()
# Check _presale_prompt function exists
import ast
tree = ast.parse(prompt_text)
funcs = {n.name: n for n in ast.walk(tree) if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))}
assert '_presale_prompt' in funcs, '_presale_prompt function missing'
# Check staged-specific content
assert 'ТРЁХСТАДИЙНАЯ' in prompt_text or 'трёхстадийная' in prompt_text.lower(), 'Staged flow not in prompt'
assert 'stage_1_financials' in prompt_text, 'stage_1_financials not referenced'
assert 'stage_2_under_the_hood' in prompt_text, 'stage_2_under_the_hood not referenced'
assert 'stage_3_market' in prompt_text, 'stage_3_market not referenced'
assert 'ПРАВИЛО ПЕРВОГО ХОДА' in prompt_text, 'First-move rule removed — must stay'
# Verify anti-hallucination rules still present
assert 'АНТИГАЛЛЮЦИНАЦИЯ' in prompt_text, 'Anti-hallucination rules removed'
print('OK: PRESALE prompt updated with staged flow rules')
print(f'   _presale_prompt size: ~{len(prompt_text)} chars in agent_wrapper.py')
"</automated>
  </verify>
  <done>PRESALE prompt teaches staged narration (3 sequential messages); stage_1/2/3 fields referenced explicitly; first-move rule preserved; anti-hallucination rules intact</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hermes → AIM API | Staged prescan call over internal Docker network |
| Hermes → Telegram | Progress messages sent via Bot API (existing channel) |
| DeepSeek LLM → end user | DeepSeek generates human-readable text — risk of hallucination if prompt rules are weak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-01 | Spoofing | DeepSeek → client chat | mitigate | DeepSeek is less disciplined than Claude. Explicit ANTI-HALLUCINATION rules with MUST/MUST NOT language. Pre-computed fields (web_speed) to prevent numeric hallucination. |
| T-23-03-02 | Information Disclosure | Stage summaries in Telegram | mitigate | Progress messages show only aggregate data (counts, scores, trends) — no PII or confidential financial details. |
| T-23-03-03 | Tampering | run_prescan HTTP communication | mitigate | Internal Docker network; httpx with timeout; response schema validated before use. |
| T-23-03-04 | Denial of Service | Staged endpoint unavailable | mitigate | Fallback to old /api/presale/prescan endpoint if 404. Graceful degradation in SOUL.md instructions. |
| T-23-03-05 | Repudiation | Stage narration | accept | Conversations logged via existing session mechanism and Telegram history. No additional audit needed. |
| T-23-03-SC | Tampering | pip installs | mitigate | No new packages needed — all in existing requirements.txt |
</threat_model>

<verification>
### Automated
- `python -c "import ast; ast.parse(open('AIM/hermes/app/tools/run_prescan.py').read()); print('Syntax OK')"`
- `python -c "import ast; ast.parse(open('AIM/hermes/app/agent_wrapper.py').read()); print('Syntax OK')"`
- SOUL.md content checks (as specified in Task 2 verify)

### Manual (after deploy)
1. Start a PRESALE chat with a test URL
2. Verify Hermes calls `run_prescan` (not old prescan)
3. Verify 3 Telegram progress messages appear (Stage 1 → Stage 2 → Stage 3)
4. Verify Hermes narrates financials immediately (Stage 1 data in first message)
5. Verify Hermes mentions "5 агентов" or similar theatrical language
6. Verify cached re-visit returns instantly with `cached: true`
7. Verify fallback to old prescan when staged endpoint is down
</verification>

<success_criteria>
- [ ] `run_prescan` tool calls `/api/presale/prescan-staged` (staged endpoint)
- [ ] 3 progress messages pushed via `push_tool_progress` (Stage 1, 2, 3)
- [ ] Tool returns merged JSON with stage_1_financials, stage_2_under_the_hood, stage_3_market
- [ ] Falls back to old `/api/presale/prescan` if staged endpoint returns 404
- [ ] SOUL.md describes 3-stage flow in Шаг 2 with explicit stage descriptions
- [ ] SOUL.md includes АНТИГАЛЛЮЦИНАЦИЯ rules for staged data fields
- [ ] PRESALE prompt teaches sequential narration (3 messages, not 1)
- [ ] PRESALE prompt references stage_1_financials, stage_2_under_the_hood, stage_3_market by name
- [ ] "ПРАВИЛО ПЕРВОГО ХОДА" (run_prescan first, alone) preserved
- [ ] Anti-hallucination rules preserved in prompt
- [ ] No new external dependencies added
</success_criteria>

<output>
Create `.planning/phases/23-ultra-deep-prescan/23-03-SUMMARY.md` when done
</output>
