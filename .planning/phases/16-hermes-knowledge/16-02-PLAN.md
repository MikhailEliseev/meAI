---
phase: 16-hermes-knowledge
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - AIM/hermes/skills/aim/SOUL.md
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10]

must_haves:
  truths:
    - "Every D-XX decision has a verifiable grep check proving it's implemented in SOUL.md"
    - "Human reviewer confirms SOUL.md accurately represents the AIM system"
    - "Hermes can deploy with new SOUL.md without breaking existing functionality"
  artifacts:
    - path: "AIM/hermes/skills/aim/SOUL.md"
      provides: "Verified, complete, accurate Hermes knowledge file"
      min_lines: 550
      max_lines: 700
  key_links:
    - from: "SOUL.md subagent names"
      to: "AIM/src/aim/subagents/*.py"
      via: "Every named subagent file exists in codebase"
      pattern: "find by name in codebase"
    - from: "SOUL.md tool schemas"
      to: "AIM/hermes/app/tools/*.py registry.register()"
      via: "Parameter names and types match"
---

<objective>
Validate the rewritten SOUL.md from Plan 16-01 against all 10 locked decisions (D-01 through D-10), verify subagent names match the actual codebase, check tool schemas match registry.register() calls, ensure no secrets leaked, and conduct a human checkpoint review before deployment.

Purpose: SOUL.md is the ONLY source of truth the Hermes LLM sees. Every inaccuracy, missing tool, or wrong subagent name directly degrades Hermes's ability to operate the agency. This validation phase catches errors before SOUL.md is deployed via Docker image rebuild.

Output: A verified, accuracy-guaranteed SOUL.md ready for production deployment. Any issues found get fixed inline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-hermes-knowledge/16-RESEARCH.md (Decision-to-section mapping, tool schemas, knowledge gap closure targets)
@.planning/phases/16-hermes-knowledge/16-CONTEXT.md (10 locked decisions with exact requirements)
@AIM/hermes/skills/aim/SOUL.md (The rewritten file from Plan 16-01)
@AIM/hermes/app/tools/ (8 tool implementations — verify schemas match)
@AIM/src/aim/subagents/ (70+ subagent files — verify names match SOUL.md claims)
@AIM/hermes/Dockerfile (Deployment context — verify SOUL.md will be picked up by copy_soul.sh)
@AIM/hermes/scripts/copy_soul.sh (Deployment script — verify source/target paths)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Automated Verification — D-01 through D-10 Completeness + Accuracy Checks</name>
  <files>AIM/hermes/skills/aim/SOUL.md</files>

  <read_first>
  - /Users/mikhaileliseev/Desktop/Dev/!meAI/AIM/hermes/skills/aim/SOUL.md (the complete rewritten file from Plan 16-01 — must read to verify content)
  - /Users/mikhaileliseev/Desktop/Dev/!meAI/.planning/phases/16-hermes-knowledge/16-RESEARCH.md (verification criteria: lines 676-688 "Phase Requirements → Test Map")
  - /Users/mikhaileliseev/Desktop/Dev/!meAI/.planning/phases/16-hermes-knowledge/16-CONTEXT.md (D-01 through D-10 — know what each decision requires)
  </read_first>

  <action>
  Run a comprehensive automated verification suite against SOUL.md. Every check is a grep command. If any check FAILS, fix the issue in SOUL.md before proceeding to Task 2 (human checkpoint).

  **Step 1: Decision Coverage Verification (D-01 through D-10)**

  Run each grep check. Record PASS/FAIL. For any FAIL, identify the specific gap and edit SOUL.md to fix it.

  | Check ID | Decision | What to grep | Expected |
  |----------|----------|-------------|----------|
  | V-D01-a | D-01: SEO Magister subagents | `grep -c "ci_tech\|ci_tech_real\|ci_site_crawler\|keyword_research_agent\|ci_backlink\|ci_rank_tracker\|ci_scout\|ci_strategist\|ci_orchestrator\|ci_auditor\|ci_deep_analyzer\|ci_reputation\|ci_ecosystem\|links_agent\|onpage_optimizer\|schema_generator\|serp_tracker\|seo_orchestrator\|semrush\|ahrefs\|yandex_metrica\|ga4_client\|web_scraper\|topic_clusterer\|cluster_analyzer\|embeddings_generator\|technical_agent" SOUL.md` | >= 10 distinct real file names |
  | V-D01-b | D-01: Content Magister subagents | `grep -c "content_writer_agent\|content_quality_checker\|content_brief_generator\|content_calendar_manager\|content_optimizer\|ci_factchecker\|ai_content_detector\|text_extractor\|gap_detector\|serp_overlap_clusterer\|opportunity_scorer\|brief_generator\|architecture_planner\|eeat_scorer\|content_orchestrator" SOUL.md` | >= 8 distinct real file names |
  | V-D01-c | D-01: Ads Magister subagents | `grep -c "yandex_direct_client\|bid_strategy_optimizer\|ad_copy_generator\|landing_page_analyzer\|conversion_tracker\|campaign_service\|google_ads_client\|oauth_flow\|mcp_server\|ads_orchestrator\|analytics_service" SOUL.md` | >= 6 distinct real file names |
  | V-D01-d | D-01: Analytics Magister subagents | `grep -c "analytics_agent\|analytics_service\|traffic_analyzer\|report_generator\|business_report\|ci_finance\|analytics_orchestrator" SOUL.md` | >= 4 distinct real file names |
  | V-D01-e | D-01: CI ecosystem | `grep -c "ci_auditor\|ci_deep_analyzer\|ci_ecosystem\|ci_marketing_strategy\|ci_pricing\|ci_offer_generator\|ci_url_validator\|ci_qa_validator\|ci_vacancies\|ci_prioritizer\|competitive_intel" SOUL.md` | >= 5 mentions |
  | V-D02-a | D-02: PRESALE mode | `grep -c "PRESALE" SOUL.md` | >= 3 (mode section + tool annotations + Token Economy) |
  | V-D02-b | D-02: ACTIVE mode | `grep -c "ACTIVE" SOUL.md` | >= 3 |
  | V-D02-c | D-02: ADMIN mode | `grep -c "ADMIN" SOUL.md` | >= 4 (mode section + tool gates + Telegram tools + Token Economy) |
  | V-D03 | D-03: 7 WOW blocks | `grep -c "Block [1-7]" SOUL.md` | >= 5 (at least 5 of 7 blocks numbered) |
  | V-D04 | D-04: 3 numbers | `grep -c "3 числа\|три цифры\|ТРИ ЦИФРЫ\|пациентов.*месяц.*срок.*стоимость\|СКОЛЬКО пациентов" SOUL.md` | >= 2 |
  | V-D05-a | D-05: Token Economy tiers | `grep -c "Tier 0\|Tier 1\|Tier 2" SOUL.md` | >= 4 (table + rules) |
  | V-D05-b | D-05: Token Economy rules | `grep -c "PRESALE.*Tier\|Tier.*PRESALE\|после контракта\|после квалификации" SOUL.md` | >= 1 |
  | V-D06 | D-06: Lead Dossier statuses | `grep -c "new.*qualified.*audited.*contacted.*active\|qualified → audited\|audited → contacted" SOUL.md` | >= 1 |
  | V-D07 | D-07: Omni-Channel | `grep -c "День 0\|День 3\|День 7\|День 14\|Hot lead\|Warm lead\|Cold lead" SOUL.md` | >= 3 |
  | V-D08 | D-08: Agent Orchestration | `grep -c "app:8000\|AIM Backend.*принимает\|tool handler.*HTTP\|НЕ запускаю Magisters напрямую" SOUL.md` | >= 2 |
  | V-D09-a | D-09: Russian market | `grep -c "ФЗ-152\|ФЗ-323\|ФЗ.*О рекламе" SOUL.md` | >= 2 |
  | V-D09-b | D-09: Russian platforms | `grep -c "ЮKassa\|Контур.Диадок\|Яндекс.Директ\|Яндекс.Метрика\|Яндекс.Вебмастер\|2ГИС\|ПроДокторов\|НаПоправку\|VK.*основ\|Telegram.*основ" SOUL.md` | >= 5 |
  | V-D09-c | D-09: NOT Western | `grep -c "Stripe.*не работает\|HIPAA.*не примен\|Google Ads.*НЕ\|DocuSign.*НЕ" SOUL.md` | >= 2 |
  | V-D10-a | D-10: All 8 tools | `grep -c "run_seo_audit\|run_content_analysis\|run_ads_report\|show_project_status\|collect_contact\|show_all_leads\|search_telegram_chats\|send_telegram_message" SOUL.md` | Each unique tool >= 1, total matches >= 12 |
  | V-D10-b | D-10: Tool I/O schemas | `grep -c "Входные параметры\|Выходные данные\|Input\|Output\|REQUIRED\|обязателен" SOUL.md` | >= 3 |

  **Step 2: Accuracy Verification vs Actual Codebase**

  Run these verification commands against the actual filesystem:

  ```bash
  # Verify every subagent name in SOUL.md maps to a real file
  # Extract all .py filenames mentioned in SOUL.md, check they exist in AIM/src/aim/subagents/
  grep -oP '[a-z_]+\.py' AIM/hermes/skills/aim/SOUL.md | sort -u | while read f; do
    find AIM/src/aim/subagents -name "$f" | grep -q . && echo "OK: $f" || echo "MISSING: $f"
  done
  ```

  For any "MISSING" file, either:
  - Fix the filename in SOUL.md to match the actual file
  - Or remove the reference if the file doesn't exist

  ```bash
  # Verify tool names match registry.register() calls
  for tool in run_seo_audit run_content_analysis run_ads_report show_project_status collect_contact show_all_leads search_telegram_chats send_telegram_message; do
    grep -r "name=\"$tool\"" AIM/hermes/app/tools/ -l | grep -q . && echo "OK: $tool" || echo "MISSING REGISTRATION: $tool"
  done
  ```

  ```bash
  # Verify NO old/wrong subagent names from current SOUL.md remain
  grep -c "Technical SEO Auditor\|Medical Copywriter\|Medical Editor\|Blog Manager\|GEO Content Optimizer\|YandexDirect Manager\|VK Ads Manager\|Telegram Ads Manager\|Budget Optimizer\|Creative Generator\|A/B Testing Agent\|Data Collector\|Data Processor\|KPI Tracker\|Anomaly Detector\|Forecast Engine\|Backlink Analyst\|SERP Monitor" AIM/hermes/skills/aim/SOUL.md
  ```
  Expected: 0 (none of the old inaccurate names should remain)

  **Step 3: Security Verification**

  ```bash
  # NO secrets, API keys, tokens in SOUL.md
  grep -iE '(sk-|sk-ant-|api_key\s*=|token\s*=\s*["'"'"'][a-zA-Z0-9]|secret\s*=\s*["'"'"'][a-zA-Z0-9]|password\s*=\s*["'"'"'][a-zA-Z0-9])' AIM/hermes/skills/aim/SOUL.md
  ```
  Expected: EMPTY OUTPUT (no matches)

  ```bash
  # ADMIN-gated tools must appear with "ADMIN" context
  for tool in show_all_leads search_telegram_chats send_telegram_message; do
    echo "=== $tool ===" 
    grep -A2 -B2 "$tool" AIM/hermes/skills/aim/SOUL.md | grep -c "ADMIN"
  done
  ```
  Each tool must return >= 1

  **Step 4: Structure Verification**

  ```bash
  # Required H2 sections
  echo "=== Required sections ==="
  for section in "Режимы работы" "Инструменты" "Магистры и субагенты" "WOW-Данные" "Token Economy" "Lead Dossier" "Follow-up" "Российский рынок" "Услуги и цены" "KPI" "Стиль общения" "Самообучение" "Ежедневный чек-лист"; do
    grep -q "## $section" AIM/hermes/skills/aim/SOUL.md && echo "OK: $section" || echo "MISSING: $section"
  done
  ```

  ```bash
  # Frontmatter intact
  head -1 AIM/hermes/skills/aim/SOUL.md | grep -q "^---$" && echo "OK: frontmatter opens" || echo "MISSING: frontmatter"
  grep -q "^name: aim-operator" AIM/hermes/skills/aim/SOUL.md && echo "OK: name field" || echo "MISSING: name"
  grep -q "^license: MIT" AIM/hermes/skills/aim/SOUL.md && echo "OK: license" || echo "MISSING: license"

  # Closing line
  tail -1 AIM/hermes/skills/aim/SOUL.md | grep -q "Operator агентства AIM" && echo "OK: closing signature" || echo "MISSING: closing"

  # Line count range
  LINES=$(wc -l < AIM/hermes/skills/aim/SOUL.md)
  if [ $LINES -ge 550 ] && [ $LINES -le 700 ]; then
    echo "OK: line count $LINES (target: 550-650, acceptable: 550-700)"
  elif [ $LINES -lt 550 ]; then
    echo "WARNING: too short ($LINES lines). May be missing sections."
  else
    echo "WARNING: too long ($LINES lines). May exceed effective context window."
  fi
  ```

  **Step 5: Fix Any Failures**

  For each failed check, read the relevant section of SOUL.md, identify the gap, and edit the file to fix it. Re-run the failed check to confirm it passes.

  At the end of this task, ALL automated checks must PASS.
  </action>

  <verify>
  <automated>cd /Users/mikhaileliseev/Desktop/Dev/!meAI && bash -c '
  FAILS=0
  echo "=== V-D01: Magister Architecture ==="
  grep -c "ci_tech\|ci_orchestrator\|keyword_research_agent\|yandex_direct_client\|analytics_agent\|ci_auditor" AIM/hermes/skills/aim/SOUL.md | tail -1
  echo "=== V-D02: Mode Sections ==="
  grep -c "^###.*PRESALE\|^###.*ACTIVE\|^###.*ADMIN" AIM/hermes/skills/aim/SOUL.md
  echo "=== V-D05: Token Economy ==="
  grep -c "Tier 0\|Tier 1\|Tier 2" AIM/hermes/skills/aim/SOUL.md
  echo "=== V-D10: All 8 Tools ==="
  for t in run_seo_audit run_content_analysis run_ads_report show_project_status collect_contact show_all_leads search_telegram_chats send_telegram_message; do
    grep -q "$t" AIM/hermes/skills/aim/SOUL.md && echo "  OK: $t" || { echo "  MISSING: $t"; FAILS=$((FAILS+1)); }
  done
  echo "=== Security: No Secrets ==="
  SECRETS=$(grep -iEc "sk-ant-|api_key\s*=\s*.a-zA-Z|token\s*=\s*.a-zA-Z" AIM/hermes/skills/aim/SOUL.md)
  if [ "$SECRETS" -eq 0 ]; then echo "  OK: No secrets found"; else echo "  FAIL: $SECRETS potential secrets!"; FAILS=$((FAILS+1)); fi
  echo "=== Line Count ==="
  wc -l AIM/hermes/skills/aim/SOUL.md
  echo "=== Total Failures: $FAILS ==="
  exit $FAILS
  '</automated>
  </verify>

  <acceptance_criteria>
  - ALL V-D01 through V-D10 checks pass (0 failures from the bash verification script)
  - All 8 tool names found in SOUL.md (each >= 1 match)
  - No old inaccurate subagent names ("Technical SEO Auditor", "Medical Copywriter", etc.) remain — grep must return 0
  - No secrets, API keys, or tokens in SOUL.md — grep -iE must return empty
  - Every ADMIN-gated tool (show_all_leads, search_telegram_chats, send_telegram_message) appears near "ADMIN" context
  - All 13 required H2 sections present
  - YAML frontmatter intact (opens with ---, has name: aim-operator, has license: MIT)
  - File ends with "Operator агентства AIM"
  - Line count between 550-700
  - `grep -oP '[a-z_]+\.py' SOUL.md | sort -u | while read f; do find AIM/src/aim/subagents -name "$f" | grep -q . || echo "MISSING: $f"; done` returns no MISSING entries
  </acceptance_calls>

  <done>All automated verification checks against D-01 through D-10 pass. SOUL.md is complete, accurate, and secure. Any gaps found during verification have been fixed. File is 550-650 lines with correct subagent names, tool schemas, and structure. Ready for human review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human Review — Manual Verification of SOUL.md Completeness & Accuracy</name>
  <files>AIM/hermes/skills/aim/SOUL.md</files>

  <what-built>
  A comprehensive SOUL.md (550-650 lines) that encodes complete AIM system knowledge for the Hermes AIAgent. The file has passed all automated verification checks:
  - All 10 knowledge domains (D-01 through D-10) covered
  - Subagent names verified against actual codebase files
  - All 8 MCP tools documented with I/O schemas
  - Mode sections are self-contained
  - No secrets or credentials in the file
  - All required sections present
  </what-built>

  <how-to-verify>
  **Review instructions for Mikhail:**

  1. **Read the complete SOUL.md:**
     ```
     cat AIM/hermes/skills/aim/SOUL.md
     ```
     Or open in your editor. Read through all sections.

  2. **Check these critical items:**
     - [ ] Identity — does Hermes sound right? The "iPhone маркетинга" metaphor, Operator identity
     - [ ] Mode Switching — are PRESALE/ACTIVE/ADMIN behavior rules correct? Would Hermes act appropriately in each?
     - [ ] Tool Catalog — all 8 tools listed correctly? Especially the I/O schemas for run_seo_audit and collect_contact
     - [ ] Magister Architecture — do the subagent names look right? Any missing?
     - [ ] WOW-Data + 3 Numbers — is the progressive reveal strategy correct?
     - [ ] Token Economy — are the tiers and mode-based rules right?
     - [ ] Lead Dossier + Follow-up — status flow and day-based rules correct?
     - [ ] Russian Market — legal compliance, platforms, "what doesn't work" table accurate?
     - [ ] Services & Pricing — prices correct? Any services missing?
     - [ ] Communication Style — tone and rules appropriate?
     - [ ] Self-Improvement — auto-skill creation rules make sense?

  3. **Verify supplementary files are correctly referenced:**
     - services.md, processes.md, kpi.md — are they mentioned with the right context?

  4. **Check for anything wrong or missing:**
     - Any outdated information?
     - Any capability Hermes should know about but doesn't?
     - Any rules that would cause Hermes to behave incorrectly?
  </how-to-verify>

  <resume-signal>Type "approved" if SOUL.md is complete and accurate, or describe specific issues to fix (e.g., "missing X subagent", "wrong price for Y", "mode Z rule is incorrect")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SOUL.md content → AIAgent | SOUL.md is loaded verbatim into LLM context. Verification ensures no inaccuracies or leaked secrets. |
| Human reviewer → SOUL.md | The human checkpoint is the final gate before SOUL.md enters production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Information Disclosure | SOUL.md automated check | mitigate | Step 3 of Task 1 runs grep for secrets patterns (sk-ant-, api_key=, token=). Checkpoint exits with FAIL if any found. |
| T-16-06 | Elevation of Privilege | Mode gating in SOUL.md | mitigate | Step 3 verifies ADMIN-only tools (show_all_leads, search_telegram_chats, send_telegram_message) appear with ADMIN context. |
| T-16-07 | Tampering | SOUL.md accuracy | mitigate | Step 2 cross-references subagent .py filenames against actual filesystem. Any missing file = FAIL. Human checkpoint is the final accuracy gate. |
</threat_model>

<verification>
After human approves, the phase is complete. SOUL.md is production-ready.
</verification>

<success_criteria>
- [ ] All automated verification checks pass (0 failures)
- [ ] Human reviewer approves SOUL.md as complete and accurate
- [ ] SOUL.md ready for deployment via Docker image rebuild
- [ ] git diff shows the rewritten SOUL.md with all knowledge domains
</success_criteria>

<output>
After completion, create `.planning/phases/16-hermes-knowledge/16-02-SUMMARY.md`
</output>
