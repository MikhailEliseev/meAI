---
phase: 3
plan: 03-01
type: execute
name: deploy-and-wire-instagram-tools
wave: 1
depends_on: []
requirements: [IG-01]
tags: [instagram, deploy, pipeline, tool-handlers, docker-cp]

files_modified:
  - AIM/hermes/app/pipeline/engine.py

user_setup:
  - service: docker
    why: "Deploy v2 of run_instagram_content.py to live aim-hermes container via docker cp (no image rebuild, per D-12)"
    env_vars: []
    dashboard_config:
      - task: "Verify SSH access to Polish server"
        location: "ssh aim (Polish server 78.17.128.169 — see auto-memory deploy-target.md)"
      - task: "Verify aim-hermes container is running and healthy before deploy"
        location: "ssh aim 'docker ps | grep aim-hermes'"

must_haves:
  truths:
    - "D-12: Local v2 of run_instagram_content.py (Perplexity-based, 718 lines) is deployed to aim-hermes container via `docker cp` (no image rebuild), replacing v1 (Apify-based, 371 lines, broken). Gateway is restarted, not the container"
    - "Local find_doctor_handles.py (1205 lines) is present in the container (it should already be there — verify)"
    - "D-13: Both run_instagram_content and find_doctor_handles are registered in engine.py:_TOOL_HANDLERS dict with their (module_path, handler_fn) tuples — two new entries added"
    - "PipelineEngine._get_handler('run_instagram_content') returns the handler, not raises 'No handler mapping for tool'"
    - "PipelineEngine._get_handler('find_doctor_handles') returns the handler, not raises 'No handler mapping for tool'"
    - "aim-hermes container remains healthy after deploy (no production regression)"
    - "Existing 22 _TOOL_HANDLERS entries are untouched (no rename, no removal)"
  artifacts:
    - path: "AIM/hermes/app/pipeline/engine.py"
      provides: "Two new entries in _TOOL_HANDLERS dict for run_instagram_content and find_doctor_handles"
      contains: "run_instagram_content"
    - path: "aim-hermes:/opt/hermes/app/tools/run_instagram_content.py"
      provides: "Deployed v2 Perplexity-based Instagram tool"
      contains: "Perplexity"
  key_links:
    - from: "app/pipeline/engine.py:_TOOL_HANDLERS"
      to: "app.tools.run_instagram_content.handle_run_instagram_content"
      via: "dict entry with module_path + function name"
      pattern: "run_instagram_content.*app\\.tools\\.run_instagram_content"
    - from: "app/pipeline/engine.py:_TOOL_HANDLERS"
      to: "app.tools.find_doctor_handles.handle_find_doctor_handles"
      via: "dict entry with module_path + function name"
      pattern: "find_doctor_handles.*app\\.tools\\.find_doctor_handles"

# requires user verification of docker cp + container restart
autonomous: false
---

<objective>
Deploy the existing local v2 of `run_instagram_content.py` (Perplexity-based, working) to the live `aim-hermes` container, replacing the broken v1 (Apify-based). Wire both Instagram tools (`run_instagram_content` + `find_doctor_handles`) into `PipelineEngine._TOOL_HANDLERS` so the pipeline fallback path (ORC-05) can invoke them — not just the LLM-orchestrator path.

Purpose: Phase 1 Research (RES-05) confirmed v1 is broken in container (`No active Apify keys available` due to `k["token"]` vs `k["key"]` field name bug), v2 is ready locally but never deployed, and both tools are absent from `_TOOL_HANDLERS` (23 entries). This plan closes all three gaps in one atomic deploy — the foundation for Phase 3 Instagram integration.

Output: Modified `AIM/hermes/app/pipeline/engine.py` (2 new entries); deployed v2 in container; verified handler resolution.
</objective>

## Phase Goal (MVP)

**As a** Hermes presale orchestrator,
**I want to** call `run_instagram_content` and `find_doctor_handles` through the standard pipeline dispatch path (not only the LLM-registry path),
**so that** PipelineEngine fallback (ORC-05) can also use Instagram analysis when ORCHESTRATOR_MODE=0 or when orchestrator falls back on exception.

## User Story (Vertical Slice)

After this plan, when the orchestrator OR PipelineEngine calls `run_instagram_content` with a doctor handle, the call succeeds (no `No handler mapping` error, no `No active Apify keys available` error), returning real data via Perplexity or honest "no data" JSON per the v2 contract verified in Plan 01-04.

---

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-instagram-integration/03-CONTEXT.md
@.planning/phases/01-research-diagnosis/RESEARCH.md
@.planning/phases/01-research-diagnosis/evidence/instagram-tool-test.md
@.planning/phases/02-3-pass-orchestrator-coverage-checklist/02-VERIFICATION.md

@AIM/hermes/app/pipeline/engine.py
@AIM/hermes/app/tools/run_instagram_content.py
@AIM/hermes/app/tools/find_doctor_handles.py
@CLAUDE.md

<interfaces>
<!-- Executor: read these signatures from the actual files before touching anything. -->

From AIM/hermes/app/pipeline/engine.py (current state — 22 entries):
```python
# Lines 41-65 (approximate — read the file to confirm)
_TOOL_HANDLERS: dict[str, tuple[str, str]] = {
    "web_search":              ("app.tools.run_web_search",         "handle_run_web_search"),
    # ... 21 more entries ...
    "scrapy_crawl":            ("app.tools.scrapy_runner",          "handle_scrapy_crawl"),
}
```

From AIM/hermes/app/tools/run_instagram_content.py (local v2, 718 lines):
```python
async def handle_run_instagram_content(handle=None, handles=None, **kwargs) -> str:
    """Deep Instagram content analysis via Perplexity for one or multiple accounts.
    Args:
        handle: Single Instagram handle WITHOUT @ (e.g., "dr_ivanova").
        handles: Array of Instagram handles WITHOUT @. Up to 5 will be analysed.
    Returns:
        JSON with either single profile analysis or aggregated array of analyses.
    """
```

From AIM/hermes/app/tools/find_doctor_handles.py (local, 1205 lines):
```python
async def handle_find_doctor_handles(url=None, **kwargs) -> str:
    # Discover Instagram handles for doctors listed at the clinic URL.
```

From engine.py:_get_handler (line 71-89):
```python
def _get_handler(tool_name: str):
    entry = _TOOL_HANDLERS.get(tool_name)
    if not entry:
        raise ValueError(f"No handler mapping for tool: {tool_name}")
    module_path, fn_name = entry
    module = importlib.import_module(module_path)
    handler = getattr(module, fn_name)
    _handler_cache[tool_name] = handler
    return handler
```
</interfaces>
</context>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add run_instagram_content + find_doctor_handles entries to _TOOL_HANDLERS dict in engine.py</name>
  <files>AIM/hermes/app/pipeline/engine.py</files>

  <read_first>
    - AIM/hermes/app/pipeline/engine.py (lines 38-65 — read the full _TOOL_HANDLERS dict to see the existing 22 entries and the surrounding comment blocks; confirm exact line numbers before editing)
    - AIM/hermes/app/tools/run_instagram_content.py (line 1 — module docstring confirms `app.tools.run_instagram_content` is the correct module path; the handler function is named `handle_run_instagram_content` — confirmed by signature `async def handle_run_instagram_content(handle=None, handles=None, **kwargs)`)
    - AIM/hermes/app/tools/find_doctor_handles.py (line 1 — module docstring + grep `def handle_find_doctor_handles` confirms handler name)
    - .planning/phases/01-research-diagnosis/evidence/instagram-tool-test.md (Section "Handler registration gap" — confirms both tools are currently ABSENT from `_TOOL_HANDLERS`)
  </read_first>

  <action>
    Open AIM/hermes/app/pipeline/engine.py, locate the `_TOOL_HANDLERS` dict (starts at line 41, ends at line 65 with the closing `}`).

    Add two new entries to the dict, immediately AFTER the existing `"scrapy_crawl": (...)` entry (the last v7.1 entry) and BEFORE the closing brace. Use this exact format aligned with the existing column alignment (spaces, not tabs):

    ```
        # ── Phase 3 / IG-01: Instagram tools ────────────────────────────
        "run_instagram_content":   ("app.tools.run_instagram_content", "handle_run_instagram_content"),
        "find_doctor_handles":     ("app.tools.find_doctor_handles",   "handle_find_doctor_handles"),
    ```

    The module paths `app.tools.run_instagram_content` and `app.tools.find_doctor_handles` MUST match the actual module locations (verified via `read_first` — these are the canonical paths used by `__init__.py:74-75` for LLM-registry imports).

    The handler function names `handle_run_instagram_content` and `handle_find_doctor_handles` MUST match the actual `async def` names in the respective files (verified via `read_first`).

    Do NOT modify, rename, or remove any of the existing 22 entries. Do NOT reorder existing entries. The only change is two new lines plus one comment line.

    Do NOT change the dict's type annotation (`dict[str, tuple[str, str]]`). The new entries match the existing schema.

    Do NOT change `_handler_cache`, `_get_handler`, or any other function in the file.

    After the edit, save the file. Do NOT run any commands that modify production state (e.g., no `docker restart`, no `docker cp` — those are Task 2).
  </action>

  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes && python3 -c "
import ast
with open('app/pipeline/engine.py') as f:
    tree = ast.parse(f.read())
# Find _TOOL_HANDLERS assignment and count entries
for node in ast.walk(tree):
    if isinstance(node, ast.Assign) and any(isinstance(t, ast.Name) and t.id == '_TOOL_HANDLERS' for t in node.targets):
        keys = [k.value if isinstance(k, ast.Constant) else '?' for k in node.value.keys]
        assert 'run_instagram_content' in keys, 'run_instagram_content missing'
        assert 'find_doctor_handles' in keys, 'find_doctor_handles missing'
        assert len(keys) == 24, f'Expected 24 entries (22+2), got {len(keys)}'
        print(f'OK: _TOOL_HANDLERS has {len(keys)} entries including run_instagram_content and find_doctor_handles')
        break
else:
    raise AssertionError('_TOOL_HANDLERS not found in engine.py')
"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "run_instagram_content" AIM/hermes/app/pipeline/engine.py` returns at least 1
    - `grep -c "find_doctor_handles" AIM/hermes/app/pipeline/engine.py` returns at least 1
    - `python3 -c "import ast; ast.parse(open('AIM/hermes/app/pipeline/engine.py').read())"` exits 0 (syntax valid)
    - The 22 pre-existing entries (web_search, run_pagespeed, run_seo_audit, find_competitors, run_review_platforms, run_content_analysis, run_hh_analysis, run_doctor_dossiers, run_ci_analysis, run_smi_mentions, run_content_gaps, find_company_financials, generate_html_report, publish_scout_report, perplexity_search, perplexity_deep_analyze, firecrawl_extract, firecrawl_batch_scrape, firecrawl_agent, crawlee_scrape, crawlee_search, scrapy_crawl) are ALL still present and unchanged
    - `_TOOL_HANDLERS` dict has exactly 24 entries (22 original + 2 new)
    - Both new entries use the `"tool_name": ("module.path", "handler_fn")` tuple format matching the existing entries
  </acceptance_criteria>

  <done>
    engine.py contains the two new `_TOOL_HANDLERS` entries with correct module paths and handler function names; dict has 24 entries total; syntax valid; existing 22 entries unchanged.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy local v2 of run_instagram_content.py to aim-hermes container via docker cp (per D-12)</name>
  <files>aim-hermes:/opt/hermes/app/tools/run_instagram_content.py (server-side path)</files>

  <read_first>
    - .planning/phases/01-research-diagnosis/evidence/instagram-tool-test.md (Section "Step 3" — v1 failure mode; Section "Gaps for Phase 3" — exact `docker cp` command)
    - .planning/phases/03-instagram-integration/03-CONTEXT.md (decision D-12 — deploy via docker cp, no image rebuild)
    - AIM/hermes/app/tools/run_instagram_content.py (local — verify md5 matches the documented v2 md5 `0bf035e1d7faaf621bc921b9db531b63` before deploying)
    - CLAUDE.md (section "SSH-подключения" — `ssh aim` convention for server commands)
    - auto-memory `deploy-target.md` (Polish server access details)
  </read_first>

  <action>
    This task requires SSH access to the Polish server (`ssh aim`) and cannot be executed autonomously — the user must initiate the SSH command from their local machine, and Docker writes inside the running production container are irreversible without a restart.

    The Claude executor should attempt the SSH deploy directly (via `ssh aim "docker cp ..."`). If SSH requires interactivity (password prompt, TTY), the executor must surface this checkpoint to the user with the exact command to run.

    Steps the executor attempts:

    1. Verify SSH connectivity: `ssh aim "hostname && whoami && pwd"` — confirm host is `AIM-Server-PL` (Polish server 78.17.128.169), user is `root`, working directory is `/root` or similar.

    2. Verify aim-hermes container is running: `ssh aim "docker ps --format '{{.Names}}\t{{.Status}}' | grep aim-hermes"` — confirm status contains `Up` and `(healthy)`.

    3. Compute local md5 of the v2 file: `md5 -q AIM/hermes/app/tools/run_instagram_content.py` (macOS) — must match `0bf035e1d7faaf621bc921b9db531b63` per Plan 01-04 evidence.

    4. Compute container-side md5 of the current v1 file BEFORE deploy: `ssh aim "docker exec aim-hermes md5sum /opt/hermes/app/tools/run_instagram_content.py"` — must match `a7a7a1dde5dc4cfc8bf8b6c1543c122f` (v1, broken). This confirms we are replacing the right file.

    5. Create a backup of the container's v1: `ssh aim "docker exec aim-hermes cp /opt/hermes/app/tools/run_instagram_content.py /opt/hermes/app/tools/run_instagram_content.py.v1-backup-$(date +%Y%m%d)"` — rollback safety net (per PROJECT.md rollback pattern).

    6. Deploy local v2 to container: `ssh aim "docker cp AIM/hermes/app/tools/run_instagram_content.py aim-hermes:/opt/hermes/app/tools/run_instagram_content.py"` — this is the atomic deploy step. If this fails with "no such container" or "permission denied", surface to user.

       IMPORTANT: This command must be run from the repo root `/Users/mikhaileliseev/Desktop/Dev/meAI` so the local path resolves. Use absolute source path: `docker cp /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/app/tools/run_instagram_content.py aim-hermes:/opt/hermes/app/tools/run_instagram_content.py` is safer if SSH working directory is ambiguous.

    7. Verify post-deploy md5: `ssh aim "docker exec aim-hermes md5sum /opt/hermes/app/tools/run_instagram_content.py"` — must now match the v2 md5 `0bf035e1d7faaf621bc921b9db531b63`.

    8. Verify v2 docstring mentions Perplexity (sanity check the right version landed): `ssh aim "docker exec aim-hermes head -20 /opt/hermes/app/tools/run_instagram_content.py | grep -i perplexity"` — must return at least one line. The v1 docstring mentions Apify, not Perplexity.

    9. Restart gateway only (NOT the container, NOT the image) — per D-12 / DPL-01: `ssh aim "docker exec aim-hermes pkill -HUP uvicorn || docker restart aim-hermes-nginx"` — BUT this may not be necessary because Python imports the handler lazily on first call (per `_get_handler` pattern). Default action: skip restart, verify import works via next step. If import fails, fall back to gateway restart.

    10. Verify the new handler is import-resolvable from inside the container: `ssh aim "docker exec aim-hermes python -c 'from app.tools.run_instagram_content import handle_run_instagram_content; print(handle_run_instagram_content.__doc__[:80])'"` — must return a docstring starting with "Deep Instagram content analysis via Perplexity" (the v2 docstring). If it returns "via Apify" — v1 is still loaded; restart container.

    11. Verify `find_doctor_handles` is already present in the container (it should be — only `run_instagram_content` was missing): `ssh aim "docker exec aim-hermes ls -la /opt/hermes/app/tools/find_doctor_handles.py"` — file must exist. If absent, also deploy it: `ssh aim "docker cp /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/app/tools/find_doctor_handles.py aim-hermes:/opt/hermes/app/tools/find_doctor_handles.py"`.

    12. Final handler-resolution check: `ssh aim "docker exec aim-hermes python -c \"
import sys; sys.path.insert(0, '/opt/hermes')
from app.pipeline.engine import _TOOL_HANDLERS, _get_handler
assert 'run_instagram_content' in _TOOL_HANDLERS, 'run_instagram_content missing from _TOOL_HANDLERS'
assert 'find_doctor_handles' in _TOOL_HANDLERS, 'find_doctor_handles missing from _TOOL_HANDLERS'
h1 = _get_handler('run_instagram_content')
h2 = _get_handler('find_doctor_handles')
print('run_instagram_content handler:', h1.__name__)
print('find_doctor_handles handler:', h2.__name__)
print('OK: both handlers resolve')
\""` — must print "OK: both handlers resolve" with no exception.

    13. Health check: `ssh aim "curl -s -o /dev/null -w '%{http_code}' http://localhost:8000/health"` — must return 200 (no production regression).

    If any step fails, document the failure in the plan SUMMARY and surface to user. Do NOT proceed to Plan 03-02 until this plan completes cleanly — downstream plans depend on a working Instagram tool.
  </action>

  <verify>
    <automated>ssh aim "docker exec aim-hermes python -c \"
import sys; sys.path.insert(0, '/opt/hermes')
from app.pipeline.engine import _TOOL_HANDLERS, _get_handler
assert 'run_instagram_content' in _TOOL_HANDLERS
assert 'find_doctor_handles' in _TOOL_HANDLERS
h1 = _get_handler('run_instagram_content')
h2 = _get_handler('find_doctor_handles')
assert h1.__name__ == 'handle_run_instagram_content'
assert h2.__name__ == 'handle_find_doctor_handles'
print('OK: 24 entries, both Instagram handlers resolve')
\""</automated>
    <automated>ssh aim "curl -s -o /dev/null -w '%{http_code}' http://localhost:8000/health"</automated>
  </verify>

  <acceptance_criteria>
    - Local md5 of `AIM/hermes/app/tools/run_instagram_content.py` equals `0bf035e1d7faaf621bc921b9db531b63` (v2 confirmed before deploy)
    - Container md5 of `/opt/hermes/app/tools/run_instagram_content.py` AFTER deploy equals `0bf035e1d7faaf621bc921b9db531b63` (v2 deployed)
    - `docker exec aim-hermes head -20 /opt/hermes/app/tools/run_instagram_content.py | grep -i perplexity` returns at least one match (v2 docstring landed)
    - `docker exec aim-hermes python -c 'from app.tools.run_instagram_content import handle_run_instagram_content; print(handle_run_instagram_content.__doc__[:80])'` returns a docstring containing "Perplexity" (NOT "Apify")
    - `docker exec aim-hermes ls /opt/hermes/app/tools/find_doctor_handles.py` exits 0 (file exists in container)
    - `docker exec aim-hermes python -c 'from app.pipeline.engine import _TOOL_HANDLERS; assert \"run_instagram_content\" in _TOOL_HANDLERS and \"find_doctor_handles\" in _TOOL_HANDLERS; print(\"OK\")'` prints "OK"
    - `curl http://localhost:8000/health` from the server returns HTTP 200
    - `docker ps | grep aim-hermes` shows status `Up ... (healthy)` (no container restart needed, OR if restarted, healthy within 60s)
    - Backup of v1 exists at `/opt/hermes/app/tools/run_instagram_content.py.v1-backup-YYYYMMDD` inside the container (rollback path available per DPL-05)
  </acceptance_criteria>

  <done>
    Container's `/opt/hermes/app/tools/run_instagram_content.py` md5 = `0bf035e1d7faaf621bc921b9db531b63` (v2); both Instagram tools resolve through `_TOOL_HANDLERS` from inside the container; `/health` returns 200; v1 backup exists for rollback.
  </done>

  <resume-signal>Type "deployed" if all steps succeeded, or describe the failing step + error message if SSH/docker cp encountered issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → Production container | `docker cp` writes a file into a running production container — irreversible without restart, bypasses CI/CD |
| Production container → AIM internal network | Tool handler now resolvable; LLM may invoke it and trigger Perplexity API calls (cost + rate limit surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-S | Spoofing | SSH key used for `ssh aim` | accept | Polish server access controlled by SSH key (per auto-memory server-ssh-key.md); no new attack surface introduced |
| T-03-01-T | Tampering | `docker cp` of run_instagram_content.py | mitigate | Step 5 creates `.v1-backup-YYYYMMDD` inside container before overwrite; md5 verification before+after deploy confirms content integrity |
| T-03-01-R | Repudiation | Deploy action not logged | mitigate | Plan SUMMARY records timestamp + md5 + step-by-step outcome; executor log retained in session archive |
| T-03-01-I | Info disclosure | Perplexity API key accessed by deployed v2 | accept | Key was already in env (`PERPLEXITY_API_KEY`); v2 only reads it — no new exfil path; container isolation unchanged |
| T-03-01-D | DoS | New handlers enable LLM to call Perplexity in batch (8-10 handles) | accept | Bounded by Plan 03-04 batch-size cap; Perplexity has its own account-level rate limits; cost is bounded (single presale run) |
| T-03-01-E | EoP | PipelineEngine now dispatches to tools it previously could not | accept | Tools run under the same process/user as existing handlers; no privilege change; lazy import via `_get_handler` unchanged |
| T-03-01-SC | Supply chain | No new packages installed — only local repo file deployed | accept | No pip install, no npm — supply chain unchanged |
</threat_model>

<verification>
**Post-plan automated checks:**

1. `python3 -c "import ast; ast.parse(open('AIM/hermes/app/pipeline/engine.py').read())"` exits 0 locally
2. AST scan confirms `_TOOL_HANDLERS` dict has exactly 24 entries including the two new Instagram tools
3. `ssh aim "docker exec aim-hermes python -c 'from app.pipeline.engine import _TOOL_HANDLERS; assert len(_TOOL_HANDLERS) >= 24; print(\"OK\")'"` prints "OK"
4. `ssh aim "curl -s -o /dev/null -w '%{http_code}' http://localhost:8000/health"` returns 200
5. `ssh aim "docker exec aim-hermes python -c 'from app.tools.run_instagram_content import handle_run_instagram_content; assert \"Perplexity\" in (handle_run_instagram_content.__doc__ or \"\"); print(\"v2 deployed\")'"` prints "v2 deployed"
6. The 22 pre-existing entries are byte-identical (executor confirms via grep for each name before and after the edit)

**Regression checks (per Phase 2 VERIFICATION.md):**
- `ORCHESTRATOR_MODE=0` (default) → existing PRESALE flow path unaffected (engine.py changes only ADD entries; no existing logic touched)
- Pass 1, Pass 2, Pass 3 orchestrator cycle unchanged (engine.py is NOT imported by orchestrator/)
- PipelineEngine (ORC-05 fallback) only GAINS capability — cannot lose existing behaviour
</verification>

<success_criteria>
- IG-01 SATISFIED: `run_instagram_content` is callable by both LLM-orchestrator (already was) AND PipelineEngine (now wired in `_TOOL_HANDLERS`)
- `find_doctor_handles` also wired (required for Plan 03-04 doctor discovery flow)
- v2 deployed to container (replaces broken v1)
- aim-hermes container healthy post-deploy (no production regression)
- v1 backup retained inside container for rollback (per DPL-05)
- Existing 22 `_TOOL_HANDLERS` entries unchanged (no rename, no removal)
</success_criteria>

<output>
Create `.planning/phases/03-instagram-integration/03-01-SUMMARY.md` when done. Record:
- Local + container md5 values (before/after)
- Timestamp of deploy
- Output of each step (especially any SSH/docker cp errors encountered)
- Health-check URL response
- Rollback instructions (delete v2, rename v1-backup back to original name)
</output>
