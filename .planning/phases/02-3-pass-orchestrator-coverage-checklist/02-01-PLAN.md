---
phase: 02-3-pass-orchestrator-coverage-checklist
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AIM/hermes/app/tools/generate_html_report.py
  - AIM/hermes/app/tools/publish_scout_report.py
autonomous: true
requirements: [ORC-05]
tags: [p0-bugfix, html-build, regression]

must_haves:
  truths:
    - "generate_html_report выполняется без NameError на любой сессии из sessions-archive/"
    - "publish_scout_report выполняется без NameError"
    - "HTML BUILD фаза больше не падает с `_unwrap_tool_output is not defined`"
    - "PipelineEngine fallback режим (ORC-05) снова работает end-to-end включая генерацию HTML"
  artifacts:
    - path: "AIM/hermes/app/tools/generate_html_report.py"
      provides: "Рабочий generate_html_report без NameError"
      contains: "_unwrap_tool_output"
    - path: "AIM/hermes/app/tools/publish_scout_report.py"
      provides: "Рабочий publish_scout_report без NameError"
  key_links:
    - from: "AIM/hermes/app/tools/generate_html_report.py"
      to: "AIM/hermes/app/tools/publish_scout_report.py"
      via: "shared utility _unwrap_tool_output (импорт или локальное определение)"
      pattern: "_unwrap_tool_output"
---

<objective>
Исправить P0 регрессию `_unwrap_tool_output NameError`, которая ломает HTML BUILD + PRESENTATION фазы в 40% сессий (см. RESEARCH.md Section 5.3 P0 и evidence/session-log-analysis.md).

Purpose: Восстановить работающий пресейл-поток PipelineEngine перед любыми работами по оркестратору. Без этого фикса невозможно отличить «оркестратор не работает» от «HTML BUILD падает из-за старого бага».

Output: 2 файла исправлены, генерация HTML восстановлена, PipelineEngine fallback режим (ORC-05) снова functional end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-research-diagnosis/RESEARCH.md

@AIM/hermes/app/tools/generate_html_report.py
@AIM/hermes/app/tools/publish_scout_report.py
@AIM/hermes/app/tools/session_archive.py

<bug_evidence>
Из evidence/session-log-analysis.md (Plan 01-02):
- Session `tg:322367335`: NameError на HTML BUILD
- Session `4975ef15-de5`: NameError, сессия упала на 1/13 фаз
- Bug введён между Jun 20 (test-iphk-002 unaffected) и Jun 21 (tg/full-test affected)

Из generate_html_report.py:
- Line 22: `from app.tools.session_archive import load_all_data`
- Line 208: `def _unwrap_tool_output(phase_data: dict) -> dict | None:` (локальное определение)
- Line 298: вызов `_unwrap_tool_output(phase_data)` (в той же функции/области видимости?)

Из publish_scout_report.py:
- Line 20: `from app.tools.session_archive import load_all_data, SESSIONS_ROOT`
- Нет локального определения `_unwrap_tool_output` (вероятно ожидает импорт)

Гипотеза: функция определена только в generate_html_report.py:208, но publish_scout_report.py тоже её вызывает — отсюда NameError. ИЛИ в generate_html_report.py есть путь вызова функции (line 298) до её определения (line 208) — это возможно если вызов происходит на верхнем уровне модуля или в импорте.
</bug_evidence>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Диагностика и исправление _unwrap_tool_output NameError</name>
  <files>AIM/hermes/app/tools/generate_html_report.py, AIM/hermes/app/tools/publish_scout_report.py</files>
  <read_first>
    - AIM/hermes/app/tools/generate_html_report.py (весь файл, обратить внимание на line 208 определение и line 298 вызов)
    - AIM/hermes/app/tools/publish_scout_report.py (весь файл, искать вызовы _unwrap_tool_output)
    - AIM/hermes/app/tools/session_archive.py (что экспортирует: load_all_data, и возможно другие утилиты)
    - .planning/phases/01-research-diagnosis/evidence/session-log-analysis.md (точные строки ошибок из логов)
  </read_first>
  <behavior>
    - Случай 1 (внутри generate_html_report.py): вызов `_unwrap_tool_output(phase_data)` на line 298 происходит внутри функции, а определение на line 208 — на уровне модуля. Если вызов происходит ДО определения при импорте модуля → переместить определение выше или сделать lazy.
    - Случай 2 (publish_scout_report.py вызывает _unwrap_tool_output): функция НЕ импортирована. Нужно либо добавить импорт `from app.tools.generate_html_report import _unwrap_tool_output`, либо вынести функцию в общий модуль (например session_archive.py или новый utils).
    - Случай 3 (оба файла её вызывают, но определена только в одном): вынести в общий utils модуль и импортировать в обоих.
    - Все случаи: после фикса `python3 -c "from app.tools.generate_html_report import handle_generate_html_report"` и `python3 -c "from app.tools.publish_scout_report import handle_publish_scout_report"` должны работать без ошибок.
  </behavior>
  <action>
    1. Прочитать generate_html_report.py полностью. Найти все места вызова `_unwrap_tool_output` и место определения (line 208). Понять почему `NameError` возникает — вызов ДО определения? Или функция определена внутри другой функции (closure)?

    2. Прочитать publish_scout_report.py полностью. Найти все места вызова `_unwrap_tool_output`. Если вызов есть, а определения нет — это и есть баг.

    3. Выбрать стратегию фикса:
       - Если функция нужна в обоих модулях: вынести в общий модуль. Лучший кандидат — `app/tools/session_archive.py` (уже imported обоими файлами, имеет утилитарный характер). Определить там, обновить импорты в обоих файлах.
       - Если проблема только в generate_html_report.py (порядок определения): переместить определение `_unwrap_tool_output` выше места первого вызова.
       - Если проблема только в publish_scout_report.py (нет импорта): добавить `from app.tools.generate_html_report import _unwrap_tool_output` или вынести в общий модуль (предпочтительно).

    4. Внести исправления минимально инвазивно. НЕ рефакторить, НЕ переименовывать, НЕ менять сигнатуру. Цель — восстановить работу, не улучшать.

    5. Проверить что нет других мест в `app/tools/` которые тоже вызывают `_unwrap_tool_output`: `grep -rn "_unwrap_tool_output" AIM/hermes/app/`. Если есть — убедиться что они работают после фикса.

    6. Зафиксировать в комментарии кода (1 строка): "Per Phase 2 P0 fix — RESEARCH.md Section 5.3" для traceability.
  </action>
  <verify>
    <automated>cd AIM/hermes &amp;&amp; python3 -c "from app.tools.generate_html_report import handle_generate_html_report, _unwrap_tool_output; from app.tools.publish_scout_report import handle_publish_scout_report; print('OK: imports clean')"</automated>
    <automated>cd AIM/hermes &amp;&amp; python3 -c "import ast; ast.parse(open('app/tools/generate_html_report.py').read()); ast.parse(open('app/tools/publish_scout_report.py').read()); print('OK: syntax valid')"</automated>
    <automated>grep -c "_unwrap_tool_output" AIM/hermes/app/tools/generate_html_report.py AIM/hermes/app/tools/publish_scout_report.py | grep -v ":0$"</automated>
  </verify>
  <acceptance_criteria>
    - `from app.tools.generate_html_report import _unwrap_tool_output` выполняется без ImportError
    - `from app.tools.generate_html_report import handle_generate_html_report` выполняется без NameError
    - `from app.tools.publish_scout_report import handle_publish_scout_report` выполняется без NameError
    - В обоих файлах нет функции, которая вызывает `_unwrap_tool_output` БЕЗ доступа к её определению (через локальное определение или импорт)
    - Синтаксис обоих файлов валиден (`ast.parse` без ошибок)
    - Количество упоминаний `_unwrap_tool_output` в коде не уменьшилось (только если не был мёртвый код)
    - В одном из файлов (или в новом общем модуле) есть определение `def _unwrap_tool_output(phase_data: dict) -> dict | None:`
  </acceptance_criteria>
  <done>Оба файла (`generate_html_report.py`, `publish_scout_report.py`) импортируются без NameError. Функция `_unwrap_tool_output` либо определена локально до первого вызова, либо импортирована из общего модуля. PipelineEngine HTML BUILD фаза больше не падает с этой ошибкой.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session-archive → HTML generator | Данные из `/opt/data/sessions-archive/` читаются и рендерятся в HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-S | Spoofing | `_unwrap_tool_output` parser | accept | Парсер читает структурированные данные из session-archive (внутренняя FS), не из user input |
| T-02-01-T | Tampering | HTML BUILD output | accept | Существующая `_esc()` функция (line 50) уже эскейпит user-controlled данные; этот фикс не меняет HTML санитизацию |
| T-02-01-I | Information Disclosure | Error messages | mitigate | После фикса NameError не должен появляться в логах (которые могут содержать данные сессий) — баг фиксится, ошибка исчезает |
| T-02-01-D | Denial of Service | HTML BUILD crash | mitigate | Сам фикс устраняет DoS: 40% сессий больше не падают на HTML BUILD |
| T-02-01-SC | Tampering | pip installs | accept | Не планируются новые пакеты — только правка существующих файлов |
</threat_model>

<verification>
## Plan-Level Verification

1. **Import check:** `python3 -c "from app.tools.generate_html_report import handle_generate_html_report"` выполняется без NameError внутри контейнера `aim-hermes` (или локально если есть Python окружение).

2. **Regression check:** `grep -rn "NameError.*_unwrap_tool_output" .planning/phases/01-research-diagnosis/evidence/` находит свидетельства бага в Phase 1 research — после фикса эти логи больше не должны воспроизводиться.

3. **No new errors:** После фикса запустить существующий тест если есть: `ls AIM/hermes/tests/test_*html*` или `ls AIM/hermes/app/tools/test_*html*`. Если есть — запустить.

4. **Smoke test на сервере (ОПЦИОНАЛЬНО, только если deploy одобрен отдельно):** НЕ делать в этом плане. Деплой — отдельный шаг (см. Plan 02-04).
</verification>

<success_criteria>
- [ ] `_unwrap_tool_output` NameError больше не воспроизводится в generate_html_report.py и publish_scout_report.py
- [ ] Оба файла импортируются чисто
- [ ] Фикс минимальный (без рефакторинга, без переименования, без изменения сигнатур)
- [ ] Корневая причина зафиксирована в комментарии кода со ссылкой на RESEARCH.md Section 5.3
- [ ] PipelineEngine fallback режим (ORC-05) снова генерирует HTML без падений
</success_criteria>

<output>
Создать `.planning/phases/02-3-pass-orchestrator-coverage-checklist/02-01-SUMMARY.md` когда готово.

В SUMMARY зафиксировать:
- Какая из 3 гипотез подтвердилась (Случай 1, 2, или 3)
- Точная строка и файл где был баг
- Минимальный diff применённый
- Результат `python3 -c "from app.tools.generate_html_report import ..."` (подтверждение что ImportError ушёл)
</output>
