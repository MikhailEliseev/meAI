---
phase: 4
plan: 04-07
type: execute
name: html-llm-sections-strategy-offer-whitefields-experts-content
wave: 4
depends_on: [04-06]
requirements: [SEC-01, SEC-02, SEC-03, SEC-04, SEC-05]
tags: [html, report, llm-sections, strategy, offer, whitefields, experts, content-analysis]

files_modified:
  - AIM/hermes/app/tools/generate_html_report.py

autonomous: true

must_haves:
  truths:
    - "SEC-01/D-02: _build_strategy_section(strategy_data) renders 5 Strategy directions (content, Telegram, GEO, reputation, cross-promo) with LLM-generated concrete steps per direction"
    - "SEC-02: _build_offer_section(offer_data) renders 'Что AIM может сделать для клиники' with concrete steps + CTA"
    - "SEC-03/D-05..06: _build_whitefields_matrix(whitefields_data) renders a 4×4 matrix (4 categories × client + 3 competitors) using HTML <table>"
    - "SEC-04: _build_experts_with_regalia(experts_data) renders top-5 experts with регалии (degree, academic_title, experience_years) + Instagram metrics (followers, avg_likes, avg_views)"
    - "SEC-05/D-11: _build_content_analysis_with_fears(content_data) renders per-doctor content analysis + top-5 patient fears with mention counts"
    - "_build_report_html accepts new kwargs: strategy_data, offer_data, whitefields_data, experts_data, content_data, patient_fears — all default to None (backward compatible)"
    - "All new sections use AIM design-system CSS (glass cards, metric-tag badges, blockquotes for key insights)"
    - "All new sections gracefully handle missing data — render honest 'данные недоступны' or skip section"
  artifacts:
    - path: "AIM/hermes/app/tools/generate_html_report.py"
      provides: "5 new LLM-section builders + extended _build_report_html signature"
      contains: "_build_strategy_section"
  key_links:
    - from: "pass_fill_assemble._build_prompt (items 7-11)"
      to: "_build_strategy_section + _build_offer_section + _build_whitefields_matrix"
      via: "LLM calls generate_html_report with kwargs from Plan 04-05 instructions"
      pattern: "strategy_data"
    - from: "generate_html_report._build_report_html"
      to: "_build_strategy_section(strategy_data)"
      via: "function call with kwarg from LLM"
      pattern: "_build_strategy_section"
---

<objective>
Add 5 new HTML section renderers for Phase 4's LLM-generated sections:

1. **_build_strategy_section** — 5 Strategy directions with LLM-generated steps (SEC-01, D-01..03)
2. **_build_offer_section** — "Что AIM может сделать" with CTA (SEC-02, D-04)
3. **_build_whitefields_matrix** — 4×4 comparison matrix (SEC-03, D-05..07)
4. **_build_experts_with_regalia** — Experts with регалии + Instagram metrics (SEC-04, D-08..09)
5. **_build_content_analysis_with_fears** — Content analysis + top-5 patient fears (SEC-05, D-10..11)

These sections render content that the Pass 3 LLM generates (per Plan 04-05 instructions). The LLM passes the generated content as kwargs to `generate_html_report`. This plan adds the renderers that turn LLM-generated data structures into design-system HTML.

Purpose: Plan 04-06 added data-driven sections (revenue table, media list, competitor cards). This plan adds the sections that require LLM generation — Strategy (09), Offer (10), Whitefields matrix (07), and extends Experts (03) and Content Analysis (04) with Phase 4 data (регалии + страхи).

Output: Modified `generate_html_report.py` with 5 new section-builder functions + extended `_build_report_html` signature (5 new optional kwargs).
</objective>

## Phase Goal (MVP)

**As a** reader of the presale HTML report,
**I want to** see Strategy with 5 concrete directions, Offer with CTA, Whitefields 4×4 matrix, Experts with регалии+metrics, and Content Analysis with patient fears,
**so that** the report matches all 10 sections of the reference `ИПХиК (2).html`.

---

<context>
@.planning/PROJECT.md
@.planning/phases/04-new-sections-data-depth/04-CONTEXT.md

@AIM/hermes/app/tools/generate_html_report.py
@AIM/wordpress-core/wp-content/themes/aim-theme/design-showcase-dual-theme.html

<interfaces>
<!-- Current _build_report_html signature (after Plan 04-06) -->

From AIM/hermes/app/tools/generate_html_report.py:
```python
def _build_report_html(
    data: dict,
    title: str,
    coverage_metadata: dict | None = None,
    niche: str = "unknown",
    instagram_data: dict | None = None,
) -> str:
    # Phase 4 / Plan 04-07 adds 5 new optional kwargs:
    #   strategy_data: dict | None = None,
    #   offer_data: dict | None = None,
    #   whitefields_data: dict | None = None,
    #   experts_data: dict | None = None,  # merged experts from _merge_doctor_data
    #   content_data: dict | None = None,  # content analysis + patient fears
```

**Expected data shapes from LLM (per Plan 04-05 Pass 3 prompt):**

Strategy data (strategy_data kwarg):
```python
{
    "directions": [
        {
            "name": "Контент",  # one of: Контент, Telegram, GEO, Репутация, Кросс-промо
            "steps": ["step 1...", "step 2...", "step 3..."],
            "basis": "конкуренты + content_gaps",  # which basis sources used
            "expected_impact": "описание ожидаемого эффекта",
        },
        # ... 5 directions total
    ]
}
```

Offer data (offer_data kwarg):
```python
{
    "steps": [
        {"service": "Контент-продакшн", "description": "...", "timeline": "2 недели"},
        # ...
    ],
    "cta": "Запишитесь на бесплатный аудит →",
}
```

Whitefields data (whitefields_data kwarg):
```python
{
    "categories": ["Услуги", "Цены", "Врачи", "Digital presence"],
    "columns": [
        {"name": "Клиент", "is_client": True},
        {"name": "Конкурент 1", "is_client": False},
        {"name": "Конкурент 2", "is_client": False},
        {"name": "Конкурент 3", "is_client": False},
    ],
    "cells": {
        # key: "{category}_{column_index}" → value string
        "Услуги_0": "пластика груди ✓, липосакция ✓, инъекции ✗",
        "Услуги_1": "пластика груди ✓, липосакция ✓, инъекции ✓",
        # ... 4 categories × 4 columns = 16 cells
    },
}
```

Experts data (experts_data kwarg — from _merge_doctor_data output):
```python
[
    {
        "name": "Иванов И.И.",
        "structured_regalia": {"degree": "КМН", "academic_title": "доцент", "experience_years": 15, "education": ["МГМУ"]},
        "instagram_metrics": {"followers_count": 50000, "avg_likes": 2000, "avg_views": 15000, "content_style": "образовательный"},
        "source": "both",  # or "site" or "instagram_only"
    },
    # ... up to 5 experts
]
```

Content data (content_data kwarg — content analysis + patient fears combined):
```python
{
    "doctor_analyses": [
        {"name": "Иванов И.И.", "style": "...", "themes": [{"name": "education", "pct": 40}], "gaps": ["..."], "potential": "..."},
        # ...
    ],
    "patient_fears": [
        {"fear": "Больно", "mention_count": 47, "context": "47 упоминаний из 120 отзывов"},
        # ... top 5
    ],
}
```

**Existing helpers available:**
- `_esc(text)` — XSS-safe escaping
- `_fmt_num(val, default="—")` — number formatting
- `_fmt_revenue_short(val)` — "4.3 млрд" format
- `_fmt_instagram(details)` — Instagram metrics formatting (line 111)
- Design-system CSS classes: `.section`, `.glass-card`, `.metric-tag-success`, `.metric-tag-warning`, `.metric-tag-danger`, `.metric-tag-info`, `blockquote`
</interfaces>
</context>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add _build_strategy_section + _build_offer_section (SEC-01, SEC-02)</name>
  <files>AIM/hermes/app/tools/generate_html_report.py</files>

  <read_first>
    - AIM/hermes/app/tools/generate_html_report.py lines 762-830 (_build_report_html signature and data extraction — understand where to add new kwargs)
    - The 5 data-section functions added in Plan 04-06 (confirm the rendering pattern to follow)
    - AIM/hermes/app/tools/generate_html_report.py lines 196-270 (_build_no_instagram_block — model for honest "данные недоступны" pattern)
  </read_first>

  <action>
    **Step A: Add _build_strategy_section() function.**

    Add AFTER the last function from Plan 04-06 (`_build_competitor_cards_section`). Position: after existing section builders, before `_build_report_html`.

    Signature: `def _build_strategy_section(strategy_data: dict | None) -> str:`

    Logic:
    1. If `strategy_data` is None or empty: return empty string (section not rendered).
    2. Extract `directions = strategy_data.get("directions", [])`.
    3. If no directions: return honest block "Стратегия не сгенерирована — недостаточно данных для рекомендаций".
    4. Render section:
       ```html
       <section class="section">
         <span class="section-label">Strategy (SEC-01)</span>
         <h2>Стратегия развития</h2>
         <blockquote>
           <p>{LLM-generated main insight or summary}</p>
         </blockquote>
         <div class="strategy-directions">
           <!-- for each direction in directions -->
           <div class="glass-card strategy-direction">
             <h3>{direction_icon} {_esc(direction["name"])}</h3>
             <div class="direction-basis">
               <span class="text-dim text-meta">Базис: {_esc(direction.get("basis", "—"))}</span>
             </div>
             <ol class="direction-steps">
               <!-- for each step in direction["steps"] -->
               <li>{_esc(step)}</li>
             </ol>
             <div class="direction-impact">
               <span class="metric-tag metric-tag-info">{_esc(direction.get("expected_impact", "—"))}</span>
             </div>
           </div>
         </div>
       </section>
       ```
    5. Direction icons: map direction name to emoji — "Контент" → "📝", "Telegram" → "📱", "GEO" → "📍", "Репутация" → "⭐", "Кросс-промо" → "🤝". Unknown direction → "💡".
    6. XSS-safe: all LLM-generated text wrapped in `_esc()`.
    7. Per D-02: exactly 5 directions should be present. If fewer, render what's available with honest note. If more, render first 5.

    **Step B: Add _build_offer_section() function.**

    Add immediately AFTER `_build_strategy_section()`.

    Signature: `def _build_offer_section(offer_data: dict | None) -> str:`

    Logic:
    1. If `offer_data` is None or empty: return empty string.
    2. Extract `steps = offer_data.get("steps", [])` and `cta = offer_data.get("cta", "")`.
    3. If no steps: return honest block "Offer не сгенерирован — недостаточно данных".
    4. Render section (matching reference section 10 — "Что AIM может сделать для клиники"):
       ```html
       <section class="section">
         <span class="section-label">Offer (SEC-02)</span>
         <h2>Что AIM может сделать для клиники</h2>
         <blockquote>
           <p>На основе анализа мы предлагаем следующие шаги:</p>
         </blockquote>
         <div class="offer-steps">
           <!-- for each step in steps -->
           <div class="glass-card offer-step">
             <h3>{_esc(step.get("service", "Услуга"))}</h3>
             <p>{_esc(step.get("description", ""))}</p>
             <div class="step-meta">
               <span class="metric-tag metric-tag-info">⏱ {_esc(step.get("timeline", "—"))}</span>
             </div>
           </div>
         </div>
         <div class="offer-cta">
           <p class="cta-text">{_esc(cta)}</p>
         </div>
       </section>
       ```
    5. CTA rendered prominently (larger font, accent color).

    **Step C: Extend _build_report_html signature with 2 new kwargs.**

    Modify the function signature (line 762):
    ```python
    def _build_report_html(
        data: dict,
        title: str,
        coverage_metadata: dict | None = None,
        niche: str = "unknown",
        instagram_data: dict | None = None,
        strategy_data: dict | None = None,  # Phase 4 / SEC-01
        offer_data: dict | None = None,      # Phase 4 / SEC-02
    ) -> str:
    ```

    All new kwargs default to None — backward compatible with Phase 3 callers.

    Add the sections to `sections` list at the END (after all existing sections, matching reference order: section 09 Strategy → section 10 Offer):
    ```python
    # Phase 4 / SEC-01: Strategy section
    strategy_html = _build_strategy_section(strategy_data)
    if strategy_html:
        sections.append(strategy_html)

    # Phase 4 / SEC-02: Offer section
    offer_html = _build_offer_section(offer_data)
    if offer_html:
        sections.append(offer_html)
    ```

    Do NOT modify existing section assembly or kwargs.
  </action>

  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes && python3 -c "
import ast
with open('app/tools/generate_html_report.py') as f:
    ast.parse(f.read())

source = open('app/tools/generate_html_report.py').read()
assert 'def _build_strategy_section(' in source
assert 'def _build_offer_section(' in source
assert 'strategy_data' in source
assert 'offer_data' in source

from app.tools.generate_html_report import _build_strategy_section, _build_offer_section

# Strategy with 5 directions
html = _build_strategy_section({
    'directions': [
        {'name': 'Контент', 'steps': ['step 1', 'step 2'], 'basis': 'content_gaps', 'expected_impact': '+30% engagement'},
        {'name': 'Telegram', 'steps': ['create channel'], 'basis': 'конкуренты', 'expected_impact': '50K followers'},
        {'name': 'GEO', 'steps': ['step'], 'basis': 'reputation', 'expected_impact': 'top 3'},
        {'name': 'Репутация', 'steps': ['step'], 'basis': 'review gaps', 'expected_impact': '+0.5 stars'},
        {'name': 'Кросс-промо', 'steps': ['step'], 'basis': 'competitors', 'expected_impact': '+20% reach'},
    ]
})
assert 'Контент' in html and 'Telegram' in html
assert 'GEO' in html and 'Репутация' in html and 'Кросс-промо' in html
assert 'step 1' in html
assert 'blockquote' in html
assert 'glass-card' in html

# Strategy with None (section skipped)
assert _build_strategy_section(None) == ''
assert _build_strategy_section({}) == '' or 'не сгенерирована' in _build_strategy_section({}).lower()

# Offer section
html = _build_offer_section({
    'steps': [
        {'service': 'Контент-продакшн', 'description': '15 постов/мес', 'timeline': '2 недели'},
        {'service': 'SEO оптимизация', 'description': 'Тех аудит + правки', 'timeline': '1 месяц'},
    ],
    'cta': 'Запишитесь на бесплатный аудит →',
})
assert 'Контент-продакшн' in html
assert 'SEO' in html
assert 'Запишитесь' in html or 'бесплатный' in html
assert 'cta-text' in html or 'cta' in html.lower()

print('OK: strategy + offer sections work correctly')
"</automated>
  </verify>

  <done>
    `_build_strategy_section()` renders 5 LLM-generated directions with steps + basis + impact. `_build_offer_section()` renders offer steps + CTA. Both wired into `_build_report_html` via new optional kwargs (backward compatible).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add _build_whitefields_matrix + extend _build_report_html for Whitefields (SEC-03, D-05..07)</name>
  <files>AIM/hermes/app/tools/generate_html_report.py</files>

  <read_first>
    - The `_build_strategy_section` and `_build_offer_section` functions from Task 1 (confirm pattern)
    - AIM/hermes/app/tools/generate_html_report.py lines 128-195 (_build_competitor_table — existing table rendering pattern)
  </read_first>

  <action>
    **Step A: Add _build_whitefields_matrix() function.**

    Add AFTER `_build_offer_section()`.

    Signature: `def _build_whitefields_matrix(whitefields_data: dict | None) -> str:`

    Logic (per D-05: 4 categories × 4 columns matrix):
    1. If `whitefields_data` is None or empty: return empty string.
    2. Extract `categories`, `columns`, `cells` from the data.
    3. If any are missing: return honest block "Whitefields matrix не построена — недостаточно данных по конкурентам".
    4. Render an HTML table (4 rows × 5 columns including header):
       ```html
       <section class="section">
         <span class="section-label">Whitefields Matrix (SEC-03)</span>
         <h2>Сравнение с конкурентами</h2>
         <blockquote>
           <p>{LLM insight about competitive position}</p>
         </blockquote>
         <div class="glass-card" style="padding: 1.5rem;">
           <table class="whitefields-matrix-table">
             <thead>
               <tr>
                 <th>Категория</th>
                 <!-- for each column -->
                 <th {client_class}>{_esc(column["name"])}</th>
               </tr>
             </thead>
             <tbody>
               <!-- for each category -->
               <tr>
                 <td class="category-cell">{_esc(category)}</td>
                 <!-- for each column index -->
                 <td class="value-cell">{_esc(cells.get(f"{category}_{col_idx}", "—"))}</td>
               </tr>
             </tbody>
           </table>
         </div>
       </section>
       ```
    5. Client column (first data column, index 0) gets a distinct CSS class (`client-column` — bold, highlighted background).
    6. Per D-06: if fewer than 3 competitor columns (total < 4 columns), render what's available with honest note "Менее 3 конкурентов найдено — матрица неполная".
    7. Per D-07: cells are filled from already-collected data — no additional API calls. Empty cells show "—".
    8. XSS-safe: all cell values wrapped in `_esc()`.
    9. Responsive: table should scroll horizontally on mobile (`overflow-x: auto` on the table container).

    **Step B: Add whitefields_data kwarg to _build_report_html.**

    Extend the function signature (continuing from Task 1):
    ```python
    def _build_report_html(
        data: dict,
        title: str,
        coverage_metadata: dict | None = None,
        niche: str = "unknown",
        instagram_data: dict | None = None,
        strategy_data: dict | None = None,
        offer_data: dict | None = None,
        whitefields_data: dict | None = None,  # Phase 4 / SEC-03
    ) -> str:
    ```

    Add the Whitefields section to the sections list AFTER the Competitors section (reference order: section 06 Competitors → section 07 Whitefields matrix) and BEFORE Strategy:
    ```python
    # Phase 4 / SEC-03: Whitefields matrix
    whitefields_html = _build_whitefields_matrix(whitefields_data)
    if whitefields_html:
        # Insert BEFORE strategy/offer sections
        # Find the right position: after competitor cards, before strategy
        sections.append(whitefields_html)
    ```

    To position correctly, insert the whitefields_html call BEFORE the strategy_html call (from Task 1) in the sections assembly.
  </action>

  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes && python3 -c "
import ast
with open('app/tools/generate_html_report.py') as f:
    ast.parse(f.read())

from app.tools.generate_html_report import _build_whitefields_matrix

# Full 4×4 matrix
html = _build_whitefields_matrix({
    'categories': ['Услуги', 'Цены', 'Врачи', 'Digital presence'],
    'columns': [
        {'name': 'Клиент', 'is_client': True},
        {'name': 'Конкурент 1', 'is_client': False},
        {'name': 'Конкурент 2', 'is_client': False},
        {'name': 'Конкурент 3', 'is_client': False},
    ],
    'cells': {
        'Услуги_0': 'пластика ✓, липосакция ✓',
        'Услуги_1': 'пластика ✓, липосакция ✓, инъекции ✓',
        'Услуги_2': 'пластика ✓',
        'Услуги_3': 'пластика ✓, липосакция ✓',
        'Цены_0': '500K-1M',
        'Цены_1': '400K-800K',
        'Цены_2': '600K-1.2M',
        'Цены_3': '450K-900K',
    },
})
assert '<table' in html, 'Table missing from matrix'
assert 'Услуги' in html and 'Цены' in html
assert 'Клиент' in html and 'Конкурент' in html
assert 'пластика' in html
assert 'blockquote' in html

# None case (section skipped)
assert _build_whitefields_matrix(None) == ''

# Fewer than 4 columns (D-06 honest note)
html = _build_whitefields_matrix({
    'categories': ['Услуги'],
    'columns': [{'name': 'Клиент', 'is_client': True}, {'name': 'Конкурент 1', 'is_client': False}],
    'cells': {'Услуги_0': 'test'},
})
assert 'неполная' in html.lower() or 'менее' in html.lower() or html == '', 'D-06 honest note missing for <3 competitors'

print('OK: whitefields_matrix section works correctly')
"</automated>
  </verify>

  <done>
    `_build_whitefields_matrix()` renders 4×4 HTML table with client column highlighted. D-06 honest note for <3 competitors. Wired into `_build_report_html` via new `whitefields_data` kwarg.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add _build_experts_with_regalia + _build_content_analysis_with_fears (SEC-04, SEC-05)</name>
  <files>AIM/hermes/app/tools/generate_html_report.py</files>

  <read_first>
    - AIM/hermes/app/tools/generate_html_report.py lines 111-127 (_fmt_instagram — existing Instagram formatting helper)
    - The functions from Tasks 1 and 2 (confirm pattern consistency)
    - AIM/hermes/app/tools/generate_html_report.py around lines 1050-1200 (existing section 03 Experts and section 04 Content Analysis — understand where to extend)
  </read_first>

  <action>
    **Step A: Add _build_experts_with_regalia() function.**

    Add AFTER `_build_whitefields_matrix()`.

    Signature: `def _build_experts_with_regalia(experts_data: list | None) -> str:`

    Logic (per D-08: регалии from site + D-09: merged with Instagram):
    1. If `experts_data` is None or empty list: return empty string (Phase 3 existing experts section handles the no-data case).
    2. Render an enhanced Experts section (section 03 in reference):
       ```html
       <section class="section">
         <span class="section-label">Experts (SEC-04)</span>
         <h2>Топ-эксперты клиники</h2>
         <div class="experts-grid">
           <!-- for each expert in experts_data (up to 5) -->
           <div class="glass-card expert-card">
             <h3>{_esc(expert["name"])}</h3>
             <!-- Регалии block (D-08) -->
             <div class="expert-regalia">
               {degree_badge if degree}
               {title_badge if academic_title}
               {experience_text if experience_years}
             </div>
             <!-- Instagram metrics (D-09, if available) -->
             {instagram_block if expert.get("instagram_metrics")}
             <!-- Source indicator -->
             <span class="text-dim text-meta">Источник: {_esc(expert.get("source", "—"))}</span>
           </div>
         </div>
       </section>
       ```
    3. Degree badge: "КМН" → `metric-tag metric-tag-info`, "ДМН" → `metric-tag metric-tag-success`.
    4. Title badge: "профессор" → `metric-tag metric-tag-success`, "доцент" → `metric-tag metric-tag-info`.
    5. Experience text: `"Стаж {experience_years} лет"` if experience_years is not None.
    6. Instagram block: use existing `_fmt_instagram()` helper to format followers/likes/views. Show handle if available.
    7. If `expert["source"] == "site"` (no Instagram): show only регалии — this is a valid expert.
    8. If `expert["source"] == "instagram_only"`: show only Instagram metrics, note "Регалии недоступны — врач не на сайте клиники".
    9. XSS-safe: all text through `_esc()`.

    **Step B: Add _build_content_analysis_with_fears() function.**

    Add AFTER `_build_experts_with_regalia()`.

    Signature: `def _build_content_analysis_with_fears(content_data: dict | None) -> str:`

    Logic (per D-10: content analysis + D-11: top-5 страхов):
    1. If `content_data` is None or empty: return empty string.
    2. Extract `doctor_analyses = content_data.get("doctor_analyses", [])` and `patient_fears = content_data.get("patient_fears", [])`.
    3. Render enhanced Content Analysis section (section 04 in reference):
       ```html
       <section class="section">
         <span class="section-label">Content Analysis + Страхи (SEC-05)</span>
         <h2>Анализ контента и страхов пациентов</h2>

         <!-- Part 1: Per-doctor content analysis -->
         <div class="content-analyses">
           <!-- for each doctor_analysis in doctor_analyses -->
           <div class="glass-card content-analysis-card">
             <h3>{_esc(doctor["name"])}</h3>
             <p><strong>Стиль:</strong> {_esc(doctor.get("style", "—"))}</p>
             <div class="content-themes">
               <!-- for each theme in doctor["themes"] -->
               <span class="metric-tag metric-tag-info">{_esc(theme["name"])}: {theme["pct"]}%</span>
             </div>
             <div class="content-gaps">
               <p><strong>Пробелы:</strong> {_esc(", ".join(doctor.get("gaps", [])))}</p>
             </div>
             <p class="text-dim"><strong>Потенциал:</strong> {_esc(doctor.get("potential", "—"))}</p>
           </div>
         </div>

         <!-- Part 2: Top-5 patient fears (D-10, D-11) -->
         <div class="patient-fears-block">
           <h3>🔥 Топ-5 страхов пациентов</h3>
           <blockquote>
             <p>На основе {total_reviews} отзывов с ПроДокторов, Otzovik, IRecommend, Woman.ru</p>
           </blockquote>
           <ol class="fears-list">
             <!-- for each fear in patient_fears (up to 5) -->
             <li>
               <strong>{_esc(fear["fear"])}</strong>
               <span class="metric-tag metric-tag-warning">{fear["mention_count"]} упоминаний</span>
               <p class="text-dim">{_esc(fear.get("context", ""))}</p>
             </li>
           </ol>
         </div>
       </section>
       ```
    4. If `patient_fears` is empty: render honest note "Страхи пациентов не собраны — run_forum_pains не вызван или не дал данных".
    5. If `doctor_analyses` is empty: skip the per-doctor part, show only patient fears (if available).
    6. Fear icon: "🔥" for the header. Mention count as `metric-tag-warning` badge.

    **Step C: Extend _build_report_html signature with experts_data + content_data kwargs.**

    ```python
    def _build_report_html(
        data: dict,
        title: str,
        coverage_metadata: dict | None = None,
        niche: str = "unknown",
        instagram_data: dict | None = None,
        strategy_data: dict | None = None,
        offer_data: dict | None = None,
        whitefields_data: dict | None = None,
        experts_data: list | None = None,   # Phase 4 / SEC-04
        content_data: dict | None = None,    # Phase 4 / SEC-05
    ) -> str:
    ```

    Add the new sections to the sections list:
    - Experts with регалии: REPLACE or AUGMENT the existing Experts section (section 03). Position: same as existing section 03. If `experts_data` is provided, use the new enhanced renderer. If None, fall back to existing Phase 3 rendering.
    - Content Analysis with fears: REPLACE or AUGMENT the existing Content Analysis section (section 04). Same pattern.

    ```python
    # Phase 4 / SEC-04: Enhanced Experts with регалии
    if experts_data:
        experts_html = _build_experts_with_regalia(experts_data)
        if experts_html:
            sections.append(experts_html)

    # Phase 4 / SEC-05: Enhanced Content Analysis with patient fears
    if content_data:
        content_html = _build_content_analysis_with_fears(content_data)
        if content_html:
            sections.append(content_html)
    ```

    Position these sections in the correct reference order (03 Experts, 04 Content Analysis) — after About/Revenue, before Media section.
  </action>

  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes && python3 -c "
import ast
with open('app/tools/generate_html_report.py') as f:
    ast.parse(f.read())

source = open('app/tools/generate_html_report.py').read()
assert 'def _build_experts_with_regalia(' in source
assert 'def _build_content_analysis_with_fears(' in source
assert 'experts_data' in source
assert 'content_data' in source

from app.tools.generate_html_report import _build_experts_with_regalia, _build_content_analysis_with_fears

# Experts with регалии + Instagram
html = _build_experts_with_regalia([
    {
        'name': 'Иванов И.И.',
        'structured_regalia': {'degree': 'КМН', 'academic_title': 'доцент', 'experience_years': 15, 'education': ['МГМУ']},
        'instagram_metrics': {'followers_count': 50000, 'avg_likes': 2000, 'avg_views': 15000, 'content_style': 'образовательный'},
        'source': 'both',
    },
    {
        'name': 'Петров П.П.',
        'structured_regalia': {'degree': 'ДМН', 'academic_title': None, 'experience_years': None, 'education': []},
        'instagram_metrics': None,
        'source': 'site',
    },
])
assert 'Иванов' in html and 'Петров' in html
assert 'КМН' in html and 'ДМН' in html
assert 'доцент' in html
assert '15' in html  # experience years
assert '50' in html or '50000' in html  # Instagram followers

# Content analysis with patient fears
html = _build_content_analysis_with_fears({
    'doctor_analyses': [
        {'name': 'Иванов И.И.', 'style': 'образовательный', 'themes': [{'name': 'education', 'pct': 40}], 'gaps': ['нет видео'], 'potential': 'высокий'},
    ],
    'patient_fears': [
        {'fear': 'Больно', 'mention_count': 47, 'context': '47 упоминаний из 120 отзывов'},
        {'fear': 'Дорого', 'mention_count': 35, 'context': ''},
    ],
})
assert 'Иванов' in html
assert 'образовательный' in html
assert 'Больно' in html and 'Дорого' in html
assert '47' in html
assert 'metric-tag' in html

# None cases
assert _build_experts_with_regalia(None) == ''
assert _build_content_analysis_with_fears(None) == ''

# Content with fears but no doctor analyses
html = _build_content_analysis_with_fears({'doctor_analyses': [], 'patient_fears': [{'fear': 'Test', 'mention_count': 1, 'context': ''}]})
assert 'Test' in html

print('OK: experts_with_regalia + content_analysis_with_fears work correctly')
"</automated>
  </verify>

  <done>
    `_build_experts_with_regalia()` renders top-5 experts with регалии (degree, title, experience, education) + Instagram metrics, handles source variants (both/site/instagram_only). `_build_content_analysis_with_fears()` renders per-doctor content analysis + top-5 patient fears with mention counts. Both wired into `_build_report_html` via new optional kwargs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → HTML section builders | LLM-generated content (strategy steps, fears, matrix cells) rendered as HTML — XSS surface |
| HTML → User browser | Rendered HTML displayed — standard XSS risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-X | XSS | LLM-generated text (strategy steps, fears, offer descriptions, matrix cells) is untrusted | mitigate | Every LLM-generated string wrapped in `_esc()`; URLs escaped in href attributes; existing _build_no_instagram_block pattern followed |
| T-04-07-T | Tampering | LLM could inject malformed HTML into strategy/fear text | mitigate | _esc() converts < > & to HTML entities — prevents script injection even if LLM tries |
| T-04-07-I | Info disclosure | Strategy/Offer sections reveal competitive analysis | accept | Data already in report — rendering doesn't expose new info |
| T-04-07-D | DoS | Large strategy/fears data → large HTML | mitigate | Max 5 directions, 5 experts, 5 fears rendered; bounded output |
| T-04-07-E | EoP | Not applicable | accept | Pure HTML generation |
| T-04-07-SC | Supply chain | No new packages | accept | Pure Python + existing helpers |
</threat_model>

<verification>
**Post-plan automated checks:**

1. `generate_html_report.py` passes syntax check (ast.parse)
2. 5 new functions: `_build_strategy_section`, `_build_offer_section`, `_build_whitefields_matrix`, `_build_experts_with_regalia`, `_build_content_analysis_with_fears`
3. `_build_report_html` signature extended with 5 new optional kwargs (all default None — backward compatible)
4. All functions handle None/empty data gracefully (return empty string or honest block)
5. Strategy: 5 fixed directions rendered (D-02)
6. Whitefields: 4×4 table with client column highlighted (D-05..06)
7. Experts: регалии badges (КМН/ДМН/профессор/доцент) + Instagram metrics (D-08..09)
8. Content Analysis: per-doctor themes + top-5 patient fears with counts (D-10..11)
9. Offer: concrete steps + CTA (D-04)
10. XSS: all LLM-generated text wrapped in `_esc()`
11. All sections use design-system CSS classes

**Regression checks:**
- `_build_report_html` backward compatible (Phase 3 callers work without new kwargs)
- Existing section 03/04 rendering (Phase 3) still works when experts_data/content_data are None
- Plan 04-06 sections (revenue, media, ratings, competitor cards, clinic metrics) still render correctly
- Phase 3 Instagram no-data block still renders
</verification>

<success_criteria>
- SEC-01/D-01..03 SATISFIED (HTML layer): Strategy section with 5 LLM-generated directions, each with concrete steps from 4 basis sources
- SEC-02/D-04 SATISFIED (HTML layer): Offer section with steps + CTA
- SEC-03/D-05..07 SATISFIED (HTML layer): Whitefields 4×4 matrix table with client column highlighted
- SEC-04/D-08..09 SATISFIED (HTML layer): Experts with structured регалии + Instagram metrics, handles all source variants
- SEC-05/D-10..11 SATISFIED (HTML layer): Content analysis + top-5 patient fears with mention counts
- All sections gracefully handle missing data
- XSS-safe: all LLM-generated text escaped via `_esc()`
- `_build_report_html` backward compatible with Phase 3 callers
</success_criteria>

<output>
Create `.planning/phases/04-new-sections-data-depth/04-07-SUMMARY.md` when done. Record:
- Functions added: 5 new LLM-section builders
- _build_report_html signature: 5 new optional kwargs (strategy_data, offer_data, whitefields_data, experts_data, content_data)
- Sections positioned in reference order (03 Experts, 04 Content, 07 Whitefields, 09 Strategy, 10 Offer)
- XSS mitigation: _esc() applied to all LLM-generated text
- Test results (all automated checks pass)
- Any positioning adjustments needed in _build_report_html section assembly
</output>
