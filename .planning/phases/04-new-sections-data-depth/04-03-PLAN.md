---
phase: 4
plan: 04-03
type: execute
name: create-forum-pains-media-urls-tools
wave: 1
depends_on: []
requirements: [SEC-05, DAT-02]
tags: [forums, media, new-tools, tool-handlers, perplexity, firecrawl]

files_modified:
  - AIM/hermes/app/tools/run_forum_pains.py
  - AIM/hermes/app/tools/run_media_urls.py
  - AIM/hermes/app/pipeline/engine.py

autonomous: true

must_haves:
  truths:
    - "SEC-05/D-10: run_forum_pains tool exists and returns review/forum texts from at least 4 sources (ПроДокторов, Otzovik, IRecommend, Woman.ru) for a given clinic — raw texts for LLM fear extraction"
    - "SEC-05/D-11: run_forum_pains output includes a patient_fears_hint field with Perplexity-extracted top fears (LLM refines in Pass 3) — each fear has a name + mention_count estimate"
    - "DAT-02/D-15: run_media_urls tool exists and searches 5 specific СМИ (Forbes, RBC, Vademecum, Kommersant, ТАСС) for clinic mentions"
    - "DAT-02/D-16: run_media_urls uses firecrawl_search when available; falls back to perplexity_search if firecrawl unavailable"
    - "DAT-02/D-17: run_media_urls output is a simple list of {source, title, url, date} per mention — not card-grid with logos"
    - "DAT-02/D-18: run_media_urls returns honest 'В СМИ не упоминалась' block when 0 mentions found"
    - "Both tools registered in engine.py _TOOL_HANDLERS (24 → 26 entries)"
    - "Both tools registered in LLM registry (aim-operations toolset) — visible to LLM-orchestrator"
  artifacts:
    - path: "AIM/hermes/app/tools/run_forum_pains.py"
      provides: "Patient forum/review scraper with Perplexity-based fear extraction"
      contains: "handle_run_forum_pains"
    - path: "AIM/hermes/app/tools/run_media_urls.py"
      provides: "Targeted 5-СМИ search with concrete URL/date/title results"
      contains: "handle_run_media_urls"
    - path: "AIM/hermes/app/pipeline/engine.py"
      provides: "26 entries in _TOOL_HANDLERS (was 24)"
      contains: "run_forum_pains"
  key_links:
    - from: "engine.py:_TOOL_HANDLERS"
      to: "app.tools.run_forum_pains.handle_run_forum_pains"
      via: "dict entry"
      pattern: "run_forum_pains.*app\\.tools\\.run_forum_pains"
    - from: "engine.py:_TOOL_HANDLERS"
      to: "app.tools.run_media_urls.handle_run_media_urls"
      via: "dict entry"
      pattern: "run_media_urls.*app\\.tools\\.run_media_urls"
---

<objective>
Create two new Hermes tools:

1. **run_forum_pains** (SEC-05, D-10..11) — scrapes patient review platforms and forums for raw review texts; uses Perplexity to extract top-5 patient fears. Each fear has a name + mention count estimate. The Pass 3 LLM refines these into the final "Топ-5 страхов пациентов" block in section 04.

2. **run_media_urls** (DAT-02, D-15..18) — searches 5 specific Russian media outlets (Forbes, RBC, Vademecum, Kommersant, ТАСС) for clinic mentions. Returns concrete URLs with titles and dates — not category counters. Uses firecrawl_search with perplexity_search fallback.

Register both tools in `engine.py:_TOOL_HANDLERS` (24 → 26 entries) so both the LLM-orchestrator and PipelineEngine fallback can dispatch them.

Purpose: Phase 1 research showed section 04 "Content Analysis" lacks patient fears, and section 05 "Media" only has category counters. The reference report `ИПХиК (2).html` section 05 shows "Forbes — 'Топ-10 клиник' — 12.03.24 → ссылка" (concrete URLs) and section 04 shows "Больно — 47 упоминаний из 120 отзывов" (patient fears with counts).

Output: Two new tool files + modified engine.py with 2 new `_TOOL_HANDLERS` entries.
</objective>

## Phase Goal (MVP)

**As a** Hermes presale orchestrator,
**I want to** call run_forum_pains to collect patient fears AND run_media_urls to get concrete СМИ URLs,
**so that** sections 04 and 05 of the HTML report match the reference depth — fears with mention counts and media mentions with clickable links.
</objective>

---

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-new-sections-data-depth/04-CONTEXT.md

@AIM/hermes/app/tools/run_smi_mentions.py
@AIM/hermes/app/tools/run_review_platforms.py
@AIM/hermes/app/pipeline/engine.py

<interfaces>
<!-- Existing tool patterns the executor should follow -->

From AIM/hermes/app/tools/run_smi_mentions.py (218 lines — existing СMEDIA tool):
```python
# Pattern: Perplexity/DuckDuckGo search with category breakdown
# Returns: {"categories": {cat_key: {"mentions": [...]}}, "top_mentions": [...]}
# Uses _search_fallback for DuckDuckGo
async def handle_run_smi_mentions(url=None, company_name="", **kwargs) -> str:
    ...
registry.register(
    name="run_smi_mentions",
    toolset="aim-operations",
    schema={...},
    handler=handle_run_smi_mentions,
    ...
)
```

From AIM/hermes/app/tools/run_review_platforms.py (297 lines — existing review platform scraper):
```python
# Pattern: Perplexity sonar-pro with structured prompt
# Returns: {"clinic": str, "analysis": str (Perplexity text), "source": str}
# Uses PERPLEXITY_API_KEY env var, falls back to LLM if not set
async def handle_run_review_platforms(url=None, company_name="", city="", **kwargs) -> str:
    ...
```

From AIM/hermes/app/pipeline/engine.py (lines 41-68 — _TOOL_HANDLERS):
```python
_TOOL_HANDLERS: dict[str, tuple[str, str]] = {
    # ... 24 existing entries (lines 42-67) ...
    "run_instagram_content":   ("app.tools.run_instagram_content",  "handle_run_instagram_content"),
    "find_doctor_handles":     ("app.tools.find_doctor_handles",    "handle_find_doctor_handles"),
}
# After this plan: 26 entries (add run_forum_pains, run_media_urls)
```

**Environment variables available:**
- `PERPLEXITY_API_KEY` — Perplexity API (sonar-pro, web search)
- `FIRECRAWL_API_KEY` — Firecrawl API (search + scrape)
- `LLM_BASE_URL` / `LLM_API_KEY` / `LLM_MODEL` — fallback LLM (DeepSeek via OMNIROUTE)

**Existing _search_fallback import pattern (from run_smi_mentions.py line 113):**
```python
from app.tools._search_fallback import search as fallback_search
```
This provides DuckDuckGo-based search with provider fallback.
</interfaces>
</context>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Create run_forum_pains.py — patient fears scraper (SEC-05, D-10..11)</name>
  <files>AIM/hermes/app/tools/run_forum_pains.py</files>

  <read_first>
    - AIM/hermes/app/tools/run_review_platforms.py (FULL FILE — model the new tool on this pattern: Perplexity call, env-var fallback, cache, push_tool_progress)
    - AIM/hermes/app/tools/run_smi_mentions.py (lines 60-100 — _normalize_args pattern, cache pattern)
  </read_first>

  <action>
    Create a new file `AIM/hermes/app/tools/run_forum_pains.py` (~200-250 lines).

    **Module docstring (first 15 lines):**
    ```
    run_forum_pains — Hermes tool: Patient Fears Extractor (Phase 4 / SEC-05)

    Per D-10: Scrape patient review texts from 4 platforms:
    - ПроДокторов (prodoctorov.ru) — doctor/clinic reviews
    - Otzovik (otzovik.com) — general reviews
    - IRecommend (irecommend.ru) — product/service reviews
    - Woman.ru (woman.ru, health section) — forum discussions

    Per D-11: LLM extracts top-5 patient fears from review texts.
    This tool returns raw texts + Perplexity-extracted fear hints.

    Uses Perplexity (sonar-pro) for web search + fear extraction.
    Fallback: DeepSeek LLM (no web search) if PERPLEXITY_API_KEY not set.
    ```

    **Constants and env vars (mirror run_review_platforms.py pattern):**
    - `PERPLEXITY_API_KEY`, `PERPLEXITY_BASE_URL`, `PERPLEXITY_MODEL` (sonar-pro)
    - `USE_PERPLEXITY = bool(PERPLEXITY_API_KEY)`
    - LLM fallback: `LLM_BASE_URL`, `LLM_API_KEY`, `LLM_MODEL`
    - `REQUEST_TIMEOUT = 90.0`, `MAX_TOKENS = 8000`
    - `_cache: dict[str, tuple[float, str]] = {}`, `_CACHE_TTL = 600`

    **FORUM_SOURCES constant:**
    ```python
    FORUM_SOURCES = [
        {"name": "ПроДокторов", "domain": "prodoctorov.ru", "weight": 0.35},
        {"name": "Otzovik", "domain": "otzovik.com", "weight": 0.25},
        {"name": "IRecommend", "domain": "irecommend.ru", "weight": 0.20},
        {"name": "Woman.ru", "domain": "woman.ru", "weight": 0.20},
    ]
    ```

    **_build_query(company_name, city, url) function:**
    Build a Perplexity query that asks for patient fears from the 4 sources. The query should be step-by-step like run_review_platforms._build_query (each source gets its own step). The prompt should ask Perplexity to:
    1. Search each of the 4 platforms for the clinic
    2. Extract patient FEARS/concerns from review texts (not star ratings)
    3. For each fear, estimate the mention count (how many reviews mention it)
    4. Return the top 5 fears with counts

    The query MUST ask for fears like: "больно", "дорого", "неквалифицированный врач", "долгое ожидание", "грубость персонала", "неэффективное лечение", "осложнения", "антисанитария" — these are common medical patient fears to look for.

    **_build_system_prompt() function:**
    System prompt telling Perplexity: "Ты — аналитик пациентского опыта медицинских клиник. Твоя задача — найти ТОП-5 страхов/жалоб пациентов из текстов отзывов (не из звёздных рейтингов)."

    **_call_perplexity(prompt) and _call_llm(prompt) functions:**
    Same pattern as run_review_platforms.py (use openai.AsyncOpenAI client).

    **_extract_fears(answer: str) -> list[dict] function:**
    Parse the Perplexity response text to extract fear entries. Look for patterns like:
    - `"Больно — 47 упоминаний из 120 отзывов"` → `{"fear": "Больно", "mention_count": 47, "context": "47 упоминаний из 120 отзывов"}`
    - Lines with "—" followed by mention count
    - Regex: `r"([А-Яа-яЁё\s]{3,50}?)\s*—\s*(\d+)\s*упоминан"` (case-insensitive)
    Return up to 5 fears, sorted by mention_count descending. If parsing fails, return empty list (the raw answer is still available for LLM).

    **handle_run_forum_pains(url=None, company_name="", city="", **kwargs) function:**
    - Validate input (url or company_name required)
    - Check cache
    - Call _build_query + _call_perplexity (or _call_llm fallback)
    - Call _extract_fears on the answer
    - Return JSON:
    ```python
    {
        "clinic": query_name,
        "city": city or "не указан",
        "sources_checked": len(FORUM_SOURCES),
        "patient_fears_hint": [...],  # up to 5 dicts from _extract_fears
        "raw_analysis": answer,  # full Perplexity text for LLM
        "source": "perplexity (sonar-pro)" or "llm (deepseek) — без веб-поиска",
        "searched_at": timestamp,
    }
    ```

    **registry.register(...) call at the bottom:**
    - name="run_forum_pains"
    - toolset="aim-operations"
    - schema: function with parameters url (required), company_name (optional), city (optional)
    - handler=handle_run_forum_pains
    - check_fn=lambda: True
    - is_async=True
    - description="Extract top-5 patient fears from review platforms (ПроДокторов, Otzovik, IRecommend, Woman.ru) via Perplexity"
    - emoji="😰"

    The tool description in the schema should explain: "Ищет отзывы пациентов на 4 платформах и извлекает топ-5 страхов/жалоб из текстов отзывов. Возвращает страхи с количеством упоминаний. Pass 3 LLM использует это для секции Content Analysis (04)."
  </action>

  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes && python3 -c "
import ast

# Syntax check
with open('app/tools/run_forum_pains.py') as f:
    tree = ast.parse(f.read())

source = open('app/tools/run_forum_pains.py').read()

# Required components
assert 'async def handle_run_forum_pains(' in source, 'handler missing'
assert 'def _build_query(' in source
assert 'def _build_system_prompt(' in source
assert 'def _extract_fears(' in source
assert 'FORUM_SOURCES' in source
assert 'ПроДокторов' in source and 'Otzovik' in source and 'IRecommend' in source and 'Woman.ru' in source
assert 'patient_fears_hint' in source, 'patient_fears_hint field missing'

# Registry register call
assert 'registry.register(' in source
assert 'name=\"run_forum_pains\"' in source
assert 'aim-operations' in source

# Import test
from app.tools.run_forum_pains import handle_run_forum_pains, FORUM_SOURCES, _extract_fears
assert len(FORUM_SOURCES) == 4

# Test _extract_fears
test_answer = '''
Топ-5 страхов пациентов:
1. Больно — 47 упоминаний из 120 отзывов
2. Дорого — 35 упоминаний
3. Долгое ожидание — 28 упоминаний
4. Грубость персонала — 15 упоминаний
5. Неэффективное лечение — 12 упоминаний
'''
fears = _extract_fears(test_answer)
assert len(fears) >= 1, f'Expected fears extracted, got {fears}'
assert fears[0]['mention_count'] >= 1

print('OK: run_forum_pains.py created with all required components')
"</automated>
  </verify>

  <done>
    `run_forum_pains.py` exists with handle_run_forum_pains, _extract_fears, FORUM_SOURCES (4 platforms), and registry.register call. Syntax valid. _extract_fears parses test input correctly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create run_media_urls.py — targeted 5-СМИ search (DAT-02, D-15..18)</name>
  <files>AIM/hermes/app/tools/run_media_urls.py</files>

  <read_first>
    - AIM/hermes/app/tools/run_smi_mentions.py (FULL FILE — understand existing media search pattern; this new tool is more TARGETED)
    - AIM/hermes/app/tools/run_review_platforms.py (Perplexity call pattern, cache, env vars)
  </read_first>

  <action>
    Create a new file `AIM/hermes/app/tools/run_media_urls.py` (~200-250 lines).

    **Module docstring:**
    ```
    run_media_urls — Hermes tool: Targeted 5-СМИ URL Search (Phase 4 / DAT-02)

    Per D-15: Multi-search по 5 конкретным СМИ через firecrawl_search:
    Forbes, RBC, Vademecum, Kommersant, ТАСС.

    Per D-16: Fallback на perplexity_search если firecrawl недоступен.

    Per D-17: Возвращает простой список с гиперссылками (source, title, url, date).
    Не карточки с лого (избыточно для MVP).

    Per D-18: Если 0 упоминаний — честный блок 'В СМИ не упоминалась'.
    ```

    **Constants and env vars (same pattern as run_forum_pains + run_review_platforms):**
    - `FIRECRAWL_API_KEY` from env
    - `PERPLEXITY_API_KEY` from env
    - `USE_FIRECRAWL = bool(FIRECRAWL_API_KEY)`
    - LLM fallback vars (LLM_BASE_URL, etc.)
    - `REQUEST_TIMEOUT = 60.0`
    - `_cache`, `_CACHE_TTL = 600`

    **TARGET_MEDIA constant:**
    ```python
    TARGET_MEDIA = [
        {"name": "Forbes", "domain": "forbes.ru"},
        {"name": "RBC", "domain": "rbc.ru"},
        {"name": "Vademecum", "domain": "vademec.ru"},
        {"name": "Kommersant", "domain": "kommersant.ru"},
        {"name": "ТАСС", "domain": "tass.ru"},
    ]
    ```

    **_firecrawl_search(query: str) -> list[dict] function:**
    Use the existing firecrawl integration. Check if `app.tools.firecrawl_web` module exists and import its search function. If not, use the firecrawl-py SDK directly:
    ```python
    from firecrawl import FirecrawlApp
    app = FirecrawlApp(api_key=FIRECRAWL_API_KEY)
    result = app.search(query, {"limit": 5})
    ```
    Return list of `{"title": str, "url": str, "description": str}` dicts.

    **_perplexity_search_fallback(query: str) -> list[dict] function:**
    Use Perplexity to search for the same query. Parse the response for URLs and titles.

    **_build_media_search_query(clinic_name: str, media_domain: str) -> str:**
    Build a targeted query for one СМИ source: `f'"{clinic_name}" site:{media_domain}'`

    **handle_run_media_urls(url=None, company_name="", **kwargs) function:**
    1. Validate input (url or company_name)
    2. Extract clinic name from URL domain if company_name is empty
    3. Check cache
    4. For EACH of the 5 TARGET_MEDIA:
       a. Build query: `_build_media_search_query(clinic_name, media["domain"])`
       b. If USE_FIRECRAWL: call `_firecrawl_search(query)` — if it returns results, use them
       c. Else (or firecrawl fails): call `_perplexity_search_fallback(query)`
       d. Collect results: `{"source": media["name"], "domain": media["domain"], "mentions": [{"title": ..., "url": ..., "date": ...}]}`
    5. Run all 5 searches in parallel via `asyncio.gather` (like run_smi_mentions.py pattern)
    6. Aggregate:
       - `total_mentions` = sum of mentions across all 5 sources
       - `media_with_mentions` = count of sources with ≥1 mention
       - `all_mentions_flat` = flattened list of all mentions for easy rendering

    7. Return JSON (per D-17 + D-18):
    ```python
    {
        "clinic": clinic_name,
        "total_mentions": int,
        "media_with_mentions": int,
        "media_total": 5,
        "mentions_by_source": [
            {
                "source": "Forbes",
                "domain": "forbes.ru",
                "mentions_found": int,
                "mentions": [{"title": str, "url": str, "date": str}],
            },
            # ... 4 more sources
        ],
        "all_mentions": [...],  # flat list for easy HTML rendering
        "source": "firecrawl" or "perplexity (fallback)" or "mixed",
        "searched_at": timestamp,
        # D-18: honest block when 0 mentions
        "pr_needed": total_mentions == 0,  # flag for Strategy section
    }
    ```

    **registry.register(...) call:**
    - name="run_media_urls"
    - toolset="aim-operations"
    - schema: function with url (required), company_name (optional)
    - handler=handle_run_media_urls
    - description="Search 5 target media outlets (Forbes, RBC, Vademecum, Kommersant, ТАСС) for clinic mentions with concrete URLs and dates. Uses firecrawl_search with perplexity fallback."
    - emoji="📰"
  </action>

  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes && python3 -c "
import ast

with open('app/tools/run_media_urls.py') as f:
    tree = ast.parse(f.read())

source = open('app/tools/run_media_urls.py').read()

# Required components
assert 'async def handle_run_media_urls(' in source
assert 'TARGET_MEDIA' in source
assert 'forbes.ru' in source and 'rbc.ru' in source and 'vademec.ru' in source
assert 'kommersant.ru' in source and 'tass.ru' in source
assert 'mentions_by_source' in source
assert 'all_mentions' in source
assert 'pr_needed' in source, 'D-18 honest block flag missing'

# Registry
assert 'registry.register(' in source
assert 'name=\"run_media_urls\"' in source

# Import
from app.tools.run_media_urls import handle_run_media_urls, TARGET_MEDIA
assert len(TARGET_MEDIA) == 5

print('OK: run_media_urls.py created with all required components')
"</automated>
  </verify>

  <done>
    `run_media_urls.py` exists with handle_run_media_urls, TARGET_MEDIA (5 sources: Forbes, RBC, Vademecum, Kommersant, ТАСС), firecrawl+perplexity fallback, and D-18 pr_needed flag. Syntax valid.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register both tools in engine.py _TOOL_HANDLERS (24 → 26 entries)</name>
  <files>AIM/hermes/app/pipeline/engine.py</files>

  <read_first>
    - AIM/hermes/app/pipeline/engine.py lines 41-68 (the _TOOL_HANDLERS dict — confirm exact location of Phase 3 Instagram entries at lines 65-67)
  </read_first>

  <action>
    Open `AIM/hermes/app/pipeline/engine.py`, locate the `_TOOL_HANDLERS` dict (lines 41-68).

    Add two new entries AFTER the existing Phase 3 Instagram entries (lines 66-67) and BEFORE the closing `}` (line 68). Use this exact format:

    ```python
        # ── Phase 4 / SEC-05 + DAT-02: Forum pains + Media URLs ────────
        "run_forum_pains":        ("app.tools.run_forum_pains",       "handle_run_forum_pains"),
        "run_media_urls":         ("app.tools.run_media_urls",        "handle_run_media_urls"),
    ```

    The module paths `app.tools.run_forum_pains` and `app.tools.run_media_urls` MUST match the actual file locations (confirmed by Tasks 1 and 2 creating these files).

    The handler function names `handle_run_forum_pains` and `handle_run_media_urls` MUST match the actual `async def` names (confirmed by Tasks 1 and 2).

    Do NOT modify, rename, or remove any of the existing 24 entries. Do NOT reorder existing entries.

    Do NOT change the dict type annotation or any other function in the file.
  </action>

  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes && python3 -c "
import ast
with open('app/pipeline/engine.py') as f:
    tree = ast.parse(f.read())
for node in ast.walk(tree):
    if isinstance(node, ast.Assign) and any(isinstance(t, ast.Name) and t.id == '_TOOL_HANDLERS' for t in node.targets):
        keys = [k.value if isinstance(k, ast.Constant) else '?' for k in node.value.keys]
        assert 'run_forum_pains' in keys, 'run_forum_pains missing from _TOOL_HANDLERS'
        assert 'run_media_urls' in keys, 'run_media_urls missing from _TOOL_HANDLERS'
        assert len(keys) == 26, f'Expected 26 entries (24+2), got {len(keys)}'
        print(f'OK: _TOOL_HANDLERS has {len(keys)} entries including run_forum_pains and run_media_urls')
        break
else:
    raise AssertionError('_TOOL_HANDLERS not found')

# Verify handlers resolve
from app.pipeline.engine import _get_handler
h1 = _get_handler('run_forum_pains')
h2 = _get_handler('run_media_urls')
assert h1.__name__ == 'handle_run_forum_pains', f'Wrong handler: {h1.__name__}'
assert h2.__name__ == 'handle_run_media_urls', f'Wrong handler: {h2.__name__}'
print('OK: both handlers resolve via _get_handler')
"</automated>
  </verify>

  <done>
    `_TOOL_HANDLERS` dict has 26 entries (24 original + 2 new). Both `run_forum_pains` and `run_media_urls` resolve via `_get_handler`. Existing 24 entries unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool → Perplexity API | New external API calls for forum pains + media URLs (existing API key, existing pattern) |
| Tool → Firecrawl API | New external API calls for media search (existing API key) |
| Tool → Review platforms | Indirect via Perplexity (no direct scraping) — respects robots.txt via search API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-S | Spoofing | Perplexity/Firecrawl API keys | accept | Keys already in env from Phase 1/2; no new secret management |
| T-04-03-T | Tampering | Search results could be wrong/incomplete | mitigate | D-18 honest block for 0 results; Perplexity cross-references multiple sources |
| T-04-03-R | Repudiation | Tool calls not logged | accept | push_tool_progress + logger.info provide execution trail |
| T-04-03-I | Info disclosure | Clinic URL sent to Perplexity/Firecrawl | accept | URL is public website; search APIs are privacy-policy bound |
| T-04-03-D | DoS | 5 parallel firecrawl searches per call | mitigate | asyncio.gather with return_exceptions=True; each search has timeout; cache prevents repeat calls |
| T-04-03-E | EoP | Not applicable | accept | Tools run under same process as existing handlers |
| T-04-03-SC | Supply chain | No new pip packages | accept | Uses existing firecrawl-py/openai SDKs already in requirements.txt |
</threat_model>

<verification>
**Post-plan automated checks:**

1. `run_forum_pains.py` and `run_media_urls.py` pass syntax check (ast.parse)
2. Both tools registered in LLM registry (registry.register called at import time)
3. Both tools registered in `_TOOL_HANDLERS` (26 entries total)
4. `_get_handler('run_forum_pains')` and `_get_handler('run_media_urls')` resolve without error
5. `_extract_fears()` in run_forum_pains parses test input correctly
6. TARGET_MEDIA in run_media_urls has exactly 5 sources (Forbes, RBC, Vademecum, Kommersant, ТАСС)
7. FORUM_SOURCES in run_forum_pains has exactly 4 platforms (ПроДокторов, Otzovik, IRecommend, Woman.ru)
8. D-18 pr_needed flag present in run_media_urls output shape

**Regression checks:**
- Existing 24 `_TOOL_HANDLERS` entries unchanged
- Existing tools (run_smi_mentions, run_review_platforms) still importable and functional
- LLM-orchestrator sees both new tools in its registry (aim-operations toolset)
</verification>

<success_criteria>
- SEC-05/D-10..11 SATISFIED: `run_forum_pains` scrapes 4 platforms for patient fears, extracts top-5 with mention counts
- DAT-02/D-15..18 SATISFIED: `run_media_urls` searches 5 target СМИ with concrete URLs, uses firecrawl with perplexity fallback, honest pr_needed block when 0 mentions
- Both tools registered in both LLM registry (aim-operations) and PipelineEngine `_TOOL_HANDLERS`
- `_TOOL_HANDLERS` has 26 entries (24 + 2 new)
- Existing tools and entries unchanged
</success_criteria>

<output>
Create `.planning/phases/04-new-sections-data-depth/04-03-SUMMARY.md` when done. Record:
- Files created: run_forum_pains.py, run_media_urls.py
- engine.py: 26 entries (was 24)
- Tool schemas (function name, parameters, descriptions)
- _extract_fears regex pattern used
- TARGET_MEDIA and FORUM_SOURCES lists
- Any issues importing firecrawl-py or calling the SDK
</output>
