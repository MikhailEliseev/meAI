---
phase: 06-e2e-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/__init__.py
  - tests/e2e/test_seo_workflow.py
  - tests/e2e/test_content_workflow.py
  - tests/e2e/test_ads_workflow.py
autonomous: true
requirements: [E2E-01, E2E-02, E2E-03]

must_haves:
  truths:
    - "SEO workflow executes end-to-end (Operator → SEO Magister → Keyword Research Agent)"
    - "Content workflow executes end-to-end (Operator → Content Magister → Content Writer Agent)"
    - "Ads workflow executes end-to-end (Operator → Ads Magister → Campaign Creator Agent)"
    - "All workflows return success status with aggregated results"
  artifacts:
    - path: "tests/e2e/__init__.py"
      provides: "E2E test package initialization"
      min_lines: 1
    - path: "tests/e2e/test_seo_workflow.py"
      provides: "SEO workflow E2E tests"
      min_lines: 100
    - path: "tests/e2e/test_content_workflow.py"
      provides: "Content workflow E2E tests"
      min_lines: 100
    - path: "tests/e2e/test_ads_workflow.py"
      provides: "Ads workflow E2E tests"
      min_lines: 100
  key_links:
    - from: "tests/e2e/test_seo_workflow.py"
      to: "src/aim/magisters/seo_magister.py"
      via: "direct import and instantiation"
      pattern: "from.*seo_magister import SEOMagister"
    - from: "tests/e2e/test_content_workflow.py"
      to: "src/aim/magisters/content_magister.py"
      via: "direct import and instantiation"
      pattern: "from.*content_magister import ContentMagister"
    - from: "tests/e2e/test_ads_workflow.py"
      to: "src/aim/magisters/ads_magister.py"
      via: "direct import and instantiation"
      pattern: "from.*ads_magister import AdsMagister"
---

<objective>
Create end-to-end tests for individual domain workflows (SEO, Content, Ads) to validate complete execution chains from Magister to Subagent.

Purpose: Ensure each domain workflow executes correctly with proper task delegation, result aggregation, and error handling.

Output: 3 E2E test files with 9+ tests total covering success paths, error scenarios, and timeout handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing test patterns
@tests/integration/test_seo_workflow_e2e.py
@tests/fixtures/magister_fixtures.py

# Magister implementations
@src/aim/magisters/seo_magister.py
@src/aim/magisters/content_magister.py
@src/aim/magisters/ads_magister.py

# Key subagents
@src/aim/subagents/keyword_research_agent.py
@src/aim/subagents/content_writer_agent.py
@src/aim/subagents/ads_campaign_creator_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test infrastructure</name>
  <files>tests/e2e/__init__.py</files>
  <action>
Create E2E test package initialization file. This establishes the e2e test directory as a proper Python package for pytest discovery.

File content:
```python
"""End-to-End Tests for AIM Agency System

Tests complete workflows from Magister to Subagent coordination.
"""
```
  </action>
  <verify>
    <automated>test -f tests/e2e/__init__.py && python -c "import tests.e2e"</automated>
  </verify>
  <done>E2E test package created and importable</done>
</task>

<task type="auto">
  <name>Task 2: Create SEO workflow E2E tests</name>
  <files>tests/e2e/test_seo_workflow.py</files>
  <action>
Create comprehensive E2E tests for SEO workflow covering:

1. **test_seo_workflow_keyword_research_success** - Full workflow from SEO Magister to Keyword Research Agent
   - Mock Keyword Research Agent to return keyword data
   - Verify SEO Magister receives and processes results
   - Check result structure (keywords, priorities, clusters)
   - Validate correlation ID propagation

2. **test_seo_workflow_with_multiple_subagents** - SEO Magister coordinating multiple subagents
   - Mock Technical, Content, Links agents
   - Verify parallel execution
   - Check weighted score aggregation (40% tech, 30% content, 30% links)
   - Validate recommendations generation

3. **test_seo_workflow_subagent_failure** - Error handling when subagent fails
   - Mock one subagent to raise exception
   - Verify graceful degradation (partial results)
   - Check error status in response
   - Ensure other subagents complete successfully

Pattern: Use existing test_seo_workflow_e2e.py as reference but focus on Magister→Subagent coordination, not just Magister orchestration.

Key differences from integration tests:
- Test actual subagent instantiation (not just mocked methods)
- Verify Event Bus message passing (if used)
- Test timeout handling at workflow level
- Validate end-to-end data flow
  </action>
  <verify>
    <automated>pytest tests/e2e/test_seo_workflow.py -v</automated>
  </verify>
  <done>3+ SEO workflow E2E tests passing, covering success, multi-agent, and failure scenarios</done>
</task>

<task type="auto">
  <name>Task 3: Create Content workflow E2E tests</name>
  <files>tests/e2e/test_content_workflow.py</files>
  <action>
Create comprehensive E2E tests for Content workflow covering:

1. **test_content_workflow_writer_success** - Full workflow from Content Magister to Content Writer Agent
   - Mock Content Writer Agent to return generated content
   - Verify Content Magister receives and validates content
   - Check result structure (content, metadata, quality_score)
   - Validate SEO optimization applied

2. **test_content_workflow_gap_analysis** - Content Magister coordinating Gap Analysis Agent
   - Mock Content Gap Analysis Agent to return competitor gaps
   - Verify gap identification and prioritization
   - Check recommendations for content creation
   - Validate topic clustering

3. **test_content_workflow_timeout** - Timeout handling when subagent takes too long
   - Mock subagent with long delay (asyncio.sleep)
   - Set short timeout on Content Magister
   - Verify TimeoutError raised or handled gracefully
   - Check partial results if available

Pattern: Follow SEO workflow test structure but adapt for Content domain specifics (content quality, SEO optimization, gap analysis).

Key validations:
- Content quality metrics (readability, keyword density)
- SEO optimization flags (meta tags, headers, structure)
- Gap analysis results (competitor topics, content opportunities)
  </action>
  <verify>
    <automated>pytest tests/e2e/test_content_workflow.py -v</automated>
  </verify>
  <done>3+ Content workflow E2E tests passing, covering writer, gap analysis, and timeout scenarios</done>
</task>

<task type="auto">
  <name>Task 4: Create Ads workflow E2E tests</name>
  <files>tests/e2e/test_ads_workflow.py</files>
  <action>
Create comprehensive E2E tests for Ads workflow covering:

1. **test_ads_workflow_campaign_creation_success** - Full workflow from Ads Magister to Campaign Creator Agent
   - Mock Campaign Creator Agent to return campaign data
   - Verify Ads Magister receives and validates campaign
   - Check result structure (campaign_id, budget, targeting, ad_groups)
   - Validate budget allocation logic

2. **test_ads_workflow_budget_optimization** - Ads Magister coordinating budget optimization
   - Mock Campaign Creator with multiple campaigns
   - Verify budget distribution across campaigns
   - Check CTR and conversion rate predictions
   - Validate A/B test setup

3. **test_ads_workflow_invalid_budget** - Error handling for invalid budget constraints
   - Mock Campaign Creator with budget exceeding limit
   - Verify validation error raised
   - Check error message clarity
   - Ensure no partial campaign creation

Pattern: Follow SEO/Content workflow test structure but adapt for Ads domain specifics (budget, targeting, campaign metrics).

Key validations:
- Budget constraints (min/max, allocation)
- Campaign metrics (CTR, conversion rate, CPC)
- Targeting parameters (keywords, demographics, geo)
- A/B test configuration
  </action>
  <verify>
    <automated>pytest tests/e2e/test_ads_workflow.py -v</automated>
  </verify>
  <done>3+ Ads workflow E2E tests passing, covering campaign creation, budget optimization, and validation scenarios</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Magister | Test code invokes Magister methods with mock data |
| Magister → Subagent | Magister delegates tasks to subagents (mocked in tests) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Mock subagent responses | accept | Test environment only, no production impact |
| T-06-02 | Denial of Service | Timeout handling | mitigate | Validate timeout logic prevents infinite waits |
| T-06-03 | Information Disclosure | Test data in logs | accept | Test data is synthetic, no real PII |

</threat_model>

<verification>
Run all E2E workflow tests:
```bash
pytest tests/e2e/test_seo_workflow.py tests/e2e/test_content_workflow.py tests/e2e/test_ads_workflow.py -v
```

Expected: 9+ tests passing (3 per workflow)

Verify test coverage:
```bash
pytest tests/e2e/ --cov=src/aim/magisters --cov-report=term-missing
```

Expected: Coverage increase for Magister coordination logic
</verification>

<success_criteria>
- [ ] E2E test package created (tests/e2e/__init__.py)
- [ ] SEO workflow tests created (3+ tests)
- [ ] Content workflow tests created (3+ tests)
- [ ] Ads workflow tests created (3+ tests)
- [ ] All 9+ tests passing
- [ ] Success paths validated (Magister → Subagent → Results)
- [ ] Error scenarios tested (failures, timeouts, validation)
- [ ] Correlation ID propagation verified
- [ ] Result aggregation validated
</success_criteria>

<output>
After completion, create `.planning/phases/06-e2e-tests/06-01-SUMMARY.md`
</output>
