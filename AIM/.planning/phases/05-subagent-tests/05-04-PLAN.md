---
phase: 05-subagent-tests
plan: 04
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - tests/unit/test_ads_campaign_creator_agent.py
autonomous: true
requirements: [REQ-5]

must_haves:
  truths:
    - "Ads Campaign Creator Agent tests cover 3 scenarios: campaign creation, ad copy generation, bid optimization"
    - "Yandex Direct API integration tested"
    - "Ad copy compliance validated"
    - "Bid strategy optimization tested"
  artifacts:
    - path: "tests/unit/test_ads_campaign_creator_agent.py"
      provides: "3 unit tests for Ads Campaign Creator Agent"
      min_lines: 200
---

<objective>
Test Ads Campaign Creator Agent campaign creation, ad copy generation, and bid optimization.

Purpose: Validate that Ads Campaign Creator Agent correctly creates campaigns in Yandex Direct, generates compliant ad copy, and optimizes bid strategies.

Output: 3 passing tests covering campaign creation, ad copy, and bid optimization.
</objective>

<execution_context>
@/Users/mikhaileliseev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mikhaileliseev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mikhaileliseev/Desktop/Dev/!meAI/AIM/.planning/phases/05-subagent-tests/05-CONTEXT.md
@/Users/mikhaileliseev/Desktop/Dev/!meAI/AIM/src/aim/subagents/ads_campaign_creator_agent.py
@/Users/mikhaileliseev/Desktop/Dev/!meAI/AIM/tests/fixtures/subagent_fixtures.py
@/Users/mikhaileliseev/Desktop/Dev/!meAI/AIM/tests/fixtures/subagent_data.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Ads Campaign Creator Agent unit tests</name>
  <read_first>
    - AIM/.planning/phases/05-subagent-tests/05-CONTEXT.md (Decision 2: Test Scope)
    - src/aim/subagents/ads_campaign_creator_agent.py (implementation)
    - tests/fixtures/subagent_data.py (YANDEX_CAMPAIGN)
  </read_first>
  <files>tests/unit/test_ads_campaign_creator_agent.py</files>
  <action>
Create 3 unit tests for Ads Campaign Creator Agent.

**Create:** `tests/unit/test_ads_campaign_creator_agent.py`

**Content:**
```python
"""Unit tests for Ads Campaign Creator Agent

Tests campaign creation, ad copy generation, and bid optimization.
"""

import pytest
from unittest.mock import AsyncMock

from meai.agents.base_agent import Task
from src.aim.subagents.ads_campaign_creator_agent import AdsCampaignCreatorAgent
from tests.fixtures.subagent_data import YANDEX_CAMPAIGN
from tests.fixtures.subagent_fixtures import mock_api_clients, ads_campaign_agent


@pytest.mark.asyncio
async def test_campaign_creation_success(ads_campaign_agent, mock_api_clients):
    """Test campaign creation in Yandex Direct"""
    # Mock Yandex Direct API response
    mock_api_clients["yandex_direct"].create_campaign.return_value = {
        "campaign_id": "12345",
        "name": "Dental Implants Campaign",
        "status": "active",
        "budget": 50000,
        "daily_budget": 1666,
    }
    
    # Create task
    task = Task(
        task_id="test-ads-001",
        task_type="campaign_creation",
        parameters={
            "campaign_name": "Dental Implants Campaign",
            "budget": 50000,
            "target_keywords": ["dental implants", "implant cost"],
            "geo_targeting": ["Moscow", "Saint Petersburg"],
        },
    )
    
    # Execute
    result = await ads_campaign_agent.execute_task(task)
    
    # Verify success
    assert result.status == "success"
    assert "campaign" in result.data
    
    # Verify campaign created
    campaign = result.data["campaign"]
    assert campaign["campaign_id"] == "12345"
    assert campaign["name"] == "Dental Implants Campaign"
    assert campaign["status"] == "active"
    
    # Verify budget allocation
    assert campaign["budget"] == 50000
    assert campaign["daily_budget"] == 1666
    
    # Verify API call
    mock_api_clients["yandex_direct"].create_campaign.assert_called_once()
    call_args = mock_api_clients["yandex_direct"].create_campaign.call_args
    assert call_args[1]["name"] == "Dental Implants Campaign"
    assert call_args[1]["budget"] == 50000


@pytest.mark.asyncio
async def test_ad_copy_generation(ads_campaign_agent, mock_api_clients):
    """Test ad copy generation with compliance"""
    # Mock LLM response for ad copy
    mock_api_clients["openai"] = AsyncMock()
    mock_api_clients["openai"].generate.return_value = {
        "title": "Dental Implants from $999",
        "text": "Professional dental implants. Free consultation. Call now!",
        "display_url": "dentalclinic.ru",
    }
    
    # Create task
    task = Task(
        task_id="test-ads-002",
        task_type="ad_copy_generation",
        parameters={
            "target_keyword": "dental implants",
            "unique_selling_point": "affordable prices, free consultation",
            "max_title_length": 56,
            "max_text_length": 81,
        },
    )
    
    # Execute
    result = await ads_campaign_agent.execute_task(task)
    
    # Verify success
    assert result.status == "success"
    assert "ad_copy" in result.data
    
    # Verify ad copy
    ad_copy = result.data["ad_copy"]
    assert "title" in ad_copy
    assert "text" in ad_copy
    assert "display_url" in ad_copy
    
    # Verify character limits
    assert len(ad_copy["title"]) <= 56
    assert len(ad_copy["text"]) <= 81
    
    # Verify compliance check
    assert "compliance_check" in result.data
    compliance = result.data["compliance_check"]
    assert compliance["passed"] is True
    assert compliance["risk_level"] == "low"
    
    # Verify keyword integration
    assert "dental implants" in ad_copy["title"].lower() or "dental implants" in ad_copy["text"].lower()


@pytest.mark.asyncio
async def test_bid_strategy_optimization(ads_campaign_agent, mock_api_clients):
    """Test bid strategy optimization"""
    # Mock historical performance data
    mock_performance = {
        "keywords": [
            {
                "keyword": "dental implants",
                "avg_cpc": 8.50,
                "conversion_rate": 0.05,
                "avg_position": 2.5,
            },
            {
                "keyword": "implant cost",
                "avg_cpc": 6.20,
                "conversion_rate": 0.03,
                "avg_position": 3.2,
            },
        ],
        "total_budget": 50000,
        "target_roi": 3.0,
    }
    
    # Create task
    task = Task(
        task_id="test-ads-003",
        task_type="bid_optimization",
        parameters={
            "campaign_id": "12345",
            "performance_data": mock_performance,
            "optimization_goal": "maximize_conversions",
        },
    )
    
    # Execute
    result = await ads_campaign_agent.execute_task(task)
    
    # Verify success
    assert result.status == "success"
    assert "bid_strategy" in result.data
    
    # Verify bid strategy
    strategy = result.data["bid_strategy"]
    assert "keyword_bids" in strategy
    assert len(strategy["keyword_bids"]) == 2
    
    # Verify bid calculations
    for bid in strategy["keyword_bids"]:
        assert "keyword" in bid
        assert "recommended_bid" in bid
        assert "expected_position" in bid
        assert "expected_conversions" in bid
        assert bid["recommended_bid"] > 0
    
    # Verify budget allocation
    assert "budget_allocation" in strategy
    allocation = strategy["budget_allocation"]
    assert allocation["total_budget"] == 50000
    assert "daily_budget" in allocation
    
    # Verify ROI prediction
    assert "roi_prediction" in result.data
    roi = result.data["roi_prediction"]
    assert roi["expected_roi"] > 0
```

**Why:** Deep testing of campaign creation, ad copy compliance, and bid optimization algorithms.
  </action>
  <commit>
test(05-04): add Ads Campaign Creator Agent unit tests (3 tests)

- test_campaign_creation_success: Yandex Direct API integration
- test_ad_copy_generation: Compliance + character limits
- test_bid_strategy_optimization: Bid calculation + ROI prediction
- Real bid optimization algorithm tested
- 200+ lines of quality tests

Co-Authored-By: Claude Sonnet 4 <noreply@anthropic.com>
  </commit>
</task>

</tasks>

<success_criteria>
- ✅ 3 Ads Campaign Creator Agent tests passing
- ✅ Yandex Direct API integration tested
- ✅ Ad copy compliance validated
- ✅ Character limits enforced
- ✅ Bid optimization algorithm tested
- ✅ ROI prediction validated
- ✅ Atomic commit
</success_criteria>
