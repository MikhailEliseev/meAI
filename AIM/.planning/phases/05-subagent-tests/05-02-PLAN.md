---
phase: 05-subagent-tests
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - tests/unit/test_content_gap_analysis_agent.py
autonomous: true
requirements: [REQ-5]

must_haves:
  truths:
    - "Content Gap Analysis Agent tests cover 3 scenarios: gap detection, competitor analysis, brief generation"
    - "SERP overlap calculation tested with real algorithm"
    - "Content quality metrics validated"
    - "Brief generation produces structured output"
  artifacts:
    - path: "tests/unit/test_content_gap_analysis_agent.py"
      provides: "3 unit tests for Content Gap Analysis Agent"
      min_lines: 200
  key_links:
    - from: "tests/unit/test_content_gap_analysis_agent.py"
      to: "tests/fixtures/subagent_fixtures.py"
      via: "pytest fixture import"
      pattern: "from.*subagent_fixtures import"
---

<objective>
Test Content Gap Analysis Agent gap detection, competitor content analysis, and brief generation.

Purpose: Validate that Content Gap Analysis Agent correctly identifies content gaps through SERP overlap, analyzes competitor content quality, and generates structured content briefs.

Output: 3 passing tests covering gap detection, competitor analysis, and brief generation.
</objective>

<execution_context>
@/Users/mikhaileliseev/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mikhaileliseev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mikhaileliseev/Desktop/Dev/!meAI/AIM/.planning/phases/05-subagent-tests/05-CONTEXT.md
@/Users/mikhaileliseev/Desktop/Dev/!meAI/AIM/src/aim/subagents/content_gap_analysis_agent.py
@/Users/mikhaileliseev/Desktop/Dev/!meAI/AIM/tests/fixtures/subagent_fixtures.py
@/Users/mikhaileliseev/Desktop/Dev/!meAI/AIM/tests/fixtures/subagent_data.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Content Gap Analysis Agent unit tests</name>
  <read_first>
    - AIM/.planning/phases/05-subagent-tests/05-CONTEXT.md (Decision 2: Test Scope)
    - src/aim/subagents/content_gap_analysis_agent.py (implementation)
    - tests/fixtures/subagent_data.py (COMPETITOR_CONTENT)
  </read_first>
  <files>tests/unit/test_content_gap_analysis_agent.py</files>
  <action>
Create 3 unit tests for Content Gap Analysis Agent.

**Create:** `tests/unit/test_content_gap_analysis_agent.py`

**Content:**
```python
"""Unit tests for Content Gap Analysis Agent

Tests gap detection, competitor analysis, and brief generation.
"""

import pytest
from unittest.mock import AsyncMock

from meai.agents.base_agent import Task
from src.aim.subagents.content_gap_analysis_agent import ContentGapAnalysisAgent
from tests.fixtures.subagent_data import COMPETITOR_CONTENT
from tests.fixtures.subagent_fixtures import mock_api_clients, content_gap_agent


@pytest.mark.asyncio
async def test_gap_detection_success(content_gap_agent, mock_api_clients):
    """Test content gap detection through SERP overlap"""
    # Mock SERP fetching
    mock_serp_data = {
        "our_site": {
            "url": "https://oursite.com",
            "ranking_keywords": ["dental implants", "implant cost"],
            "content_topics": ["procedure", "cost"],
        },
        "competitors": [
            {
                "url": "https://competitor.com",
                "ranking_keywords": ["dental implants", "implant cost", "recovery time", "aftercare"],
                "content_topics": ["procedure", "cost", "recovery", "maintenance"],
            },
        ],
    }
    
    # Create task
    task = Task(
        task_id="test-gap-001",
        task_type="content_gap_analysis",
        parameters={
            "our_url": "https://oursite.com",
            "competitor_urls": ["https://competitor.com"],
            "target_keyword": "dental implants",
        },
    )
    
    # Execute
    result = await content_gap_agent.execute_task(task)
    
    # Verify success
    assert result.status == "success"
    assert "gaps" in result.data
    
    # Verify gap detection
    gaps = result.data["gaps"]
    assert len(gaps) > 0
    
    # Verify missing keywords identified
    missing_keywords = [g["keyword"] for g in gaps if g["type"] == "keyword"]
    assert "recovery time" in missing_keywords
    assert "aftercare" in missing_keywords
    
    # Verify missing topics identified
    missing_topics = [g["topic"] for g in gaps if g["type"] == "topic"]
    assert "recovery" in missing_topics
    assert "maintenance" in missing_topics


@pytest.mark.asyncio
async def test_competitor_content_analysis(content_gap_agent, mock_api_clients):
    """Test competitor content quality analysis"""
    # Mock content extraction
    mock_api_clients["playwright"] = AsyncMock()
    mock_api_clients["playwright"].extract_content.return_value = {
        "url": "https://competitor.com/dental-implants-guide",
        "title": "Complete Guide to Dental Implants",
        "word_count": 2500,
        "headings": ["What are dental implants?", "Cost breakdown", "Procedure steps"],
        "keywords": ["dental implants", "implant cost", "implant procedure"],
        "images": 8,
        "internal_links": 12,
        "external_links": 5,
    }
    
    # Create task
    task = Task(
        task_id="test-gap-002",
        task_type="competitor_analysis",
        parameters={
            "competitor_url": "https://competitor.com/dental-implants-guide",
        },
    )
    
    # Execute
    result = await content_gap_agent.execute_task(task)
    
    # Verify success
    assert result.status == "success"
    assert "analysis" in result.data
    
    # Verify quality metrics
    analysis = result.data["analysis"]
    assert analysis["word_count"] == 2500
    assert analysis["quality_score"] > 0
    assert analysis["quality_score"] <= 100
    
    # Verify structure analysis
    assert "headings_count" in analysis
    assert "images_count" in analysis
    assert "links_count" in analysis
    
    # Verify recommendations
    assert "recommendations" in result.data
    recommendations = result.data["recommendations"]
    assert len(recommendations) > 0


@pytest.mark.asyncio
async def test_brief_generation(content_gap_agent, mock_api_clients):
    """Test content brief generation from gaps"""
    # Mock gap data
    gaps = [
        {
            "type": "keyword",
            "keyword": "recovery time",
            "volume": 3000,
            "difficulty": 50,
            "priority": "high",
        },
        {
            "type": "topic",
            "topic": "aftercare",
            "related_keywords": ["post-surgery care", "healing process"],
            "priority": "medium",
        },
    ]
    
    # Create task
    task = Task(
        task_id="test-gap-003",
        task_type="brief_generation",
        parameters={
            "target_keyword": "dental implants",
            "gaps": gaps,
            "target_word_count": 2000,
        },
    )
    
    # Execute
    result = await content_gap_agent.execute_task(task)
    
    # Verify success
    assert result.status == "success"
    assert "brief" in result.data
    
    # Verify brief structure
    brief = result.data["brief"]
    assert "title" in brief
    assert "target_keyword" in brief
    assert "target_word_count" in brief
    assert "sections" in brief
    
    # Verify sections
    sections = brief["sections"]
    assert len(sections) > 0
    
    # Verify gap keywords integrated
    section_titles = [s["title"] for s in sections]
    assert any("recovery" in title.lower() for title in section_titles)
    assert any("aftercare" in title.lower() for title in section_titles)
    
    # Verify keyword integration
    assert "keywords_to_include" in brief
    keywords = brief["keywords_to_include"]
    assert "recovery time" in keywords
    
    # Verify quality guidelines
    assert "quality_guidelines" in brief
    guidelines = brief["quality_guidelines"]
    assert len(guidelines) > 0
```

**Why:** Deep testing of gap detection algorithm, competitor analysis, and brief generation.
  </action>
  <commit>
test(05-02): add Content Gap Analysis Agent unit tests (3 tests)

- test_gap_detection_success: SERP overlap gap identification
- test_competitor_content_analysis: Content quality metrics
- test_brief_generation: Structured brief from gaps
- Real gap detection algorithm tested
- 200+ lines of quality tests

Co-Authored-By: Claude Sonnet 4 <noreply@anthropic.com>
  </commit>
</task>

</tasks>

<success_criteria>
- ✅ 3 Content Gap Analysis Agent tests passing
- ✅ Gap detection algorithm tested (SERP overlap)
- ✅ Competitor content quality metrics validated
- ✅ Brief generation produces structured output
- ✅ All tests use real business logic
- ✅ Atomic commit with descriptive message
</success_criteria>
