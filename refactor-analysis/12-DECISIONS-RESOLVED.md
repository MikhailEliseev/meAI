# 12 — Decisions RESOLVED (ответы Михаила)

**Дата:** 30 июня 2026
**Источник:** голосовой ответ Михаила после прочтения `10-DECISIONS-NEEDED.md`

---

## ✅ Решение #1: aim-paperclip — УДАЛИТЬ

**Вопрос был:** Что за контейнер `aim-paperclip` (2.76 GB образ) и что за `/opt/data/bin/tirith` (22 MB)?

**Ответ Михаила:**
> "M Pepper Clip — его нафиг не нужно, его удалить. Это был эксперимент, который должен был быть на другом сервере, а не на этом, и всё просто перепуталось. Его точно удалить."

**Действие:**
```bash
# Остановить и удалить контейнер
docker stop aim-paperclip
docker rm aim-paperclip

# Удалить образ (2.76 GB)
docker rmi paperclip-paperclip:latest

# Убрать default_server из nginx конфига
# (location / proxy_pass http://aim-paperclip:3100)

# Перезапустить nginx
docker-compose restart nginx
```

**Также удалить:**
- `/opt/data/bin/tirith` (22 MB binary) — связан с paperclip, удалить

**Освобождаемое место:** ~2.76 GB образ + ~22 MB binary = ~2.8 GB

**Фаза рефакторинга:** День 2 (когда чистим инфраструктуру)

---

## 🟡 Решение #2: Phase 09 — АНАЛИЗИРОВАТЬ ПЕРЕД РЕШЕНИЕМ

**Вопрос был:** Деплоить Phase 09 или удалить?

**Ответ Михаила:**
> "Фаза 9. Я не знаю, как будто бы на фазе 9 я приблизился и объяснил LLM максимальный результат, который я хочу сделать, и какие ошибки были там. Я думаю, что фаза 0-9 она более-менее разумная, но тебе нужно её максимально точно проанализировать и понять, применима она или нет, и что оттуда чётко применимо, а что не применимо."

**Это означает:**
- ❌ НЕ деплоить вслепую
- ❌ НЕ удалять вслепую
- ✅ **Провести глубокий анализ Phase 09** → извлечь ценное → потом решать

**Что нужно сделать (отдельная задача анализа):**

1. Найти все файлы Phase 09:
   - `main.py.backup-phase09-20260628-075019`
   - `main.py.backup-phase09-20260628-075030`
   - `aim-pro-endpoints.php` (в WordPress theme)
   - `chat-inline-pro.php` (в WordPress theme)
   - `functions.php.bak` (если содержит Phase 09 код)
   - Git history: commits `843d653`, `5b3bc13`, `43d5cd3` (Phase 09 related)
   - Local: `~/Desktop/phase09-COMPLETE-20260628-022838.tar.gz` (упоминается в SESSION.md)

2. Извлечь из Phase 09:
   - **Phase Tracker** логику (8 фаз с прогрессом)
   - **Report Preview** компонент
   - **Fallback Form** для сбора email/telegram
   - **3-сообщений формат** промпт (это уже в `agent_wrapper.py`)
   - **aim-pro-endpoints.php** REST endpoints

3. Оценить что применимо к Путь C:
   - ✅ / ❌ Phase Tracker UI (если упрощаем — может лишнее)
   - ✅ Report Preview (полезно)
   - ✅ Fallback Form (полезно для capture контакта)
   - ✅ 3-сообщений промпт (уже в коде)
   - ✅ / ❌ aim-pro-endpoints.php (зависит от архитектуры)

**Фаза рефакторинга:** Анализ в Дни 6-7 (после стабилизации), деплой (если решим) в Дни 8-9

**Документ с анализом:** создать `13-PHASE09-ANALYSIS.md` (отдельная задача)

---

## 🟢 Решение #3: HeadroomGuard — ОТЛОЖИТЬ (не сейчас)

**Вопрос был:** Возвращать HeadroomGuard или удалить упоминания?

**Ответ Михаила:**
> "Hedrum, я думаю, что пока стоит отложить его в сторону. Это система, которая экономит токены, но это как бы обвес, который хорошо было бы через него работать, но, наверное, не на стадии MVP, то есть не на стадии, когда мы даже без обвесов и без доп. инструментов не можем гарантировать результат.

Поэтому хедрум — это надстройка, который абсолютно точно важен и нужен, если, конечно, её можно интегрировать сразу и через неё работать, и у нас будет экономия в деньгах. Это хорошо, но если у нас это только усложнит архитектуру и без того сложную, и будет дополнительно обузой, то нет."

**Решение:**
- ❌ НЕ возвращать на стадии MVP
- ❌ НЕ интегрировать сейчас (уже не активен)
- ✅ Удалить конфиги и упоминания из SESSION.md / CLAUDE.md
- ✅ Сохранить `docker-compose.headroom.yml` в архив (`compose-archive/`) на будущее
- ✅ Зафиксировать в backlog: "HeadroomGuard — revisit после MVP"

**Действие:**
```bash
# Удалить конфиги (не активны)
rm /opt/aim/docker-compose.headroom.yml
rm /opt/aim/AIM/docker-compose.headroom-deepseek.yml.backup  # уже .backup

# Удалить env file (если есть)
rm /opt/aim/AIM/.env.headroom

# Обновить SESSION.md — убрать секцию HeadroomGuard
# Обновить CLAUDE.md — убрать HeadroomGuard упоминания
```

**Логика Михаила:** "не на стадии, когда мы даже без обвесов не можем гарантировать результат" — сначала ядро, потом оптимизация.

**Фаза рефакторинга:** День 4 (когда чистим docs)

---

## 🟢 Решение #4: Backend CRM — ИЗБЫТОЧЕН (пока)

**Вопрос был:** Backend CRM (leads/sales/onboarding/analytics) будет использоваться?

**Ответ Михаила:**
> "БКМ ЦРМ: я до неё даже пока не дошел.

Пятый пункт: пока ещё до БКМ не дошел. Да, она заложена в логике, но ещё раз повторю: я пока не увидел и не добился за эти два месяца результата, которого хотел от хотя бы конкурентной разведки и чата с клиентом. Поэтому пятый пункт БКМ ЦРМ — очень круто, но опять же мне хочется на простых результатах хотя бы что-то прощупать. Я скажу: да, окей, наконец-то мы хоть что-то получили, пока мы не получили вообще ничего. Этот пункт, наверное, избыточен."

**Решение:**
- ❌ НЕ использовать backend CRM сейчас
- ❌ НЕ чинить PostgreSQL ради CRM (но починить ради tools — если нужно)
- ✅ Удалить aim-app endpoints, которые зависят от PostgreSQL CRM таблиц
- ✅ Сфокусироваться на "простых результатах" = конкурентная разведка + чат

**Принцип:** "Сначала хоть что-то получить, потом расширять"

**Действие:**
1. Проверить: какие Hermes tools зависят от aim-app?
2. Если tools могут работать без aim-app backend → удалить aim-app полностью
3. Если tools зависят → оставить минимально нужные endpoints, остальное удалить
4. PostgreSQL: либо удалить, либо оставить как SQLite-аналог для будущего

**Фаза рефакторинга:** День 6-7 (когда упрощаем tools)

---

## 🎯 Сводка решений

| # | Компонент | Решение | Фаза |
|---|---|---|---|
| 1 | aim-paperclip (2.76 GB) + tirith binary | **УДАЛИТЬ** | День 2 |
| 2 | Phase 09 | **АНАЛИЗИРОВАТЬ** потом решить | Анализ: Дни 6-7 |
| 3 | HeadroomGuard | **ОТЛОЖИТЬ** (не MVP) | День 4 (убрать docs) |
| 4 | Backend CRM (leads/sales/onboarding) | **ИЗБЫТОЧЕН** (пока) | День 6-7 |

---

## 🚨 Неотвеченный вопрос

**#5 tirith binary (22 MB):** Михаил не ответил прямо, но из контекста ответа про paperclip следует что tirith тоже от paperclip.

**Предположение:** tirith = часть paperclip experiment → удалить вместе с paperclip.

**Действие:** подтвердить с Михаилом при случае, но пока планировать удалить.

---

## 📋 Обновлённый приоритет действий

```
День 1: PG auth fix + session_archive fix + SOUL.md sync
День 2: Удалить paperclip + tirith + magisters + subagents + pipeline v7
День 3: Удалить .venv, .planning, backups, logs, node_modules, дубликаты
День 4: Обновить SESSION.md + CLAUDE.md (убрать headroom упоминания)
День 5: Smoke test очищенной системы
День 6-7: Анализ Phase 09 (создать 13-PHASE09-ANALYSIS.md)
День 8-9: Упростить tools для LLM + починить run_full_scout
День 10: UX проверка финального отчёта
```

---

## 💡 Ключевая фраза Михаила (цитата)

> "Мне хочется на простых результатах хотя бы что-то прощупать. Я скажу: да, окей, наконец-то мы хоть что-то получили, пока мы не получили вообще ничего."

**Это главный критерий успеха.** Не "идеально", не "масштабируемо", не "feature-complete". **"Хоть что-то получили"** = клиент URL → отчёт на сайте. Полный цикл.

**Stop criterion:** когда это работает End-to-End без глюков.

---

*Этот документ — зафиксированные решения Михаила. Не подлежат пересмотру без явного согласования.*
