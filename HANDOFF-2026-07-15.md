# Памятка для новой сессии — чат iamaim.ru

## Что сделано (2026-07-15)

### Скорость
- Базовый анализ URL: **4.5 мин → 1.5-2 мин** (2-3x быстрее)
- Параллельное выполнение тулов через `asyncio.gather` в `llm.py`
- Лишние тулзы убраны (perplexity_search больше не вызывается автоматически)

### Тулзы (13 штук, все зарегистрированы в `hermes-v2/app/tools/`)
1. `find_competitors` — Apify → Google Maps (прокси к aim-app)
2. `enrich_competitors` — ИНН → выручка: ФНС через aim-app, fallback Perplexity
3. `extract_clinic_profile` — Perplexity (ИНН, юрлицо, адрес)
4. `quick_overview` — Perplexity (врачи, услуги, соцсети)
5. `company_financials` — **ФНС (ЕГРЮЛ)**, точная выручка по ИНН (~1-3с)
6. `company_profile` — aim-app DB
7. `analyze_website` — aim-app (Playwright + SEO + репутация)
8. `seo_audit` — aim-app
9. `perplexity_search` — гибкий поиск
10. `run_smi_mentions` — Perplexity
11. `run_review_platforms` — Perplexity (Яндекс.Карты, 2ГИС, ПроДокторов)
12. `run_instagram_content` — Apify
13. `run_ads_intelligence` — Firecrawl (14 ключей)

### Дизайн чата — ЭТАЛОН (не менять!)
- Файл `CHAT-GOLDEN-STATE.md` — правила
- Golden files: `AIM/theme/chat-inline-golden.php`, `theme-golden.css`, `front-page-golden.php`, `footer-golden.php`
- Бэкапы на сервере: `/opt/backups/chat-golden-FINAL-20260715/`
- `theme.css` — оригинальный, НЕ трогать `--glass-bg` глобально
- Плашка убрана **точечно** в `.chat-wrapper` (`background: #ffffff` напрямую)
- Phase Tracker (`chat-inline-pro.php`) отключён
- Брендинг: «AI-ассистент AIM» (без «Гермес», без технологий)
- Кнопка «AIM Ассистент» скрывается при открытии чата (все 3 пути: openChat, openChatDirect, closeChat)

### Прогресс-сообщения (SSE)
Каждый тул шлёт человекочитаемые сообщения (`_TOOL_MESSAGES` в `llm.py`):
- 📋 Определяю клинику…
- 🔍 Собираю обзор…
- 🗺️ Ищу конкурентов (~1-2 минуты)…
- 💰 Получаю выручку из ФНС…
- Markdown рендерится на лету через `marked.parse()` + `DOMPurify`

### Лендинг
- Убраны обещания «24/7 мониторинг»
- Текст про AI-модели вместо нереальных функций

## Что НЕ сделано (следующие задачи)

### Качество данных конкурентов — ГЛАВНОЕ
1. **find_competitors возвращает конкурентов БЕЗ ИНН** → enrich не может получить выручку из ФНС. Нужно: либо aim-app возвращает ИНН, либо добавляем ресолв ИНН по названию
2. **В мегаполисах конкуренты по локации — мелкие**. Для крупных клиник (ИПХиК 4 млрд ₽) нужны конкуренты по выручке, не по геолокации. Логика: если выручка клиента > 100М → искать крупных игроков, а не ближайших
3. **Логика отбора «достижимые цели»** (±30% выручки) написана в `competitors.py` (`filter_competitors`), но LLM её пока не вызывает — нужно внедрить в pipeline
4. **count=5 лимит aim-app** — LLM передаёт count=3, нужно хардкодить минимум 5

### Источники данных
- aim-app `/api/companies/financials?inn=X` — работает, точные данные из ФНС
- aim-app `/api/content/analyze` — глубокий аудит (Playwright+SEO+репутация), работает
- rusprofile.ru через Firecrawl — НЕ протестировано (может дать выручку по ИНН конкурентов)

## Архитектура (как добраться)

```
iamaim.ru (пользователь)
  → nginx (theme.css, front-page.php, footer.php)
  → WordPress (chat-inline.php — чат UI)
  → POST /api/chat/stream
  → aim-hermes-v2:8000 (FastAPI, llm.py + 13 тулов)
  → aim-app:8000 (финансы, конкуренты, аудит, Playwright)
  → Perplexity / Apify / Firecrawl / ФНС
```

## Ключевые файлы

| Файл | Назначение |
|------|-----------|
| `AIM/hermes-v2/app/llm.py` | LLM клиент, параллельные тулзы, прогресс-сообщения |
| `AIM/hermes-v2/app/main.py` | SSE endpoint `/api/chat/stream` |
| `AIM/hermes-v2/app/prompts/dialogue.py` | Системный промпт |
| `AIM/hermes-v2/app/tools/competitors.py` | find/enrich/filter competitors |
| `AIM/hermes-v2/app/tools/aim_app_tools.py` | Прокси к aim-app (financials, audit) |
| `AIM/hermes-v2/app/tools/perplexity_tools.py` | Perplexity тулзы |
| `AIM/theme/chat-inline-golden.php` | Чат UI (ЭТАЛОН) |
| `CHAT-GOLDEN-STATE.md` | Правила: стили не трогать |

## Сервер
- `ssh aim` (78.17.128.169)
- Контейнеры: `aim-hermes-v2` (v0.3.0), `aim-app`, `aim-wordpress`, `aim-nginx`
- Деплой: `rsync → docker compose build hermes-v2 → up -d hermes-v2`
- Логи: `docker logs aim-hermes-v2 -f`
