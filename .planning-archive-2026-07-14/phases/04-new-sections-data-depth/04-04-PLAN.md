---
phase: 4
plan: 04-04
type: execute
name: pass1-pass2-prompts-qc-checklist-expansion
wave: 2
depends_on: [04-01, 04-02, 04-03]
requirements: [SEC-01, SEC-02, SEC-03, SEC-04, SEC-05, DAT-01, DAT-02, DAT-04, DAT-05]
tags: [orchestrator, pass1, pass2, qc-checklist, prompts]

files_modified:
  - AIM/hermes/app/orchestrator/pass_collect.py
  - AIM/hermes/app/orchestrator/pass_gap_analyze.py
  - AIM/hermes/app/orchestrator/qc_checklist.py
  - AIM/hermes/app/orchestrator/coverage_reporter.py

autonomous: true

must_haves:
  truths:
    - "D-25: QC_CHECKLIST expanded from 15 to 18 items — 3 new items added (clinic_metrics DAT-04, ratings DAT-05, expert_regalia SEC-04)"
    - "VERSION bumped from 1.1.0 to 1.2.0 in qc_checklist.py"
    - "Pass 1 prompt (_build_pass_collect_prompt) extended with collection rules: run_forum_pains (SEC-05), run_media_urls (DAT-02), find_company_financials multi-year (DAT-01), run_review_platforms (DAT-05)"
    - "Pass 2 prompt template updated to evaluate 18 items instead of 15 (total in summary, fallback report)"
    - "Pass 2 prompt includes niche_instruction for new items (applicable to all niches)"
    - "coverage_reporter.py: CoverageReport.total_items default updated from 15 to 18"
    - "coverage_reporter.py: calc_coverage reads total from len(QC_CHECKLIST) dynamically (already does — verify)"
  artifacts:
    - path: "AIM/hermes/app/orchestrator/qc_checklist.py"
      provides: "18-item QC_CHECKLIST with VERSION 1.2.0"
      contains: "VERSION = \"1.2.0\""
    - path: "AIM/hermes/app/orchestrator/pass_collect.py"
      provides: "Extended _build_pass_collect_prompt with Phase 4 collection rules"
      contains: "run_forum_pains"
    - path: "AIM/hermes/app/orchestrator/pass_gap_analyze.py"
      provides: "Pass 2 prompt template with 18-item evaluation"
      contains: "18"
    - path: "AIM/hermes/app/orchestrator/coverage_reporter.py"
      provides: "CoverageReport with total_items=18 default"
      contains: "total_items"
  key_links:
    - from: "pass_gap_analyze._CHECKLIST_PROMPT_TEMPLATE"
      to: "qc_checklist.QC_CHECKLIST (18 items)"
      via: "render_checklist_for_llm() renders all items"
      pattern: "render_checklist_for_llm"
    - from: "pass_collect._build_pass_collect_prompt"
      to: "new tools run_forum_pains + run_media_urls"
      via: "explicit collection rules in prompt text"
      pattern: "run_forum_pains"
---

<objective>
Expand the orchestrator prompts and QC checklist for Phase 4's new data types and sections:

1. **QC_CHECKLIST expansion (D-25)**: 15 → 18 items. Add 3 new items: clinic_metrics (DAT-04), ratings (DAT-05), expert_regalia (SEC-04). Refine existing items to match Phase 4 depth (e.g., item 8 SMI mentions → "concrete URLs from 5 target СМИ", item 11 revenue dynamics → "3-year trend with YoY %").

2. **Pass 1 prompt extension**: Add explicit collection rules telling the LLM to call `run_forum_pains`, `run_media_urls`, `find_company_financials` (for 3-year revenue + clinic metrics), and `run_review_platforms` (for ratings).

3. **Pass 2 prompt update**: Template references 18 items (was 15). Fallback report total updated. Summary calculation handles the expanded checklist.

Purpose: Phase 3's prompts and checklist cover Instagram + base sections. Phase 4 adds new data types (forums, media URLs, structured regalia, ratings, clinic metrics). Without prompt-level collection rules, the LLM won't call the new tools (Plan 04-03 created them, but the LLM needs instructions). Without QC checklist expansion, coverage % won't reflect the new sections.

Output: Modified orchestrator modules with expanded prompts and checklist.
</objective>

## Phase Goal (MVP)

**As a** Hermes presale orchestrator,
**I want to** Pass 1 to collect all Phase 4 data types AND Pass 2 to evaluate coverage against an 18-item checklist,
**so that** the final report includes forum pains, media URLs, 3-year revenue, clinic metrics, ratings, and expert регалии — matching the reference report depth.
</objective>

---

<context>
@.planning/PROJECT.md
@.planning/phases/04-new-sections-data-depth/04-CONTEXT.md
@.planning/phases/03-instagram-integration/03-VERIFICATION.md

@AIM/hermes/app/orchestrator/qc_checklist.py
@AIM/hermes/app/orchestrator/pass_collect.py
@AIM/hermes/app/orchestrator/pass_gap_analyze.py
@AIM/hermes/app/orchestrator/coverage_reporter.py

<interfaces>
<!-- Current QC_CHECKLIST structure (15 items, VERSION 1.1.0) -->

From qc_checklist.py (303 lines):
```python
VERSION = "1.1.0"  # → bump to "1.2.0"
PASS_THRESHOLD: float = 0.80
PASS_MIN_ITEMS: int = 12  # 80% of 15 — update to 15 (80% of 18 = 14.4)
QC_CHECKLIST: list[dict] = [
    {"id": 1, "category": "about", "name": "About data collected", ...},
    # ... items 2-15 (see file) ...
    {"id": 15, "category": "offer", "name": "Offer section", ...},
]
# Phase 4 adds items 16, 17, 18
```

From pass_collect.py (223 lines):
```python
def _build_pass_collect_prompt(state: OrchestratorState) -> str:
    # Currently builds: base_prompt + instagram_rule (niche-aware) + closing
    # Phase 4 adds: phase4_collection_rules block
    # The closing stays "НЕ генерируй HTML" — unchanged
```

From pass_gap_analyze.py (301 lines):
```python
_CHECKLIST_PROMPT_TEMPLATE = """Ты только что собрал данные...
Сравни собранное с ПОЛНЫМ 15-item QC checklist:  # → 18-item
{checklist_render}
{niche_instruction}
...
Для КАЖДОГО из 15 пунктов оцени:  # → 18 пунктов
...
"""

def _ensure_summary(report: dict) -> None:
    # Line 286: report["summary"].setdefault("total", len(items) if items else 15)
    # → change 15 to 18

def _fallback_report(reason: str, raw: str) -> dict:
    # Line 296: "total": 15  → change to 18
    # Line 295: "missing": 15  → change to 18
```

From coverage_reporter.py (196 lines):
```python
@dataclass
class CoverageReport:
    total_items: int = 15  # → change default to 18

def calc_coverage(gap_report: dict) -> CoverageReport:
    total = len(QC_CHECKLIST)  # 15 → 18 (reads dynamically — no code change needed)
```
</interfaces>
</context>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Expand QC_CHECKLIST from 15 to 18 items + update VERSION (D-25)</name>
  <files>AIM/hermes/app/orchestrator/qc_checklist.py</files>

  <read_first>
    - AIM/hermes/app/orchestrator/qc_checklist.py (FULL FILE — 303 lines. Read all 15 existing items, the helpers at the bottom, and the VERSION constant)
  </read_first>

  <action>
    Open `AIM/hermes/app/orchestrator/qc_checklist.py`.

    **Step A: Bump VERSION (line 36):**
    Change `VERSION = "1.1.0"` to `VERSION = "1.2.0"`.

    Update the comment block above VERSION (lines 33-35) to document Phase 4 additions:
    ```python
    # 1.1.0 (Phase 3 / Plan 03-03): item 5 conditional_on_niche flag + 3 helpers.
    # 1.2.0 (Phase 4 / Plan 04-04): items 16-18 added (clinic_metrics, ratings,
    #   expert_regalia). Item 8 refined (concrete URLs from 5 target СМИ).
    #   Item 11 refined (3-year trend with YoY %). PASS_MIN_ITEMS updated.
    ```

    **Step B: Update PASS_MIN_ITEMS (line 42):**
    Change `PASS_MIN_ITEMS: int = 12` to `PASS_MIN_ITEMS: int = 15`.
    Rationale: 80% of 18 = 14.4 → round up to 15.

    **Step C: Refine existing items 8 and 11 to match Phase 4 depth:**

    Item 8 (around line 121-128) — refine pass_criteria:
    ```python
    {
        "id": 8,
        "category": "media",
        "name": "SMI mentions with concrete URLs",
        "pass_criteria": (
            ">=3 mentions with concrete URLs from target СМИ "
            "(Forbes, RBC, Vademecum, Kommersant, ТАСС) — via run_media_urls tool"
        ),
        "source": "Plan 01 — Media shallow + Phase 4 / DAT-02",
    },
    ```

    Item 11 (around line 144-151) — refine pass_criteria:
    ```python
    {
        "id": 11,
        "category": "financials",
        "name": "Revenue dynamics 3 years with YoY %",
        "pass_criteria": (
            "3-year revenue trend with YoY % AND total growth % — "
            "from find_company_financials revenue_dynamics block. "
            "If <3 years available: item must be marked 'missing' with "
            "reason 'недостаточно данных для динамики' (D-13 strict rule)"
        ),
        "source": "Plan 01 — only current year + Phase 4 / DAT-01 / D-13",
    },
    ```

    **Step D: Add 3 new items (16, 17, 18) AFTER item 15:**

    ```python
    {
        "id": 16,
        "category": "financials",
        "name": "Clinic metrics (DAT-04)",
        "pass_criteria": (
            "Clinic metrics block present: revenue, profit, employees (if available), "
            "licenses (from prescan), ОКВЭД codes (LLM translates to human language in Pass 3) — "
            "from find_company_financials clinic_metrics block"
        ),
        "source": "Phase 4 / DAT-04 / D-21",
    },
    {
        "id": 17,
        "category": "reputation",
        "name": "Ratings on 2 platforms (DAT-05)",
        "pass_criteria": (
            "Ratings present for at least 2 platforms: "
            "ПроДокторов + Яндекс.Карты — via run_review_platforms. "
            "Each platform: rating + review count"
        ),
        "source": "Phase 4 / DAT-05 / D-22-23",
    },
    {
        "id": 18,
        "category": "experts",
        "name": "Expert регалии from site scrape (SEC-04)",
        "pass_criteria": (
            ">=3 doctors with structured_regalia: degree (КМН/ДМН), "
            "academic_title (профессор/доцент), experience_years, education — "
            "from find_doctor_handles structured_regalia field"
        ),
        "source": "Phase 4 / SEC-04 / D-08",
    },
    ```

    **Step E: Verify helper functions still work with 18 items.**

    The existing `is_item_applicable(item_id, niche)` function (line 249) only checks item 5 for niche-conditional logic. Items 16-18 are universally applicable — no changes needed.

    The existing `applicable_items(niche)` function (line 285) filters by `is_item_applicable` — works with 18 items automatically.

    Do NOT change `is_niche_instagram_critical`, `is_item_applicable`, `applicable_items`, or `render_checklist_for_llm` — they work dynamically with the list length.

    Do NOT change CRITICAL_NICHES — Phase 4 doesn't add new niche-conditional items.
  </action>

  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes && python3 -c "
from app.orchestrator.qc_checklist import (
    VERSION, QC_CHECKLIST, PASS_THRESHOLD, PASS_MIN_ITEMS,
    is_item_applicable, applicable_items, is_niche_instagram_critical,
    render_checklist_for_llm, get_item_by_id,
)

assert VERSION == '1.2.0', f'Wrong VERSION: {VERSION}'
assert len(QC_CHECKLIST) == 18, f'Expected 18 items, got {len(QC_CHECKLIST)}'
assert PASS_MIN_ITEMS == 15, f'Wrong PASS_MIN_ITEMS: {PASS_MIN_ITEMS}'

# New items present
item16 = get_item_by_id(16)
item17 = get_item_by_id(17)
item18 = get_item_by_id(18)
assert item16 is not None and 'Clinic metrics' in item16['name']
assert item17 is not None and 'Ratings' in item17['name']
assert item18 is not None and 'регали' in item18['name'].lower()

# Existing items still present
assert get_item_by_id(1) is not None
assert get_item_by_id(15) is not None

# Niche helpers still work with 18 items
assert len(applicable_items('plastic_surgery')) == 18  # all items for critical niche
assert len(applicable_items('dental')) == 17  # 18 - 1 (item 5 filtered)
assert is_item_applicable(16, 'dental') == True  # clinic metrics applies to all
assert is_item_applicable(18, 'dental') == True  # regalia applies to all

# render_checklist_for_llm includes all 18 items
rendered = render_checklist_for_llm()
assert '16.' in rendered and '17.' in rendered and '18.' in rendered

print('OK: QC_CHECKLIST expanded to 18 items, VERSION 1.2.0')
"</automated>
  </verify>

  <done>
    QC_CHECKLIST has 18 items (15 existing + 3 new). VERSION is 1.2.0. PASS_MIN_ITEMS is 15. Items 8 and 11 refined. All niche helpers work with 18 items.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend Pass 1 prompt with Phase 4 collection rules + update Pass 2 + coverage_reporter</name>
  <files>AIM/hermes/app/orchestrator/pass_collect.py, AIM/hermes/app/orchestrator/pass_gap_analyze.py, AIM/hermes/app/orchestrator/coverage_reporter.py</files>

  <read_first>
    - AIM/hermes/app/orchestrator/pass_collect.py (FULL FILE — 223 lines. Focus on _build_pass_collect_prompt, lines 116-223)
    - AIM/hermes/app/orchestrator/pass_gap_analyze.py (FULL FILE — 301 lines. Focus on _CHECKLIST_PROMPT_TEMPLATE lines 73-102, _ensure_summary line 286, _fallback_report line 296)
    - AIM/hermes/app/orchestrator/coverage_reporter.py (FULL FILE — 196 lines. Focus on CoverageReport.total_items line 65)
  </read_first>

  <action>
    **Step A: Extend _build_pass_collect_prompt in pass_collect.py (lines 116-223).**

    Add a new `phase4_rules` string block AFTER the existing `instagram_rule` variable (around line 219) and BEFORE the `closing` variable (line 221).

    The `phase4_rules` block must contain:

    ```python
    phase4_rules = (
        "ПРАВИЛА СБОРА ДАННЫХ (Phase 4):\n"
        "1. run_media_urls — вызови ОБЯЗАТЕЛЬНО для 5 целевых СМИ: "
        "Forbes, RBC, Vademecum, Kommersant, ТАСС. Это для секции Media (05). "
        "Если 0 упоминаний — это легитимный результат (в Strategy (09) "
        "рекомендуешь PR-активность).\n"
        "2. run_forum_pains — вызови для сбора страхов пациентов с "
        "ПроДокторов, Otzovik, IRecommend, Woman.ru. Это для секции "
        "Content Analysis (04). Если 0 страхов — honestly отметь.\n"
        "3. find_company_financials — вызови с INN клиента (если есть). "
        "В ответе смотри поля revenue_dynamics (3-летняя динамика) и "
        "clinic_metrics (метрики клиники). Если dynamics_available=False — "
        "значит данных <3 лет, НЕ показывай секцию динамики (D-13).\n"
        "4. run_review_platforms — вызови для рейтингов: ПроДокторов + "
        "Яндекс.Карты минимум (DAT-05). Остальные платформы опциональны.\n"
        "5. find_doctor_handles — ответ содержит structured_regalia "
        "(degree, academic_title, experience_years, education). Это для "
        "секции Experts (03) + Content Analysis (04).\n\n"
    )
    ```

    Then modify the `return` statement (line 223) to include `phase4_rules`:

    ```python
    return base_prompt + instagram_rule + phase4_rules + closing
    ```

    Do NOT change `base_prompt`, `instagram_rule`, or `closing`. Only ADD the `phase4_rules` block and include it in the return.

    **Step B: Update pass_gap_analyze.py for 18-item checklist.**

    In `_CHECKLIST_PROMPT_TEMPLATE` (lines 73-102):
    - Line 75: Change `"ПОЛНЫМ 15-item QC checklist покрытия presale:"` to `"ПОЛНЫМ 18-item QC checklist покрытия presale:"`
    - Line 81: Change `"Для КАЖДОГО из 15 пунктов оцени:"` to `"Для КАЖДОГО из 18 пунктов оцени:"`
    - Line 97: Change the JSON summary total from 15 to 18:
      `"total": 15` → `"total": 18`

    In `_ensure_summary` function (around line 286):
    - Change `report["summary"].setdefault("total", len(items) if items else 15)` to `... else 18`

    In `_fallback_report` function (around lines 293-301):
    - Change `"missing": 15` to `"missing": 18`
    - Change `"total": 15` to `"total": 18`

    Add a note in the module docstring about Phase 4 expansion:
    ```python
    # Phase 4 / Plan 04-04: Checklist expanded from 15 to 18 items.
    # Items 16 (clinic_metrics), 17 (ratings), 18 (expert_regalia) added.
    # Template references 18 items. Fallback report defaults to 18 total.
    ```

    **Step C: Update coverage_reporter.py for 18-item default.**

    In `CoverageReport` dataclass (line 65):
    - Change `total_items: int = 15` to `total_items: int = 18`
    - Update the docstring comment on line 47 from `"Always 15"` to `"Always 18 (len of QC_CHECKLIST after Phase 4 expansion)"`

    The `calc_coverage` function (line 85) already reads `total = len(QC_CHECKLIST)` dynamically — no code change needed (it will be 18 after Task 1).

    The `format_coverage_text` function reads `report.total_items` dynamically — no change needed.
  </action>

  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes && python3 -c "
# Verify qc_checklist VERSION is 1.2.0 with 18 items
from app.orchestrator.qc_checklist import VERSION, QC_CHECKLIST
assert VERSION == '1.2.0'
assert len(QC_CHECKLIST) == 18

# Verify pass_collect prompt includes Phase 4 rules
from app.orchestrator.pass_collect import _build_pass_collect_prompt
from app.orchestrator.states import OrchestratorState
state = OrchestratorState(session_id='test', client_url='https://test.ru', client_name='Test')
prompt = _build_pass_collect_prompt(state)
assert 'run_media_urls' in prompt, 'run_media_urls missing from Pass 1 prompt'
assert 'run_forum_pains' in prompt, 'run_forum_pains missing from Pass 1 prompt'
assert 'find_company_financials' in prompt
assert 'run_review_platforms' in prompt
assert 'structured_regalia' in prompt
assert 'Phase 4' in prompt or 'phase4' in prompt.lower()

# Verify pass_gap_analyze template references 18 items
from app.orchestrator.pass_gap_analyze import _CHECKLIST_PROMPT_TEMPLATE, _ensure_summary, _fallback_report
assert '18-item' in _CHECKLIST_PROMPT_TEMPLATE or '18 пунктов' in _CHECKLIST_PROMPT_TEMPLATE, 'Template still says 15'
assert '18' in _CHECKLIST_PROMPT_TEMPLATE

# Verify fallback report defaults to 18
fr = _fallback_report('test', '')
assert fr['summary']['total'] == 18, f\"Fallback total should be 18, got {fr['summary']['total']}\"
assert fr['summary']['missing'] == 18

# Verify coverage_reporter default
from app.orchestrator.coverage_reporter import CoverageReport, calc_coverage
r = CoverageReport()
assert r.total_items == 18, f'Default total_items should be 18, got {r.total_items}'

# Verify calc_coverage works with 18-item gap_report
gap = {
    'items': [{'id': i, 'status': 'filled', 'detail': 'test'} for i in range(1, 19)],
    'summary': {'filled': 18, 'missing': 0, 'total': 18},
}
report = calc_coverage(gap)
assert report.total_items == 18
assert len(report.filled_items) == 18
assert report.status == 'PASS'  # 18/18 = 100%

print('OK: Pass 1 prompt + Pass 2 template + coverage_reporter all updated for 18 items')
"</automated>
  </verify>

  <done>
    Pass 1 prompt includes Phase 4 collection rules (run_media_urls, run_forum_pains, find_company_financials multi-year, run_review_platforms, structured_regalia). Pass 2 template references 18 items. coverage_reporter defaults to 18. Fallback report defaults to 18 total. All syntax valid.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orchestrator → LLM (Pass 1) | New collection rules instruct LLM to call Phase 4 tools — prompt-level only, no new trust boundary |
| Orchestrator → LLM (Pass 2) | Expanded checklist — LLM evaluates 18 items instead of 15 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-T | Tampering | LLM could mark items 16-18 as filled without calling tools | accept | Existing soft QC gate pattern (Phase 2/3) — LLM is trusted to self-evaluate honestly; hard-FAIL override only exists for item 5 (Phase 3 niche-critical) |
| T-04-04-D | DoS | More items → longer Pass 2 LLM call | accept | _PASS_GAP_TIMEOUT already 240s (up from 180s in 02-02); 18 items vs 15 is ~20% more work — within timeout |
| T-04-04-E | EoP | Not applicable | accept | Pure prompt expansion — no privilege change |
| T-04-04-SC | Supply chain | No new packages | accept | Pure Python string changes in existing modules |
</threat_model>

<verification>
**Post-plan automated checks:**

1. All 4 files pass syntax check (ast.parse)
2. QC_CHECKLIST has exactly 18 items, VERSION 1.2.0
3. Pass 1 prompt contains: run_media_urls, run_forum_pains, find_company_financials, run_review_platforms, structured_regalia
4. Pass 2 template references 18 items (not 15)
5. _fallback_report defaults to 18 total
6. CoverageReport default total_items = 18
7. calc_coverage handles 18-item gap_report correctly
8. Niche helpers (applicable_items, is_item_applicable) work with 18 items
9. Items 16-18 are universally applicable (not niche-conditional)

**Regression checks:**
- Item 5 (Instagram) still conditional_on_niche=True
- CRITICAL_NICHES unchanged ("plastic_surgery", "cosmetology")
- _apply_niche_conditional_coverage in three_pass.py still works (only checks item 5, not items 16-18)
- Existing Phase 3 prompts and logic unchanged
</verification>

<success_criteria>
- D-25 SATISFIED: QC checklist expanded to 18 items with 3 new Phase 4 items (clinic_metrics, ratings, expert_regalia)
- Pass 1 prompt instructs LLM to collect all Phase 4 data types (forums, media URLs, 3-year revenue, ratings, regalia)
- Pass 2 prompt evaluates 18 items (was 15)
- coverage_reporter handles 18-item checklist correctly
- VERSION bumped to 1.2.0
- All existing Phase 2/3 logic preserved (niche helpers, Instagram hard-FAIL, soft QC gate)
</success_criteria>

<output>
Create `.planning/phases/04-new-sections-data-depth/04-04-SUMMARY.md` when done. Record:
- New items 16, 17, 18 (names, pass_criteria, source)
- VERSION: 1.1.0 → 1.2.0
- PASS_MIN_ITEMS: 12 → 15
- Pass 1 prompt additions (phase4_rules block)
- Pass 2 template changes (15 → 18 references)
- coverage_reporter default change (15 → 18)
- Test results (all automated checks pass)
</output>
