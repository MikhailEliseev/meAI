---
phase: 4
plan: 04-02
type: execute
name: extend-find-doctor-handles-regalia-structured
wave: 1
depends_on: []
requirements: [SEC-04]
tags: [experts, regalia, doctor-handles, site-scrape]

files_modified:
  - AIM/hermes/app/tools/find_doctor_handles.py

autonomous: true

must_haves:
  truths:
    - "SEC-04/D-08: _scrape_doctor_profile returns a structured_regalia dict with typed fields: degree (КМН/ДМН/профессор/доцент — None if not found), academic_title (профессор/доцент/академик — None if not found), experience_years (int parsed from 'стаж N лет' patterns — None if not found), education (list of institution strings parsed from 'окончил' patterns — empty list if not found)"
    - "SEC-04/D-09: _merge_doctor_data(site_doctors, instagram_data) helper exists and returns a merged list where each doctor has both regalia fields (from site scrape) AND Instagram metrics (from run_instagram_content batch response) when ФИО matches"
    - "Existing regalia keyword-list extraction is PRESERVED (backward compat) — structured_regalia is ADDED alongside the existing regalia list field"
    - "Existing _scrape_doctor_profile output fields (title, bio, specializations, regalia, social_links) are unchanged — structured_regalia is a new key"
  artifacts:
    - path: "AIM/hermes/app/tools/find_doctor_handles.py"
      provides: "_extract_structured_regalia() + _merge_doctor_data() helpers"
      contains: "structured_regalia"
  key_links:
    - from: "find_doctor_handles._scrape_doctor_profile"
      to: "_extract_structured_regalia(text)"
      via: "helper call during profile scraping"
      pattern: "_extract_structured_regalia"
    - from: "Pass 3 LLM (via generate_html_report)"
      to: "_merge_doctor_data output (merged doctor cohort)"
      via: "LLM reads merged data from collected_data in conversation history"
      pattern: "merged_doctors"
---

<objective>
Extend `find_doctor_handles` to produce two new structured outputs:
1. **structured_regalia** per doctor — typed fields (degree, academic_title, experience_years, education) from site scraping (SEC-04, D-08)
2. **_merge_doctor_data()** helper — merges site-scraped регалии with Instagram metrics by ФИО (SEC-04, D-09)

Purpose: Phase 3 gave per-doctor Instagram metrics. Phase 4 adds per-doctor регалии from the clinic site. The reference report section 03 shows both: "ФИО, регалии (КМН, профессор), подписчики, avg лайки/просмотры, стиль контента". The merge helper combines both data sources for the HTML reporter and Pass 3 LLM.

Output: Modified `find_doctor_handles.py` with two new helper functions and a `structured_regalia` field per doctor in the JSON output.
</objective>

## Phase Goal (MVP)

**As a** Hermes presale orchestrator,
**I want to** get structured regalia (degree, experience, education) per doctor from the clinic site AND a merged view of site-data + Instagram-metrics,
**so that** the HTML Experts section can show "Иванов И.И. — КМН, стаж 15 лет, 50K подписчиков, avg 2K лайков" matching reference section 03.

---

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-new-sections-data-depth/04-CONTEXT.md
@.planning/phases/03-instagram-integration/03-VERIFICATION.md

@AIM/hermes/app/tools/find_doctor_handles.py

<interfaces>
<!-- Current extraction structure — from file read -->

From AIM/hermes/app/tools/find_doctor_handles.py (1205 lines):

**Existing regalia extraction (lines 393-407):**
```python
# ── Extract regalia ───────────────────────────────────────
regalia = []
for kw in _REGALIA_KEYWORDS:
    if kw.lower() in text_lower:
        regalia.append(kw)

return {
    "title": title,
    "bio": bio,
    "specializations": specializations[:5],
    "regalia": regalia[:5],        # ← keyword list (str stays)
    "social_links": social_links,
}
```

**Existing _REGALIA_KEYWORDS (line 310):**
```python
_REGALIA_KEYWORDS = [
    "профессор", "доцент", "академик", "член-корр", "член корр",
    "доктор медицинских наук", "д.м.н", "д.м.н.", "к.м.н", "к.м.н.",
    "кандидат медицинских наук", "заведующи", "зав.", "руководитель",
    "главный врач", "главврач", ...
]
```

**Existing handler output shape (handle_find_doctor_handles, line 837):**
Returns JSON with `doctors` array. Each doctor has:
- `name`, `full_name`, `profile_url`, `specializations`, `regalia` (keyword list), `social_links`, `instagram_handle`, `confidence_score`

**Instagram batch response shape (from Phase 3 run_instagram_content.py):**
```python
{
    "analyzed_count": int,
    "top_by_followers": [{"handle": str, "full_name": str, "followers_count": int, "avg_likes": int, "avg_views": int, "content_style": str, "content_themes": [...], "content_gaps": [...]}],
    "profiles": [...],
}
```
The `top_by_followers` array has `full_name` which the merge helper uses to match with site-scraped doctor names.
</interfaces>
</context>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _extract_structured_regalia() helper with typed regalia fields (D-08)</name>
  <files>AIM/hermes/app/tools/find_doctor_handles.py</files>

  <behavior>
    - _extract_structured_regalia("") → {"degree": None, "academic_title": None, "experience_years": None, "education": []}
    - _extract_structured_regalia("Иванов И.И., кандидат медицинских наук, стаж 15 лет") → {"degree": "КМН", "academic_title": None, "experience_years": 15, "education": []}
    - _extract_structured_regalia("Петров П.П., доктор медицинских наук, профессор, окончил МГМУ им. Сеченова") → {"degree": "ДМН", "academic_title": "профессор", "experience_years": None, "education": ["МГМУ им. Сеченова"]}
    - _extract_structured_regalia("Сидорова С.С., стаж работы 20 лет, окончила РНИМУ им. Пирогова") → {"degree": None, "academic_title": None, "experience_years": 20, "education": ["РНИМУ им. Пирогова"]}
  </behavior>

  <action>
    Read `AIM/hermes/app/tools/find_doctor_handles.py`. Add a new module-level helper function `_extract_structured_regalia(text: str) -> dict` immediately AFTER the `_REGALIA_KEYWORDS` list definition (around line 318, before `_SPEC_KEYWORDS`).

    The helper extracts TYPED regalia fields from the doctor profile page text (the cleaned text from `_scrape_doctor_profile` line 384: `text = re.sub(r'\s+', ' ', text).strip()`).

    **Fields to extract (per D-08):**

    1. **degree** (str or None): Academic degree. Match patterns (case-insensitive, search the full text):
       - `"доктор медицинских наук"` or `"д.м.н"` or `"д.м.н."` → return `"ДМН"`
       - `"кандидат медицинских наук"` or `"к.м.н"` or `"к.м.н."` → return `"КМН"`
       - If neither found → `None`

    2. **academic_title** (str or None): Academic title. Match:
       - `"профессор"` → `"профессор"`
       - `"доцент"` → `"доцент"`
       - `"академик"` → `"академик"`
       - `"член-корр"` or `"член корр"` → `"член-корреспондент"`
       - If multiple match, return the highest-ranking one (профессор > доцент > академик > член-корр). If none → `None`.

    3. **experience_years** (int or None): Years of experience. Match regex patterns (in priority order):
       - `r"стаж\s+(?:работы\s+)?(\d+)\s*лет"` → group 1 as int
       - `r"(\d+)\s*лет\s+опыта"` → group 1 as int
       - `r"опыт\s+работы\s+(\d+)\s*лет"` → group 1 as int
       - If none match → `None`

    4. **education** (list of str): Educational institutions. Match:
       - `r"окончил[а]?\s+([^.]{5,100})"` → group 1 (institution name, stripped). May match multiple times → collect all.
       - `r"образование[:\s]+([^.]{5,100})"` → group 1
       - Filter out false positives: skip if the captured text contains common non-institution words ("работу", "врачом", "стаж"). Use a denylist of 5-10 common false-positive substrings.
       - Deduplicate. Max 3 entries. Each entry truncated to 100 chars.
       - If none found → `[]`

    Return dict:
    ```python
    {
        "degree": "КМН" | "ДМН" | None,
        "academic_title": "профессор" | "доцент" | ... | None,
        "experience_years": int | None,
        "education": [str, ...],  # max 3
    }
    ```

    Write unit tests in a companion test file `AIM/hermes/app/tools/test_regalia_extraction.py` covering all 4 behavior cases listed in `<behavior>` plus edge cases (empty text, text without regalia, text with multiple degrees).

    Do NOT modify `_scrape_doctor_profile` yet — that is Task 2.
  </action>

  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes && python3 -c "
from app.tools.find_doctor_handles import _extract_structured_regalia

# Empty
r = _extract_structured_regalia('')
assert r['degree'] is None
assert r['experience_years'] is None
assert r['education'] == []

# КМН + стаж
r = _extract_structured_regalia('Иванов И.И., кандидат медицинских наук, стаж 15 лет')
assert r['degree'] == 'КМН', f'Expected КМН, got {r[\"degree\"]}'
assert r['experience_years'] == 15

# ДМН + профессор + образование
r = _extract_structured_regalia('Петров П.П., доктор медицинских наук, профессор, окончил МГМУ им. Сеченова')
assert r['degree'] == 'ДМН'
assert r['academic_title'] == 'профессор'
assert 'МГМУ' in ' '.join(r['education'])

# Стаж + образование (no degree)
r = _extract_structured_regalia('Сидорова С.С., стаж работы 20 лет, окончила РНИМУ им. Пирогова')
assert r['degree'] is None
assert r['experience_years'] == 20
assert len(r['education']) > 0

# Title priority: профессор + доцент → профессор wins
r = _extract_structured_regalia('доцент и профессор кафедры')
assert r['academic_title'] == 'профессор', f'Expected профессор (higher rank), got {r[\"academic_title\"]}'

print('OK: _extract_structured_regalia passes all tests')
"</automated>
  </verify>

  <done>
    `_extract_structured_regalia()` extracts typed regalia fields (degree, academic_title, experience_years, education) from doctor profile text. All behavior cases pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire structured_regalia into _scrape_doctor_profile + add _merge_doctor_data helper (D-08, D-09)</name>
  <files>AIM/hermes/app/tools/find_doctor_handles.py</files>

  <read_first>
    - AIM/hermes/app/tools/find_doctor_handles.py lines 331-408 (full `_scrape_doctor_profile` function — understand the return dict shape and where to add structured_regalia)
    - AIM/hermes/app/tools/find_doctor_handles.py lines 837-1000 (handler output shape — understand where `doctors` array is built)
    - The `_extract_structured_regalia()` function from Task 1 (confirm signature)
  </read_first>

  <action>
    **Step A: Wire structured_regalia into _scrape_doctor_profile.**

    Modify `_scrape_doctor_profile()` (line 331). After the existing `regalia = []` keyword-list extraction block (lines 393-397) and BEFORE the `return {` statement (line 402), add:

    ```python
    # ── Extract structured regalia (Phase 4 / SEC-04 / D-08) ───
    structured_regalia = _extract_structured_regalia(text)
    ```

    Then in the `return {...}` dict (line 402-408), add a new key AFTER `"regalia"`:
    ```python
    "structured_regalia": structured_regalia,
    ```

    Do NOT remove or rename the existing `"regalia"` keyword list — it stays for backward compatibility (existing code that reads `regalia` still works).

    **Step B: Propagate structured_regalia through the handler output.**

    In `handle_find_doctor_handles()` (line 837), find where the `doctors` array is built (search for where doctor dicts are assembled with `name`, `profile_url`, `regalia` keys). For each doctor dict in the output, add `"structured_regalia": doctor.get("structured_regalia", {})` — pulling from the scraped profile data.

    If the handler uses Perplexity enrichment (Step 2, line 415+) instead of site scraping for some doctors, set `structured_regalia` to `{"degree": None, "academic_title": None, "experience_years": None, "education": []}` (default empty) — Perplexity enrichment returns different data shape; structured regalia comes from site scrape only.

    **Step C: Add _merge_doctor_data() helper.**

    Add a new module-level function `_merge_doctor_data(site_doctors: list[dict], instagram_data: dict) -> list[dict]` immediately AFTER `_extract_structured_regalia()`.

    The helper merges site-scraped doctors (with регалии) and Instagram-analyzed doctors (with metrics) by normalized ФИО.

    Logic:

    1. Normalize ФИО: lowercase, strip, collapse spaces, remove dots/hyphens. Example: `"Иванов И.И."` → `"иванов и и"`.

    2. Build a lookup dict from `instagram_data`: for each profile in `instagram_data.get("top_by_followers", [])` + `instagram_data.get("profiles", [])`, key = normalized `full_name`, value = the full profile dict (followers_count, avg_likes, avg_views, content_style, content_themes, content_gaps).

    3. For each `site_doctor` in `site_doctors`:
       - Look up Instagram profile by normalized ФИО.
       - If found: merge — add `instagram_metrics` field with the matched profile data to the doctor dict.
       - If NOT found: add `instagram_metrics: None` (doctor has регалии but no Instagram data — still valid for section 03).

    4. Also add Instagram-active doctors NOT on the site list (from `top_by_followers` not matched to any site doctor) as separate entries with `structured_regalia: {}` empty and `source: "instagram_only"`.

    5. Return the merged list. Each doctor dict has at minimum:
       ```python
       {
           "name": str,
           "structured_regalia": dict,  # from site or empty
           "instagram_metrics": dict | None,  # from Instagram or None
           "source": "site" | "instagram_only" | "both",
       }
       ```

    Per D-09: "LLM ответственный за resolution неоднозначностей" — the merge helper does NOT call LLM. It uses deterministic normalized-ФИО matching. If two doctors have similar names (e.g., "Иванов И." and "Иванов Иван Иванович"), both appear in the merged list — the LLM resolves in Pass 3 by reading context.

    **Step D: Expose merge helper in handler output.**

    In `handle_find_doctor_handles()`, at the END of the function (after `doctors` array is fully built, before `return json.dumps(...)`), add:

    ```python
    # Phase 4 / SEC-04 / D-09: merge helper exposed for Pass 3 LLM
    # The merge itself happens in Pass 3 where instagram_data is available.
    # Here we just expose the function for import.
    ```

    Do NOT call `_merge_doctor_data` inside the handler — the Instagram data is not available at this point (it comes from a separate `run_instagram_content` call). The merge function is called by the orchestrator or the LLM in Pass 3.

    Add `_merge_doctor_data` to the module's `__all__` if one exists, or ensure it's importable (it's a module-level function — always importable).

    Do NOT modify the existing tool schema in `registry.register(...)`.
  </action>

  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes && python3 -c "
import ast

# Syntax check
with open('app/tools/find_doctor_handles.py') as f:
    tree = ast.parse(f.read())

source = open('app/tools/find_doctor_handles.py').read()

# Confirm helpers exist
assert 'def _extract_structured_regalia(' in source, '_extract_structured_regalia missing'
assert 'def _merge_doctor_data(' in source, '_merge_doctor_data missing'

# Confirm structured_regalia wired into _scrape_doctor_profile return
assert '\"structured_regalia\"' in source or \"'structured_regalia'\" in source, 'structured_regalia not in return dict'

# Test _merge_doctor_data
from app.tools.find_doctor_handles import _merge_doctor_data

site_doctors = [
    {'name': 'Иванов И.И.', 'structured_regalia': {'degree': 'КМН', 'experience_years': 15}},
    {'name': 'Петров П.П.', 'structured_regalia': {'degree': 'ДМН'}},
]

instagram_data = {
    'top_by_followers': [
        {'full_name': 'Иванов Иван Иванович', 'followers_count': 50000, 'avg_likes': 2000},
        {'full_name': 'Сидоров С.С.', 'followers_count': 30000, 'avg_likes': 1500},
    ],
    'profiles': [],
}

merged = _merge_doctor_data(site_doctors, instagram_data)
assert len(merged) >= 3, f'Expected at least 3 merged doctors, got {len(merged)}'

# Иванов should have both regalia and metrics (matched by normalized ФИО)
ivanov = [d for d in merged if 'иванов' in d['name'].lower()]
assert len(ivanov) >= 1
assert ivanov[0]['structured_regalia'].get('degree') == 'КМН'
assert ivanov[0].get('instagram_metrics') is not None, 'Иванов should have Instagram metrics'

# Сидоров should be instagram_only
sidorov = [d for d in merged if 'сидоров' in d['name'].lower()]
assert len(sidorov) >= 1
assert sidorov[0]['source'] == 'instagram_only', f'Сидоров should be instagram_only, got source={sidorov[0].get(\"source\")}'

# Петров should have regalia but no Instagram
petrov = [d for d in merged if 'петров' in d['name'].lower()]
assert len(petrov) >= 1
assert petrov[0]['structured_regalia'].get('degree') == 'ДМН'
assert petrov[0].get('instagram_metrics') is None

print('OK: _merge_doctor_data + structured_regalia wiring pass all tests')
"</automated>
  </verify>

  <done>
    `_scrape_doctor_profile` returns `structured_regalia` dict with typed fields per doctor. `_merge_doctor_data()` merges site-scraped doctors with Instagram-analyzed doctors by normalized ФИО. Both are importable from `find_doctor_handles` module. Existing `regalia` keyword list preserved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool → clinic website | Existing HTTP scrape of clinic staff pages — unchanged by this plan (structured_regalia extracted from the same text) |
| Tool → LLM consumer | Structured regalia gives LLM typed fields instead of keyword lists — no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-T | Tampering | Regex parsing could extract wrong experience_years | mitigate | Unit tests verify edge cases; regex patterns are specific ("стаж N лет", not bare numbers) |
| T-04-02-I | Info disclosure | Doctor bio text exposed to LLM | accept | Bio already exposed by existing tool — structured_regalia adds typed fields, not new data sources |
| T-04-02-D | DoS | Not applicable — no new external calls | accept | Same single HTTP call per doctor profile; parsing is local |
| T-04-02-E | EoP | _merge_doctor_data does name matching | accept | Pure deterministic matching — no privileged operation; LLM resolves ambiguities |
| T-04-02-SC | Supply chain | No new packages | accept | Pure Python stdlib (re, json); no pip installs |
</threat_model>

<verification>
**Post-plan automated checks:**

1. `python3 -c "import ast; ast.parse(open('AIM/hermes/app/tools/find_doctor_handles.py').read())"` exits 0
2. `_extract_structured_regalia()` handles: empty, degree-only, title-only, experience-only, education-only, all-fields, false-positive filtering
3. `_merge_doctor_data()` handles: matched doctors, unmatched site-only, unmatched Instagram-only, similar-name duplicates
4. `_scrape_doctor_profile` return dict includes both `regalia` (keyword list, existing) and `structured_regalia` (typed dict, new)
5. Handler output `doctors` array includes `structured_regalia` per doctor

**Regression checks:**
- Existing `regalia` keyword list field preserved in all outputs
- Tool input schema unchanged (url, company_name, city, specialization)
- Phase 3 callers that read `regalia` still work (backward compat)
</verification>

<success_criteria>
- SEC-04/D-08 SATISFIED (tool layer): `structured_regalia` provides typed regalia fields (degree, academic_title, experience_years, education) per doctor from site scraping
- SEC-04/D-09 SATISFIFIED (tool layer): `_merge_doctor_data()` helper merges site регалии with Instagram metrics by ФИО; LLM resolves ambiguities in Pass 3
- Existing tool output preserved — new fields are additive
- Unit tests cover all behavior cases including edge cases
</success_criteria>

<output>
Create `.planning/phases/04-new-sections-data-depth/04-02-SUMMARY.md` when done. Record:
- Functions added: `_extract_structured_regalia`, `_merge_doctor_data`
- New output field per doctor: `structured_regalia`
- Test results (all behavior cases pass)
- Regex patterns used for experience_years and education extraction
- Any deviations (e.g., if site text has unusual formatting that breaks regex)
</output>
