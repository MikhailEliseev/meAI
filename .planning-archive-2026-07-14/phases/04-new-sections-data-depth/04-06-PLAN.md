---
phase: 4
plan: 04-06
type: execute
name: html-data-sections-revenue-media-ratings-cards
wave: 3
depends_on: [04-04, 04-05]
requirements: [DAT-01, DAT-02, DAT-03, DAT-04, DAT-05]
tags: [html, report, data-sections, revenue, media, ratings, competitor-cards]

files_modified:
  - AIM/hermes/app/tools/generate_html_report.py

autonomous: true

must_haves:
  truths:
    - "DAT-01/D-14: _build_revenue_dynamics_section(data) renders a 3-year revenue table (year, amount, YoY %) + blockquote with summary_text when dynamics_available=True"
    - "DAT-01/D-13: When dynamics_available=False, renders honest 'Динамика выручки недоступна' block (NO partial data table)"
    - "DAT-02/D-17: _build_media_urls_section(data) renders a simple list of hyperlinks: 'Source — Title — Date → URL' (not card-grid with logos)"
    - "DAT-02/D-18: When 0 media mentions (pr_needed=True), renders honest 'В СМИ не упоминалась' block"
    - "DAT-03/D-20: _build_competitor_cards_section(data) renders detailed cards per competitor: name, year founded, revenue, # surgeons, Instagram handle, специфика"
    - "DAT-04/D-21: _build_clinic_metrics_block(data) renders clinic metrics in About section: revenue, profit, employees, licenses, ОКВЭД (humanized by LLM)"
    - "DAT-05/D-22: _build_ratings_section(data) renders ratings for ПроДокторов + Яндекс.Карты with rating + review count + themes"
    - "All new sections use existing AIM design-system CSS classes (glass cards, metric-tag badges, section-label) — consistent with Phase 3"
    - "_build_report_html function extended to call new section builders at the correct position in the report"
    - "All new sections gracefully handle missing data (empty dict, None values) — render honest 'данные недоступны' blocks"
  artifacts:
    - path: "AIM/hermes/app/tools/generate_html_report.py"
      provides: "5 new section builders: revenue_dynamics, media_urls, competitor_cards, clinic_metrics, ratings"
      contains: "_build_revenue_dynamics_section"
  key_links:
    - from: "generate_html_report._build_report_html"
      to: "_build_revenue_dynamics_section(data)"
      via: "function call inside section assembly"
      pattern: "_build_revenue_dynamics_section"
    - from: "generate_html_report._build_report_html"
      to: "_build_media_urls_section(data)"
      via: "function call inside section assembly"
      pattern: "_build_media_urls_section"
---

<objective>
Add 5 new HTML section renderers to `generate_html_report.py` for Phase 4's data-driven sections:

1. **_build_revenue_dynamics_section** — 3-year revenue table + blockquote (DAT-01, D-13..14)
2. **_build_media_urls_section** — simple hyperlink list of СМИ mentions (DAT-02, D-17..18)
3. **_build_competitor_cards_section** — detailed cards per competitor (DAT-03, D-20)
4. **_build_clinic_metrics_block** — clinic metrics in About section (DAT-04, D-21)
5. **_build_ratings_section** — ratings from ПроДокторов + Яндекс.Карты (DAT-05, D-22)

Each section builder reads from the `data` dict (session archive format) and renders design-system HTML using existing CSS classes (glass cards, metric-tag badges, section-label spans). All sections gracefully handle missing data with honest "данные недоступны" blocks per ORC-04.

Purpose: The reference report `ИПХиК (2).html` has 10 sections. Current `generate_html_report.py` (1680 lines, Phase 3) renders sections for prescan, competitors, Instagram, QC — but lacks revenue dynamics table, media URL list, competitor cards, clinic metrics block, and ratings. This plan adds these 5 data-driven renderers.

Output: Modified `generate_html_report.py` with 5 new section-builder functions + wiring into `_build_report_html`.
</objective>

## Phase Goal (MVP)

**As a** reader of the presale HTML report,
**I want to** see a 3-year revenue table, concrete media URLs, detailed competitor cards, clinic metrics, and platform ratings,
**so that** the report matches the reference depth — not just "Revenue: 4.3B ₽" but "+79% over 3 years (2.4B → 3.4B → 4.3B)".

---

<context>
@.planning/PROJECT.md
@.planning/phases/04-new-sections-data-depth/04-CONTEXT.md

@AIM/hermes/app/tools/generate_html_report.py
@AIM/wordpress-core/wp-content/themes/aim-theme/design-showcase-dual-theme.html

<interfaces>
<!-- Existing HTML report structure (1680 lines) -->

From AIM/hermes/app/tools/generate_html_report.py:

**Key existing functions:**
```python
def _esc(text: str) -> str:  # line 68 — XSS-safe text escaping
def _fmt_num(val, default="—"):  # line 72 — number formatting
def _fmt_revenue_short(val) -> str:  # line 80 — "4.3 млрд" format
def _build_competitor_table(details: list[dict], client_url: str = "") -> str:  # line 128
def _build_no_instagram_block(reason: str) -> str:  # line 196 — Phase 3 pattern
def _build_qc_coverage_section(metadata: dict) -> str:  # line 344
def _build_report_html(
    data: dict,
    title: str,
    coverage_metadata: dict | None = None,
    niche: str = "unknown",
    instagram_data: dict | None = None,
) -> str:  # line 762 — MAIN HTML builder
```

**_build_report_html data extraction (lines 795-814):**
```python
data = _normalize_pipeline_keys(data)
metadata = data.get("metadata", {}) or {}
prescan = data.get("prescan", {}) or {}
financials = data.get("financials", {}) or {}
reviews = data.get("review_platforms", {}) or {}
smi_mentions = data.get("smi_mentions", {}) or {}
# ... etc
```

**_build_report_html section assembly (line 829+):**
```python
sections = []
sections.append(f"""<section class="section">...</section>""")  # Hero
# ... Executive Summary, then data sections
# Phase 4 adds: revenue dynamics, media URLs, competitor cards, clinic metrics, ratings
```

**Design-system CSS classes (from design-showcase-dual-theme.html):**
- `.section` — main section wrapper
- `.section-label` — span with uppercase label (AIM Scout Report)
- `.glass-card` — backdrop-filter: blur(20px) glass card
- `.metric-tag-success` / `.metric-tag-warning` / `.metric-tag-danger` — colored badges
- `.text-dim` — dimmed text
- `blockquote` — styled blockquote with left border

**Data dict keys for Phase 4 (set by Pass 3 LLM via generate_html_report kwargs or collected_data):**
- `data["financials"]["revenue_dynamics"]` — from Plan 04-01 find_company_financials output
- `data["financials"]["clinic_metrics"]` — from Plan 04-01 find_company_financials output
- `data["media_urls"]` — from Plan 04-03 run_media_urls output
- `data["review_platforms"]` — existing key, restructured for DAT-05
- `data["competitors"]` — existing key, extended for DAT-03 cards

The LLM passes these as kwargs to generate_html_report or includes them in the data dict. The section builders read defensively with `.get()` and empty-default patterns.
</interfaces>
</context>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Add _build_revenue_dynamics_section + _build_clinic_metrics_block (DAT-01, DAT-04)</name>
  <files>AIM/hermes/app/tools/generate_html_report.py</files>

  <read_first>
    - AIM/hermes/app/tools/generate_html_report.py lines 68-130 (helpers: _esc, _fmt_num, _fmt_revenue_short, _build_competitor_table — understand the HTML generation pattern)
    - AIM/hermes/app/tools/generate_html_report.py lines 196-270 (_build_no_instagram_block — Phase 3 pattern for honest "данные недоступны" block — MODEL the new sections on this)
    - AIM/hermes/app/tools/generate_html_report.py lines 762-860 (_build_report_html — understand the function signature, data extraction, and section assembly)
  </read_first>

  <action>
    **Step A: Add _build_revenue_dynamics_section() function.**

    Add a new function AFTER `_build_competitor_table` (line 128) and BEFORE `_build_no_instagram_block` (line 196). Position: around line 195.

    Signature: `def _build_revenue_dynamics_section(financials: dict) -> str:`

    Logic:
    1. Extract `revenue_dynamics = financials.get("revenue_dynamics", {})`
    2. If `not revenue_dynamics` or `not revenue_dynamics.get("dynamics_available")`:
       Return honest block:
       ```html
       <section class="section">
         <span class="section-label">Динамика выручки</span>
         <div class="glass-card" style="padding: 1.5rem;">
           <p class="text-dim">Динамика выручки недоступна — {_esc(revenue_dynamics.get("reason", "недостаточно данных в открытых источниках"))}.</p>
         </div>
       </section>
       ```
       This enforces D-13 strict <3-year rule at the rendering layer.
    3. If `dynamics_available=True`:
       Build a table from `revenue_dynamics["years"]` (list of {year, revenue, yoy_pct}):
       ```html
       <section class="section">
         <span class="section-label">Динамика выручки</span>
         <h2>Выручка за 3 года</h2>
         <div class="glass-card" style="padding: 1.5rem;">
           <table class="data-table">
             <thead>
               <tr><th>Год</th><th>Выручка</th><th>Прирост YoY</th></tr>
             </thead>
             <tbody>
               <!-- for each year in years (descending) -->
               <tr>
                 <td>{year}</td>
                 <td>{_fmt_revenue_short(revenue)}</td>
                 <td>{yoy_pct formatted with + sign if positive, red/green color}</td>
               </tr>
             </tbody>
           </table>
           <blockquote>
             <p>{_esc(revenue_dynamics.get("summary_text", ""))}</p>
           </blockquote>
         </div>
       </section>
       ```
    4. YoY % rendering: if positive, show `+{pct}%` with `color: var(--success)`; if negative, show `{pct}%` with `color: var(--danger)`; if None (oldest year), show `—`.
    5. Use `_fmt_revenue_short()` for revenue values (existing helper — "4.3 млрд" format).

    The function MUST handle: empty financials dict, missing revenue_dynamics key, dynamics_available=False, dynamics_available=True with partial year data.

    **Step B: Add _build_clinic_metrics_block() function.**

    Add immediately AFTER `_build_revenue_dynamics_section()`.

    Signature: `def _build_clinic_metrics_block(financials: dict) -> str:`

    Logic:
    1. Extract `clinic_metrics = financials.get("clinic_metrics", {})`
    2. If empty: return empty string (block not rendered — About section still shows other data)
    3. Build a grid of metric tags:
       ```html
       <div class="clinic-metrics-grid">
         <!-- Revenue -->
         <div class="metric-item">
           <span class="metric-label">Выручка</span>
           <span class="metric-value">{_fmt_revenue_short(clinic_metrics.get("revenue_latest"))}</span>
         </div>
         <!-- Profit -->
         <div class="metric-item">
           <span class="metric-label">Прибыль</span>
           <span class="metric-value">{_fmt_revenue_short(clinic_metrics.get("profit_latest"))}</span>
         </div>
         <!-- Employees (if available) -->
         <!-- Status (Действующее/Ликвидировано) -->
         <div class="metric-item">
           <span class="metric-label">Статус</span>
           <span class="metric-value">{_esc(clinic_metrics.get("status", "—"))}</span>
         </div>
         <!-- ОКВЭД (humanized by LLM — read from clinic_metrics.okved_humanized if LLM set it, else raw code) -->
         <div class="metric-item">
           <span class="metric-label">Профиль (ОКВЭД)</span>
           <span class="metric-value">{_esc(clinic_metrics.get("okved_humanized") or clinic_metrics.get("okved_codes", [{}])[0].get("code", "—"))}</span>
         </div>
       </div>
       ```
    4. Handle None values gracefully with `_fmt_num(val, "—")` (existing helper).
    5. Return the HTML block (no `<section>` wrapper — this goes INSIDE the About section).

    **Step C: Wire into _build_report_html.**

    In `_build_report_html` (line 762), find the About/Executive Summary section assembly (around lines 840-860). After the existing executive summary metrics, add:

    ```python
    # Phase 4 / DAT-04: Clinic metrics block inside About section
    clinic_metrics_html = _build_clinic_metrics_block(financials)
    ```

    Then insert `clinic_metrics_html` into the About section HTML (after the existing metrics grid, before the closing `</section>`).

    After the About section, add the Revenue Dynamics section:
    ```python
    # Phase 4 / DAT-01: Revenue dynamics section
    sections.append(_build_revenue_dynamics_section(financials))
    ```

    The revenue dynamics section goes AFTER About but BEFORE Market section (matching reference report order: section 1 About → section 1b Revenue dynamics → section 2 Market).

    Do NOT modify existing sections or the function signature.
  </action>

  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes && python3 -c "
import ast
with open('app/tools/generate_html_report.py') as f:
    tree = ast.parse(f.read())

source = open('app/tools/generate_html_report.py').read()
assert 'def _build_revenue_dynamics_section(' in source, '_build_revenue_dynamics_section missing'
assert 'def _build_clinic_metrics_block(' in source, '_build_clinic_metrics_block missing'

from app.tools.generate_html_report import _build_revenue_dynamics_section, _build_clinic_metrics_block

# Test 1: dynamics_available=False (D-13 strict rule)
html = _build_revenue_dynamics_section({'revenue_dynamics': {'dynamics_available': False, 'reason': 'только 2 года данных'}})
assert 'недоступна' in html.lower() or 'недостаточно' in html.lower(), 'D-13 honest block missing'
assert '<table' not in html, 'D-13: should NOT render table when unavailable'

# Test 2: dynamics_available=True (D-14 table + blockquote)
html = _build_revenue_dynamics_section({
    'revenue_dynamics': {
        'dynamics_available': True,
        'years': [
            {'year': '2023', 'revenue': 4300000000, 'yoy_pct': 26.5},
            {'year': '2022', 'revenue': 3400000000, 'yoy_pct': 41.7},
            {'year': '2021', 'revenue': 2400000000, 'yoy_pct': None},
        ],
        'total_growth_pct': 79.2,
        'summary_text': 'Выручка выросла на 79.2% за 3 года',
    }
})
assert '<table' in html, 'Table missing when dynamics available'
assert '2023' in html and '2022' in html and '2021' in html
assert 'blockquote' in html, 'D-14 blockquote missing'
assert '79' in html, 'summary text missing'

# Test 3: empty financials
html = _build_revenue_dynamics_section({})
assert 'недоступна' in html.lower() or '<section' not in html or html == '', 'Empty financials not handled'

# Test 4: clinic metrics block
html = _build_clinic_metrics_block({
    'clinic_metrics': {
        'revenue_latest': 4300000000,
        'profit_latest': 500000000,
        'status': 'Действующее',
        'okved_codes': [{'code': '86.21', 'description': ''}],
    }
})
assert '4' in html and 'млрд' in html, 'Revenue not formatted'
assert 'Действующее' in html, 'Status missing'

print('OK: revenue_dynamics + clinic_metrics sections work correctly')
"</automated>
  </verify>

  <done>
    `_build_revenue_dynamics_section()` renders 3-year table with blockquote (D-14) and honest block when unavailable (D-13). `_build_clinic_metrics_block()` renders clinic metrics grid. Both wired into `_build_report_html`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add _build_media_urls_section + _build_ratings_section (DAT-02, DAT-05)</name>
  <files>AIM/hermes/app/tools/generate_html_report.py</files>

  <read_first>
    - The `_build_revenue_dynamics_section` and `_build_clinic_metrics_block` functions from Task 1 (confirm they exist and the pattern)
    - AIM/hermes/app/tools/generate_html_report.py lines 860-1000 (continue reading _build_report_html to find where sections are assembled — identify the position where Media and Ratings sections should go)
  </read_first>

  <action>
    **Step A: Add _build_media_urls_section() function.**

    Add AFTER `_build_clinic_metrics_block()` (from Task 1).

    Signature: `def _build_media_urls_section(data: dict) -> str:`

    Logic:
    1. Extract `media_urls = data.get("media_urls", {})` (from run_media_urls tool output, passed by LLM as kwarg or in data dict)
    2. If empty or `total_mentions == 0`:
       Per D-18, render honest block:
       ```html
       <section class="section">
         <span class="section-label">Media (DAT-02)</span>
         <h2>Упоминания в СМИ</h2>
         <div class="glass-card" style="padding: 1.5rem;">
           <p class="text-dim">В СМИ не упоминалась за последние 3 года.</p>
           <p class="text-dim"><em>Рекомендация: PR-активность для повышения узнаваемости (см. Strategy).</em></p>
         </div>
       </section>
       ```
    3. If mentions exist, render SIMPLE LIST (D-17 — not cards with logos):
       ```html
       <section class="section">
         <span class="section-label">Media (DAT-02)</span>
         <h2>Упоминания в СМИ</h2>
         <div class="glass-card" style="padding: 1.5rem;">
           <ul class="media-mentions-list">
             <!-- for each mention in media_urls["all_mentions"] -->
             <li>
               <span class="metric-tag metric-tag-info">{_esc(source)}</span>
               <a href="{_esc(url)}" target="_blank" rel="noopener">
                 <strong>{_esc(title)}</strong>
               </a>
               <span class="text-dim">{_esc(date)}</span>
             </li>
           </ul>
         </div>
       </section>
       ```
    4. Group mentions by source if there are many (optional: use `mentions_by_source` for grouping).
    5. XSS-safe: all user-provided text goes through `_esc()`. URLs go through `_esc()` in href attributes.

    **Step B: Add _build_ratings_section() function.**

    Add AFTER `_build_media_urls_section()`.

    Signature: `def _build_ratings_section(reviews: dict) -> str:`

    Logic:
    1. Extract rating data from `reviews` dict (the existing `review_platforms` key in data).
    2. Per D-22, only 2 platforms required: ПроДокторов + Яндекс.Карты.
    3. The review_platforms tool returns a text `analysis` field. The LLM (Pass 3) should extract structured ratings from this text and pass them in `reviews["ratings_extracted"]`:
       ```python
       ratings = reviews.get("ratings_extracted", [])
       # Each rating: {"platform": str, "rating": float, "review_count": int, "positive_themes": [str], "negative_themes": [str]}
       ```
    4. If no extracted ratings: return honest block "Рейтинги не собраны — run_review_platforms не вызван или не дал данных".
    5. Render a grid of rating cards:
       ```html
       <section class="section">
         <span class="section-label">Рейтинги и отзывы (DAT-05)</span>
         <h2>Репутация на платформах</h2>
         <div class="ratings-grid">
           <!-- for each rating in ratings -->
           <div class="glass-card rating-card">
             <h3>{_esc(platform)}</h3>
             <div class="rating-score">
               <span class="rating-stars">{'★' * int(rating)}{'☆' * (5-int(rating))}</span>
               <span class="rating-number">{rating}</span>
             </div>
             <p class="text-dim">{review_count} отзывов</p>
             <!-- Positive themes -->
             <!-- Negative themes -->
           </div>
         </div>
       </section>
       ```
    6. Use metric-tag-success for positive themes, metric-tag-warning for negative themes.

    **Step C: Wire both sections into _build_report_html.**

    Find the section assembly in `_build_report_html` (after line 860). Add the Media section and Ratings section at the correct positions:

    - Media section: AFTER Competitors/Market section, BEFORE Presence/SEO section (matches reference order: section 05 Media)
    - Ratings section: AFTER Media section or as part of Reputation section

    ```python
    # Phase 4 / DAT-02: Media URLs section
    sections.append(_build_media_urls_section(data))

    # Phase 4 / DAT-05: Ratings section
    sections.append(_build_ratings_section(reviews))
    ```

    Do NOT modify existing sections. The new sections are appended at appropriate positions.

    Do NOT change the `_build_report_html` function signature.
  </action>

  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes && python3 -c "
import ast
with open('app/tools/generate_html_report.py') as f:
    ast.parse(f.read())

source = open('app/tools/generate_html_report.py').read()
assert 'def _build_media_urls_section(' in source, '_build_media_urls_section missing'
assert 'def _build_ratings_section(' in source, '_build_ratings_section missing'

from app.tools.generate_html_report import _build_media_urls_section, _build_ratings_section

# Test media section with mentions (D-17 simple list)
html = _build_media_urls_section({
    'media_urls': {
        'total_mentions': 3,
        'all_mentions': [
            {'source': 'Forbes', 'title': 'Топ-10 клиник', 'url': 'https://forbes.ru/...', 'date': '12.03.24'},
            {'source': 'RBC', 'title': 'Медицина 2024', 'url': 'https://rbc.ru/...', 'date': '15.01.24'},
        ],
    }
})
assert '<a href' in html or 'href=' in html, 'D-17 hyperlinks missing'
assert 'Forbes' in html
assert 'RBC' in html

# Test media section with 0 mentions (D-18 honest block)
html = _build_media_urls_section({
    'media_urls': {'total_mentions': 0, 'pr_needed': True}
})
assert 'не упоминалась' in html.lower() or '0 упоминаний' in html, 'D-18 honest block missing'

# Test ratings section
html = _build_ratings_section({
    'ratings_extracted': [
        {'platform': 'ПроДокторов', 'rating': 4.5, 'review_count': 120, 'positive_themes': ['Профессионализм'], 'negative_themes': ['Очереди']},
        {'platform': 'Яндекс.Карты', 'rating': 4.2, 'review_count': 85, 'positive_themes': ['Цены'], 'negative_themes': []},
    ]
})
assert 'ПроДокторов' in html
assert 'Яндекс' in html or 'Я.Карты' in html
assert '4.5' in html or '★' in html

print('OK: media_urls + ratings sections work correctly')
"</automated>
  </verify>

  <done>
    `_build_media_urls_section()` renders simple hyperlink list (D-17) with honest pr_needed block (D-18). `_build_ratings_section()` renders 2-platform rating cards (D-22). Both wired into `_build_report_html`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add _build_competitor_cards_section (DAT-03, D-20)</name>
  <files>AIM/hermes/app/tools/generate_html_report.py</files>

  <read_first>
    - AIM/hermes/app/tools/generate_html_report.py lines 128-195 (_build_competitor_table — understand existing competitor rendering pattern)
    - The new functions from Tasks 1 and 2 (confirm pattern consistency)
  </read_first>

  <action>
    Add a new function `_build_competitor_cards_section(competitors: dict) -> str` AFTER `_build_ratings_section()` (from Task 2).

    Logic:
    1. Extract `competitor_cards = competitors.get("competitor_cards", [])` — a list of card dicts. Each card has:
       ```python
       {
           "name": str,
           "year_founded": int | None,
           "revenue_latest": int | None,
           "revenue_trend": str,  # "растущая" / "стабильная" / "падающая"
           "surgeons_count": int | None,
           "instagram_handle": str | None,
           "instagram_followers": int | None,
           "specialization": str,  # LLM-generated from site scrape
           "okved_main": str | None,
       }
       ```
    2. If no competitor cards: return honest block "Карточки конкурентов не собраны".
    3. Render a grid of glass-card competitor cards (D-20):
       ```html
       <section class="section">
         <span class="section-label">Конкуренты (DAT-03)</span>
         <h2>Детальные карточки конкурентов</h2>
         <div class="competitor-cards-grid">
           <!-- for each card in competitor_cards -->
           <div class="glass-card competitor-card">
             <h3>{_esc(name)}</h3>
             <div class="card-metrics">
               <div class="metric-item">
                 <span class="metric-label">Год основания</span>
                 <span class="metric-value">{year_founded or "—"}</span>
               </div>
               <div class="metric-item">
                 <span class="metric-label">Выручка</span>
                 <span class="metric-value">{_fmt_revenue_short(revenue_latest)}</span>
                 <span class="metric-tag {trend_class}">{_esc(revenue_trend)}</span>
               </div>
               <div class="metric-item">
                 <span class="metric-label">Хирургов/Косметологов</span>
                 <span class="metric-value">{surgeons_count or "—"}</span>
               </div>
               <!-- Instagram (if available) -->
               {instagram_block if instagram_handle else ""}
             </div>
             <div class="card-specialization">
               <p class="text-dim"><strong>Специфика:</strong> {_esc(specialization)}</p>
             </div>
           </div>
         </div>
       </section>
       ```
    4. Trend class mapping: "растущая" → `metric-tag-success`, "стабильная" → `metric-tag-info`, "падающая" → `metric-tag-danger`.
    5. Instagram block: if handle exists, show `@{handle} · {_fmt_num(followers)} подписчиков` with a link.
    6. XSS-safe: all text through `_esc()`.

    **Wire into _build_report_html:**

    Add the competitor cards section AFTER the existing competitor table section (reference order: section 02 Market → section 06 Competitor cards). This is a MORE DETAILED view than the existing `_build_competitor_table` — both can appear, with the table as summary and cards as detail.

    ```python
    # Phase 4 / DAT-03: Competitor cards section (detailed)
    sections.append(_build_competitor_cards_section(competitors))
    ```

    Do NOT remove or replace the existing `_build_competitor_table` — it stays as the summary view. The new cards section adds detail.
  </action>

  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes && python3 -c "
import ast
with open('app/tools/generate_html_report.py') as f:
    ast.parse(f.read())

source = open('app/tools/generate_html_report.py').read()
assert 'def _build_competitor_cards_section(' in source

from app.tools.generate_html_report import _build_competitor_cards_section

html = _build_competitor_cards_section({
    'competitor_cards': [
        {
            'name': 'Конкурент 1',
            'year_founded': 2010,
            'revenue_latest': 3000000000,
            'revenue_trend': 'растущая',
            'surgeons_count': 8,
            'instagram_handle': 'clinic1',
            'instagram_followers': 25000,
            'specialization': 'Пластика лица',
        },
    ]
})
assert 'Конкурент 1' in html
assert '2010' in html
assert '3' in html and 'млрд' in html
assert 'растущая' in html
assert 'clinic1' in html or '@clinic1' in html
assert 'Пластика лица' in html

# Empty case
html = _build_competitor_cards_section({})
assert 'не собраны' in html.lower() or html == '' or len(html) < 200

print('OK: competitor_cards section works correctly')
"</automated>
  </verify>

  <done>
    `_build_competitor_cards_section()` renders detailed glass-card per competitor with all D-20 fields (year, revenue, trend, surgeons, Instagram, specialization). Wired into `_build_report_html` after the existing competitor table.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool → HTML output | New section builders render data from collected_data into HTML — XSS-safe via _esc() |
| HTML → User browser | Rendered HTML displayed in browser — standard XSS surface, mitigated by escaping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-X | XSS | All new section builders handle untrusted data (competitor names, media titles, review text) | mitigate | Every user-facing string wrapped in `_esc()`; URLs escaped in href attributes; existing pattern from _build_no_instagram_block followed |
| T-04-06-T | Tampering | Revenue table could show wrong YoY % | accept | Formatting only — data validated in Plan 04-01 _format_revenue_dynamics with unit tests |
| T-04-06-D | DoS | Large competitor cards list → large HTML | mitigate | Max 10 cards rendered (top competitors); rest summarized in existing table |
| T-04-06-E | EoP | Not applicable | accept | Pure HTML string generation |
| T-04-06-SC | Supply chain | No new packages | accept | Pure Python stdlib + existing helpers |
</threat_model>

<verification>
**Post-plan automated checks:**

1. `generate_html_report.py` passes syntax check (ast.parse)
2. 5 new section-builder functions exist: `_build_revenue_dynamics_section`, `_build_clinic_metrics_block`, `_build_media_urls_section`, `_build_ratings_section`, `_build_competitor_cards_section`
3. All functions handle: empty data, None values, missing keys → graceful honest blocks
4. Revenue dynamics: D-13 strict rule enforced (no table when unavailable), D-14 table+blockquote when available
5. Media URLs: D-17 simple list (not cards), D-18 honest block for 0 mentions
6. Competitor cards: D-20 all fields rendered (year, revenue, trend, surgeons, Instagram, specialization)
7. Clinic metrics: D-21 ОКВЭД rendered (humanized by LLM via okved_humanized field)
8. Ratings: D-22 2-platform minimum (ПроДокторов + Яндекс.Карты)
9. All 5 sections wired into `_build_report_html` at correct positions
10. XSS: all user-facing text wrapped in `_esc()`

**Regression checks:**
- Existing `_build_competitor_table` preserved (summary view alongside new detail cards)
- Existing `_build_no_instagram_block` and `_build_qc_coverage_section` unchanged (Phase 3)
- `_build_report_html` signature unchanged (data, title, coverage_metadata, niche, instagram_data)
- Existing HTML report structure (Hero → About → Market → ... → QC) preserved
</verification>

<success_criteria>
- DAT-01/D-13..14 SATISFIED (HTML layer): Revenue dynamics section with 3-year table + blockquote, strict <3-year honest block
- DAT-02/D-17..18 SATISFIED (HTML layer): Media URLs as simple hyperlink list, honest pr_needed block for 0 mentions
- DAT-03/D-20 SATISFIED (HTML layer): Competitor cards with all D-20 fields per competitor
- DAT-04/D-21 SATISFIED (HTML layer): Clinic metrics block in About section with ОКВЭД (LLM-humanized)
- DAT-05/D-22 SATISFIED (HTML layer): Ratings from 2 platforms with stars + review counts + themes
- All sections use design-system CSS classes (glass cards, metric tags, section labels)
- All sections handle missing data gracefully (honest "данные недоступны" blocks)
- XSS-safe: all text escaped via _esc()
</success_criteria>

<output>
Create `.planning/phases/04-new-sections-data-depth/04-06-SUMMARY.md` when done. Record:
- Functions added: 5 new section builders
- Sections wired into _build_report_html at positions matching reference report
- CSS classes used (glass-card, metric-tag, section-label, blockquote)
- XSS mitigation: _esc() applied to all user-facing text
- Test results (all automated checks pass)
- Any design-system deviations (if CSS class names differ from expected)
</output>
