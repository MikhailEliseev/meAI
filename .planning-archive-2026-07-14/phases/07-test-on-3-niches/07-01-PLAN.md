---
phase: 7
plan: 07-01
type: execute
name: pre-flight-scout-and-test-harness-setup
wave: 1
depends_on: []
requirements: [TST-01, TST-05]
tags: [phase7, scout, test-harness, setup, autonomous]

files_modified:
  - .planning/phases/07-test-on-3-niches/07-01-selected-clinics.md
  - .planning/phases/07-test-on-3-niches/07-01-scout-report.md
  - .planning/phases/07-test-on-3-niches/run_presale_test.py
  # Server-side (via ssh aim docker exec):
  # - aim-hermes:/opt/data/phase7/run_presale_test.py (new)
  # - aim-hermes:/opt/data/memories/proposals/plastic-iphk/ (mkdir)
  # - aim-hermes:/opt/data/memories/proposals/dental-{slug}/ (mkdir)
  # - aim-hermes:/opt/data/memories/proposals/cosmetology-{slug}/ (mkdir)

autonomous: true
must_haves:
  truths:
    - "aim-hermes container is verified healthy (Up, healthy) with TOOL_HANDLERS=26 + QC_CHECKLIST 18 items v1.2.0"
    - "Reference HTML md5 e957099790fd65a59065d5df6f21bed5 matches between /opt/data/report-reference.html (server) and ИПХиК (2).html (local)"
    - "ORCHESTRATOR_MODE can be injected per-exec via `docker exec -e ORCHESTRATOR_MODE=1` without container restart"
    - "Test harness run_presale_test.py exists locally + deployed to /opt/data/phase7/ (md5 match) + argparse --help works"
    - "3 niche clinic URLs selected + recorded in 07-01-selected-clinics.md (plastic locked, dental + cosmetology chosen)"
    - "Output directories exist under /opt/data/memories/proposals/ for all 3 niches"
  artifacts:
    - path: ".planning/phases/07-test-on-3-niches/07-01-scout-report.md"
      provides: "Container state verification (8 checks all PASS)"
      contains: "PASS"
    - path: ".planning/phases/07-test-on-3-niches/07-01-selected-clinics.md"
      provides: "3 selected clinic URLs with slugs + modes"
      contains: "plastic-iphk"
    - path: ".planning/phases/07-test-on-3-niches/run_presale_test.py"
      provides: "Reusable Python test harness for orchestrator invocation"
      contains: "run_three_pass"
    - path: "aim-hermes:/opt/data/phase7/run_presale_test.py"
      provides: "Deployed harness on server (md5 matches local)"
      contains: "run_three_pass"
  key_links:
    - from: "run_presale_test.py"
      to: "app.orchestrator.three_pass.run_three_pass"
      via: "Python import + asyncio.run"
      pattern: "from app\.orchestrator\.three_pass import run_three_pass"
    - from: "run_presale_test.py --slug <slug>"
      to: "/opt/data/memories/proposals/<slug>/proposal.html"
      via: "harness output writer"
      pattern: "/opt/data/memories/proposals"
---

<objective>
Verify aim-hermes container is healthy and ready for 3 niche tests. Build a reusable Python test harness that directly invokes the orchestrator (`run_three_pass`) without requiring container restart. Find specific dental + cosmetology clinic URLs for subsequent test plans. Create output directories under `/opt/data/memories/proposals/`.

Purpose: This plan is the foundation for Plans 07-02/03/04. It produces: (1) a confirmed-ready container state, (2) a reusable test harness script deployed to the server, (3) specific clinic URLs for dental/cosmetology (plastic is already locked to iphk.ru per D-01), (4) empty output directories ready to receive proposal.html + feedback.md artifacts. Downstream plans depend entirely on these prerequisites — a faulty scout here blocks all 3 niche tests.

Output: Three local artifacts (`07-01-selected-clinics.md`, `07-01-scout-report.md`, `run_presale_test.py`) + four server-side artifacts (test harness deployed to `/opt/data/phase7/`, three empty client-slug directories under `/opt/data/memories/proposals/`).
</objective>

## Phase Goal (MVP)

**As a** Hermes system validator,
**I want to** verify the deployed container state and prepare a reusable test harness,
**so that** the 3 niche tests (Plans 07-02/03/04) can run autonomously without user intervention.

## User Story (Vertical Slice)

After this plan, Claude executor has everything needed to trigger a presale test in one command: `ssh aim "docker exec -e ORCHESTRATOR_MODE=1 aim-hermes python /opt/data/phase7/run_presale_test.py --url <CLINIC> --slug <SLUG> --mode <PRESALE|ADMIN> --niche <niche>"`. The script runs for up to 30 minutes, then saves proposal.html + metadata.json to the correct directory. No user interaction required.

---

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-test-on-3-niches/07-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Executor: key contracts extracted from codebase. Verify signatures before touching anything. -->

From aim-hermes:/opt/hermes/app/orchestrator/three_pass.py (verified via grep):
```python
async def run_three_pass(
    session_id: str,
    client_url: str,
    client_name: str = "",
    mode: str = "PRESALE",   # "PRESALE" | "ADMIN" — controls mode-prompt
    chat_id: int = 0,
) -> app.orchestrator.states.OrchestratorState
```

From aim-hermes:/opt/hermes/app/orchestrator/states.py (verified):
```python
@dataclass
class OrchestratorState:
    session_id: str
    client_url: str
    client_name: str = ""
    mode: str = "PRESALE"
    chat_id: int = 0
    pass_status: dict          # {"collect": "completed", "gap_analyze": ..., "fill_assemble": ...}
    collected_data: dict       # Raw pass results
    gap_report: dict           # Pass 2 output with 18-item checklist scoring
    html_report_path: str      # Path/URL to generated HTML (empty if LLM didn't call generator)
    started_at: str            # ISO timestamp
    completed_at: str           # ISO timestamp
    error_message: str         # Empty if no error
    niche: str                 # "plastic_surgery" | "cosmetology" | "dental" | "unknown"
```

From aim-hermes:/opt/hermes/app/orchestrator/qc_checklist.py (verified — 18 items v1.2.0):
- Items 0-17 enumerated. Item 4 (Instagram) is `conditional_on_niche=True`.
- For non-critical niches (dental/general), item 4 moves to `not_applicable_items`.

Verified container state (scout output from planner investigation, 2026-06-24):
- `TOOL_HANDLERS`: 26 entries
- `QC_CHECKLIST`: 18 items, version 1.2.0
- `ORCHESTRATOR_MODE`: unset (tests will inject via `docker exec -e`)
- Container: Up 46 hours, healthy
- `/opt/data/report-reference.html` md5 = `e957099790fd65a59065d5df6f21bed5` (matches local `ИПХиК (2).html`)
- `/opt/data/memories/proposals/` exists with only `arclinic/` subdir (empty)
- `/opt/data/run_phase0_iphk.py` + `/opt/data/run_phase1_iphk.py` already exist as example scripts (not modified by this plan)

Server access pattern (from CLAUDE.md SSH convention):
- `ssh aim` → root@AIM-Server-PL (78.17.128.169)
- `docker exec aim-hermes <cmd>` → run inside container
- `docker exec -e ORCHESTRATOR_MODE=1 aim-hermes <cmd>` → run with env override (no restart needed)
</interfaces>
</context>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Verify container state, reference HTML, and prepare output directories</name>
  <files>.planning/phases/07-test-on-3-niches/07-01-scout-report.md</files>

  <read_first>
    - .planning/phases/07-test-on-3-niches/07-CONTEXT.md (decisions D-01..D-13, canonical_refs)
    - CLAUDE.md section "SSH-подключения" (ssh aim convention)
    - .planning/phases/03-instagram-integration/03-VERIFICATION.md (deployed state baseline)
  </read_first>

  <action>
    Run the following verification commands via `ssh aim` and record results in `07-01-scout-report.md`. Each check must PASS for the plan to proceed. If any check FAILS, halt and document the failure in the scout report (do NOT proceed to Task 2 — downstream plans depend on a healthy container).

    **Check 1 — Container health:**
    `ssh aim "docker ps --format '{{.Names}}\t{{.Status}}' | grep aim-hermes"`
    Expected: line containing `aim-hermes` and `Up` and `(healthy)`. Record actual status.

    **Check 2 — Tool handlers count (expect 26):**
    `ssh aim "docker exec aim-hermes python3 -c 'import sys; sys.path.insert(0, \"/opt/hermes\"); from app.pipeline.engine import _TOOL_HANDLERS; print(len(_TOOL_HANDLERS))'"`
    Expected: `26`. Record actual count.

    **Check 3 — QC checklist (expect 18 items, version 1.2.0):**
    `ssh aim "docker exec aim-hermes python3 -c 'import sys; sys.path.insert(0, \"/opt/hermes\"); from app.orchestrator.qc_checklist import QC_CHECKLIST, VERSION; print(len(QC_CHECKLIST), VERSION)'"`
    Expected: `18 1.2.0`. Record actual.

    **Check 4 — Orchestrator imports clean:**
    `ssh aim "docker exec aim-hermes python3 -c 'import sys; sys.path.insert(0, \"/opt/hermes\"); from app.orchestrator.three_pass import run_three_pass; from app.orchestrator.states import OrchestratorState; print(\"OK\")'"`
    Expected: `OK`. If ImportError, halt — orchestrator modules not deployable.

    **Check 5 — Reference HTML match (expect md5 e957099790fd65a59065d5df6f21bed5):**
    `ssh aim "docker exec aim-hermes md5sum /opt/data/report-reference.html"`
    AND locally:
    `md5 "/Users/mikhaileliseev/Downloads/ИПХиК (2).html"`
    Expected: both return `e957099790fd65a59065d5df6f21bed5`. If mismatch, downstream style comparison breaks.

    **Check 6 — Proposals directory exists:**
    `ssh aim "docker exec aim-hermes ls -ld /opt/data/memories/proposals/"`
    Expected: directory exists. If missing, create via `ssh aim "docker exec aim-hermes mkdir -p /opt/data/memories/proposals"`.

    **Check 7 — Phase 7 test directory on server:**
    `ssh aim "docker exec aim-hermes mkdir -p /opt/data/phase7 && ls -ld /opt/data/phase7"`
    Expected: directory created/exists. This is where the test harness will live.

    **Check 8 — Create output directories for 3 niche tests:**
    `ssh aim "docker exec aim-hermes mkdir -p /opt/data/memories/proposals/plastic-iphk /opt/data/memories/proposals/dental-phase7 /opt/data/memories/proposals/cosmetology-phase7 && ls /opt/data/memories/proposals/"`
    NOTE: `dental-phase7` and `cosmetology-phase7` are placeholder slugs. Task 2 will rename them based on selected clinics. Plastic is locked to `plastic-iphk` per D-01.

    Write all results to `07-01-scout-report.md` using this format:
    ```markdown
    # Phase 7 Pre-flight Scout Report

    Date: 2026-06-24 (autonomous)
    Container: aim-hermes

    ## Check Results

    | # | Check | Expected | Actual | Status |
    |---|-------|----------|--------|--------|
    | 1 | Container health | Up, healthy | <actual> | PASS/FAIL |
    | 2 | Tool handlers | 26 | <actual> | PASS/FAIL |
    | ... |

    ## Container Configuration Confirmed
    - TOOL_HANDLERS: 26
    - QC_CHECKLIST: 18 items v1.2.0
    - ORCHESTRATOR_MODE: unset (will be injected per-test via docker exec -e)
    - Reference HTML md5: e957099790fd65a59065d5df6f21bed5 (matches local ИПХиК (2).html)

    ## Blockers (if any)
    <list or "none">

    ## Ready for Task 2
    <yes/no>
    ```

    If any check FAILS, do NOT proceed to Task 2. Surface the blocker in the SUMMARY and abort the plan.
  </action>

  <verify>
    <automated>test -f /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/07-test-on-3-niches/07-01-scout-report.md && grep -q "PASS" /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/07-test-on-3-niches/07-01-scout-report.md && ! grep -q "Status.*FAIL" /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/07-test-on-3-niches/07-01-scout-report.md</automated>
    <automated>ssh aim "docker exec aim-hermes ls -d /opt/data/phase7 /opt/data/memories/proposals/plastic-iphk /opt/data/memories/proposals/dental-phase7 /opt/data/memories/proposals/cosmetology-phase7"</automated>
  </verify>

  <done>
    All 8 checks PASS. Scout report written with confirmed container state. Server-side directories `/opt/data/phase7/`, `/opt/data/memories/proposals/plastic-iphk/`, `/opt/data/memories/proposals/dental-phase7/`, `/opt/data/memories/proposals/cosmetology-phase7/` all exist.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build test harness script + select dental/cosmetology clinics</name>
  <files>.planning/phases/07-test-on-3-niches/run_presale_test.py, .planning/phases/07-test-on-3-niches/07-01-selected-clinics.md</files>

  <read_first>
    - .planning/phases/07-test-on-3-niches/07-01-scout-report.md (from Task 1 — confirms container is ready)
    - aim-hermes:/opt/hermes/app/orchestrator/three_pass.py (signature: `async def run_three_pass(session_id, client_url, client_name="", mode="PRESALE", chat_id=0) -> OrchestratorState`)
    - aim-hermes:/opt/hermes/app/orchestrator/states.py (OrchestratorState fields — see <interfaces> above)
    - aim-hermes:/opt/data/run_phase0_iphk.py (existing example pattern — standalone Python script using openai SDK directly, reads env, saves to /opt/data/projects/<slug>/)
    - .planning/phases/07-test-on-3-niches/07-CONTEXT.md (D-01: iphk.ru plastic locked; D-02: dental/cosmetology via Perplexity search or demo URLs)
  </read_first>

  <action>
    **Step A — Build test harness script locally:**

    Create `/Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/07-test-on-3-niches/run_presale_test.py`. The script:

    1. Accepts CLI args: `--url`, `--slug`, `--mode` (PRESALE|ADMIN), `--niche` (plastic_surgery|dental|cosmetology), `--session-id` (optional, defaults to `phase7-<slug>-<timestamp>`)
    2. Sets `os.environ["ORCHESTRATOR_MODE"] = "1"` at process start (before any app imports)
    3. Adds `/opt/hermes` to sys.path
    4. Imports `asyncio`, `json`, `sys`, `os`, `time`, `traceback`, `argparse`, `datetime`
    5. Imports `from app.orchestrator.three_pass import run_three_pass`
    6. Creates output directory `/opt/data/memories/proposals/<slug>/` (exist_ok=True)
    7. Invokes `await run_three_pass(session_id=session_id, client_url=url, client_name=slug, mode=mode)` with a 30-minute hard timeout via `asyncio.wait_for(..., timeout=1800)`
    8. On success: extracts `state.html_report_path`, copies/reads the HTML file at that path into `<output_dir>/proposal.html`. If `html_report_path` is empty (LLM didn't call generate_html_report), checks `/opt/data/sessions-archive/<session_id>/report.html` as fallback. Writes `<output_dir>/metadata.json` with: session_id, client_url, mode, niche, started_at, completed_at, pass_status, niche_detected, gap_report summary, html_report_path, html_char_count, orchestrator_version.
    9. On timeout (30 min exceeded): writes `<output_dir>/metadata.json` with `status: "TIMEOUT"`, exit code 2.
    10. On exception: writes `<output_dir>/metadata.json` with `status: "FAILED", error: str(e), traceback: <full>`, exit code 1.
    11. Prints a final summary line to stdout: `PHASE7_RESULT slug=<slug> status=<SUCCESS|TIMEOUT|FAILED> html_path=<path> duration=<seconds>`

    The script MUST be Python 3.11-compatible (container runs Python 3.11.15). No f-string backslash-escaped quotes inside expression parts (Plan 03-05 gotcha). No 3.12+ syntax (no `type*` statement, no PEP 695 generics).

    The script MUST log progress to stdout every 60 seconds via a background task: `[<ISO>] pass_status=<dict> elapsed=<seconds>`. This helps Claude executor monitor progress from the host via `tail -f`.

    Keep the script under 200 lines. Use simple `async def main()` + `asyncio.run(main())` pattern.

    **Step B — Deploy script to container:**

    `cat /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/07-test-on-3-niches/run_presale_test.py | ssh aim "docker exec -i aim-hermes tee /opt/data/phase7/run_presale_test.py > /dev/null"`

    Verify deployment:
    - `ssh aim "docker exec aim-hermes md5sum /opt/data/phase7/run_presale_test.py"` — compare with local md5.
    - `ssh aim "docker exec aim-hermes python3 -c 'import ast; ast.parse(open(\"/opt/data/phase7/run_presale_test.py\").read()); print(\"syntax OK\")'"` — verify syntax parses in container Python 3.11.
    - `ssh aim "docker exec aim-hermes python3 /opt/data/phase7/run_presale_test.py --help"` — verify argparse works.

    **Step C — Select dental and cosmetology clinic URLs:**

    Per D-02: use Perplexity search or demo URLs. Since the user is sleeping (no manual selection possible), Claude executor selects clinics using this deterministic procedure:

    1. Invoke Perplexity via the Hermes container (use existing `perplexity_search` tool infrastructure):
       `ssh aim "docker exec aim-hermes python3 -c '
       import asyncio, sys, json
       sys.path.insert(0, \"/opt/hermes\")
       from app.tools.perplexity_search import handle_perplexity_search
       async def main():
           r = await handle_perplexity_search(query=\"Топ-5 частных стоматологических клиник Москва (не государственных, ООО или ЗАО). Для каждой: название, URL сайта, адрес. Только частные коммерческие.\", model=\"sonar-pro\")
           print(r)
       asyncio.run(main())
       '"`
       Capture the output to a temp file.

    2. Repeat for cosmetology:
       Query: `"Топ-5 частных косметологических клиник Москва (не государственных, ООО или ЗАО). Для каждой: название, URL сайта, адрес. Только частные коммерческие."`

    3. From the Perplexity responses, pick ONE clinic per niche using these criteria:
       - Must be private (ООО/ЗАО/АО/ИП), NOT state (no ГАУЗ/ГБУЗ/МУЗ)
       - Must have a working website URL (not just social media)
       - Prefer well-known names with likely deep web presence (better for data collection)
       - For dental: prefer clinics with 3+ doctors on the website (needed for SEC-04 expert regalia)
       - For cosmetology: prefer clinics with active Instagram (needed for IG-02/03 testing)

    4. Write `07-01-selected-clinics.md` with this format:
       ```markdown
       # Phase 7 Selected Clinics

       ## Niche 1: Plastic Surgery (locked per D-01)
       - **Clinic:** Институт пластической хирургии и косметологии
       - **URL:** https://iphk.ru
       - **Slug:** plastic-iphk
       - **Niche tag:** plastic_surgery (Instagram-critical)
       - **Reference:** /Users/mikhaileliseev/Downloads/ИПХиК (2).html (md5 e957099790fd65a59065d5df6f21bed5)
       - **Mode for test:** PRESALE (per D-06)

       ## Niche 2: Dentistry
       - **Clinic:** <selected name>
       - **URL:** <selected url>
       - **Slug:** dental-<normalized-name>
       - **Niche tag:** dental (Instagram NOT critical)
       - **Selection rationale:** <1-2 sentences why this clinic>
       - **Mode for test:** ADMIN (per D-07)

       ## Niche 3: Cosmetology
       - **Clinic:** <selected name>
       - **URL:** <selected url>
       - **Slug:** cosmetology-<normalized-name>
       - **Niche tag:** cosmetology (Instagram-critical — tests IG-02/03 path)
       - **Selection rationale:** <1-2 sentences>
       - **Mode for test:** PRESALE (alternate from dental, exercises both paths)

       ## Perplexity Raw Output
       <paste both query responses verbatim for audit trail>
       ```

    5. Create the final server-side directories using the selected slugs:
       `ssh aim "docker exec aim-hermes mkdir -p /opt/data/memories/proposals/<dental-slug> /opt/data/memories/proposals/<cosmetology-slug>"`
       (Plastic dir already exists from Task 1.)

       Remove the placeholder dirs from Task 1 if they're now unused:
       `ssh aim "docker exec aim-hermes rmdir /opt/data/memories/proposals/dental-phase7 /opt/data/memories/proposals/cosmetology-phase7 2>/dev/null || true"` (only if empty — they should be)

    **Clinic selection fallback (if Perplexity unavailable):**
    - Dental: `https://family-dent.ru` (Семейная стоматология, placeholder) — or another clinic Claude knows from prior session context
    - Cosmetology: `https://lprime.ru` (Lprime clinic) — or another from prior context
    - Document which path was taken in `07-01-selected-clinics.md`.

    The goal is to have 2 concrete URLs ready for Plans 07-03 and 07-04. The selection does not need to be perfect — it needs to be a real, working clinic URL that the orchestrator can scan.
  </action>

  <verify>
    <automated>test -f /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/07-test-on-3-niches/run_presale_test.py && python3 -c "import ast; ast.parse(open('/Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/07-test-on-3-niches/run_presale_test.py').read()); print('local syntax OK')"</automated>
    <automated>ssh aim "docker exec aim-hermes python3 /opt/data/phase7/run_presale_test.py --help" | grep -q "usage: run_presale_test.py"</automated>
    <automated>test -f /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/07-test-on-3-niches/07-01-selected-clinics.md && grep -q "plastic-iphk" /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/07-test-on-3-niches/07-01-selected-clinics.md && grep -q "dental-" /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/07-test-on-3-niches/07-01-selected-clinics.md && grep -q "cosmetology-" /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/07-test-on-3-niches/07-01-selected-clinics.md</automated>
    <automated>ssh aim "docker exec aim-hermes ls /opt/data/memories/proposals/" | grep -q "plastic-iphk"</automated>
  </verify>

  <done>
    Test harness script `run_presale_test.py` exists locally + deployed to `/opt/data/phase7/run_presale_test.py` in container (md5 match) + argparse `--help` works. `07-01-selected-clinics.md` exists with 3 concrete clinic URLs (plastic locked to iphk.ru, dental selected, cosmetology selected). Three server-side output directories exist under `/opt/data/memories/proposals/` matching the selected slugs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → Production container | `docker exec -i ... tee` writes a test harness script into the container's `/opt/data/phase7/` directory. This is a NEW directory, not a system path — no existing files overwritten. |
| Test harness → Production state | The harness invokes `run_three_pass` which calls real external APIs (Perplexity, Apify, Firecrawl). Costs accrue. Session DB is written (state.db). Proposals directory is written. No production PRESALE flow is interrupted because the harness uses unique session IDs (`phase7-<slug>-<timestamp>`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-S | Spoofing | SSH key for `ssh aim` | accept | Existing SSH key access (per auto-memory server-ssh-key.md); no new attack surface |
| T-07-01-T | Tampering | Test harness script deployed to container | mitigate | md5 verification after `tee` confirms byte-identity; script only invokes public orchestrator API; no file overwrite of existing paths |
| T-07-01-R | Repudiation | Scout report + clinics file | mitigate | Both files written to `.planning/phases/07-test-on-3-niches/` — git-tracked, timestamped, attributable |
| T-07-01-I | Info disclosure | Perplexity queries expose clinic research activity | accept | Perplexity API already used by production Hermes; no new exfil path; queries are benign (public clinic info) |
| T-07-01-D | DoS | Test harness could spawn unbounded API calls | mitigate | Harness uses 30-min asyncio.wait_for hard timeout; one presale per plan; orchestrator's own rate limits (key bank rotation, Perplexity quota) apply |
| T-07-01-E | EoP | Test harness runs as container root | accept | Same privilege as production Hermes; no privilege change; harness only invokes orchestrator's public Python API |
| T-07-01-SC | Supply chain | No new packages installed — script uses stdlib + existing app imports | accept | No pip install, no npm — supply chain unchanged |
</threat_model>

<verification>
**Post-plan automated checks:**

1. `ssh aim "docker exec aim-hermes python3 /opt/data/phase7/run_presale_test.py --help"` exits 0 and prints usage
2. `ssh aim "docker exec aim-hermes ls /opt/data/memories/proposals/"` shows `plastic-iphk`, plus the selected dental + cosmetology slugs
3. `test -f .planning/phases/07-test-on-3-niches/07-01-scout-report.md` and all checks marked PASS
4. `test -f .planning/phases/07-test-on-3-niches/07-01-selected-clinics.md` and file contains 3 clinic entries (plastic, dental, cosmetology)
5. `test -f .planning/phases/07-test-on-3-niches/run_presale_test.py` and Python syntax valid locally and in container

**Integration checks (confirmed by planner during scouting, recorded for executor):**
- Container: aim-hermes Up 46 hours healthy
- TOOL_HANDLERS: 26 entries
- QC_CHECKLIST: 18 items v1.2.0
- ORCHESTRATOR_MODE: unset (will be injected per-test via `docker exec -e ORCHESTRATOR_MODE=1`)
- Reference HTML md5 matches local ИПХиК (2).html
- Auth: HERMES_API_KEY unset in env → `/api/chat` returns 200 without Authorization header (dev-no-key mode)

**Regression checks:**
- Existing 26 `_TOOL_HANDLERS` entries unchanged (this plan does not touch engine.py)
- Existing orchestrator modules unchanged (this plan only CREATES a new file in a new directory)
- `/opt/data/run_phase0_iphk.py` and `/opt/data/run_phase1_iphk.py` untouched
</verification>

<success_criteria>
- TST-01 (3 niches): pre-flight ready — iphk.ru locked, dental + cosmetology URLs selected and recorded
- TST-05 (output format): directories prepared under `/opt/data/memories/proposals/<slug>/` for all 3 niches
- Test harness deployed and `--help` works — ready for Plans 07-02/03/04 to invoke
- Container state verified (26 handlers, QC 1.2.0, healthy, reference HTML match)
- No container restart needed (ORCHESTRATOR_MODE injected per-test via `docker exec -e`)
- No production regression (existing presale flow unaffected)
</success_criteria>

<output>
Create `.planning/phases/07-test-on-3-niches/07-01-SUMMARY.md` when done. Record:
- Confirmation of all 8 scout checks (with actual values)
- md5 of deployed `run_presale_test.py` (local + container)
- Selected clinic URLs for dental + cosmetology (with rationale)
- Any deviations from the plan (e.g., Perplexity fallback used)
- Ready signal for Plan 07-02 (plastic surgery test)
</output>
