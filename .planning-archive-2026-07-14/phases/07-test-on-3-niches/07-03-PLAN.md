---
phase: 7
plan: 07-03
type: execute
name: dental-admin-mode-test
wave: 3
depends_on: [07-02]
requirements: [TST-01, TST-02, TST-04, TST-05]
tags: [phase7, niche-test, dental, admin-mode, autonomous]

files_modified:
  - .planning/phases/07-test-on-3-niches/07-03-SUMMARY.md
  - .planning/phases/07-test-on-3-niches/07-03-feedback.md
  # Server-side:
  # - aim-hermes:/opt/data/memories/proposals/dental-<slug>/proposal.html (orchestrator output)
  # - aim-hermes:/opt/data/memories/proposals/dental-<slug>/metadata.json (harness output)
  # - aim-hermes:/opt/data/memories/proposals/dental-<slug>/feedback.md (Claude-written copy)
  # - aim-hermes:/opt/data/memories/proposals/dental-<slug>/harness.log

autonomous: true
must_haves:
  truths:
    - "Dental clinic presale in ADMIN mode ran to completion (SUCCESS/TIMEOUT/FAILED within 30 min)"
    - "Niche detected as 'dental' or 'general_medicine' (item 4 correctly not_applicable for dental)"
    - "proposal.html exists at /opt/data/memories/proposals/dental-<slug>/ (>10,000 chars if SUCCESS)"
    - "QC 17-item coverage % computed (dental: item 4 not_applicable, denominator 17, threshold >=80%)"
    - "feedback.md written with Overall verdict + ADMIN mode observations vs PRESALE"
  artifacts:
    - path: "aim-hermes:/opt/data/memories/proposals/dental-<slug>/proposal.html"
      provides: "Orchestrator-generated HTML report for dental ADMIN test"
      contains: "<html"
    - path: "aim-hermes:/opt/data/memories/proposals/dental-<slug>/metadata.json"
      provides: "Test harness metadata with mode=ADMIN niche=dental"
      contains: "ADMIN"
    - path: "aim-hermes:/opt/data/memories/proposals/dental-<slug>/feedback.md"
      provides: "QC 17-item scoring + ADMIN mode observations"
      contains: "17-item denominator"
    - path: ".planning/phases/07-test-on-3-niches/07-03-SUMMARY.md"
      provides: "Dental test outcome + niche detection result"
      contains: "ADMIN"
    - path: ".planning/phases/07-test-on-3-niches/07-03-feedback.md"
      provides: "Local copy for git tracking"
      contains: "17-item"
  key_links:
    - from: "run_presale_test.py --mode ADMIN --niche dental"
      to: "ADMIN mode prompt + niche-conditional coverage logic"
      via: "agent_wrapper.py + Plan 03-06 _apply_niche_conditional_coverage"
      pattern: "mode.*ADMIN"
---

<objective>
Run full presale on the dental clinic selected in Plan 07-01 in ADMIN mode. Wait up to 30 minutes for completion. Score the generated report against the 18-item QC checklist (target ≥80% per QC-04). Compare style against the canonical reference `ИПХиК (2).html`. Write `feedback.md` with results.

Purpose: This is test 2 of 3. Dental niche is NOT Instagram-critical (item 4 becomes `not_applicable` per Plan 03-06 conditional logic), so it exercises a different code path than plastic. It also validates ADMIN mode (D-07) — the mode used when Mikhail manually triggers a presale for a specific clinic. TST-04 (ADMIN mode) is satisfied here.

Output: Two artifacts in `/opt/data/memories/proposals/dental-<slug>/`: `proposal.html` + `feedback.md`. Plus local `07-03-SUMMARY.md`.
</objective>

## Phase Goal (MVP)

**As a** Hermes system validator,
**I want to** trigger an ADMIN-mode presale on a dental clinic,
**so that** I confirm ADMIN mode works end-to-end AND the non-Instagram-critical niche path (item 4 not_applicable) produces a passing QC score.

## User Story (Vertical Slice)

After this plan, the user can compare the dental report quality against plastic (07-02) and cosmetology (07-04). The dental test specifically validates:
- ADMIN mode prompt (different tool-calling behavior than PRESALE per agent_wrapper.py)
- Niche-conditional QC logic (item 4 not_applicable, denominator effectively 17 for dental)
- Reference-style narrative quality for a niche WITHOUT Instagram data (most dental clinics don't have doctor Instagrams)

---

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-test-on-3-niches/07-CONTEXT.md
@.planning/phases/07-test-on-3-niches/07-01-selected-clinics.md
@.planning/phases/07-test-on-3-niches/07-01-scout-report.md
@.planning/phases/07-test-on-3-niches/07-02-SUMMARY.md
@.planning/phases/07-test-on-3-niches/07-02-feedback.md
@CLAUDE.md

<interfaces>
<!-- Test harness CLI — same script as 07-02, different args: -->
```
docker exec -e ORCHESTRATOR_MODE=1 aim-hermes python3 /opt/data/phase7/run_presale_test.py \
    --url <DENTAL_URL_FROM_07-01-selected-clinics.md> \
    --slug <DENTAL_SLUG> \
    --mode ADMIN \
    --niche dental \
    --session-id phase7-<DENTAL_SLUG>
```

<!-- QC scoring scope — DIFFERENT from plastic surgery: -->
For `dental` niche, item 4 (Instagram analysis) is NOT applicable.
Per Plan 03-06 `_apply_niche_conditional_coverage`:
- `total_items` drops from 18 to 17 (item 4 excluded)
- Item 4 moves to `not_applicable_items` list
- Pass threshold recalculated: ≥14 of 17 items filled (≥80% of 17)
- HARD-FAIL override does NOT trigger for dental niche

<!-- ADMIN mode vs PRESALE mode (from agent_wrapper.py): -->
- PRESALE: mode prompt focuses on client-facing flow (collect contact, qualify lead)
- ADMIN: mode prompt allows direct tool invocation without client communication
- Both modes invoke the same orchestrator (run_three_pass) — difference is in LLM behavior within passes

<!-- Reference for style comparison: SAME as 07-02 -->
- Server: /opt/data/report-reference.html
- Local: /Users/mikhaileliseev/Downloads/ИПХиК (2).html
- Reference is for plastic surgery — style criteria apply cross-niche, but content density expectations should be adjusted (dental may have fewer competitors with Instagram data)
</interfaces>
</context>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Trigger presale on dental clinic (ADMIN mode) + poll for completion (up to 30 min)</name>
  <files>.planning/phases/07-test-on-3-niches/07-03-SUMMARY.md</files>

  <read_first>
    - .planning/phases/07-test-on-3-niches/07-01-selected-clinics.md (Section "Niche 2: Dentistry" — extract URL + slug)
    - .planning/phases/07-test-on-3-niches/07-02-SUMMARY.md (Pattern from plastic test — adapt for dental)
    - .planning/phases/07-test-on-3-niches/07-02-feedback.md (Lessons from plastic — apply fixes if applicable)
  </read_first>

  <action>
    **Read clinic details from Plan 07-01 selected-clinics file:**

    Extract from `07-01-selected-clinics.md`:
    - `DENTAL_URL` = the URL recorded in Section "Niche 2: Dentistry"
    - `DENTAL_SLUG` = the slug recorded (e.g., `dental-family-dent` or similar)
    - `DENTAL_NAME` = the clinic name (for client_name argument)

    If the file doesn't contain a dental URL (Plan 07-01 failed to select), HALT — surface as blocker in SUMMARY. Per D-02 fallback, use `https://family-dent.ru` or another known clinic. Document the fallback choice in SUMMARY.

    **Step 1 — Clean previous test artifacts (if any):**

    `ssh aim "docker exec aim-hermes sh -c 'rm -f /opt/data/memories/proposals/<DENTAL_SLUG>/proposal.html /opt/data/memories/proposals/<DENTAL_SLUG>/metadata.json /opt/data/memories/proposals/<DENTAL_SLUG>/harness.log 2>/dev/null; mkdir -p /opt/data/memories/proposals/<DENTAL_SLUG>'"`

    **Step 2 — Launch test harness in background on the server:**

    `ssh aim "nohup docker exec -e ORCHESTRATOR_MODE=1 aim-hermes python3 /opt/data/phase7/run_presale_test.py --url <DENTAL_URL> --slug <DENTAL_SLUG> --mode ADMIN --niche dental --session-id phase7-<DENTAL_SLUG> > /opt/data/memories/proposals/<DENTAL_SLUG>/harness.log 2>&1 &"`

    NOTE: `--mode ADMIN` satisfies TST-04. `--niche dental` triggers the conditional coverage logic (item 4 not_applicable).

    **Step 3 — Verify process launched (wait 30 seconds):**

    Wait 30s, then: `ssh aim "docker exec aim-hermes ps -ef | grep run_presale_test | grep -v grep"`
    Expected: one Python process line. Tail log: `ssh aim "docker exec aim-hermes tail -20 /opt/data/memories/proposals/<DENTAL_SLUG>/harness.log"`

    **Step 4 — Poll for completion (up to 30 minutes):**

    Same pattern as Plan 07-02 Task 1 Step 4. Poll every 5 min, max 6 iterations.

    Checks per iteration:
    a) Process still running? `ssh aim "docker exec aim-hermes ps -ef | grep run_presale_test.py | grep -v grep | wc -l"`
    b) metadata.json exists? `ssh aim "docker exec aim-hermes test -f /opt/data/memories/proposals/<DENTAL_SLUG>/metadata.json && echo DONE || echo RUNNING"`
    c) Tail log: `ssh aim "docker exec aim-hermes tail -20 /opt/data/memories/proposals/<DENTAL_SLUG>/harness.log"`

    **Step 5 — Read metadata.json outcome:**

    `ssh aim "docker exec aim-hermes cat /opt/data/memories/proposals/<DENTAL_SLUG>/metadata.json"`

    Expected fields same as 07-02 but with:
    - `"mode": "ADMIN"`
    - `"niche_detected": "dental"` (or "unknown" if detector failed — note this if it happens)
    - `"gap_report_summary"` may show item 4 as not_applicable in gap_report

    **Step 6 — Write initial SUMMARY:**

    Record outcome in `07-03-SUMMARY.md`:
    ```markdown
    # Plan 07-03 Summary: Dental (<CLINIC_NAME>) ADMIN Test

    **Date:** 2026-06-24 (autonomous)
    **Clinic:** <DENTAL_NAME> (<DENTAL_URL>)
    **Slug:** <DENTAL_SLUG>
    **Mode:** ADMIN
    **Niche expected:** dental | **Niche detected:** <from metadata>
    **Status:** SUCCESS | TIMEOUT | FAILED
    **Duration:** <sec>
    **HTML generated:** <yes/no>, size=<chars>

    ## metadata.json (verbatim)
    <paste>

    ## Notable observations
    - <e.g., "niche_detector returned 'unknown' — Pass 2 may have over-required Instagram">
    - <e.g., "ADMIN mode produced shorter Pass 1 than PRESALE mode in 07-02 — expected per agent_wrapper.py">

    ## Next Step
    Proceed to Task 2 (QC scoring + feedback.md) regardless of outcome per D-11.
    ```

    **Step 7 — Verify proposal.html exists:**

    `ssh aim "docker exec aim-hermes test -f /opt/data/memories/proposals/<DENTAL_SLUG>/proposal.html && wc -c /opt/data/memories/proposals/<DENTAL_SLUG>/proposal.html"`

    Fallback retrieval if missing: `ssh aim "docker exec aim-hermes sh -c 'cp <html_report_path_from_metadata> /opt/data/memories/proposals/<DENTAL_SLUG>/proposal.html 2>/dev/null || cp /opt/data/sessions-archive/phase7-<DENTAL_SLUG>/report.html /opt/data/memories/proposals/<DENTAL_SLUG>/proposal.html'"`
  </action>

  <verify>
    <automated>ssh aim "docker exec aim-hermes test -f /opt/data/memories/proposals/$(grep -oE 'dental-[a-z-]+' /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/07-test-on-3-niches/07-01-selected-clinics.md | head -1)/metadata.json && echo METADATA_EXISTS || echo MISSING"</automated>
    <automated>test -f /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/07-test-on-3-niches/07-03-SUMMARY.md</automated>
  </verify>

  <done>
    Test harness has run to completion (or timeout). `metadata.json` exists. `07-03-SUMMARY.md` records the outcome. If SUCCESS, `proposal.html` exists in `/opt/data/memories/proposals/<DENTAL_SLUG>/`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Score QC coverage (dental: 17-item denominator) + write feedback.md</name>
  <files>.planning/phases/07-test-on-3-niches/07-03-feedback.md</files>

  <read_first>
    - /Users/mikhaileliseev/Downloads/ИПХиК (2).html (same reference as 07-02)
    - .planning/phases/07-test-on-3-niches/07-03-SUMMARY.md (from Task 1)
    - .planning/phases/07-test-on-3-niches/07-02-feedback.md (pattern from plastic — mirror format for consistency)
    - .planning/phases/07-test-on-3-niches/07-CONTEXT.md (D-08, D-09, D-10, D-11)
  </read_first>

  <action>
    **Step 1 — Retrieve generated proposal.html:**

    `ssh aim "docker exec aim-hermes cat /opt/data/memories/proposals/<DENTAL_SLUG>/proposal.html" > /tmp/07-03-dental-proposal.html`

    If missing, set `<html_present>` = false and skip scoring.

    **Step 2 — Compute QC checklist coverage (17-item denominator for dental):**

    Apply the SAME 18-item detection heuristics from Plan 07-02 Task 2 Step 2, BUT:

    - For dental niche, item 4 (Instagram analysis) is `not_applicable` — exclude from denominator
    - `coverage_pct = sum(scored_values for items 0-3, 5-17) / 17 * 100`
    - Pass threshold: ≥80% of 17 = ≥14 items filled
    - HARD-FAIL override does NOT trigger (only applies to plastic_surgery/cosmetology)

    Special case: if niche_detector returned "unknown" (not "dental"), the orchestrator may have treated item 4 as APPLICABLE. In that case:
    - Note this as BUG: "niche_detector returned 'unknown' instead of 'dental' — Pass 2 may have over-required Instagram, causing unnecessary gap-analysis failures"
    - Score item 4 normally (likely `missing` since dental clinics often lack Instagram)
    - Use 18-item denominator for a secondary "what would plastic scoring look like" score, BUT use 17-item denominator for the official verdict per Plan 03-06 logic

    **Step 3 — Style comparison with reference (D-09, 5 criteria):**

    Apply same 5-criterion style comparison as 07-02 Task 2 Step 3. Note: dental clinic may have fewer doctors with Instagram than plastic surgery — this affects content density expectations for sections 03 (Experts) and 04 (Content Analysis). The reference is plastic-surgery-specific, so:
    - Criterion 1 (Narrative): expect comparable
    - Criterion 2 (Business language): expect comparable
    - Criterion 3 (Gap-blocks): expect comparable (≥3 blocks)
    - Criterion 4 (Blockquotes): expect comparable (one per major section)
    - Criterion 5 (Cross-references): expect comparable

    If Instagram data is scarce (which is normal for dental), the report should honestly mark item 4 as not_applicable rather than fabricate data. This is per D-11 honest reporting.

    **Step 4 — Identify bugs and missing sections:**

    Cross-reference with 07-02 plastic results. Common bugs to check:
    - Did niche_detector classify correctly? (expect "dental" or "general_medicine" — both acceptable for non-critical path)
    - Did Pass 2 properly mark item 4 as not_applicable in gap_report?
    - Did ADMIN mode produce a meaningfully different report than PRESALE would have? (compare to 07-02 plastic PRESALE for mode-effect signal)
    - Did the HTML rendering include the no-Instagram block per Plan 03-05 for sections 03/04?

    For each bug: BUG-ID, description, evidence, likely cause, Phase 8 recommendation.

    **Step 5 — Write feedback.md:**

    Mirror the format of 07-02-feedback.md, with these adjustments:
    ```markdown
    # Phase 7 Feedback: Dental (<CLINIC_NAME>) ADMIN Test

    **Date:** 2026-06-24 (autonomous)
    **Clinic:** <DENTAL_NAME> (<DENTAL_URL>)
    **Niche:** dental (NOT Instagram-critical — item 4 not_applicable per Plan 03-06)
    **Mode:** ADMIN (per D-07)
    **Duration:** <sec>
    **Overall verdict:** PASS | FAIL | PARTIAL

    ## QC Checklist Coverage (17-item denominator)

    **Total: <filled_count>/17 = <coverage_pct>%** (threshold: ≥80% of 17 = ≥14 items)
    **Item 4 (Instagram):** not_applicable (correctly excluded for dental niche)

    | # | Item | Status | Evidence |
    |---|------|--------|----------|
    | 0 | About data | filled/partial/missing | <grep evidence> |
    | ... |
    | 4 | Instagram analysis | not_applicable | N/A — dental niche per Plan 03-06 |
    | ... |
    | 17 | Expert регалии | ... | ... |

    ## Style Comparison vs Reference

    **Total: <score>/25**

    [Same 5-criterion table as 07-02]

    ## ADMIN Mode Observations

    [Compare to PRESALE mode from 07-02 — did mode affect report quality, tool selection, or narrative depth?]

    ## Missing Sections
    ## Identified Bugs
    ## Recommendations for Phase 8

    - GO/NO-GO for Phase 8 deploy of dental niche: <GO|NO-GO>
    ```

    Save to `.planning/phases/07-test-on-3-niches/07-03-feedback.md` locally AND deploy to server:
    `cat /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/07-test-on-3-niches/07-03-feedback.md | ssh aim "docker exec -i aim-hermes tee /opt/data/memories/proposals/<DENTAL_SLUG>/feedback.md > /dev/null"`

    **Failure handling (D-11):**
    - If FAIL: mark as KNOWN ISSUE, continue to Plan 07-04 (cosmetology + aggregate)
    - If PASS: unblocked for dental niche in Phase 8
    - D-12 blocker triggers ONLY if all 3 plans (07-02, 07-03, 07-04) fail
  </action>

  <verify>
    <automated>test -f /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/07-test-on-3-niches/07-03-feedback.md</automated>
    <automated>grep -qE "Overall verdict:.*PASS|Overall verdict:.*FAIL|Overall verdict:.*PARTIAL" /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/07-test-on-3-niches/07-03-feedback.md</automated>
    <automated>grep -q "17-item denominator" /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/07-test-on-3-niches/07-03-feedback.md</automated>
    <automated>ssh aim "docker exec aim-hermes sh -c 'ls /opt/data/memories/proposals/dental-*/feedback.md 2>/dev/null | head -1'" | grep -q feedback.md</automated>
  </verify>

  <done>
    `07-03-feedback.md` exists locally with: 17-item QC coverage %, style score /25, ADMIN mode observations, missing sections, bugs, Phase 8 recommendations, GO/NO-GO. Same file deployed to `/opt/data/memories/proposals/<DENTAL_SLUG>/feedback.md` on server. TST-01 (2/3 niches), TST-02, TST-04 (ADMIN mode), TST-05 all satisfied for dental.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → Production container | Same as 07-02. Session ID `phase7-<DENTAL_SLUG>` is unique. ADMIN mode prompt is used (different from PRESALE in 07-02). |
| Orchestrator → External APIs | Same API cost surface as 07-02 (~$1-5 in Perplexity/Apify/Firecrawl calls). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-S | Spoofing | ssh aim (existing key) | accept | Existing access pattern |
| T-07-03-T | Tampering | Dental proposal HTML | accept | Test artifact, post-hoc verified |
| T-07-03-R | Repudiation | Run attribution | mitigate | Git-tracked + timestamped outputs |
| T-07-03-I | Info disclosure | Dental clinic data collected | accept | Public business info, no PII |
| T-07-03-D | DoS | 30-min orchestrator run | mitigate | Hard timeout, sequential execution (not parallel with 07-02) |
| T-07-03-E | EoP | Container process | accept | Same as production |
| T-07-03-SC | Supply chain | No new packages | accept | Stdlib + existing imports only |
</threat_model>

<verification>
**Post-plan automated checks:**

1. `ssh aim "docker exec aim-hermes sh -c 'ls /opt/data/memories/proposals/dental-*/proposal.html 2>/dev/null'"` exits 0 with file size >10,000 chars
2. `ssh aim "docker exec aim-hermes sh -c 'ls /opt/data/memories/proposals/dental-*/metadata.json 2>/dev/null'"` exits 0
3. `ssh aim "docker exec aim-hermes sh -c 'ls /opt/data/memories/proposals/dental-*/feedback.md 2>/dev/null'"` exits 0
4. `test -f .planning/phases/07-test-on-3-niches/07-03-SUMMARY.md` and contains outcome
5. `test -f .planning/phases/07-test-on-3-niches/07-03-feedback.md` and contains "Overall verdict" + "17-item denominator"
6. `ssh aim "docker ps | grep aim-hermes"` still shows healthy (no container regression)

**Regression checks:**
- Production presale flow unaffected: session ID isolated
- ORCHESTRATOR_MODE injection was per-exec, not permanent
- 07-02 plastic artifacts still intact (this plan does NOT touch plastic-iphk/ directory)
</verification>

<success_criteria>
- TST-01 (2/3 niches): dental presale completed — 2 of 3 niches tested
- TST-02 (QC scoring): 17-item checklist scored (item 4 not_applicable for dental)
- TST-04 (ADMIN mode): test invoked with `--mode ADMIN` — ADMIN code path exercised
- TST-05 (output format): `proposal.html` + `feedback.md` in `/opt/data/memories/proposals/<DENTAL_SLUG>/`
- Decision D-07 honored: ADMIN mode trigger via direct harness invocation (curl transport deferred to Phase 8 — same code path)
- Decision D-08 honored: 17-item conditional coverage logic applied correctly
- Decision D-11 honored: if FAIL, marked as KNOWN ISSUE, plan continues to 07-04
- 07-02 plastic artifacts unchanged (no regression)
</success_criteria>

<output>
Create `.planning/phases/07-test-on-3-niches/07-03-SUMMARY.md` when done. Record:
- Test outcome + duration + clinic name
- Coverage % + style score
- Niche detection result (dental vs unknown)
- ADMIN mode observations (vs PRESALE from 07-02)
- Top missing sections + bugs
- GO/NO-GO for dental niche
- Ready signal for Plan 07-04 (cosmetology + aggregate)
</output>
