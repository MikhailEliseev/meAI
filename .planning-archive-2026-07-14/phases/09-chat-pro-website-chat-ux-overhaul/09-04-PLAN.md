---
phase: 09-chat-pro-website-chat-ux-overhaul
plan: 04
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - AIM/hermes/app/agent_wrapper_optimized.py
  - /opt/data/services.md
  - AIM/hermes/app/tools/escalate_to_manager.py
  - /var/www/iamaim.ru/wp-content/themes/aim-theme/chat/chat.js
  - /var/www/iamaim.ru/wp-content/themes/aim-theme/chat/chat.css
autonomous: true
requirements: [CTX-01, CTX-02, SRV-01, SRV-02, SRV-03]
must_haves:
  truths:
    - Hermes requests contact info naturally in chat dialog (no forms)
    - Contact collection happens after wow-effect, before/after report link
    - Services catalogue exists and is readable by LLM
    - LLM matches findings to relevant AIM services
    - Personalized offer includes 3-5 services with prices
    - CTA button triggers escalate_to_manager tool
  artifacts:
    - path: /opt/data/services.md
      provides: AIM services catalogue with prices, descriptions, niches
      min_lines: 50
    - path: AIM/hermes/app/agent_wrapper_optimized.py
      provides: Contact collection + services sales assistant prompt
      contains: "collect_contact"
    - path: AIM/hermes/app/tools/escalate_to_manager.py
      provides: Manager escalation tool
      contains: "TELEGRAM_ADMIN_CHAT_ID"
  key_links:
    - from: _presale_prompt()
      to: collect_contact tool
      via: LLM decision
      pattern: "collect_contact.*contact_type"
    - from: LLM
      to: services.md
      via: skill_view or direct file read
      pattern: "services.*AIM.*price"
    - from: offer message
      to: escalate_to_manager tool
      via: CTA button
      pattern: "escalate_to_manager"

---

# Plan 09-04: Contact Collection + Services Sales Assistant

**Phase:** 9 (Chat Pro — Website Chat UX Overhaul)
**Effort:** M
**Risk:** Low

## Objective

Transform Hermes from passive data provider into an active sales assistant: (1) naturally request contact info in conversational dialog, (2) read AIM services catalogue, (3) semantically match analysis findings to relevant services, (4) generate personalized offers with 3-5 services + prices, (5) provide CTA for manager escalation.

**Purpose:** Close the sales loop — wow-effect → findings → contact → personalized offer → handoff to manager.

**Output:** Contact collection dialog flow + services catalogue + semantic matching + offer generation + manager escalation

## Execution Context

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-chat-pro-website-chat-ux-overhaul/09-CONTEXT.md

### Canonical References Read

- **Existing tool:** `AIM/hermes/app/tools/collect_contact.py` — already registered, accepts name/email/phone
- **Mode prompt:** `AIM/hermes/app/agent_wrapper_optimized.py` — _presale_prompt() defines behavior
- **PRESALE mode flow:** Lines 20-68 describe 3-phase presale (Фаза 1 хук → Фаза 2+3 фон)

### Key Design Decisions

Per D-21: Conversational dialog for contact collection (no forms)
Per D-22: Use existing `collect_contact` tool
Per D-23: Timing — after main analysis, before final offer
Per D-24: Fields — name (required), email (required), phone (optional)
Per D-25: Frontend validation — email regex, name not empty
Per D-26: Save to PostgreSQL via AIM API `/api/leads`
Per D-27: If refusal — continue without blocking, log the event
Per D-28: LLM reads `/opt/data/services.md` catalogue
Per D-29: Semantic matching — findings → relevant services
Per D-30: Offer generation — 3-5 services + prices
Per D-31: Consultative tone ("Мы можем помочь", not "Купите пакет X")
Per D-32: Timing — after wow-commentary, before report link
Per D-33: Offer structure — problems identified → solutions offered → CTA
Per D-34: CTA button "Обсудить с менеджером" → `escalate_to_manager` → Telegram notification

## Tasks

<task type="auto">
  <name>Task 1: Create AIM services catalogue</name>
  <files>/opt/data/services.md</files>
  <action>
    SSH to server and create `/opt/data/services.md` with AIM's service offerings:

    ```bash
    ssh aim
    cat > /opt/data/services.md << 'EOF'
    # AIM Services Catalogue

    ## SEO Optimization

    **Description:** Полный SEO-аудит + исправление технических ошибок + контент-оптимизация + линкбилдинг

    **What it solves:**
    - Низкие позиции в Яндекс/Google
    - Медленная загрузка сайта (Core Web Vitals)
    - Технические ошибки (индексация, структура)
    - Слабый контент (отсутствие ключевых слов)

    **Ideal for niches:** Все ниши медицины

    **Price:** от 180 000 ₽ (базовый пакет 3 месяца)

    **Deliverables:**
    - Технический аудит (50+ точек проверки)
    - План исправлений с приоритетами
    - Реализация исправлений (разработка)
    - Контент-план на 3 месяца
    - Ежемесячные отчёты по позициям

    ---

    ## Instagram Content Production

    **Description:** Производство контента для Instagram врачей — съёмка, монтаж, копирайт, дизайн, публикация

    **What it solves:**
    - Врачи без соцсетей (0 подписчиков)
    - Нерегулярный контент (публикации раз в месяц)
    - Низкий охват и вовлечённость
    - Отсутствие визуальной идентичности

    **Ideal for niches:** Косметология, пластическая хирургия, стоматология

    **Price:** от 120 000 ₽/месяц за 1 врача (12 постов + 8 сторис)

    **Deliverables:**
    - 12 постов в месяц (съёмка + монтаж)
    - 8 сторис в неделю
    - Копирайт для всех публикаций
    - Дизайн (единый стиль)
    - Ежемесячный отчёт по метрикам (охват, вовлечённость)

    ---

    ## Яндекс.Директ Campaign

    **Description:** Запуск и ведение рекламных кампаний в Яндекс.Директ с фокусом на медицину

    **What it solves:**
    - Нет входящих заявок
    - Высокая цена лида (>5000 ₽)
    - Реклама не окупается
    - Неправильные настройки таргетинга

    **Ideal for niches:** Все ниши медицины

    **Price:** от 80 000 ₽/месяц (ведение) + рекламный бюджет от 100 000 ₽

    **Deliverables:**
    - Настройка кампаний (поиск + РСЯ)
    - A/B тесты объявлений
    - Оптимизация ставок
    - Еженедельные отчёты по CTR, CPC, CPA
    - Рекомендации по посадочным страницам

    ---

    ## Technical Site Optimization

    **Description:** Ускорение сайта, исправление технических ошибок, улучшение Core Web Vitals

    **What it solves:**
    - Медленная загрузка (LCP >3s)
    - Низкие оценки Lighthouse (<70)
    - Проблемы индексации
    - Мобильная версия не работает

    **Ideal for niches:** Все ниши медицины

    **Price:** от 60 000 ₽ (разовая оптимизация)

    **Deliverables:**
    - Аудит производительности
    - Оптимизация изображений, скриптов, CSS
    - Настройка кеширования и CDN
    - Исправление ошибок индексации
    - Отчёт "до/после" с метриками

    ---

    ## Reputation Management

    **Description:** Управление репутацией — мониторинг отзывов, работа с негативом, генерация позитивных отзывов

    **What it solves:**
    - Негативные отзывы на Яндекс.Картах/2ГИС
    - Низкий рейтинг (<4.5)
    - Отсутствие отзывов (0-10 отзывов)
    - Конкуренты с лучшей репутацией

    **Ideal for niches:** Все ниши медицины

    **Price:** от 40 000 ₽/месяц

    **Deliverables:**
    - Мониторинг всех площадок (Яндекс, Google, 2ГИС, ПроДокторов)
    - Ответы на отзывы (позитивные + негативные)
    - Стратегия генерации отзывов
    - Ежемесячный отчёт по динамике рейтинга

    ---

    ## Content Strategy + Blogging

    **Description:** Разработка контент-стратегии + производство SEO-статей для блога клиники

    **What it solves:**
    - Пустой блог (0 статей)
    - Контент не отвечает на вопросы пациентов
    - Низкий органический трафик
    - Отсутствие экспертности

    **Ideal for niches:** Все ниши медицины

    **Price:** от 90 000 ₽/месяц (8 статей по 3000+ слов)

    **Deliverables:**
    - Контент-стратегия (анализ интента + страхов пациентов)
    - 8 SEO-статей в месяц
    - Оптимизация под ключевые запросы
    - Экспертные комментарии врачей
    - Ежемесячный отчёт по трафику и позициям

    ---

    ## Whitefields Analysis + Strategy

    **Description:** Глубокий анализ белых полей рынка + разработка стратегии их захвата

    **What it solves:**
    - Непонятно, где есть пробелы
    - Конкуренты заняли все ниши
    - Нет уникального позиционирования
    - Стратегия роста неочевидна

    **Ideal for niches:** Все ниши медицины

    **Price:** от 150 000 ₽ (разовый проект 2-3 недели)

    **Deliverables:**
    - Матрица белых полей (клиент vs топ-5 конкурентов)
    - 5-7 конкретных направлений роста
    - Стратегия захвата каждого поля
    - Roadmap на 6 месяцев
    - Презентация для команды клиники

    EOF
    ```

    This catalogue covers the 7 core AIM services with prices, problem-solution mapping, and ideal niches per D-28. LLM can read this file to generate personalized offers.
  </action>
  <verify>
    <automated>ssh aim "test -f /opt/data/services.md && wc -l /opt/data/services.md | awk '{print (\$1 >= 50) ? \"OK\" : \"FAIL\"}'"</automated>
    Should print "OK" (file exists with 50+ lines).
  </verify>
  <done>/opt/data/services.md exists with 7 AIM services: prices, descriptions, problem-solution mapping, ideal niches</done>
</task>

<task type="auto">
  <name>Task 2: Extend PRESALE prompt with contact collection + sales assistant</name>
  <files>AIM/hermes/app/agent_wrapper_optimized.py</files>
  <action>
    Locate the existing "### 📡 ФАЗА 2+3" section (lines ~61-68) and replace it with an expanded version:

    ```python
    ### 📡 ФАЗА 2 - Сбор контакта + Персонализированный оффер

    После Фазы 1 (quick_overview + run_prescan + wow-комментарии) — переходим к сбору контакта и формированию оффера.

    **Шаг 1: Сбор контакта (диалоговый флоу)**

    Когда клиент увидел wow-эффект и основные данные — запроси контакт НАТИВНО в чате:

    «Отлично! Я собрал базовую картину по вашей клинике. Хотите полный анализ конкурентов, Instagram врачей, SEO и стратегию? Это займёт час-полтора — соберу ВСЕ данные и пришлю готовую презентацию. Для этого мне нужен ваш email и имя. Как вас зовут?»

    После получения имени:
    «Спасибо, [Имя]! И ваш email, чтобы я мог отправить отчёт?»

    После получения email (опционально телефон):
    «Отлично, [Имя]! Email записал. Если хотите, можете оставить телефон — позвоним обсудить детали. Но это необязательно.»

    Когда все данные собраны — вызови `collect_contact`:
    ```
    collect_contact(
      name="[Имя клиента]",
      contact_type="email",
      contact_value="[email]",
      website="[URL клиники]",
      source="web_chat"
    )
    ```

    **Если клиент отказывается давать контакт:**
    Продолжай без блокировки. Скажи: «Понял, без проблем. Я всё равно могу показать основные находки прямо тут в чате.»

    **Шаг 2: Прочитай каталог услуг AIM**

    После `collect_contact` (или если клиент отказался) — прочитай `/opt/data/services.md`:

    - 7 услуг: SEO, Instagram, Яндекс.Директ, Technical Site, Reputation, Content Strategy, Whitefields Analysis
    - У каждой: описание, что решает, цена, идеальные ниши

    **Шаг 3: Semantic Matching — находки → услуги**

    Проанализируй собранные данные и сопоставь с услугами:

    | Находка | Релевантная услуга |
    |---------|-------------------|
    | Медленный сайт (LCP >3s, Lighthouse <70) | Technical Site Optimization (60K ₽) |
    | Низкие позиции SEO, мало контента | SEO Optimization (180K ₽) |
    | Врачи без Instagram или <1000 подписчиков | Instagram Content Production (120K ₽/мес) |
    | Нет рекламы или высокая цена лида | Яндекс.Директ Campaign (80K ₽/мес) |
    | Негативные отзывы, низкий рейтинг | Reputation Management (40K ₽/мес) |
    | Пустой блог, нет экспертного контента | Content Strategy + Blogging (90K ₽/мес) |
    | Непонятно где расти, конкуренты везде | Whitefields Analysis (150K ₽) |

    Выбери **3-5 наиболее релевантных услуг** на основе критичности пробелов.

    **Шаг 4: Генерация персонализированного оффера**

    Формат (Per D-33):

    «[Имя], вот что я вижу:

    **Проблемы:**
    - [Пробел 1 с конкретными цифрами]
    - [Пробел 2 с конкретными цифрами]
    - [Пробел 3 с конкретными цифрами]

    **Что AIM может сделать:**

    1️⃣ **[Услуга 1]** — [цена] — [что даёт конкретно для клиники]

    2️⃣ **[Услуга 2]** — [цена] — [что даёт конкретно для клиники]

    3️⃣ **[Услуга 3]** — [цена] — [что даёт конкретно для клиники]

    Хотите обсудить детали с нашим менеджером? Он расскажет точнее про сроки и результаты.»

    **Tone (Per D-31):**
    Консультативный, не агрессивный. «Мы можем помочь решить», не «Купите пакет X». Как маркетинг-партнёр, не продавец.

    **Шаг 5: CTA — Escalate to Manager**

    После оффера — предложи кнопку:
    «[Кнопка: Обсудить с менеджером]»

    Если клиент нажимает — вызови `escalate_to_manager`:
    ```
    escalate_to_manager(
      lead_info="[Имя, email, телефон если есть]",
      clinic_name="[название]",
      website="[URL]",
      summary="[краткое резюме: 2-3 пробела + 3-5 услуг]"
    )
    ```

    Это отправит уведомление Михаилу в Telegram с контактом клиента + резюме.

    ### 📡 ФАЗА 3 - Фоновый пайплайн (опционально)

    Если клиент хочет ПОЛНЫЙ отчёт (все 10 секций, как ИПХиК) — после `collect_contact` можешь вызвать `run_background_pipeline` с session_hash. Это запустит 3-pass orchestrator в фоне на 1-2 часа. Клиент получит ссылку на отчёт по email.

    Но для большинства клиентов достаточно Фазы 1+2 (wow-эффект + оффер + handoff).
    ```

    Replace the existing "### 📡 ФАЗА 2+3" section entirely with this expanded version. This integrates contact collection (D-21..27) and services sales assistant (D-28..34).
  </action>
  <verify>
    <automated>grep -c "collect_contact.*name=\|services.md\|escalate_to_manager" AIM/hermes/app/agent_wrapper_optimized.py</automated>
    Should return at least 3 (contact collection + services + escalation mentioned).
  </verify>
  <done>_presale_prompt() includes contact collection dialog flow, services.md reading, semantic matching table, personalized offer generation, and manager escalation CTA</done>
</task>

<task type="auto">
  <name>Task 3: Create escalate_to_manager tool</name>
  <files>AIM/hermes/app/tools/escalate_to_manager.py</files>
  <action>
    Create a new tool that sends a Telegram notification to admin when client wants to talk to manager:

    ```python
    """
    escalate_to_manager — Hermes tool: Escalate Lead to Manager

    Sends Telegram notification to Mikhail with client contact + summary.

    Per D-34: CTA button "Обсудить с менеджером" triggers this tool.
    """

    import json
    import logging
    import os
    import httpx
    from tools.registry import registry

    logger = logging.getLogger(__name__)

    TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN", "")
    TELEGRAM_ADMIN_CHAT_ID = os.getenv("TELEGRAM_ADMIN_CHAT_ID", "")


    async def handle_escalate_to_manager(
        lead_info: str,
        clinic_name: str,
        website: str,
        summary: str,
        **kwargs
    ) -> str:
        """Escalate lead to manager via Telegram notification.

        Args:
            lead_info: Contact info (name, email, phone)
            clinic_name: Clinic name
            website: Clinic URL
            summary: Brief summary of findings + offered services

        Returns:
            JSON with status
        """
        if not TELEGRAM_BOT_TOKEN or not TELEGRAM_ADMIN_CHAT_ID:
            logger.error("Telegram credentials missing, cannot escalate")
            return json.dumps({"error": "Telegram not configured"})

        try:
            # Build notification message
            message = f"""🔥 <b>Новый горячий лид!</b>

    <b>Клиника:</b> {clinic_name}
    <b>Сайт:</b> {website}

    <b>Контакт:</b>
    {lead_info}

    <b>Резюме:</b>
    {summary}

    <i>Клиент хочет обсудить детали. Свяжись в течение 15 минут!</i>
    """

            # Send to Telegram
            async with httpx.AsyncClient(timeout=10.0) as client:
                response = await client.post(
                    f"https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN}/sendMessage",
                    json={
                        "chat_id": TELEGRAM_ADMIN_CHAT_ID,
                        "text": message,
                        "parse_mode": "HTML"
                    }
                )
                response.raise_for_status()

                logger.info("Lead escalated to manager: %s", clinic_name)
                return json.dumps({
                    "status": "escalated",
                    "clinic": clinic_name,
                    "message": "Менеджер получил уведомление и свяжется с вами в ближайшее время"
                }, ensure_ascii=False)

        except httpx.HTTPStatusError as e:
            logger.error("Telegram API error: %s", e)
            return json.dumps({"error": "Failed to send notification", "detail": str(e)})
        except Exception as e:
            logger.exception("Escalation failed")
            return json.dumps({"error": str(e)})


    registry.register(
        name="escalate_to_manager",
        toolset="aim-operations",
        schema={
            "name": "escalate_to_manager",
            "description": "Escalate lead to manager — sends Telegram notification to Mikhail",
            "parameters": {
                "type": "object",
                "properties": {
                    "lead_info": {
                        "type": "string",
                        "description": "Contact info: name, email, phone"
                    },
                    "clinic_name": {
                        "type": "string",
                        "description": "Clinic name"
                    },
                    "website": {
                        "type": "string",
                        "description": "Clinic website URL"
                    },
                    "summary": {
                        "type": "string",
                        "description": "Brief summary: findings + offered services"
                    }
                },
                "required": ["lead_info", "clinic_name", "website", "summary"]
            }
        },
        handler=handle_escalate_to_manager,
        check_fn=lambda: True,
        is_async=True,
        description="Escalate lead to manager via Telegram",
        emoji="🔥"
    )
    ```

    This creates the manager escalation flow per D-34. When LLM calls this tool, Mikhail receives a Telegram notification with all context needed to follow up.
  </action>
  <verify>
    <automated>grep -c "TELEGRAM_ADMIN_CHAT_ID\|escalate_to_manager" AIM/hermes/app/tools/escalate_to_manager.py</automated>
    Should return at least 2 (env var + function name).
  </verify>
  <done>escalate_to_manager tool created with Telegram notification to admin, HTML-formatted message with lead context</done>
</task>

<task type="auto">
  <name>Task 4: Add frontend validation for contact collection</name>
  <files>/var/www/iamaim.ru/wp-content/themes/aim-theme/chat/chat.js, /var/www/iamaim.ru/wp-content/themes/aim-theme/chat/chat.css</files>
  <action>
    BLOCKER 4 FIX: Per D-25, add email regex and name validation.
    
    SSH and edit chat.js. Add validation functions:
    function isValidEmail(email) { return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email); }
    function isValidName(name) { return name && name.trim().length > 0; }
    function showValidationError(msg) { /* display error bubble */ }
    
    Validate before sending to backend:
    - Email must match regex
    - Name non-empty after trim
    - Show error bubble if invalid
    - Block send if validation fails
    
    Add error bubble CSS in chat.css:
    .message.error-message { background: rgba(220,38,38,0.1); border-left: 3px solid red; }
  </action>
  <verify>
    <automated>ssh aim "grep -c isValidEmail /var/www/iamaim.ru/wp-content/themes/aim-theme/chat/chat.js"</automated>
  </verify>
  <done>Frontend validation added for email and name</done>
</task>


## Threat Model

| Boundary | Description |
|----------|-------------|
| Client → Hermes | Contact info (name, email, phone) transmitted via chat |
| Hermes → PostgreSQL | Contact stored in leads table via AIM API |
| Hermes → Telegram | Manager notification with contact info |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-16 | Spoofing | Client identity | accept | No authentication required for presale (public chat) |
| T-09-17 | Tampering | Contact data | mitigate | Validate email format (D-25), sanitize before DB insert |
| T-09-18 | Information Disclosure | Contact info in Telegram | accept | Admin-only chat, HTTPS, acceptable risk |
| T-09-19 | Denial of Service | Spam contact collection | mitigate | Rate limit presale flow (1 session per IP per hour via nginx) |
| T-09-20 | Elevation of Privilege | N/A | accept | No privilege boundaries in contact collection |

## Verification

**Manual check after deployment:**

1. Deploy to server:
   ```bash
   ssh aim
   # services.md already created in Task 1
   docker cp AIM/hermes/app/agent_wrapper_optimized.py aim-hermes:/opt/hermes/app/
   docker cp AIM/hermes/app/tools/escalate_to_manager.py aim-hermes:/opt/hermes/app/tools/
   docker exec aim-hermes supervisorctl restart gateway
   ```

2. Test services catalogue:
   ```bash
   ssh aim "cat /opt/data/services.md | head -50"
   # Verify 7 services listed with prices
   ```

3. End-to-end presale flow:
   - Open iamaim.ru chat
   - Send clinic URL
   - Observe Фаза 1 (quick_overview → run_prescan → wow-comments)
   - Wait for contact request: "Как вас зовут?"
   - Provide name: "Михаил"
   - Observe: "И ваш email?"
   - Provide email: "test@example.com"
   - Observe: "Отлично, Михаил! Email записал."

4. Check contact saved:
   ```bash
   ssh aim "docker exec aim-postgres psql -U aim -d aim -c 'SELECT name, email, website FROM leads ORDER BY created_at DESC LIMIT 1;'"
   ```

5. Check personalized offer generation:
   - After contact collection, observe LLM message with:
     - **Проблемы:** list with 3 concrete gaps
     - **Что AIM может сделать:** 3-5 services with prices
     - Consultative tone ("Мы можем помочь")
     - CTA: "Хотите обсудить детали с нашим менеджером?"

6. Test manager escalation:
   - Click "Обсудить с менеджером" (or type it)
   - Check Telegram admin chat for notification:
     - Contains clinic name, website, contact info
     - Contains summary of findings + offered services
     - Message format: HTML with bold headers

7. Check services semantic matching:
   - For clinic with slow site → should offer Technical Site Optimization
   - For clinic with no Instagram → should offer Instagram Content Production
   - For clinic with SEO issues → should offer SEO Optimization

## Success Criteria

- [ ] services.md exists with 7 AIM services (prices, descriptions, niches)
- [ ] Contact collection happens naturally in chat dialog (no forms)
- [ ] LLM requests name → email → phone (optional)
- [ ] collect_contact tool saves to PostgreSQL via AIM API
- [ ] LLM reads services.md after contact collection
- [ ] Semantic matching works (findings → relevant services)
- [ ] Personalized offer includes 3-5 services with prices
- [ ] Offer uses consultative tone (not aggressive sales)
- [ ] CTA button triggers escalate_to_manager tool
- [ ] Manager receives Telegram notification with full context
- [ ] If client refuses contact — flow continues without blocking

## Output

Create `.planning/phases/09-chat-pro-website-chat-ux-overhaul/09-04-SUMMARY.md` when done.
