---
phase: 06-documentation-sync
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - AIM/hermes/app/pipeline/test_engine_handlers.py
  - .planning/phases/06-documentation-sync/06-PHANTOM-PHASE-AUDIT.md
autonomous: true
requirements: [SYN-04, SYN-05]
user_setup: []

must_haves:
  truths:
    - "A pytest-runnable assertion test exists at AIM/hermes/app/pipeline/test_engine_handlers.py that asserts _TOOL_HANDLERS has >= 26 entries"
    - "The assertion test runs successfully in the aim-hermes container Python 3.11 environment"
    - "A phantom phase audit report exists at .planning/phases/06-documentation-sync/06-PHANTOM-PHASE-AUDIT.md documenting grep results for 0.5/0.75/0.8/3.2 across all documentation files"
    - "The audit report confirms zero phantom phase ID occurrences in any current documentation file (SOUL.md, SKILL.md, phases.py, PROJECT.md, RESEARCH.md if exists)"
    - "Assertion test deployed to container at /opt/hermes/app/pipeline/test_engine_handlers.py"
    - "Container remains Up and healthy after deploy"
  artifacts:
    - path: "AIM/hermes/app/pipeline/test_engine_handlers.py"
      provides: "Pytest module asserting _TOOL_HANDLERS count >= 26 to prevent future tool additions from forgetting to register handlers"
      contains: "_TOOL_HANDLERS"
    - path: ".planning/phases/06-documentation-sync/06-PHANTOM-PHASE-AUDIT.md"
      provides: "Grep audit report documenting the absence (or presence) of phantom phase IDs across all documentation files"
      contains: "phantom"
  key_links:
    - from: "test_engine_handlers.py assertion"
      to: "app/pipeline/engine.py _TOOL_HANDLERS"
      via: "pytest import + length assertion"
      pattern: "len\\(_TOOL_HANDLERS\\)"
---

<objective>
Create a pytest-runnable assertion test that guards against future tool-handler registration drift (per D-08), and produce a phantom phase grep audit report (per D-07, D-10) documenting the absence of phantom phase IDs across all documentation files. Deploy the test to the container.

Purpose: After Phases 2-5 added 4 new tools to _TOOL_HANDLERS (run_instagram_content, find_doctor_handles, run_forum_pains, run_media_urls — bringing the total from 22 to 26), there is no automated guard preventing future tool additions from forgetting to register handlers. This plan adds that guard. Additionally, while PROJECT.md mentions phantom phases 0.5/0.75/0.8/3.2 from the unused v3 server backup, no audit has confirmed they are absent from all current documentation. This plan performs that audit and records the result.

Output: One new pytest file (~50-80 lines), one audit report (~30-50 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation-sync/06-CONTEXT.md
@.planning/phases/06-documentation-sync/06-01-PLAN.md

# Source-of-truth code file the test asserts against
@AIM/hermes/app/pipeline/engine.py

# Existing test pattern to follow (co-located test convention)
@AIM/hermes/app/orchestrator/test_conditional_coverage.py

<interfaces>
<!-- The contract the assertion test verifies. -->

From AIM/hermes/app/pipeline/engine.py:
- _TOOL_HANDLERS: dict[str, tuple[str, str]] (line 41)
- Currently 26 entries (verified via server-side `python -c 'from app.pipeline.engine import _TOOL_HANDLERS; print(len(_TOOL_HANDLERS))'` → "26")
- Each entry maps tool_name → (module_path, handler_function_name)
- Both orchestrator and PipelineEngine share this registry

Expected minimum count for the assertion:
- After Phase 4 deploy: 26 (current)
- The assertion should enforce >= 26 to allow growth but catch regressions

Existing test pattern at AIM/hermes/app/orchestrator/test_conditional_coverage.py:
- Uses standard pytest with plain assert statements
- Module-level test functions: test_<scenario>()
- No external fixtures required
- Runs in <1 second
</interfaces>

<deploy_pattern>
# Deploy test file via pipe-through-stdin
cat AIM/hermes/app/pipeline/test_engine_handlers.py | ssh aim "docker exec -i aim-hermes tee /opt/hermes/app/pipeline/test_engine_handlers.py" > /dev/null

# Run test inside container
ssh aim "docker exec aim-hermes python -m pytest /opt/hermes/app/pipeline/test_engine_handlers.py -v"
</deploy_pattern>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_engine_handlers.py with _TOOL_HANDLERS count assertion (per D-08)</name>
  <files>AIM/hermes/app/pipeline/test_engine_handlers.py</files>
  <behavior>
    - File imports _TOOL_HANDLERS from app.pipeline.engine
    - Test 1 (test_tool_handlers_count_at_least_26): asserts len(_TOOL_HANDLERS) >= 26
    - Test 2 (test_tool_handlers_values_are_tuples): asserts every value is a 2-tuple of (module_path, handler_function_name) strings
    - Test 3 (test_critical_tools_present): asserts specific Phase 3-5 tool names are present (run_instagram_content, find_doctor_handles, run_forum_pains, run_media_urls)
    - Test 4 (test_handler_modules_importable): asserts every module_path in values can be imported (catches registration without implementation)
    - All 4 tests pass when run with `python -m pytest` in the container
  </behavior>
  <action>
    Create a new file at AIM/hermes/app/pipeline/test_engine_handlers.py following the established pattern from AIM/hermes/app/orchestrator/test_conditional_coverage.py (module-level test functions, standard pytest, no fixtures).

    The file should contain:

    1. Module docstring explaining the test's purpose: guard against tool-handler registration drift. Reference Phase 6 Plan 06-03 D-08. Note that _TOOL_HANDLERS grew from 22 (Phase 1 baseline) to 26 (after Phase 4 added run_forum_pains + run_media_urls and Phase 3 added run_instagram_content + find_doctor_handles).

    2. Imports:
    - from app.pipeline.engine import _TOOL_HANDLERS
    - import importlib
    - import pytest

    3. Test functions (4 total):

    test_tool_handlers_count_at_least_26:
    - Asserts len(_TOOL_HANDLERS) >= 26
    - Failure message includes actual count and the Phase 6 historical context

    test_tool_handlers_values_are_tuples:
    - Iterates over _TOOL_HANDLERS.values()
    - Asserts each value is a tuple of length 2
    - Asserts both elements are str instances
    - Failure message identifies the offending key

    test_critical_tools_present:
    - Defines expected_tools = {"run_instagram_content", "find_doctor_handles", "run_forum_pains", "run_media_urls"} (the 4 Phase 3-4 additions)
    - Asserts expected_tools.issubset(_TOOL_HANDLERS.keys())
    - Failure message lists which expected tools are missing

    test_handler_modules_importable:
    - For each (module_path, handler_function_name) in _TOOL_HANDLERS.values():
      - importlib.import_module(module_path)
      - getattr(module, handler_function_name)
    - Catches ImportError and AttributeError with descriptive failure messages
    - Uses pytest.mark.slow marker or similar to allow skipping in CI if imports are slow (but in practice each import takes <100ms due to lazy loading)

    4. Module-level constant: MIN_HANDLERS = 26 (used by test 1, makes future bumps easy).

    CRITICAL RULES:
    - Do NOT modify engine.py to make tests pass. The test verifies existing behavior.
    - Follow the project naming convention: test_<module>_<behavior>.
    - Use logger via logging.getLogger(__name__) at module level if any diagnostic logging is needed.
    - File should be 50-80 lines total — concise, focused on the 4 behaviors.
  </action>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && test -f AIM/hermes/app/pipeline/test_engine_handlers.py && echo "PASS: file exists" || echo "FAIL: file missing"</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -c "def test_" AIM/hermes/app/pipeline/test_engine_handlers.py | awk '{if ($1 == 4) {print "PASS: 4 test functions"} else {print "FAIL: "$1" test functions (expected 4)"}}'</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -c "_TOOL_HANDLERS" AIM/hermes/app/pipeline/test_engine_handlers.py | awk '{if ($1 >= 3) {print "PASS: "$1" _TOOL_HANDLERS references"} else {print "FAIL: only "$1" _TOOL_HANDLERS references"}}'</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && python3 -c "import ast; ast.parse(open('AIM/hermes/app/pipeline/test_engine_handlers.py').read()); print('PASS: local AST parse OK')"</automated>
  </verify>
  <done>
    test_engine_handlers.py created with 4 test functions covering: handler count >= 26, value tuple structure, critical Phase 3-5 tools present, handler modules importable. Local AST parse passes. File follows the test_conditional_coverage.py pattern (module-level functions, standard pytest).
  </done>
</task>

<task type="auto">
  <name>Task 2: Run phantom phase grep audit across all documentation files (per D-07, D-10)</name>
  <files>.planning/phases/06-documentation-sync/06-PHANTOM-PHASE-AUDIT.md</files>
  <action>
    Perform a comprehensive grep audit for phantom phase IDs across all documentation files in the project. Produce a markdown report at .planning/phases/06-documentation-sync/06-PHANTOM-PHASE-AUDIT.md documenting the findings.

    Audit scope (search these files for phantom phase patterns):
    1. AIM/hermes/skills/aim/SOUL.md (updated by Plan 06-01)
    2. AIM/hermes/skills/aim-scout/SKILL.md (updated by Plan 06-02)
    3. AIM/hermes/app/pipeline/phases.py (updated by Plan 06-02)
    4. AIM/hermes/app/pipeline/engine.py (not modified in Phase 6 but include in audit)
    5. AIM/hermes/skills/aim/SOUL.backup.md (v3 backup — historical reference)
    6. .planning/PROJECT.md
    7. .planning/ROADMAP.md
    8. .planning/STATE.md
    9. .planning/REQUIREMENTS.md
    10. Server-side /opt/data/SOUL.md (via docker exec grep)
    11. Server-side /opt/hermes/skills/aim-scout/SKILL.md (via docker exec grep)

    For each file, run these grep patterns and record the match count:
    - Pattern 1: `\b0\.5\b` (word-boundary to avoid matching version numbers like "10.5")
    - Pattern 2: `\b0\.75\b`
    - Pattern 3: `\b0\.8\b`
    - Pattern 4: `\b3\.2\b`
    - Pattern 5: `Phase 0\.5|Фаза 0\.5|фаза 0\.5` (phase-context-specific matches)
    - Pattern 6: `Phase 0\.75|Фаза 0\.75|фаза 0\.75`
    - Pattern 7: `Phase 0\.8|Фаза 0\.8|фаза 0\.8`
    - Pattern 8: `Phase 3\.2|Фаза 3\.2|фаза 3\.2`

    Use the Grep tool (not bash grep) for local files. For server files, use `ssh aim "docker exec aim-hermes grep -cE '...' <path>"`.

    After collecting all grep counts, write the audit report with these sections:

    Section 1 — Audit Summary:
    - Total files audited: 11
    - Total phantom phase matches: <sum>
    - Verdict: PASS (if zero phase-context matches) or FAIL (if any phase-context matches in current docs)
    - Note: matches in SOUL.backup.md (historical v3) are EXPECTED and don't count as failures — that file is explicitly a backup and out of runtime scope.

    Section 2 — Detailed Results Table:
    Markdown table with columns: File, Pattern, Match Count, Verdict.
    One row per (file, pattern) combination.

    Section 3 — Context-Verified Findings:
    For any non-zero match, quote the matching line and explain whether it's a real phantom phase (FAIL) or a false positive (e.g., decimal in a CSS value, IP address segment, version number).

    Section 4 — Cross-Reference with Plan 06-01 and 06-02:
    - Confirm Plan 06-01 SOUL.md rewrite removed (or never had) phantom phases
    - Confirm Plan 06-02 SKILL.md rewrite removed (or never had) phantom phases
    - Confirm Plan 06-02 phases.py docstring update has no phantom phases
    - If any of these confirmations fail, document the residual issue for follow-up

    Section 5 — Recommendations:
    - If PASS: recommend periodic re-audit cadence (e.g., before each phase transition)
    - If FAIL: list specific files and lines requiring cleanup

    The audit report itself is the deliverable. It does not modify any source files — only documents the verification.
  </action>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && test -f .planning/phases/06-documentation-sync/06-PHANTOM-PHASE-AUDIT.md && echo "PASS: audit report exists" || echo "FAIL: audit report missing"</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && wc -l .planning/phases/06-documentation-sync/06-PHANTOM-PHASE-AUDIT.md | awk '{if ($1 >= 30) {print "PASS: audit report "$1" lines"} else {print "FAIL: audit report "$1" lines (expected >= 30)"}}'</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -cE "^## " .planning/phases/06-documentation-sync/06-PHANTOM-PHASE-AUDIT.md | awk '{if ($1 >= 5) {print "PASS: "$1" sections"} else {print "FAIL: only "$1" sections (expected 5)"}}'</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -cE "(PASS|FAIL)" .planning/phases/06-documentation-sync/06-PHANTOM-PHASE-AUDIT.md | awk '{if ($1 >= 1) {print "PASS: verdict present"} else {print "FAIL: no verdict"}}'</automated>
  </verify>
  <done>
    06-PHANTOM-PHASE-AUDIT.md report created with 5 sections covering 11 files × 8 patterns. Verdict (PASS/FAIL) documented. Any non-zero matches explained as real phantom phase or false positive. Cross-references to Plan 06-01 and 06-02 confirmations included.
  </done>
</task>

<task type="auto">
  <name>Task 3: Deploy test_engine_handlers.py to container and run pytest inside container Python 3.11</name>
  <files>(no local file changes — deploy only)</files>
  <action>
    Deploy test_engine_handlers.py from Task 1 to the aim-hermes container and run the test suite inside the container Python 3.11 environment.

    Step 1: Deploy test file via pipe-through-stdin.
    Command: cat AIM/hermes/app/pipeline/test_engine_handlers.py | ssh aim "docker exec -i aim-hermes tee /opt/hermes/app/pipeline/test_engine_handlers.py" > /dev/null

    Step 2: Verify byte-for-byte match.
    Command: md5 -q AIM/hermes/app/pipeline/test_engine_handlers.py vs ssh aim "docker exec aim-hermes md5sum /opt/hermes/app/pipeline/test_engine_handlers.py"

    Step 3: Run pytest inside the container Python 3.11 environment.
    Command: ssh aim "docker exec aim-hermes python -m pytest /opt/hermes/app/pipeline/test_engine_handlers.py -v"
    Expected: 4 tests pass. If any test fails, investigate and fix the test (NOT the engine.py — engine.py is the source of truth per D-01). Capture the pytest output and include in the SUMMARY.

    Step 4: Run the same pytest with coverage to verify all 26 handler modules are importable (this is what test_handler_modules_importable verifies).
    This is implicitly tested by Step 3 — if test 4 passes, all 26 modules import successfully.

    Step 5: Container health check.
    Command: ssh aim "docker exec aim-hermes curl -sf http://127.0.0.1:8000/health"

    Step 6: Final Phase 6 verification — confirm orchestrator imports still work after all Phase 6 changes.
    Command: ssh aim "docker exec aim-hermes python -c 'from app.orchestrator.three_pass import run_three_pass; from app.orchestrator.qc_checklist import QC_CHECKLIST, VERSION; from app.pipeline.engine import _TOOL_HANDLERS; from app.pipeline.phases import PHASES; print(f\"orchestrator=OK qc_version={VERSION} handlers={len(_TOOL_HANDLERS)} phases={len(PHASES)}\")'"
    Expected output: orchestrator=OK qc_version=1.2.0 handlers=26 phases=14

    IMPORTANT: Do NOT restart the container. Test file is loaded by pytest on demand — no restart needed.
  </action>
  <verify>
    <automated>LOCAL=$(md5 -q /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/app/pipeline/test_engine_handlers.py) && REMOTE=$(ssh aim "docker exec aim-hermes md5sum /opt/hermes/app/pipeline/test_engine_handlers.py" | awk '{print $1}') && if [ "$LOCAL" = "$REMOTE" ]; then echo "PASS: test file match ($LOCAL)"; else echo "FAIL: local=$LOCAL remote=$REMOTE"; fi</automated>
  </verify>
  <verify>
    <automated>ssh aim "docker exec aim-hermes python -m pytest /opt/hermes/app/pipeline/test_engine_handlers.py -v" 2>&1 | tail -10 | grep -qE "4 passed|passed in" && echo "PASS: pytest 4 tests passed" || echo "FAIL: pytest did not pass 4 tests"</automated>
  </verify>
  <verify>
    <automated>ssh aim "docker exec aim-hermes curl -sf http://127.0.0.1:8000/health" | grep -qiE "(ok|healthy|status)" && echo "PASS: container healthy" || echo "FAIL: container health check failed"</automated>
  </verify>
  <verify>
    <automated>ssh aim "docker exec aim-hermes python -c 'from app.orchestrator.three_pass import run_three_pass; from app.orchestrator.qc_checklist import QC_CHECKLIST, VERSION; from app.pipeline.engine import _TOOL_HANDLERS; from app.pipeline.phases import PHASES; print(f\"orchestrator=OK qc_version={VERSION} handlers={len(_TOOL_HANDLERS)} phases={len(PHASES)}\")'" | grep -q "handlers=26 phases=14" && echo "PASS: Phase 6 final integration OK" || echo "FAIL: integration check did not return expected values"</automated>
  </verify>
  <done>
    test_engine_handlers.py deployed to /opt/hermes/app/pipeline/test_engine_handlers.py. Container Python 3.11 pytest runs all 4 tests successfully. Container health check passes. Final Phase 6 integration check confirms: orchestrator imports OK, QC_CHECKLIST version 1.2.0, _TOOL_HANDLERS has 26 entries, PHASES has 14 entries.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → SSH → aim-hermes container | Test file crosses trust boundary via pipe-through-stdin |
| pytest execution inside container | Test imports engine.py + dynamically imports 26 handler modules — broad code-execution surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10-T | Tampering | test_engine_handlers.py deploy path | mitigate | Explicit path `/opt/hermes/app/pipeline/test_engine_handlers.py`; md5 byte-for-byte verify; test is read-only relative to engine.py (assertion only, no mutation) |
| T-06-11-E | Elevation of Privilege | pytest dynamically imports 26 handler modules | accept | Each handler module is already trusted (deployed via prior phases). The test does not invoke handlers, only verifies they are importable. No new code execution paths beyond what already exists |
| T-06-12-D | DoS | test_handler_modules_importable could be slow | mitigate | Each handler module uses lazy imports of heavy dependencies (httpx, etc.) at function-call time, not module-import time. Module import is fast (<100ms each). Test completes in <5 seconds total |
| T-06-13-I | Info Disclosure | Audit report may quote documentation content | accept | All audited files are project-internal documentation; quoting snippets in an audit report is the intended purpose |
| T-06-14-T | Tampering | Audit report conclusions | mitigate | Report includes raw grep counts (objective evidence) alongside any interpretation. Verdict derived from counts, not subjective judgment |
| T-06-SC | Tampering | No new packages installed | N/A | Python test module uses only stdlib (importlib, logging) and pytest (already in container). No supply chain risk |
</threat_model>

<verification>
After all three tasks complete:

1. AIM/hermes/app/pipeline/test_engine_handlers.py exists with 4 test functions.
2. Local AST parse of test_engine_handlers.py passes.
3. Local md5 matches server md5 for test_engine_handlers.py.
4. `python -m pytest /opt/hermes/app/pipeline/test_engine_handlers.py -v` inside container shows 4 tests pass.
5. .planning/phases/06-documentation-sync/06-PHANTOM-PHASE-AUDIT.md exists with 5 sections and a verdict.
6. Audit report documents grep results for 11 files × 8 patterns.
7. Container `/health` returns 200.
8. Final Phase 6 integration command returns `orchestrator=OK qc_version=1.2.0 handlers=26 phases=14`.
9. Container remains Up throughout — no restart required.
</verification>

<success_criteria>
- Assertion test guards against future _TOOL_HANDLERS regressions (SYN-04 satisfied — engine.py _TOOL_HANDLERS includes all tools LLM can call, with automated enforcement)
- Phantom phase audit report documents the verification of zero phantom phases in current documentation (SYN-05 satisfied — the grep audit provides objective evidence)
- Test runs successfully in container Python 3.11 (D-08 satisfied)
- Phase 6 final integration confirms all Phase 2-5 modules import correctly after Phase 6 changes (no regressions introduced by documentation sync)
</success_criteria>

<output>
Create `.planning/phases/06-documentation-sync/06-03-SUMMARY.md` when done.
</output>
