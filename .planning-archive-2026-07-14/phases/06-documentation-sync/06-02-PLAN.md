---
phase: 06-documentation-sync
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - AIM/hermes/skills/aim-scout/SKILL.md
  - AIM/hermes/app/pipeline/phases.py
autonomous: true
requirements: [SYN-01, SYN-03, SYN-04]
user_setup: []

must_haves:
  truths:
    - "aim-scout SKILL.md describes 3-pass orchestrator as the primary mode (ORCHESTRATOR_MODE=1) and PipelineEngine as fallback (ORCHESTRATOR_MODE=0 or unset)"
    - "aim-scout SKILL.md contains zero occurrences of the phrase 'FULL AUTO' (the v1.0.0 framing that contradicts the QC-gated orchestrator model)"
    - "aim-scout SKILL.md references the 18-item QC checklist as the coverage metric"
    - "aim-scout SKILL.md lists the actual 26 _TOOL_HANDLERS entries grouped by category"
    - "phases.py module docstring contains the marker 'LEGACY' or 'ORCHESTRATOR_MODE=0' indicating fallback-only status"
    - "phases.py PHASES list (14 entries, 0-13) is unchanged in structure — only docstring updated"
    - "Server /opt/hermes/skills/aim-scout/SKILL.md matches repo byte-for-byte after deploy"
    - "Server /opt/hermes/app/pipeline/phases.py matches repo byte-for-byte after deploy (PipelineEngine Python file, not the LLM skill)"
    - "Container remains Up and healthy after deploy"
  artifacts:
    - path: "AIM/hermes/skills/aim-scout/SKILL.md"
      provides: "aim-scout skill describing 3-pass orchestrator primary + PipelineEngine fallback, with 26-tool catalogue and QC checklist reference"
      contains: "3-pass"
    - path: "AIM/hermes/app/pipeline/phases.py"
      provides: "PipelineEngine phase definitions with LEGACY marker in docstring (per D-06)"
      contains: "LEGACY"
  key_links:
    - from: "SKILL.md primary mode description"
      to: "app/orchestrator/three_pass.py run_three_pass"
      via: "primary execution path when ORCHESTRATOR_MODE=1"
      pattern: "ORCHESTRATOR_MODE"
    - from: "SKILL.md fallback mode description"
      to: "app/pipeline/engine.py PipelineEngine.execute"
      via: "fallback execution path when ORCHESTRATOR_MODE unset"
      pattern: "PipelineEngine"
    - from: "SKILL.md tool catalogue"
      to: "app/pipeline/engine.py _TOOL_HANDLERS"
      via: "tool handler registry (26 entries)"
      pattern: "_TOOL_HANDLERS"
---

<objective>
Rewrite aim-scout SKILL.md to describe the actual 3-pass orchestrator + PipelineEngine dual-mode architecture (per D-04, D-05) and add a LEGACY marker to phases.py docstring (per D-06). Deploy both files to the aim-hermes container.

Purpose: The current aim-scout SKILL.md (131 lines, v1.0.0) describes PipelineEngine as the ONLY execution mode and uses "FULL AUTO MODE" framing that directly contradicts the 3-pass orchestrator with QC checklist model that Phase 2 implemented. The `ORCHESTRATOR_MODE=1` switch is missing entirely. This plan brings SKILL.md into sync with the actual code, and marks phases.py as the fallback path so future readers understand the orchestrator is primary.

Output: One rewritten SKILL.md (~150-200 lines), one updated phases.py docstring (only the module-level docstring changes, no code changes), both deployed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation-sync/06-CONTEXT.md
@.planning/phases/06-documentation-sync/06-01-PLAN.md

# Source-of-truth code files (read these to mirror accurately)
@AIM/hermes/app/orchestrator/three_pass.py
@AIM/hermes/app/orchestrator/qc_checklist.py
@AIM/hermes/app/pipeline/engine.py
@AIM/hermes/app/pipeline/phases.py

# Current SKILL.md to be replaced
@AIM/hermes/skills/aim-scout/SKILL.md

<interfaces>
<!-- Code contracts the executor must mirror in SKILL.md prose. -->

From AIM/hermes/app/orchestrator/three_pass.py:
- run_three_pass(state: OrchestratorState) → OrchestratorState (3 passes, soft QC gate at 80% threshold)
- Opt-in via ORCHESTRATOR_MODE=1 env var (default OFF — PipelineEngine runs)

From AIM/hermes/app/pipeline/engine.py:
- PipelineEngine class with .execute() method — the fallback / legacy path
- _TOOL_HANDLERS dict with 26 entries — both modes share this registry
- ORCHESTRATOR_MODE env var: 1 = orchestrator, 0/unset = PipelineEngine

From AIM/hermes/app/pipeline/phases.py:
- PHASES list with 14 Phase entries (id 0 through 13)
- Each Phase has: id, name, tools, contract (max_retries, timeout, allow_no_data, on_permanent_failure), interpretation_prompt

From AIM/hermes/app/orchestrator/qc_checklist.py:
- QC_CHECKLIST v1.2.0 with 18 items
- PASS_THRESHOLD = 0.80 (soft gate, non-blocking)
- PASS_MIN_ITEMS = 15
- calc_coverage(gap_report) → CoverageReport with verdict PASS / FAIL
</interfaces>

<tool_catalogue>
<!-- The 26 _TOOL_HANDLERS entries grouped by category. SKILL.md must list these exactly. -->

Search & Research (5):
- web_search, perplexity_search, perplexity_deep_analyze, firecrawl_extract, firecrawl_batch_scrape

Scraping (4):
- firecrawl_agent, crawlee_scrape, crawlee_search, scrapy_crawl

Audit (3):
- run_pagespeed, run_seo_audit, find_competitors

Review (1):
- run_review_platforms

Content (2):
- run_content_analysis, run_content_gaps

People (3):
- find_doctor_handles, run_doctor_dossiers, run_instagram_content

Market (2):
- run_ci_analysis, run_hh_analysis

Media (2):
- run_smi_mentions, run_media_urls

Patients (1):
- run_forum_pains

Finance (1):
- find_company_financials

Report (2):
- generate_html_report, publish_scout_report

TOTAL: 26 entries (matches _TOOL_HANDLERS dict size)
</tool_catalogue>

<deploy_pattern>
# Pipe-through-stdin for SKILL.md (LLM-loaded markdown file)
cat AIM/hermes/skills/aim-scout/SKILL.md | ssh aim "docker exec -i aim-hermes tee /opt/hermes/skills/aim-scout/SKILL.md" > /dev/null

# Pipe-through-stdin for phases.py (Python module — lazy-imported, no restart needed)
cat AIM/hermes/app/pipeline/phases.py | ssh aim "docker exec -i aim-hermes tee /opt/hermes/app/pipeline/phases.py" > /dev/null

# Verify byte-for-byte
md5 -q AIM/hermes/skills/aim-scout/SKILL.md
ssh aim "docker exec aim-hermes md5sum /opt/hermes/skills/aim-scout/SKILL.md"
md5 -q AIM/hermes/app/pipeline/phases.py
ssh aim "docker exec aim-hermes md5sum /opt/hermes/app/pipeline/phases.py"
</deploy_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite aim-scout SKILL.md to describe orchestrator primary + pipeline fallback (per D-04, D-05)</name>
  <files>AIM/hermes/skills/aim-scout/SKILL.md</files>
  <behavior>
    - File YAML frontmatter version increments from 1.0.0 to 2.0.0
    - File YAML frontmatter description changes from "14-фaznая глубокая разведка" to mention both 3-pass orchestrator (primary) and PipelineEngine (fallback)
    - File contains section "РЕЖИМЫ ЗАПУСКА" describing both modes with ORCHESTRATOR_MODE switch
    - File references "QC_CHECKLIST v1.2.0" with 18 items as coverage metric
    - File contains a tool catalogue listing all 26 _TOOL_HANDLERS entries grouped by category
    - File contains zero occurrences of "FULL AUTO" (the v1.0.0 framing being removed per D-05)
    - File contains zero occurrences of "Ты НЕ оркестрируешь" (the v1.0.0 statement that contradicts the 3-pass orchestrator model)
    - File contains zero occurrences of "PipelineEngine — единственный способ запуска" (replaced with dual-mode description)
    - File retains the Iron Rules about commercial medicine filter (ООО/АО/ИП only, no ГАУЗ/ГБУЗ/etc.)
    - File retains the "no fabricated data" rule
  </behavior>
  <action>
    Replace the entire content of AIM/hermes/skills/aim-scout/SKILL.md with a new v2.0.0 version structured as follows. Target length 150-200 lines (similar to current 131 lines — keep concise).

    Section 1 — YAML frontmatter:
    - name: aim-scout (unchanged)
    - version: 2.0.0 (bumped from 1.0.0 to reflect Phase 2-5 architecture changes)
    - description: "AIM Scout — глубокая разведка клиники. Primary mode: 3-pass LLM-orchestrator with 18-item QC checklist (ORCHESTRATOR_MODE=1). Fallback mode: PipelineEngine 14 фаз (ORCHESTRATOR_MODE=0 или unset). v2.0: orchestrator-first per Phase 2 design."
    - triggers: unchanged from v1.0.0

    Section 2 — Title and primary framing (REPLACES "FULL AUTO MODE" section):
    "# AIM Scout — 3-pass LLM-orchestrator с QC-чеклистом"
    Describe the primary execution model:
    - По умолчанию (ORCHESTRATOR_MODE=1): 3-pass LLM-orchestrator — LLM выбирает инструменты по ситуации, 3 прохода (Сбор → Гэп-анализ по QC-чеклисту → Допосбор + Сборка HTML).
    - Fallback (ORCHESTRATOR_MODE=0 или unset): PipelineEngine — Python-controlled 14-фазный пайплайн. LEGACY mode per Plan 06-02 D-06.
    - Оба режима разделяют _TOOL_HANDLERS (26 инструментов) — PipelineEngine может вызвать любой из 26, оркестратор тоже.

    Section 3 — Primary mode: 3-pass orchestrator (NEW, replaces "Запуск" section):
    Brief description of run_three_pass(state) flow:
    - Pass 1 СБОР: LLM invokes tools from the 26-tool catalogue by situation (not rigid sequence)
    - Niche detection mini-call between Pass 1 and Pass 2: classifies clinic as plastic_surgery / cosmetology / other
    - Pass 2 ГЭП-АНАЛИЗ: LLM compares collected_data against 18-item QC_CHECKLIST v1.2.0
    - Pass 3 ДОПОСБОР + СБОРКА: LLM fills gaps + assembles final HTML via generate_html_report
    - Soft QC gate: warns if coverage < 80% (PASS_THRESHOLD), non-blocking per ORC-04

    Section 4 — Fallback mode: PipelineEngine 14 фаз (KEEP from v1.0.0, but mark as fallback):
    Keep the 14-phase table from current SKILL.md (lines 58-75 — PERPLEXITY through PRESENTATION). Update the section heading from "## 14 фаз (v8)" to "## Fallback Mode: PipelineEngine 14 фаз (LEGACY, ORCHESTRATOR_MODE=0)".
    Add note: "Этот режим включается только если ORCHESTRATOR_MODE не задан или =0. По умолчанию используется 3-pass оркестратор."

    Section 5 — Catalogue of 26 tools (NEW, replaces the implicit "tools come from PHASES" framing):
    Group tools by category (use the 12-category structure shown in the <tool_catalogue> context block above). For each category, list tool names with one-line descriptions. State at top of section: "PipelineEngine _TOOL_HANDLERS содержит 26 инструментов. Этот список — исчерпывающий для обоих режимов (оркестратор + PipelineEngine)."

    Section 6 — Iron Rules (UPDATE from v1.0.0):
    Keep rules 4, 5, 6 from current SKILL.md (no fabricated data, only commercial medicine, infrastructure errors not for client).
    REMOVE rule 1 ("Не оркестрируешь сам") — contradicts the orchestrator model.
    REMOVE rule 7 ("PipelineEngine — единственный способ запуска") — replaced by dual-mode description in Section 2.
    ADD rule: "В оркестраторе (по умолчанию) — LLM решает какие инструменты вызывать, основываясь на ситуации и нише. QC-чеклист направляет решение, не заменяет его."

    Section 7 — Завершение (KEEP from v1.0.0 with minor updates):
    Update to mention both modes: "Когда PipelineEngine завершил работу ИЛИ когда оркестратор завершил Pass 3: проверь metadata.json — completed_phases, failed_phases. Проверь report.html существует."

    CRITICAL RULES:
    - Remove "FULL AUTO" language entirely (D-05).
    - Remove "PipelineEngine — единственный способ запуска" (D-04).
    - Keep all client-facing protections (no fabricated data, commercial-medicine filter, no medical advice).
    - List exactly 26 tools (no more, no less) — these are the _TOOL_HANDLERS entries.
  </action>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -c "FULL AUTO" AIM/hermes/skills/aim-scout/SKILL.md | awk '{if ($1 == 0) {print "PASS: FULL AUTO removed"} else {print "FAIL: "$1" FULL AUTO mentions"}}'</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -c "PipelineEngine — единственный" AIM/hermes/skills/aim-scout/SKILL.md | awk '{if ($1 == 0) {print "PASS: exclusive-mode claim removed"} else {print "FAIL: "$1" exclusive-mode mentions"}}'</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -cE "(3-pass|3-проходн|orchestrator|оркестрат)" AIM/hermes/skills/aim-scout/SKILL.md | awk '{if ($1 >= 3) {print "PASS: "$1" orchestrator mentions"} else {print "FAIL: only "$1" orchestrator mentions"}}'</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -c "ORCHESTRATOR_MODE" AIM/hermes/skills/aim-scout/SKILL.md | awk '{if ($1 >= 2) {print "PASS: "$1" ORCHESTRATOR_MODE mentions"} else {print "FAIL: only "$1" ORCHESTRATOR_MODE mentions"}}'</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -c "QC_CHECKLIST\|QC-чеклист\|18-item" AIM/hermes/skills/aim-scout/SKILL.md | awk '{if ($1 >= 1) {print "PASS: QC checklist referenced"} else {print "FAIL: QC checklist missing"}}'</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -cE "^version: 2\.0\.0$" AIM/hermes/skills/aim-scout/SKILL.md | awk '{if ($1 == 1) {print "PASS: version bumped to 2.0.0"} else {print "FAIL: version not bumped"}}'</automated>
  </verify>
  <done>
    SKILL.md v2.0.0 written. FULL AUTO language removed. ORCHESTRATOR_MODE switch described. 3-pass orchestrator is primary mode. PipelineEngine 14 фаз marked as fallback. 26 tools listed by category. QC_CHECKLIST v1.2.0 referenced as coverage metric. Iron Rules updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add LEGACY marker to phases.py module docstring (per D-06)</name>
  <files>AIM/hermes/app/pipeline/phases.py</files>
  <behavior>
    - Module docstring first line changes to indicate LEGACY status
    - Module docstring references ORCHESTRATOR_MODE=1 as the primary mode
    - PHASES list (14 entries, 0-13) is structurally unchanged — no Phase dataclasses modified, no interpretation_prompt edits, no contract changes
    - All Phase id and name fields unchanged
    - File imports unchanged
  </behavior>
  <action>
    Update ONLY the module-level docstring at the top of AIM/hermes/app/pipeline/phases.py (lines 1-23 in the current file). Do NOT touch the Phase dataclasses, PHASES list, or any interpretation_prompt.

    Current docstring (lines 1-23) starts with: '"""Hermes v7 — Phase Definitions.\n\n14 фаз онбординг-пайплайна...'

    New docstring (replace lines 1-23):

    """Hermes v7 — PipelineEngine Phase Definitions (LEGACY fallback).

    LEGACY: Этот модуль используется только когда ORCHESTRATOR_MODE=0 или не задан.
    Primary mode с Phase 2 — 3-pass LLM-оркестратор в app/orchestrator/three_pass.py
    (ORCHESTRATOR_MODE=1). См. QC_CHECKLIST v1.2.0 в app/orchestrator/qc_checklist.py
    для coverage metric.

    14 фаз онбординг-пайплайна. Каждая фаза — изолированный шаг сбора и интерпретации данных.

    Поток данных:
      Phase 0: PERPLEXITY — deep research (рынок, город, ниша, конкуренты)
      Phase 1: COMPETITORS — поиск (Apify) + CI-анализ
      Phase 2: TECH AUDIT — Pagespeed + SEO
      Phase 3: SOCIAL VERIFIER — отзывы, рейтинги
      Phase 4: CONTENT ANALYSIS — контент сайта
      Phase 5: KEY PERSONS — врачи, Instagram-анализ
      Phase 6: HIRING SIGNALS — вакансии hh.ru как индикатор проблем в команде
      Phase 7: SMI MENTIONS — упоминания в СМИ
      Phase 8: FORUM PAINS — боли пациентов с форумов
      Phase 9: FINANCE — финансовые данные
      Phase 10: CONTENT PLAN — контент-план
      Phase 11: HTML BUILD — сборка HTML-отчёта
      Phase 12: QC CRITIQUE — LLM-проверка качества (10 пунктов)
      Phase 13: PRESENTATION — финальная презентация

    Все фазы выполняются СТРОГО последовательно (LEGACY behavior — оркестратор
    в app/orchestrator/three_pass.py не использует эту последовательность).
    NO_DATA — легитимный исход (не ошибка) для фаз с allow_no_data=True.
    """

    Note the changes:
    - First line adds "(LEGACY fallback)" after "Definitions"
    - Adds 4-line LEGACY notice paragraph after the title
    - Preserves the existing 14-phase list and flow description unchanged
    - Final paragraph clarifies that sequential execution is LEGACY behavior (orchestrator doesn't use it)

    After updating the docstring, verify the file still parses as valid Python (no syntax errors introduced) by running a Python AST parse via the container Python (which is Python 3.11-slim per CLAUDE.md — important because local Python 3.14 may permit constructs that 3.11 rejects per the Phase 4 Plan 04-08 lesson).
  </action>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -c "LEGACY" AIM/hermes/app/pipeline/phases.py | awk '{if ($1 >= 2) {print "PASS: "$1" LEGACY mentions"} else {print "FAIL: only "$1" LEGACY mentions"}}'</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -c "ORCHESTRATOR_MODE" AIM/hermes/app/pipeline/phases.py | awk '{if ($1 >= 1) {print "PASS: ORCHESTRATOR_MODE referenced"} else {print "FAIL: ORCHESTRATOR_MODE missing"}}'</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && python3 -c "import ast; ast.parse(open('AIM/hermes/app/pipeline/phases.py').read()); print('PASS: AST parse OK')"</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -c "^PHASE_[A-Z_0-9]* = Phase(" AIM/hermes/app/pipeline/phases.py | awk '{if ($1 == 14) {print "PASS: 14 Phase definitions intact"} else {print "FAIL: "$1" Phase definitions (expected 14)"}}'</automated>
  </verify>
  <done>
    phases.py module docstring updated with LEGACY marker and ORCHESTRATOR_MODE reference. 14 Phase dataclasses structurally unchanged. File parses as valid Python. AST parse passes locally (container-side AST parse verified in Task 3 deploy step).
  </done>
</task>

<task type="auto">
  <name>Task 3: Deploy SKILL.md and phases.py to aim-hermes container + container-side verification</name>
  <files>(no local file changes — deploy only)</files>
  <action>
    Deploy both files from Tasks 1+2 to the aim-hermes container using the proven pipe-through-stdin pattern. Both files are at the /opt/hermes/ path inside the container (the Docker image layer path — these are not in /opt/data volume).

    Step 1: Create server-side backups.
    Commands:
    - ssh aim "docker exec aim-hermes cp /opt/hermes/skills/aim-scout/SKILL.md /opt/hermes/skills/aim-scout/SKILL.md.phase6-backup-$(date +%Y%m%d)"
    - ssh aim "docker exec aim-hermes cp /opt/hermes/app/pipeline/phases.py /opt/hermes/app/pipeline/phases.py.phase6-backup-$(date +%Y%m%d)"

    Step 2: Deploy SKILL.md.
    Command: cat AIM/hermes/skills/aim-scout/SKILL.md | ssh aim "docker exec -i aim-hermes tee /opt/hermes/skills/aim-scout/SKILL.md" > /dev/null

    Step 3: Deploy phases.py.
    Command: cat AIM/hermes/app/pipeline/phases.py | ssh aim "docker exec -i aim-hermes tee /opt/hermes/app/pipeline/phases.py" > /dev/null

    Step 4: Verify byte-for-byte match for both files.
    - md5 -q AIM/hermes/skills/aim-scout/SKILL.md vs ssh aim "docker exec aim-hermes md5sum /opt/hermes/skills/aim-scout/SKILL.md"
    - md5 -q AIM/hermes/app/pipeline/phases.py vs ssh aim "docker exec aim-hermes md5sum /opt/hermes/app/pipeline/phases.py"
    Both pairs MUST match. If mismatch — re-deploy and re-verify.

    Step 5: Container-side Python 3.11 AST parse of phases.py (per Phase 4 Plan 04-08 lesson — local Python 3.14 AST parse is NOT sufficient).
    Command: ssh aim "docker exec aim-hermes python -c 'import ast; ast.parse(open(\"/opt/hermes/app/pipeline/phases.py\").read()); print(\"OK\")'"

    Step 6: Container-side import test (catches SyntaxError that AST parse misses, per Phase 4 lesson).
    Command: ssh aim "docker exec aim-hermes python -c 'from app.pipeline.phases import PHASES; print(f\"phases_count={len(PHASES)}\")'"

    Step 7: Container health check.
    Command: ssh aim "docker exec aim-hermes curl -sf http://127.0.0.1:8000/health"

    Step 8: Verify SKILL.md deploy took effect by greping server-side file.
    Command: ssh aim "docker exec aim-hermes grep -cE '(3-pass|3-проходн|orchestrator|оркестрат)' /opt/hermes/skills/aim-scout/SKILL.md"

    IMPORTANT: Do NOT restart the container. SKILL.md is loaded by the LLM via skill_view() on demand — the next LLM call that triggers aim-scout skill will load the new content. phases.py is lazy-imported by PipelineEngine on next execution — also no restart needed.
  </action>
  <verify>
    <automated>LOCAL_SKILL=$(md5 -q /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/skills/aim-scout/SKILL.md) && REMOTE_SKILL=$(ssh aim "docker exec aim-hermes md5sum /opt/hermes/skills/aim-scout/SKILL.md" | awk '{print $1}') && if [ "$LOCAL_SKILL" = "$REMOTE_SKILL" ]; then echo "PASS: SKILL.md match ($LOCAL_SKILL)"; else echo "FAIL: local=$LOCAL_SKILL remote=$REMOTE_SKILL"; fi</automated>
  </verify>
  <verify>
    <automated>LOCAL_PHASES=$(md5 -q /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/app/pipeline/phases.py) && REMOTE_PHASES=$(ssh aim "docker exec aim-hermes md5sum /opt/hermes/app/pipeline/phases.py" | awk '{print $1}') && if [ "$LOCAL_PHASES" = "$REMOTE_PHASES" ]; then echo "PASS: phases.py match ($LOCAL_PHASES)"; else echo "FAIL: local=$LOCAL_PHASES remote=$REMOTE_PHASES"; fi</automated>
  </verify>
  <verify>
    <automated>ssh aim "docker exec aim-hermes python -c 'from app.pipeline.phases import PHASES; print(f\"phases_count={len(PHASES)}\")'" | grep -q "phases_count=14" && echo "PASS: container phases.py imports OK with 14 phases" || echo "FAIL: container phases.py import error"</automated>
  </verify>
  <verify>
    <automated>ssh aim "docker exec aim-hermes curl -sf http://127.0.0.1:8000/health" | grep -qiE "(ok|healthy|status)" && echo "PASS: container healthy" || echo "FAIL: container health check failed"</automated>
  </verify>
  <verify>
    <automated>ssh aim "docker exec aim-hermes grep -c 'FULL AUTO' /opt/hermes/skills/aim-scout/SKILL.md" | awk '{if ($1 == 0) {print "PASS: server SKILL.md FULL AUTO removed"} else {print "FAIL: server SKILL.md has "$1" FULL AUTO mentions"}}'</automated>
  </verify>
  <done>
    SKILL.md and phases.py deployed to aim-hermes container. Both files byte-for-byte match between local and server. Container Python 3.11 successfully imports phases.py with 14 phases. Container health check passes. Server SKILL.md has FULL AUTO removed. Backups retained at .phase6-backup-YYYYMMDD suffixes for rollback.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → SSH → aim-hermes container | Two files cross trust boundary via pipe-through-stdin |
| phases.py Python module → runtime import | SyntaxError risk if Python 3.11-incompatible construct introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-T | Tampering | SKILL.md + phases.py deploy paths | mitigate | Explicit paths in tee commands; md5 byte-for-byte verify; pre-deploy backups at .phase6-backup-YYYYMMDD suffix |
| T-06-06-T | Tampering | phases.py code changes | mitigate | ONLY the module docstring changes — no Phase dataclass fields, no interpretation_prompt strings, no PHASES list order. AST parse + container import test catch any accidental code change |
| T-06-07-D | DoS | SKILL.md size | mitigate | Keep SKILL.md within 150-250 lines (similar order to current 131). No risk to context window — LLM loads skill on demand via skill_view(), not at session start |
| T-06-08-E | Elevation of Privilege | SKILL.md content shapes LLM tool calls | accept | The whole point of this plan — SKILL.md is supposed to influence LLM behavior. New content is reviewed for accuracy against code reality per D-01 (code = source of truth) |
| T-06-09-I | Info Disclosure | phases.py docstring references app/orchestrator/ path | accept | Internal container path, not exposed to LLM or clients. Pre-existing pattern in code comments |
| T-06-SC | Tampering | No new packages installed | N/A | Documentation + Python module docstring only — no supply chain risk |
</threat_model>

<verification>
After all three tasks complete:

1. `grep -c "FULL AUTO" AIM/hermes/skills/aim-scout/SKILL.md` reports 0.
2. `grep -c "ORCHESTRATOR_MODE" AIM/hermes/skills/aim-scout/SKILL.md` reports >= 2.
3. `grep -cE "(3-pass|3-проходн|orchestrator|оркестрат)" AIM/hermes/skills/aim-scout/SKILL.md` reports >= 3.
4. `grep -cE "^version: 2\.0\.0$" AIM/hermes/skills/aim-scout/SKILL.md` reports 1.
5. `grep -c "LEGACY" AIM/hermes/app/pipeline/phases.py` reports >= 2.
6. `grep -c "^PHASE_[A-Z_0-9]* = Phase(" AIM/hermes/app/pipeline/phases.py` reports 14 (unchanged).
7. Local AST parse of phases.py passes.
8. Container Python 3.11 AST parse of phases.py passes.
9. Container import of phases.py succeeds with phases_count=14.
10. Local md5 matches server md5 for both files.
11. Container `/health` returns 200.
12. Server-side SKILL.md FULL AUTO count is 0.
</verification>

<success_criteria>
- aim-scout SKILL.md describes 3-pass orchestrator as primary mode with QC checklist reference (SYN-03 satisfied)
- aim-scout SKILL.md lists all 26 _TOOL_HANDLERS tools grouped by category (SYN-04 partially satisfied — full assertion test in Plan 06-03)
- aim-scout SKILL.md "FULL AUTO" language removed (D-05 satisfied)
- phases.py marked as LEGACY with ORCHESTRATOR_MODE reference (D-06 satisfied)
- Phase desync between SKILL.md and phases.py eliminated — both now reference 14 phases as fallback only (SYN-01 satisfied for SKILL.md ↔ phases.py pair; SOUL.md desync addressed in Plan 06-01)
- Both files deployed byte-for-byte to container; container healthy (DPL-01, DPL-03 satisfied)
</success_criteria>

<output>
Create `.planning/phases/06-documentation-sync/06-02-SUMMARY.md` when done.
</output>
