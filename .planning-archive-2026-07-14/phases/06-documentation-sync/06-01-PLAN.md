---
phase: 06-documentation-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AIM/hermes/skills/aim/SOUL.md
autonomous: true
requirements: [SYN-01, SYN-02, SYN-05]
user_setup: []

must_haves:
  truths:
    - "SOUL.md describes the 3-pass orchestrator cycle (Collect → Gap-analyze → Fill+Assemble) as the primary reasoning model"
    - "SOUL.md lists the actual 18-item QC checklist as the coverage metric, not a vague 'quality > completeness' principle"
    - "SOUL.md tool catalogue references _TOOL_HANDLERS-known tools (26) and references ORCHESTRATOR_MODE env var as the opt-in switch"
    - "SOUL.md contains zero occurrences of phantom phase IDs (0.5, 0.75, 0.8, 3.2)"
    - "SOUL.md contains zero occurrences of '16 фаз' (desync value) and zero occurrences of '13 фаз' (desync value)"
    - "Server /opt/data/SOUL.md matches repo AIM/hermes/skills/aim/SOUL.md byte-for-byte after deploy"
    - "Container remains Up and healthy after deploy (no restart required for documentation update; LLM cache picks up new SOUL.md on next session after natural container restart)"
  artifacts:
    - path: "AIM/hermes/skills/aim/SOUL.md"
      provides: "Canonical AIM Assistant identity prompt that mirrors Phase 2-5 code reality (3-pass orchestrator, 18-item QC, 26 _TOOL_HANDLERS, niche detection, Instagram integration)"
      contains: "3-pass"
  key_links:
    - from: "SOUL.md 'Architecture' section"
      to: "app/orchestrator/three_pass.py"
      via: "conceptual reference to Collect → Gap-analyze → Fill+Assemble cycle"
      pattern: "(?i)(3-pass|3-проходн|orchestrator|оркестрат)"
    - from: "SOUL.md 'QC Checklist' section"
      to: "app/orchestrator/qc_checklist.py QC_CHECKLIST v1.2.0"
      via: "18-item coverage checklist reference"
      pattern: "(?i)(QC|чек-лист|coverage|покрытие)"
    - from: "SOUL.md 'Modes' section ORCHESTRATOR_MODE"
      to: ".env ORCHESTRATOR_MODE=1 opt-in switch"
      via: "environment variable name"
      pattern: "ORCHESTRATOR_MODE"
---

<objective>
Rewrite AIM/hermes/skills/aim/SOUL.md to mirror the Phase 2-5 code reality (3-pass orchestrator + 18-item QC checklist + 26 _TOOL_HANDLERS + niche detection + Instagram integration) and deploy to aim-hermes container.

Purpose: The current SOUL.md (668 lines, v4 "free artist") was written before Phase 2-5 shipped. It still describes the LLM as an unconstrained "свободный художник" with no mention of the 3-pass cycle, no QC checklist, no ORCHESTRATOR_MODE switch, and references 16 phases in the `run_aim_scout` tool description. This plan replaces the "free artist" framing with the actual 3-pass LLM-orchestrator-with-QC-checklist model that the code implements.

Output: One rewritten SOUL.md (~600-750 lines, same order of magnitude), deployed to both repo and container.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation-sync/06-CONTEXT.md

# Source-of-truth code files (read these to mirror accurately)
@AIM/hermes/app/orchestrator/three_pass.py
@AIM/hermes/app/orchestrator/qc_checklist.py
@AIM/hermes/app/orchestrator/states.py
@AIM/hermes/app/pipeline/engine.py
@AIM/hermes/app/pipeline/phases.py

# Current SOUL.md (to be replaced)
@AIM/hermes/skills/aim/SOUL.md

<interfaces>
<!-- Code contracts the executor must mirror in SOUL.md prose. Do NOT paraphrase technical names. -->

From AIM/hermes/app/orchestrator/three_pass.py:
- async def run_three_pass(state: OrchestratorState) -> OrchestratorState
- 3 distinct AIAgent.run_conversation() invocations sharing one session_id
- Pass 1: run_pass_collect(state) — LLM selects tools, collects raw material
- Between Pass 1+2: detect_instagram_critical_niche(state) — mini-call to classify niche
- Pass 2: run_pass_gap_analyze(state) — LLM compares collected vs QC checklist, emits gap_report
- Pass 3: run_pass_fill_assemble(state) — LLM fills gaps + assembles HTML report
- Soft QC gate: warns on coverage < PASS_THRESHOLD (non-blocking)

From AIM/hermes/app/orchestrator/qc_checklist.py:
- VERSION = "1.2.0"
- QC_CHECKLIST: list of 18 items
- PASS_THRESHOLD = 0.80
- PASS_MIN_ITEMS = 15
- Helper trio: is_niche_instagram_critical(niche), is_item_applicable(item, niche), applicable_items(niche)
- CRITICAL_NICHES = ("plastic_surgery", "cosmetology")
- calc_coverage(gap_report) → CoverageReport(total_items, filled_items, missing_items, partial_items, not_applicable_items, coverage_percent, verdict)

From AIM/hermes/app/orchestrator/states.py:
- @dataclass OrchestratorState with fields: session_id, client_url, client_name, client_city, client_specialization, niche (str, default ""), collected_data (dict), gap_report (dict), final_report_html (str), coverage_metadata (dict)

From AIM/hermes/app/pipeline/engine.py:
- _TOOL_HANDLERS: dict with 26 entries (verified server-side)
- ORCHESTRATOR_MODE env var: opt-in switch (default unset/0 = PipelineEngine, =1 = 3-pass orchestrator)
- PipelineEngine remains as fallback per ORC-05

From AIM/hermes/app/pipeline/phases.py:
- PHASES list has 14 entries (PHASE_0_PERPLEXITY through PHASE_13_PRESENTATION)
- This is the PipelineEngine (fallback) mode, NOT the primary orchestrator mode
</interfaces>

<deploy_pattern>
# Pipe-through-stdin (proven across Phases 02-05 — docker cp is broken for SSH-local pairs)
cat AIM/hermes/skills/aim/SOUL.md | ssh aim "docker exec -i aim-hermes tee /opt/data/SOUL.md" > /dev/null

# Also copy to Docker image layer path (SOUL.md source for rebuilds)
cat AIM/hermes/skills/aim/SOUL.md | ssh aim "docker exec -i aim-hermes tee /opt/hermes/skills/aim/SOUL.md" > /dev/null

# Verify byte-for-byte match
md5 -q AIM/hermes/skills/aim/SOUL.md
ssh aim "docker exec aim-hermes md5sum /opt/data/SOUL.md"
# (hashes must match)
</deploy_pattern>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SOUL.md to mirror Phase 2-5 code reality (per D-03)</name>
  <files>AIM/hermes/skills/aim/SOUL.md</files>
  <behavior>
    - File contains section "АРХИТЕКТУРА: 3-проходный цикл" describing Collect → Gap-analyze → Fill+Assemble
    - File references "ORCHESTRATOR_MODE" env var as the opt-in switch
    - File references "QC_CHECKLIST v1.2.0" with 18 items as coverage metric
    - File references CRITICAL_NICHES tuple ("plastic_surgery", "cosmetology") for Instagram hard-FAIL rule
    - File references PipelineEngine (14 фаз) as LEGACY fallback when ORCHESTRATOR_MODE unset
    - File contains zero occurrences of "16 фаз" (the desync value from old run_aim_scout description)
    - File contains zero occurrences of "13 фаз с ошибками" (old principle line — replace with QC-checklist language)
    - File contains zero occurrences of "0.5", "0.75", "0.8", "3.2" as phase IDs
    - File retains PRESALE / ACTIVE / ADMIN / SALES_ADMIN mode structure unchanged (per D-03)
    - File retains 7-niche specialization table unchanged
    - File retains "Bitrix-сайты → только browser" rule and other critical rules unchanged
    - File retains tone/address rules (Вы for clients, ты for Михаил) unchanged
  </behavior>
  <action>
    Replace the entire content of AIM/hermes/skills/aim/SOUL.md with a new version structured as follows.

    LARGE FILE WRITE RULE: SOUL.md is ~700 lines. The Write tool has a ~20-30KB limit per call. Split into TWO Write calls: first Write creates the file with sections 1-5, second Write appends sections 6-end using the Edit tool or append-via-Bash. Concretely: use Write for the first ~250-300 lines (frontmatter + identity + architecture + tool catalogue + modes), then use a second Write call to a temp file followed by `cat temp >> SOUL.md` via Bash, OR use Edit tool to append in chunks. The exact split point is at the executor's discretion but MUST produce a complete file at the end.

    Section structure for the new SOUL.md (per D-03):

    Section 1 — YAML Frontmatter (preserve name: aim-operator-v4, update description to mention 3-pass orchestrator).

    Section 2 — Главный принцип (NEW wording):
    Replace "LLM — ОРКЕСТРАТОР И СТРАТЕГ. Python — исполнитель" with "LLM — ОРКЕСТРАТОР с 3-проходным циклом и QC-чек-листом". Keep the spirit (LLM decides what to call) but anchor it to the actual 3-pass cycle. Remove "свободный художник" language (per D-05 — this was the v4 framing that led to 30% coverage; Phase 2 replaced it with structure).

    Section 3 — АРХИТЕКТУРА: 3-проходный цикл (NEW section, replaces "СТРАТЕГИЯ РАЗВЕДКИ"):
    Describe the 3-pass cycle literally as three_pass.py implements it:
    - Проход 1 СБОР: LLM invokes tools by situation (not rigid sequence), collects raw material into state.collected_data. Mention detect_instagram_critical_niche mini-call between Pass 1 and Pass 2 that classifies niche as plastic_surgery / cosmetology / other.
    - Проход 2 ГЭП-АНАЛИЗ: LLM compares collected_data against QC_CHECKLIST v1.2.0 (18 items). Each item gets status: filled / partial / missing / not_applicable. For plastic_surgery/cosmetology niches, item 5 (Instagram) is HARD-FAIL if missing.
    - Проход 3 ДОПОСБОР + СБОРКА: LLM fills gaps by calling additional tools, then assembles final HTML report via generate_html_report.
    - Soft QC gate: if coverage < 80% (PASS_THRESHOLD), warning emitted (non-blocking per ORC-04 — honest "данные недоступны" preferred over fabrication).
    - ORCHESTRATOR_MODE env var: =1 enables 3-pass cycle (opt-in). Default unset = PipelineEngine fallback (14 фаз, ORC-05 preserved).

    Section 4 — Каталог инструментов (26, grouped):
    Replace the inflated "40+ инструментов" claim with accurate counts. Group tools into the same categories as current SOUL.md (Быстрый осмотр, Поиск, Конкуренты, Тех.аудит, Контент, Репутация, Люди, Реклама, Финансы, Отчёты, CRM, Debug). Remove tools not in _TOOL_HANDLERS from the "autonomous pipelines" section (run_aim_scout, run_full_scout, run_background_pipeline, orchestrate — these are not in _TOOL_HANDLERS, keep mention but note they are LLM-registry-only, not PipelineEngine-callable). At the start of this section, state: "PipelineEngine имеет 26 инструментов в _TOOL_HANDLERS. LLM-registry имеет больше, но эти 26 гарантированно работают в обоих режимах."

    Section 5 — Режимы работы (PRESALE / ACTIVE / ADMIN / SALES_ADMIN):
    Unchanged from current SOUL.md. Keep mode detection (Telegram chat_id, X-Client-Mode header), keep ADMIN code-word "Привет зайка", keep tone rules.

    Section 6 — Структура финального отчёта (10 секций):
    Update to reference the 10 reference sections from ИПХиК (2).html. Current SOUL.md already has this structure (01-09). Verify it matches Phase 4 additions (Strategy=08, Offer=09, Whitefields=07, Experts+регалии=03, Content+страхи=04). Add a brief note that Pass 3 prompt generates narrative for each section per INT-01..05 rules (items 16-21 in pass_fill_assemble.py).

    Section 7 — Знание ниш (7 специализаций):
    Unchanged from current SOUL.md. Add explicit reference to detect_instagram_critical_niche function that runs between Pass 1 and Pass 2 and classifies niche using CRITICAL_NICHES tuple.

    Section 8 — Instagram Integration (NEW brief section):
    Describe the Instagram hard-FAIL rule: for plastic_surgery and cosmetology niches, missing Instagram analysis (item 5 in QC_CHECKLIST) triggers HARD-FAIL in coverage calculation regardless of other items filled. This is implemented in _apply_niche_conditional_coverage helper in three_pass.py.

    Section 9 — Правила КП (commercial proposals):
    Unchanged from current SOUL.md. Humanization Linter, Client-as-Hero 3:1, Quality Gate, 11-блочная структура, Pre-CP Checklist, Чат-выжимка, Follow-up 4 касания.

    Section 10 — Самообучение:
    Unchanged from current SOUL.md.

    Section 11 — Сохранение ключей:
    Unchanged from current SOUL.md.

    Section 12 — Критические правила:
    Unchanged from current SOUL.md. Add one new rule at top: "По умолчанию работаю в 3-pass оркестраторе (ORCHESTRATOR_MODE=1). Если режим не задан — fallback на PipelineEngine (14 фаз)."

    Section 13 (REMOVE the "16 фаз" mention from run_aim_scout tool description in Section 4 catalogue):
    Replace "Полный автономный Scout (16 фаз, фоновый)" with "Полный автономный Scout через PipelineEngine (14 фаз, ORC-05 fallback mode)".

    Throughout: remove "13 фаз с ошибками и пустышками" line (replace with "QC-checklist items with missing data" framing).

    CRITICAL RULES (project-wide):
    - No mock data in documentation — every number (26 handlers, 18 QC items, 14 phases, 3 passes) comes from actual code.
    - No phantom phases (0.5, 0.75, 0.8, 3.2) — these were only in the unused v3 backup.
    - No "свободный художник" framing — replace with "3-pass LLM-orchestrator with QC checklist".
    - Preserve all client-facing tone rules (Вы for clients, ты for Михаил).
    - Preserve Bitrix browser rule, госучреждения filter, no-medical-advice rule.
  </action>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -cE "(3-pass|3-проходн|orchestrator|оркестрат)" AIM/hermes/skills/aim/SOUL.md | grep -qv "^0$" && echo "PASS: 3-pass mentioned" || echo "FAIL: 3-pass missing"</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && wc -l AIM/hermes/skills/aim/SOUL.md | awk '{if ($1 >= 500 && $1 <= 900) {print "PASS: line count "$1} else {print "FAIL: line count "$1" out of expected 500-900 range"}}'</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -cE "(0\.5|0\.75|0\.8|3\.2)" AIM/hermes/skills/aim/SOUL.md | awk '{if ($1 == 0) {print "PASS: no phantom phases"} else {print "FAIL: "$1" phantom-phase patterns found"}}'</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -cE "16 фаз" AIM/hermes/skills/aim/SOUL.md | awk '{if ($1 == 0) {print "PASS: no 16-фaz desync"} else {print "FAIL: "$1" 16-фaz mentions"}}'</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -cE "13 фаз с ошибками" AIM/hermes/skills/aim/SOUL.md | awk '{if ($1 == 0) {print "PASS: old principle line removed"} else {print "FAIL: "$1" old-principle mentions"}}'</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -cE "QC_CHECKLIST|QC-чек-лист|18-item" AIM/hermes/skills/aim/SOUL.md | grep -qv "^0$" && echo "PASS: QC checklist referenced" || echo "FAIL: QC checklist missing"</automated>
  </verify>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && grep -cE "ORCHESTRATOR_MODE" AIM/hermes/skills/aim/SOUL.md | grep -qv "^0$" && echo "PASS: ORCHESTRATOR_MODE referenced" || echo "FAIL: ORCHESTRATOR_MODE missing"</automated>
  </verify>
  <done>
    SOUL.md rewritten with 3-pass orchestrator section, QC checklist section, ORCHESTRATOR_MODE reference, 26-tool accurate count, 14-phase PipelineEngine-as-fallback framing. Zero occurrences of "16 фаз", "13 фаз с ошибками", "0.5/0.75/0.8/3.2" as phase IDs. File size 500-900 lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy SOUL.md to aim-hermes container and verify byte-for-byte match</name>
  <files>(no local file changes — deploy only)</files>
  <action>
    Deploy the rewritten SOUL.md from Task 1 to two locations in the aim-hermes container using the proven pipe-through-stdin pattern (docker cp is broken for SSH-local file pairs — established in Phase 3 Plan 03-01 and re-confirmed in Phase 4 Plan 04-08).

    Step 1: Create a server-side backup of the current SOUL.md before overwrite.
    Command: ssh aim "docker exec aim-hermes cp /opt/data/SOUL.md /opt/data/SOUL.md.phase6-backup-$(date +%Y%m%d)"

    Step 2: Deploy to /opt/data/SOUL.md (the runtime path — _soul_md_cache loads from here).
    Command: cat AIM/hermes/skills/aim/SOUL.md | ssh aim "docker exec -i aim-hermes tee /opt/data/SOUL.md" > /dev/null

    Step 3: Deploy to /opt/hermes/skills/aim/SOUL.md (the Docker image layer path — source for any future rebuild).
    Command: cat AIM/hermes/skills/aim/SOUL.md | ssh aim "docker exec -i aim-hermes tee /opt/hermes/skills/aim/SOUL.md" > /dev/null

    Step 4: Verify byte-for-byte match between local and server.
    Local hash: md5 -q AIM/hermes/skills/aim/SOUL.md
    Server hash: ssh aim "docker exec aim-hermes md5sum /opt/data/SOUL.md"
    The two hashes MUST be identical. If mismatch — re-deploy and re-verify.

    Step 5: Verify container health (no restart needed — SOUL.md is read on next session, not at module load for tool handlers).
    Command: ssh aim "docker exec aim-hermes curl -sf http://127.0.0.1:8000/health"
    Expected: HTTP 200 with JSON response containing status:ok or similar.

    Step 6: Verify the deploy took effect by greping server-side SOUL.md.
    Command: ssh aim "docker exec aim-hermes grep -cE '(3-pass|3-проходн|orchestrator|оркестрат)' /opt/data/SOUL.md"
    Expected: non-zero count (same as local).

    IMPORTANT: Do NOT restart the container. SOUL.md is cached in _soul_md_cache module variable in agent_wrapper.py. The new SOUL.md takes effect on the NEXT session after a natural container restart OR a manual `docker restart aim-hermes` by the user at their preferred time. The user is sleeping — leave the container running. Document in the SUMMARY that the new SOUL.md activates on next container restart.
  </action>
  <verify>
    <automated>LOCAL_HASH=$(md5 -q /Users/mikhaileliseev/Desktop/Dev/meAI/AIM/hermes/skills/aim/SOUL.md) && REMOTE_HASH=$(ssh aim "docker exec aim-hermes md5sum /opt/data/SOUL.md" | awk '{print $1}') && if [ "$LOCAL_HASH" = "$REMOTE_HASH" ]; then echo "PASS: byte-for-byte match ($LOCAL_HASH)"; else echo "FAIL: local=$LOCAL_HASH remote=$REMOTE_HASH"; fi</automated>
  </verify>
  <verify>
    <automated>ssh aim "docker exec aim-hermes curl -sf http://127.0.0.1:8000/health" | grep -qiE "(ok|healthy|status)" && echo "PASS: container healthy" || echo "FAIL: container health check failed"</automated>
  </verify>
  <verify>
    <automated>ssh aim "docker exec aim-hermes grep -cE '(3-pass|3-проходн|orchestrator|оркестрат)' /opt/data/SOUL.md" | awk '{if ($1 >= 3) {print "PASS: server SOUL.md has "$1" orchestrator mentions"} else {print "FAIL: server SOUL.md has only "$1" orchestrator mentions"}}'</automated>
  </verify>
  <done>
    SOUL.md deployed to both /opt/data/SOUL.md and /opt/hermes/skills/aim/SOUL.md in aim-hermes container. Byte-for-byte match between local and server confirmed. Container remains Up and healthy. Server SOUL.md contains 3-pass orchestrator references. Backup saved as /opt/data/SOUL.md.phase6-backup-YYYYMMDD for rollback.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → SSH → aim-hermes container | Documentation file crosses trust boundary via pipe-through-stdin deploy |
| Container /opt/data → LLM prompt cache | SOUL.md read into _soul_md_cache on session start — stale cache risk until restart |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-T | Tampering | SOUL.md deploy path | mitigate | Pipe-through-stdin with explicit path `/opt/data/SOUL.md`; md5 byte-for-byte verify after deploy; pre-deploy backup retained at `/opt/data/SOUL.md.phase6-backup-YYYYMMDD` for rollback |
| T-06-02-D | DoS | LLM prompt size | mitigate | Keep SOUL.md within 500-900 lines (same order as current 668). The Pass 3 prompt is already 18.3KB — adding a 50KB SOUL.md would risk context window exhaustion. Task 1 includes explicit line-count check (500-900 range) |
| T-06-03-I | Info Disclosure | SOUL.md mentions internal paths | accept | SOUL.md already references /opt/data/memories/, /opt/data/sessions-archive/ — these are internal container paths not exposed to clients. The LLM may mention paths in responses but this is existing behavior, not new risk |
| T-06-04-E | Elevation of Privilege | ORCHESTRATOR_MODE switch | accept | Env var requires container restart to change — not exploitable via chat input. Existing constraint, no new risk introduced |
| T-06-SC | Tampering | No new npm/pip packages in this plan | N/A | Documentation-only phase — no package installs. Skip supply-chain check |
</threat_model>

<verification>
After both tasks complete:

1. `wc -l AIM/hermes/skills/aim/SOUL.md` reports between 500-900 lines.
2. `grep -cE "(3-pass|3-проходн|orchestrator|оркестрат)" AIM/hermes/skills/aim/SOUL.md` reports >= 3 matches.
3. `grep -cE "ORCHESTRATOR_MODE" AIM/hermes/skills/aim/SOUL.md` reports >= 1 match.
4. `grep -cE "QC_CHECKLIST|QC-чек-лист|18-item" AIM/hermes/skills/aim/SOUL.md` reports >= 1 match.
5. `grep -cE "(0\.5|0\.75|0\.8|3\.2)" AIM/hermes/skills/aim/SOUL.md` reports 0 matches.
6. `grep -c "16 фаз" AIM/hermes/skills/aim/SOUL.md` reports 0 matches.
7. `grep -c "13 фаз с ошибками" AIM/hermes/skills/aim/SOUL.md` reports 0 matches.
8. Local md5 matches server /opt/data/SOUL.md md5.
9. Container `curl /health` returns 200.
10. Server /opt/data/SOUL.md backup exists at `.phase6-backup-YYYYMMDD` suffix.
</verification>

<success_criteria>
- SOUL.md accurately describes the 3-pass orchestrator cycle as the primary reasoning model (SYN-02 satisfied)
- SOUL.md references the 18-item QC checklist as the coverage metric (SYN-02 satisfied)
- SOUL.md eliminates "16 фаз" desync value (SYN-01 partially satisfied — SKILL.md desync addressed in Plan 06-02)
- SOUL.md contains zero phantom phase IDs (SYN-05 partially satisfied — full grep audit in Plan 06-03)
- Server-side SOUL.md updated and verified (DPL-01 deploy pattern exercised; DPL-03 health check passed)
- Backup retained for rollback (DPL-05 partially exercised)
</success_criteria>

<output>
Create `.planning/phases/06-documentation-sync/06-01-SUMMARY.md` when done.
</output>
