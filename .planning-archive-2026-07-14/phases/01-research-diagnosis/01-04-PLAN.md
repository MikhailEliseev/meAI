---
phase: 01-research-diagnosis
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-research-diagnosis/evidence/instagram-tool-test.md
autonomous: true
requirements:
  - RES-05

must_haves:
  truths:
    - "Admin can read whether run_instagram_content works when invoked manually on a test clinic"
    - "Admin can see the data shape returned by run_instagram_content (fields, structure, completeness)"
    - "Admin can see whether the returned data is sufficient for reference sections 03 (Experts) and 04 (Content Analysis)"
    - "Admin can see a confirmed recommendation: does engine.py:_TOOL_HANDLERS need a dedicated handler for run_instagram_content (IG-01 for Phase 3)?"
  artifacts:
    - path: .planning/phases/01-research-diagnosis/evidence/instagram-tool-test.md
      provides: "Manual test results for run_instagram_content — works, data shape, handler need"
      contains: "RES-05"
  key_links:
    - from: "evidence/instagram-tool-test.md"
      to: "RESEARCH.md (Plan 03 consolidates)"
      via: "Plan 03 reads Instagram test results to test Hypothesis C (pipeline constraint)"
      pattern: "instagram-tool-test"
    - from: "evidence/instagram-tool-test.md"
      to: "Phase 3 (IG-01)"
      via: "Confirms whether engine.py handler addition is needed"
      pattern: "_TOOL_HANDLERS"
---

## Phase Goal

**As a** admin (Michael), **I want to** test run_instagram_content manually on one clinic and see what data it returns, **so that** Phase 3 knows whether to just wire the tool or also needs to build a new handler.

<objective>
Manually invoke `run_instagram_content` on 1 test clinic (e.g., iphk.ru — plastic surgery, likely has Instagram presence). Document:

1. **Does it work?** Tool executes without errors and returns data.
2. **What data shape?** Fields returned: followers, avg likes, avg views, content style, topics, etc.
3. **Sufficient for reference sections 03 (Experts) + 04 (Content Analysis)?** Map returned fields to reference requirements.
4. **Handler need confirmed?** Does engine.py:_TOOL_HANDLERS need a dedicated entry (IG-01 for Phase 3), or can the existing LLM-registry path suffice?

Purpose: CONTEXT.md states run_instagram_content is registered for LLM but NOT in _TOOL_HANDLERS (19/40+ gap). This test confirms whether the tool itself works (so Phase 3 is just wiring) or needs fixes (so Phase 3 includes tool work).

Output: `.planning/phases/01-research-diagnosis/evidence/instagram-tool-test.md` with test results, data shape, field mapping, and handler recommendation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-research-diagnosis/CONTEXT.md

<interfaces>
<!-- Phase 1 is research-only. This plan invokes an existing tool manually, does NOT modify code. -->

Tool location (in container):
- /opt/hermes/app/tools/run_instagram_content.py — the tool implementation
- Registered in /opt/hermes/app/tools/__init__.py via register_all_tools()
- NOT in /opt/hermes/app/pipeline/engine.py:_TOOL_HANDLERS (the 19-tool subset)

Reference sections 03 + 04 fields (from CONTEXT.md):
Section 03 (Experts) — for each top-5 doctor:
- ФИО (full name)
- Регалии (credentials/title)
- Подписчики (followers count)
- Avg лайки (average likes per post)
- Avg просмотры (average views per post/reel)
- Стиль контента (content style: promo / educational / personal / mixed)

Section 04 (Content Analysis) — for each top-5 doctor:
- Стиль контента (content style)
- Темы (topics in %: e.g., 40% promos, 30% education, 30% personal)
- Пробелы (content gaps: what they don't post about)
- Потенциал (growth potential: what they could add)
- + Топ-5 страхов пациентов (top 5 patient fears from forums — separate data source)

Test clinic candidates (from CONTEXT.md + ROADMAP Phase 7):
- iphk.ru — plastic surgery (has reference report ИПХиК (2).html, ideal for comparison)
- Alternative: any clinic from recent sessions in /opt/data/sessions-archive/ that has Instagram

How to invoke a tool manually (read-only investigation, does NOT modify code):
- Option A (if Hermes has a CLI): `ssh aim "docker exec aim-hermes python -c 'from app.tools.run_instagram_content import ...; result = run_instagram_content(...); print(result)'"`
- Option B (via hermes-agent API): `ssh aim "docker exec aim-hermes curl -X POST http://localhost:PORT/api/tools -d '{\"tool\":\"run_instagram_content\",\"args\":{...}}'"`
- Option C (inspect tool signature first): `ssh aim "docker exec aim-hermes grep -A 20 'def run_instagram_content' /opt/hermes/app/tools/run_instagram_content.py"` to understand parameters
- Option D (if tool requires live LLM call): note that manual test requires triggering via Telegram or admin chat — document this limitation

CRITICAL: Do NOT modify engine.py or any code. If manual invocation requires code changes, stop and document "tool not manually testable without code changes — Phase 3 must include test scaffold".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inspect run_instagram_content signature and invoke manually (RES-05 part 1)</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/01-research-diagnosis/CONTEXT.md
    - AIM/hermes/app/tools/run_instagram_content.py
  </read_first>
  <files>.planning/phases/01-research-diagnosis/evidence/instagram-tool-test.md (created)</files>
  <action>
Connect to server via `ssh aim`, investigate the tool read-only, then invoke it manually on 1 test clinic.

Step 1 — Inspect the tool implementation (read-only):
- `ssh aim "docker exec aim-hermes cat /opt/hermes/app/tools/run_instagram_content.py"` (or head -200 if large)
- Extract: function signature, parameters (clinic_url? instagram_handle? doctor_name?), return type, dependencies (Apify? Firecrawl? Instagram API?)
- Check for external service dependencies: `ssh aim "docker exec aim-hermes grep -E 'API_KEY|TOKEN|apify|firecrawl|instagram' /opt/hermes/app/tools/run_instagram_content.py"`
- Verify env vars are set: `ssh aim "docker exec aim-hermes env | grep -iE 'APIFY|FIRECRAWL|INSTAGRAM'"`

Step 2 — Check registration vs handler gap (read-only):
- `ssh aim "docker exec aim-hermes grep -n 'run_instagram_content' /opt/hermes/app/tools/__init__.py"` (confirm registered for LLM)
- `ssh aim "docker exec aim-hermes grep -n 'run_instagram_content' /opt/hermes/app/pipeline/engine.py"` (confirm NOT in _TOOL_HANDLERS — expect no match)
- If no match in engine.py → confirms CONTEXT.md finding: tool registered but pipeline can't call it

Step 3 — Determine invocation method:
- Check if Hermes has a CLI: `ssh aim "docker exec aim-hermes ls /opt/hermes/*.py /opt/hermes/cli/ 2>/dev/null"`
- Check if there's an admin API endpoint: `ssh aim "docker exec aim-hermes grep -rE 'run_instagram_content|admin.*tool' /opt/hermes/app/*.py 2>/dev/null | head -20"`
- Check if tool can be called directly via Python: `ssh aim "docker exec aim-hermes python -c 'import sys; sys.path.insert(0, \"/opt/hermes\"); from app.tools.run_instagram_content import run_instagram_content; print(run_instagram_content.__doc__)'"`

Step 4 — Invoke the tool manually on test clinic:
- Test clinic: iphk.ru (plastic surgery, reference report exists)
- Invocation (choose based on Step 3 findings):
  - Direct Python: `ssh aim "docker exec aim-hermes python -c 'from app.tools.run_instagram_content import run_instagram_content; import json; print(json.dumps(run_instagram_content(clinic_url=\"iphk.ru\"), ensure_ascii=False, indent=2))'"`
  - Via admin API: `ssh aim "docker exec aim-hermes curl -s -X POST http://localhost:8080/admin/tools -H 'Content-Type: application/json' -d '{\"tool\":\"run_instagram_content\",\"args\":{\"clinic_url\":\"iphk.ru\"}}'"`
  - Via Telegram admin chat: if no CLI/API, note "manual test requires admin to trigger via Telegram" and ask user to trigger (checkpoint — but try automated first)
- Capture full output (stdout + stderr)
- If tool errors: capture error message, traceback, and note which dependency failed
- If tool requires input not available (e.g., Instagram handle not derivable from clinic_url): note the input requirement

Step 5 — Write findings to evidence file:
- File header: "# Instagram Tool Manual Test — RES-05"
- Section: "## Tool Implementation"
  - File path, function signature, parameters, return type
  - External dependencies (Apify, Firecrawl, Instagram API, etc.)
  - Env vars required + confirmed set
- Section: "## Registration vs Handler Gap"
  - Confirmed: registered in __init__.py (line N)
  - Confirmed: NOT in engine.py _TOOL_HANDLERS (no match)
- Section: "## Manual Invocation"
  - Method used (Python direct / API / Telegram)
  - Test clinic: iphk.ru
  - Command run (exact command)
  - Output captured (full stdout/stderr, or reference output file if large)
  - Execution time
  - Success or error (with traceback if error)

DO NOT modify any code files. Read-only investigation + tool invocation only.
If tool cannot be invoked without code changes, STOP and document "tool not manually testable without code changes".
  </action>
  <verify>
    <automated>test -f /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/01-research-diagnosis/evidence/instagram-tool-test.md && grep -q "RES-05" /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/01-research-diagnosis/evidence/instagram-tool-test.md && grep -qE "(success|error|not manually testable)" /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/01-research-diagnosis/evidence/instagram-tool-test.md -i</automated>
  </verify>
  <acceptance_criteria>
    - evidence/instagram-tool-test.md exists at .planning/phases/01-research-diagnosis/evidence/instagram-tool-test.md
    - File contains "RES-05" header
    - Contains "## Tool Implementation" section with: file path, function signature, parameters, return type, external dependencies, env vars required + confirmed set
    - Contains "## Registration vs Handler Gap" section confirming: registered in __init__.py (with line number) AND NOT in engine.py _TOOL_HANDLERS (no match)
    - Contains "## Manual Invocation" section with: method used (Python/API/Telegram), test clinic (iphk.ru), exact command run, output captured, execution time, success/error status
    - If tool errored: error message + traceback + which dependency failed documented
    - If tool not testable without code changes: explicit "tool not manually testable without code changes" statement with explanation
    - Automated verify command passes (file exists + RES-05 marker + success/error/not-testable status found)
  </acceptance_criteria>
  <done>instagram-tool-test.md exists with: tool implementation details, registration vs handler gap confirmed, manual invocation results (success with output OR error with traceback OR "not testable without code changes" with explanation)</done>
</task>

<task type="auto">
  <name>Task 2: Map returned data to reference sections + confirm handler need (RES-05 part 2)</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/01-research-diagnosis/CONTEXT.md
    - .planning/phases/01-research-diagnosis/evidence/instagram-tool-test.md
    - AIM/hermes/app/tools/run_instagram_content.py
  </read_first>
  <files>.planning/phases/01-research-diagnosis/evidence/instagram-tool-test.md (appended)</files>
  <action>
Using the output from Task 1, map returned fields to reference sections 03 (Experts) and 04 (Content Analysis), and confirm whether engine.py:_TOOL_HANDLERS needs a dedicated entry.

Step 1 — Parse the tool output (from Task 1):
- If output is JSON: parse and list all top-level fields
- If output is text: identify structured sections
- If output errored: note "field mapping not possible — tool errored" and skip to Step 3

Step 2 — Map returned fields to reference requirements:
- Section 03 (Experts) field mapping table:
  | Reference Field | Returned by tool? | Field name in output | Notes |
  |-----------------|-------------------|----------------------|-------|
  | ФИО | yes/no | {field_name} | ... |
  | Регалии | yes/no | {field_name} | ... |
  | Подписчики | yes/no | {field_name} | ... |
  | Avg лайки | yes/no | {field_name} | ... |
  | Avg просмотры | yes/no | {field_name} | ... |
  | Стиль контента | yes/no | {field_name} | ... |
- Section 04 (Content Analysis) field mapping table:
  | Reference Field | Returned by tool? | Field name in output | Notes |
  |-----------------|-------------------|----------------------|-------|
  | Стиль контента | yes/no | {field_name} | ... |
  | Темы (in %) | yes/no | {field_name} | ... |
  | Пробелы | yes/no | {field_name} | ... |
  | Потенциал | yes/no | {field_name} | ... |
- Note: Топ-5 страхов пациентов (section 04) comes from forums, NOT Instagram — exclude from this mapping
- Coverage score: "X/10 reference fields present in tool output"

Step 3 — Confirm handler need (IG-01 for Phase 3):
- Question: Does engine.py:_TOOL_HANDLERS need a dedicated entry for run_instagram_content?
- Evidence:
  - Tool registered for LLM: yes (confirmed in Task 1)
  - Tool in _TOOL_HANDLERS: no (confirmed in Task 1)
  - Tool works manually: yes/no (from Task 1)
  - Tool returns sufficient data for sections 03+04: yes/no (from Step 2)
- Verdict:
  - If tool works AND returns sufficient data → "Handler needed: YES — Phase 3 (IG-01) must add run_instagram_content to engine.py:_TOOL_HANDLERS. Tool itself needs no changes."
  - If tool works BUT returns insufficient data → "Handler needed: YES, AND tool needs enhancement — Phase 3 (IG-01 + IG-03) must add handler AND extend tool to return missing fields: {list}"
  - If tool errors → "Handler needed: CANNOT CONFIRM — tool errors must be fixed first. Phase 3 must include tool debugging before handler addition."
  - If tool not manually testable → "Handler needed: ASSUMED YES — Phase 3 must add handler AND verify tool works through pipeline."

Step 4 — Append findings to evidence file:
- Section: "## Data Shape and Field Mapping"
  - Returned fields list
  - Section 03 mapping table
  - Section 04 mapping table
  - Coverage score: X/10 fields
- Section: "## Handler Need Confirmation (for IG-01, Phase 3)"
  - Evidence summary (4 points from Step 3)
  - Verdict (one of the 4 outcomes above)
  - Specific recommendations for Phase 3
- Section: "## Gaps for Phase 3"
  - Missing fields (if any) that tool should return to fully support sections 03+04
  - External service issues (if Apify/Firecrawl/Instagram API failed)
  - Edge cases to handle (clinic without Instagram, private account, etc.)

DO NOT modify server files. This task only analyzes output from Task 1.
  </action>
  <verify>
    <automated>grep -q "Field Mapping" /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/01-research-diagnosis/evidence/instagram-tool-test.md && grep -q "Handler Need" /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/01-research-diagnosis/evidence/instagram-tool-test.md && grep -qE "(Handler needed:|verdict)" /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/01-research-diagnosis/evidence/instagram-tool-test.md -i</automated>
  </verify>
  <acceptance_criteria>
    - instagram-tool-test.md appended with "## Data Shape and Field Mapping" section
    - Contains returned fields list parsed from Task 1 output
    - Contains Section 03 (Experts) field mapping table with 6 reference fields (ФИО, Регалии, Подписчики, Avg лайки, Avg просмотры, Стиль контента)
    - Contains Section 04 (Content Analysis) field mapping table with 4 reference fields (Стиль, Темы %, Пробелы, Потенциал)
    - Contains coverage score "X/10 fields" quantifying how many reference fields the tool returns
    - Contains "## Handler Need Confirmation (for IG-01, Phase 3)" section
    - Handler verdict is explicit: YES / YES+enhancement / CANNOT CONFIRM / ASSUMED YES (one of the 4 defined outcomes)
    - Verdict backed by 4-point evidence summary (registered, in handlers, works manually, returns sufficient data)
    - Contains "## Gaps for Phase 3" section listing missing fields, external service issues, edge cases
    - Automated verify command passes (Field Mapping + Handler Need + verdict markers found)
  </acceptance_criteria>
  <done>instagram-tool-test.md appended with: returned fields list, Section 03+04 field mapping tables, coverage score X/10, handler need verdict with evidence, specific Phase 3 recommendations including any tool gaps or edge cases</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ssh aim → Polish server | Existing authenticated SSH access (key-based) |
| Server → aim-hermes container | Docker exec into live container — manual tool invocation may call external APIs (Apify, Firecrawl, Instagram) |
| Tool → external services (Apify/Firecrawl/Instagram) | Tool invocation triggers outbound API calls using existing API keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Live container code (engine.py, run_instagram_content.py, __init__.py) | mitigate | Read-only inspection (cat, grep, head). Explicitly forbidden: sed -i, mv, rm, chmod, docker cp (write direction), code edits. Tool invocation via Python -c or API does NOT modify files. |
| T-04-02 | Info Disclosure | Instagram data returned by tool (doctor handles, follower counts) | accept | Data is publicly visible Instagram content. Evidence file references aggregate metrics, not private account data. |
| T-04-03 | DoS | Tool invocation triggers expensive Apify/Firecrawl job on live container | mitigate | Invoke tool ONCE on ONE clinic only. Do not loop. If tool has pagination, limit to first page. Monitor execution time — abort if >300s. |
| T-04-04 | Elevation | Accidental privileged commands | mitigate | Non-root ssh. No sudo. Python -c runs as container user, not root. |
| T-04-05 | Repudiation | Tool invocation not traceable | mitigate | Evidence file records exact command, timestamp, output. Bash commands visible in conversation history. |
| T-04-06 | Spoofing | Tool returns fabricated data instead of real Instagram data | mitigate | Cross-check returned follower counts against public Instagram profile (if accessible). Note: if tool returns test/mock data, flag as "TOOL RETURNS MOCK DATA — violates Mock Data Rule in CLAUDE.md". |

Note: No package installs — package legitimacy gate not applicable. Tool uses existing registered dependencies (Apify, Firecrawl) already in container.
</threat_model>

<verification>
- instagram-tool-test.md exists at `.planning/phases/01-research-diagnosis/evidence/instagram-tool-test.md`
- RES-05 section present with: tool implementation details, registration vs handler gap, manual invocation results
- Data shape documented: returned fields listed
- Section 03 (Experts) field mapping table present
- Section 04 (Content Analysis) field mapping table present
- Coverage score: X/10 reference fields present
- Handler need verdict explicit: YES/NO/CANNOT CONFIRM with evidence
- Phase 3 recommendations specific (which fields missing, which edge cases to handle)
- Server code unchanged: `ssh aim "docker exec aim-hermes stat -c '%Y' /opt/hermes/app/pipeline/engine.py /opt/hermes/app/tools/run_instagram_content.py"` mtimes older than plan start
</verification>

<success_criteria>
- [ ] RES-05: run_instagram_content tested manually on 1 clinic (iphk.ru or alternative)
- [ ] Tool works: confirmed yes/no with output evidence
- [ ] Data shape documented: all returned fields listed
- [ ] Field mapping to reference sections 03+04 completed with coverage score
- [ ] Handler need confirmed: engine.py:_TOOL_HANDLERS addition required (yes/no) with evidence
- [ ] Phase 3 recommendations documented (tool gaps, edge cases, missing fields)
- [ ] No server code modified (read-only + tool invocation only, verified by stat mtime)
</success_criteria>

<output>
Create `.planning/phases/01-research-diagnosis/01-04-SUMMARY.md` when done
</output>
