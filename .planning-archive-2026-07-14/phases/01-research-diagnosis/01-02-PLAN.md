---
phase: 01-research-diagnosis
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-research-diagnosis/evidence/session-log-analysis.md
autonomous: true
requirements:
  - RES-04

must_haves:
  truths:
    - "Admin can read a structured timeline per session showing which tools were called, when, and what they returned"
    - "Admin can see specific decision points where LLM truncated output, skipped a tool, or hit NO_DATA — with timestamps and tool names"
    - "Analysis covers 3-5 actual sessions from /opt/data/sessions-archive/"
    - "Each skip/truncate point has quoted evidence from the log (not paraphrased)"
  artifacts:
    - path: .planning/phases/01-research-diagnosis/evidence/session-log-analysis.md
      provides: "Per-session structured timeline + skip/truncate decision points with evidence"
      contains: "RES-04"
  key_links:
    - from: "evidence/session-log-analysis.md"
      to: "RESEARCH.md (Plan 03 consolidates)"
      via: "Plan 03 reads skip points to test root cause hypotheses"
      pattern: "session-log-analysis"
---

## Phase Goal

**As a** admin (Michael), **I want to** see exactly where and when the LLM skips tools or truncates output in v4 presale runs, **so that** Phase 2 can fix the specific decision points instead of guessing at general patterns.

<objective>
For 3-5 recent Hermes presale runs, produce a structured per-session timeline showing:
- Every tool call (name, timestamp, result summary)
- Every phase executed by PipelineEngine (start, end, status)
- Every decision point where LLM truncated output, skipped a tool, or hit NO_DATA
- Specific quoted evidence from logs (not paraphrased)

Purpose: Plan 01 measures WHAT is missing (coverage baselines). This plan answers WHERE and WHEN the missing happens — the specific decision points. Plan 03 (root cause) uses these points to test WHY.

Output: `.planning/phases/01-research-diagnosis/evidence/session-log-analysis.md` with per-session timelines and a consolidated skip-points register.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-research-diagnosis/CONTEXT.md

<interfaces>
<!-- Phase 1 is research-only. No code interfaces consumed or produced. -->

Key server paths (from CONTEXT.md):
- Sessions archive: /opt/data/sessions-archive/{session_hash}/
- Container: aim-hermes (HERMES_HOME=/opt/data)
- Pipeline phases (13 in code): PERPLEXITY → COMPETITORS → TECH_AUDIT → SOCIAL → CONTENT → KEY_PERSONS → SMI → FORUM_PAINS → FINANCE → CONTENT_PLAN → HTML_BUILD → QC → PRESENTATION
- engine.py _TOOL_HANDLERS: 19 tools (subset of 40+ registered)

13 phases from /opt/hermes/app/pipeline/phases.py:
Phase 0:  PERPLEXITY      [perplexity_search]
Phase 0:  PRE-FLIGHT      []
Phase 1:  COMPETITORS     [find_competitors, run_ci_analysis]
Phase 2:  TECH AUDIT      [run_pagespeed, run_seo_audit]
Phase 3:  SOCIAL          [run_review_platforms]
Phase 4:  CONTENT         [run_content_analysis]
Phase 5:  KEY PERSONS     [run_hh_analysis, run_doctor_dossiers]
Phase 6:  SMI             [run_smi_mentions]
Phase 7:  FORUM PAINS     [web_search]
Phase 8:  FINANCE         [find_company_financials]
Phase 9:  CONTENT PLAN    [run_content_gaps]
Phase 10: HTML BUILD      [generate_html_report]
Phase 11: QC CRITIQUE     []
Phase 12: PRESENTATION    [publish_scout_report]

Log patterns to look for (typical hermes-agent / FastAPI patterns):
- Tool calls: "tool_use", "tool_call", "function_call", "name=", "tool_name"
- Phase transitions: "phase", "PHASE_", "entering phase", "phase complete"
- LLM messages: "assistant", "content", "message", "role"
- Errors: "error", "ERROR", "exception", "traceback", "timeout", "TIMEOUT"
- NO_DATA / empty results: "NO_DATA", "no_data", "empty", "null result", "skipped"
- Truncation: "truncated", "max_tokens", "stop_reason", "finish_reason", "length"
- Stream breaks: "stream", "connection reset", "broken pipe", "120s"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build structured per-session timeline (RES-04 part 1)</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/01-research-diagnosis/CONTEXT.md
  </read_first>
  <files>.planning/phases/01-research-diagnosis/evidence/session-log-analysis.md (created)</files>
  <action>
Connect to server via `ssh aim`, investigate aim-hermes container read-only.

Step 1 — Select 3-5 sessions (coordinate with Plan 01 if running in parallel):
- `ssh aim "docker exec aim-hermes ls -lt /opt/data/sessions-archive/ | head -20"`
- Select 3-5 most recent session_hash directories
- If Plan 01 already completed, reuse the same session list from coverage-baseline.md for cross-reference
- Record session_hash IDs and start/end timestamps

Step 2 — For each session, discover log file structure:
- `ssh aim "docker exec aim-hermes ls -la /opt/data/sessions-archive/{hash}/"`
- Identify log files: .jsonl (events), .json (state), .log (text logs), .yaml (config)
- Common hermes-agent patterns: events.jsonl, session.json, messages.jsonl, trace.jsonl
- For each log file, check size: `ssh aim "docker exec aim-hermes wc -l /opt/data/sessions-archive/{hash}/{file}"`

Step 3 — Extract structured timeline per session:
- Parse event/log files to extract chronologically ordered entries:
  - Timestamp (ISO 8601 or Unix epoch)
  - Event type (phase_start, phase_end, tool_call, tool_result, llm_message, error)
  - Tool name (if tool event)
  - Phase name (if phase event)
  - Result summary (NO_DATA, success with N items, error, truncated)
- Read-only commands:
  - `ssh aim "docker exec aim-hermes cat /opt/data/sessions-archive/{hash}/events.jsonl" | head -200`
  - Use jq if available: `ssh aim "docker exec aim-hermes cat {file} | jq -r '. | \"\\(.timestamp) \\(.type) \\(.tool_name // .phase // \"-\")\"'"`
  - If no jq, use grep + head: `ssh aim "docker exec aim-hermes grep -E 'tool_call|phase_start|phase_end' {file} | head -100"`

Step 4 — Write per-session timeline to evidence file:
- File header: "# Session Log Analysis — RES-04"
- For each session, a subsection: "## Session {hash} ({timestamp})"
- Timeline table: time | event_type | tool/phase | result_summary
- Note total duration, total tool calls, total phases executed
- Note any session that ended abnormally (crash, timeout, incomplete)

DO NOT modify server files. Read-only commands only.
If a session is corrupted or empty, skip and pick next most recent.
  </action>
  <verify>
    <automated>test -f /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/01-research-diagnosis/evidence/session-log-analysis.md && grep -q "RES-04" /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/01-research-diagnosis/evidence/session-log-analysis.md && grep -qE "Session [a-f0-9]+" /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/01-research-diagnosis/evidence/session-log-analysis.md</automated>
  </verify>
  <acceptance_criteria>
    - evidence/session-log-analysis.md exists at .planning/phases/01-research-diagnosis/evidence/session-log-analysis.md
    - File contains "RES-04" header
    - Contains 3-5 per-session subsections ("## Session {hash} ({timestamp})")
    - Each subsection has a chronological timeline table with columns: time | event_type | tool/phase | result_summary
    - Each timeline captures: tool calls (name + timestamp), phase events (start/end), LLM messages, errors
    - Each session subsection notes total duration, total tool calls, total phases executed
    - Abnormal session endings (crash, timeout, incomplete) are flagged if present
    - Automated verify command passes (file exists + RES-04 marker + "Session {hash}" pattern found)
  </acceptance_criteria>
  <done>session-log-analysis.md exists with: 3-5 per-session subsections, each containing a chronological timeline table with timestamps, event types, tool/phase names, and result summaries</done>
</task>

<task type="auto">
  <name>Task 2: Identify skip/truncate decision points with quoted evidence (RES-04 part 2)</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/phases/01-research-diagnosis/CONTEXT.md
    - .planning/phases/01-research-diagnosis/evidence/session-log-analysis.md
  </read_first>
  <files>.planning/phases/01-research-diagnosis/evidence/session-log-analysis.md (appended)</files>
  <action>
Using the timelines from Task 1, identify every decision point where the LLM or pipeline skipped, truncated, or returned no data.

Step 1 — For each session timeline, scan for skip/truncate signals:
- NO_DATA results: tool returned empty or "NO_DATA" or "no data found"
- Truncated output: "max_tokens", "stop_reason: length", "finish_reason: length", "truncated"
- Stream breaks: "stream error", "connection reset", "broken pipe", timestamps >120s apart
- Skipped tools: phases where expected tools (from phases.py mapping) were NOT called
- Skipped phases: phases in the 13-phase sequence that are missing from timeline
- LLM "не обязательно" / "optional" / "skip" decisions: grep assistant messages for these phrases
- Error recovery: tool errored, LLM continued without retry

Step 2 — For each identified skip/truncate point, capture evidence:
- Timestamp
- Tool or phase name
- Quoted log line (exact text from log, not paraphrased — use grep -B 2 -A 5 for context)
- What was expected (per phases.py mapping or SOUL.md catalogue)
- What actually happened (NO_DATA, truncated, skipped, errored)
- Category: NO_DATA | TRUNCATED | SKIPPED_TOOL | SKIPPED_PHASE | STREAM_BREAK | ERROR | LLM_DECISION

Step 3 — Build consolidated skip-points register:
- Aggregate across all 3-5 sessions
- Group by category
- Count occurrences per tool/phase
- Identify patterns: "Instagram NEVER called across all sessions", "Phase 11 QC CRITIQUE always empty", "SMI phase always truncates at ~100s"

Step 4 — Append findings to evidence file:
- Subsection: "## Skip/Truncate Decision Points"
- Per-session detailed list with quoted evidence
- Consolidated register table: category | tool/phase | occurrence_count | sessions_affected | example_quote
- Pattern analysis: "Top 5 most-skipped tools", "Phases that always truncate", "Tools that always return NO_DATA"
- Cross-reference with Plan 01 never-called tools list (if available)

Step 5 — Cross-reference with CONTEXT.md hypotheses:
- Hypothesis 1 (Instagram absent): confirmed/refuted by skip data
- Hypothesis 7 (SOUL.md too much freedom): check if LLM messages contain "не обязательно" / "optional" / "skip"
- Note: full hypothesis testing happens in Plan 03; this plan provides the evidence

DO NOT modify server files. Read-only commands only.
  </action>
  <verify>
    <automated>grep -q "Skip/Truncate" /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/01-research-diagnosis/evidence/session-log-analysis.md && grep -qE "(NO_DATA|TRUNCATED|SKIPPED|STREAM_BREAK)" /Users/mikhaileliseev/Desktop/Dev/meAI/.planning/phases/01-research-diagnosis/evidence/session-log-analysis.md</automated>
  </verify>
  <acceptance_criteria>
    - session-log-analysis.md appended with "## Skip/Truncate Decision Points" subsection
    - Per-session skip/truncate points documented with quoted log evidence (exact text, not paraphrased)
    - Each skip point categorized: NO_DATA | TRUNCATED | SKIPPED_TOOL | SKIPPED_PHASE | STREAM_BREAK | ERROR | LLM_DECISION
    - Contains consolidated register table (category | tool/phase | occurrence_count | sessions_affected | example_quote)
    - Contains pattern analysis: "Top 5 most-skipped tools", "Phases that always truncate", "Tools that always return NO_DATA"
    - Contains cross-reference to Plan 01 never-called tools list (if Plan 01 completed)
    - Contains cross-reference to CONTEXT.md hypotheses (Instagram absent, SOUL.md permissive language)
    - Automated verify command passes (Skip/Truncate section + category markers found)
  </acceptance_criteria>
  <done>session-log-analysis.md appended with: per-session skip/truncate points with quoted log evidence, consolidated register table grouped by category, pattern analysis identifying most-skipped tools/phases, cross-reference to CONTEXT.md hypotheses</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ssh aim → Polish server | Existing authenticated SSH access (key-based) |
| Server → aim-hermes container | Docker exec into live container running production presales |
| Container → session logs | Read-only access to historical presale data (may contain clinic PII, LLM prompts/responses) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Live container files (logs, sessions, code) | mitigate | Read-only commands only: cat, ls, grep, head, tail, wc, find, stat, jq (read). Explicitly forbidden: sed -i, mv, rm, chmod, docker restart, docker cp (write), config reload. |
| T-02-02 | Info Disclosure | Session logs with LLM prompts/responses, clinic PII | accept | Logs already in AIM's possession. Evidence file references by session_hash only. Quoted log lines in evidence file: redact clinic names, doctor names, financials — keep only structural/log tokens. |
| T-02-03 | DoS | Heavy cat/grep on large log files slowing container | mitigate | Use head/tail with line limits. Use `wc -l` first to size files. For files >10MB, use `head -c 100000` or `tail -c 100000` instead of full cat. Avoid `docker logs --follow`. |
| T-02-04 | Elevation | Accidental privileged commands | mitigate | Non-root ssh user. No sudo. If root needed, stop and ask user. |
| T-02-05 | Repudiation | Log analysis actions not tracked | accept | Evidence file + quoted log lines serve as audit trail. Bash commands visible in conversation history. |

Note: No package installs — package legitimacy gate not applicable.
</threat_model>

<verification>
- session-log-analysis.md exists at `.planning/phases/01-research-diagnosis/evidence/session-log-analysis.md`
- RES-04 section present with 3-5 per-session timelines
- Each timeline has: timestamps, event types, tool/phase names, result summaries
- Skip/Truncate Decision Points section present with quoted log evidence (not paraphrased)
- Consolidated register table groups skips by category (NO_DATA, TRUNCATED, SKIPPED_TOOL, etc.)
- Pattern analysis identifies most-skipped tools and always-truncate phases
- Cross-reference to CONTEXT.md hypotheses noted
- Server state unchanged: `ssh aim "docker exec aim-hermes stat -c '%Y' /opt/data/SOUL.md"` mtime older than plan start
</verification>

<success_criteria>
- [ ] RES-04: 3-5 sessions analyzed with structured per-session timelines
- [ ] Skip/truncate decision points identified with quoted log evidence (timestamps + tool names)
- [ ] Consolidated skip-points register groups by category
- [ ] Pattern analysis reveals which tools/phases are consistently skipped or truncated
- [ ] Cross-reference to CONTEXT.md hypotheses documented
- [ ] No server files modified (read-only verified)
</success_criteria>

<output>
Create `.planning/phases/01-research-diagnosis/01-02-SUMMARY.md` when done
</output>
