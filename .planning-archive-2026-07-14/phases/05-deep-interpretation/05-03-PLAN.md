---
phase: 05-deep-interpretation
plan: 03
type: execute
name: reference-calibration-few-shot-examples
wave: 2
depends_on: [05-01]
requirements: [INT-01, INT-02, INT-03, INT-04, INT-05]
tags: [orchestrator, pass3, prompt, few-shot, reference-calibration, narrative-examples, reference-html, d-11]

files_modified:
  - AIM/hermes/app/orchestrator/pass_fill_assemble.py

autonomous: true

must_haves:
  truths:
    - "D-11 SATISFIED (full): Pass 3 prompt contains an EXAMPLES BY SECTION calibration block with 10+ narrative examples extracted from `ИПХиК (2).html` — one per reference section (01-10)"
    - "Each example includes a 'metric-dump vs narrative' before/after contrast demonstrating the transformation required by INT-01"
    - "At least 2 cross-section reference examples included (e.g., Strategy section referencing fears from Content Analysis)"
    - "At least 2 gap-block examples included (1 strength with green border, 1 growth point with competitor benchmark)"
    - "At least 2 blockquote examples included (1 about-scale insight, 1 strategic recommendation)"
    - "All extracted text is RUSSIAN (matching reference language) — no translation"
    - "Calibration block is appended AFTER item 21 (from Plan 05-01) as items 22+ (or as an EXAMPLES section at end of prompt)"
    - "Function signature `_build_prompt(state: OrchestratorState) -> str` unchanged"
    - "_PASS_FILL_TIMEOUT=600 unchanged (text-only expansion)"
    - "Items 1-21 from Plan 05-01 + Phase 4 preserved unchanged (regression)"
  artifacts:
    - path: "AIM/hermes/app/orchestrator/pass_fill_assemble.py"
      provides: "Pass 3 prompt extended with EXAMPLES BY SECTION calibration block — 10+ concrete narrative examples from reference HTML"
      contains: "ПРИМЕРЫ ИЗ РЕФЕРЕНСА"
      min_lines: 360
  key_links:
    - from: "/Users/mikhaileliseev/Downloads/ИПХиК (2).html (reference)"
      to: "pass_fill_assemble._build_prompt calibration block"
      via: "10+ narrative snippets extracted from reference HTML, embedded inline in prompt"
      pattern: "ПРИМЕРЫ ИЗ РЕФЕРЕНСА|ИПХиК"
---

<objective>
Expand the Pass 3 prompt's reference calibration section (Plan 05-01 item 21) with a comprehensive EXAMPLES BY SECTION block. Extract 10+ concrete narrative examples from the canonical reference `ИПХиК (2).html` — one per section — and embed them inline in the Pass 3 prompt as few-shot calibration.

This is the REFERENCE CALIBRATION layer of Phase 5. Plan 05-01 item 21 included 4 short reference snippets as pointers. This plan expands that with a structured, section-by-section calibration block that shows the LLM EXACTLY what narrative depth is expected for each section type.

Purpose: DeepSeek V4 Pro follows few-shot examples closely. By embedding 10+ real reference snippets mapped to section types, the LLM has concrete targets to emulate — not abstract rules. This addresses the "30% coverage" root cause (Phase 1 finding) by making the expected style unambiguous.

Output: Modified `pass_fill_assemble.py` with a new EXAMPLES block appended after item 21 (~150 lines of prompt text, ~4KB / 1000 tokens added — sub-1% of DeepSeek 128K context budget).
</objective>

## Phase Goal (MVP)

**As a** DeepSeek V4 Pro LLM generating the presale report,
**I want to** see concrete narrative examples for each section type from the canonical reference,
**so that** I can emulate the reference style with high fidelity instead of guessing from abstract rules.

---

<context>
@.planning/PROJECT.md
@.planning/phases/05-deep-interpretation/05-CONTEXT.md
@.planning/phases/05-deep-interpretation/05-01-PLAN.md

@AIM/hermes/app/orchestrator/pass_fill_assemble.py

@/Users/mikhaileliseev/Downloads/ИПХиК (2).html

<interfaces>
<!-- Current state of pass_fill_assemble.py after Plan 05-01 -->

```python
def _build_prompt(state: OrchestratorState) -> str:
    # ... setup ...
    return (
        # ... items 1-15 (Phase 3+4) ...
        # ... items 16-18 (Plan 05-01 Task 1: narrative, business language, cross-references) ...
        # ... items 19-21 (Plan 05-01 Task 2: gap-block, blockquote, short reference calibration) ...
        "21. REFERENCE CALIBRATION (D-11): ИСТИНА для всех narrative правил — референс "
        "`/Users/mikhaileliseev/Downloads/ИПХиК (2).html` ..."
        # ↑ Plan 05-03 appends an EXAMPLES block AFTER item 21 ↑
    )
```

**Reference HTML structure** (10 sections, 965 lines, 78KB):
- Section 01: About (ОКВЭД, licenses, revenue dynamics)
- Section 02: Market (competitor table + gap-blocks + blockquote)
- Section 03: Experts (top-5 doctors with metrics)
- Section 04: Content Analysis (per-doctor + top-5 fears + blockquote)
- Section 05: Media (SMI publications with URLs)
- Section 06: Competitor Cards (detailed per-competitor)
- Section 07: Whitefields matrix (4×4 comparison)
- Section 08: Presence (digital audit)
- Section 09: Strategy (5 directions + gap-blocks + blockquote)
- Section 10: Offer (8 points + blockquote + CTA)
</interfaces>

<reference_extraction_contract>
<!-- The executor MUST read the reference HTML in full and extract examples -->

**Mandatory reading:** `/Users/mikhaileliseev/Downloads/ИПХиК (2).html` (78KB, 965 lines)

The executor reads the reference HTML, identifies the BEST narrative example per section (judgment: most representative of narrative style with embedded numbers + business language), and embeds them inline in the prompt.

**Extraction rules:**
1. ONE primary example per section (10 total)
2. TWO cross-section reference examples (showing how section A cites data from section B)
3. TWO gap-block examples (1 strength with ✅, 1 growth point with 📍)
4. TWO blockquote examples (1 from section 02 Market, 1 from section 09 Strategy)
5. Keep each example SHORT (50-200 chars) — long enough to show style, short enough to fit token budget
6. Preserve original Russian text (no translation)
7. Wrap examples in clear section headers so LLM can map example → section type

**Extract patterns to look for:**
- Numbers embedded in narrative ("4.3 млрд выручки против 2.3 млрд у Seline")
- Business interpretation of metrics ("отрыв в масштабе", "разрыв между...")
- Cross-references ("Видя, что {X} из секции 04...")
- Strategic conclusions in blockquote ("Отрыв не в деньгах - отрыв в масштабе")
- Competitor benchmarks in gap-blocks ("Олимп Клиник имеет Telegram-канал 15K")
</reference_extraction_contract>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract 10+ narrative examples from reference HTML and embed as EXAMPLES block in Pass 3 prompt</name>
  <files>AIM/hermes/app/orchestrator/pass_fill_assemble.py</files>
  <behavior>
    - After commit: AST parse OK under Python 3.11
    - After commit: grep finds "ПРИМЕРЫ ИЗ РЕФЕРЕНСА" (or equivalent calibration block header)
    - After commit: grep finds at least 10 references to section-specific examples (e.g., "Секция 02", "Секция 09")
    - After commit: at least 2 cross-reference examples embedded
    - After commit: at least 2 gap-block examples (✅ + 📍)
    - After commit: at least 2 blockquote examples
    - After commit: items 1-21 from Plan 05-01 + Phase 4 preserved (regression)
    - After commit: function signature `_build_prompt(state: OrchestratorState) -> str` unchanged
    - After commit: _PASS_FILL_TIMEOUT = 600 unchanged
    - After commit: prompt length increase ≤ 6KB (~1500 tokens)
  </behavior>
  <action>
PREREQUISITE: Read the reference HTML `/Users/mikhaileliseev/Downloads/ИПХиК (2).html` in FULL (all 965 lines). Identify the best narrative example per section per the extraction contract in `<reference_extraction_contract>`.

MODIFY `AIM/hermes/app/orchestrator/pass_fill_assemble.py`:

1. **Update module docstring** — add a "Phase 5 / Plan 05-03" block documenting: "Pass 3 prompt extended with EXAMPLES BY SECTION calibration block. 10+ narrative examples extracted from reference `ИПХиК (2).html` (one per section) + 2 cross-reference examples + 2 gap-block examples + 2 blockquote examples. D-11 fully satisfied (Plan 05-01 added short pointer; this plan adds comprehensive calibration)."

2. **Append EXAMPLES block to `_build_prompt` return string** — AFTER item 21 (which ends with `... возвращайся к референсу как канону.\n\n`), append a new calibration block. Structure:

```
"ПРИМЕРЫ ИЗ РЕФЕРЕНСА (стиль и глубина — CANON):\n"
"Секция 01 (About): «Динамика выручки: +79% за 3 года (2.4 млрд в 2022, "
"3.4 млрд в 2024, 4.3 млрд в 2025). Чистая прибыль 139 млн руб. в 2025. "
"Рост сотрудников: +74% за 4 года (364 в 2021, 632 в 2025)» — нарратив "
"с цифрами, не метрик-дамп.\n\n"

"Секция 02 (Market): «ИПХиК — безусловный лидер по выручке. 4.3 млрд "
"выручки против 2.3 млрд у Seline (ближайший конкурент). По числу "
"хирургов (150+) отрыв от Seline (10+) в 15 раз» — вывод первой фразой, "
"затем раскрытие с цифрами и сравнением.\n"
"  Gap-блок (сильная сторона): «✅ Сильная сторона: масштаб и академическая "
"база (150+ хирургов, 88 лет истории, 6 товарных знаков) — ни один "
"конкурент не может предложить аналогичную глубину».\n"
"  Gap-блок (точка роста): «📍 Точка роста: цифровое присутствие — "
"ориентир: Олимп Клиник (Telegram-канал, MedicalBusiness Schema)».\n"
"  Blockquote: «Отрыв не в деньгах — отрыв в масштабе».\n\n"

"Секция 03 (Experts): «Мельников (318K) — абсолютный лидер рынка "
"пластической хирургии по аудитории. Ни у одного конкурента нет врача "
"с аудиторией даже близкой к этой цифре» — вывод + сравнение.\n\n"

"Секция 04 (Content Analysis): «Только Авдошенко системно работает со "
"страхами пациентов (3 из 5). Мельников имеет колоссальную аудиторию "
"(318K), но не использует её для закрытия страхов» — анализ с цифрами.\n"
"  Cross-reference пример: «Если Мельников запустит рубрику про наркоз "
"и реабилитацию — охват будет измеряться сотнями тысяч просмотров» "
"(ссылка на топ-страх из списка ниже + на топ-врача из секции 03).\n\n"

"Секция 05 (Media): «Институт регулярно упоминается в профильных и "
"деловых изданиях. Тон преимущественно нейтральный/позитивный. Ключевые "
"нарративы: лидер рынка, инновации, экспансия, образование» — "
"структурированный вывод с классификацией.\n\n"

"Секция 06 (Competitor Cards): «Seline — 2.3 млрд выручки, 10+ хирургов, "
"27K Instagram. Растёт +19.1% (медленнее ИПХиК). Сильна в B2B-сегменте» — "
"карточка с динамикой и УТП.\n\n"

"Секция 07 (Whitefields): «Рынок пластической хирургии Москвы — абсолютно "
"белое поле для цифрового маркетинга. Telegram-каналов нет ни у кого. "
"MedicalBusiness Schema отсутствует у всех» — вывод по матрице.\n\n"

"Секция 08 (Presence): «При отраслевом бенчмарке 2.5s сайт грузится 7.3s "
"— каждая секунда задержки теряет пациентов» — бизнес-язык для тех. метрики.\n\n"

"Секция 09 (Strategy): «У ИПХиК есть 150+ хирургов, 15 из которых уже "
"имеют соцсети с аудиторией 587K. Не надо создавать маркетинг с нуля — "
"достаточно системно интегрировать то, что уже работает» — стратегический "
"вывод из данных.\n"
"  Cross-reference: «Видя, что {страх 'наркоз'} из секции 04 — главный "
"страх пациентов, стратегия по направлению Контент должна закрыть "
"именно этот страх через формат до/после».\n"
"  Blockquote: «Это даст рост органического трафика в 3-5x при бюджете "
"ниже, чем у конкурентов».\n\n"

"Секция 10 (Offer): «AIM не даёт одного человека — мы даём систему: "
"AI-агент, который делает работу 3 человек. Разведка, контент-план, "
"анализ конкурентов, GEO, Telegram — всё в одном автоматизированном "
"процессе» — конкретное УТП с отстройкой от альтернатив.\n\n"

"ОБЩИЕ ПРИНЦИПЫ (извлечённые из примеров выше):\n"
"- Первая фраза секции = ВЫВОД с цифрой, не пересказ данных.\n"
"- Каждая цифра сопровождается сравнением (с конкурентом, с отраслью, с предыдущим периодом).\n"
"- Каждая секция заканчивается blockquote с главным strategic insight.\n"
"- Cross-references делаются НЕ хардкодом, а органически вплетаются в нарратив.\n"
"- Бизнес-язык: «теряет пациентов», «отрыв в масштабе», «белое поле для маркетинга».\n\n"
```

3. **Verify Python 3.11 f-string safety** — the EXAMPLES block contains MANY double quotes inside (Russian text uses «...» but also has `"` for nested quotes). Use plain `"..."` string literals concatenated. If a double quote appears inside the string content, escape as `\"`. Verify with AST parse before commit.

4. **Regression check** — items 1-21 preserved. Test by running:
   ```
   python3 -c "content = open('AIM/hermes/app/orchestrator/pass_fill_assemble.py').read(); assert content.count('NARRATIVE STYLE') == 1; assert content.count('REFERENCE CALIBRATION') >= 1; assert content.count('ПРИМЕРЫ ИЗ РЕФЕРЕНСА') == 1"
   ```

5. **Prompt length sanity check** — verify the new prompt string length is ≤ 16KB total (~4000 tokens). If over budget, trim examples to be more concise.

6. **COMMIT atomically**: `git commit -m "feat(05-03): add EXAMPLES BY SECTION calibration block with 10+ reference snippets to Pass 3 prompt (D-11)"`.
  </action>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && python3 -c "import ast; ast.parse(open('AIM/hermes/app/orchestrator/pass_fill_assemble.py').read()); print('AST OK')" && python3 -c "
content = open('AIM/hermes/app/orchestrator/pass_fill_assemble.py').read()
assert 'ПРИМЕРЫ ИЗ РЕФЕРЕНСА' in content, 'missing calibration block header'
assert 'Секция 01' in content, 'missing section 01 example'
assert 'Секция 02' in content, 'missing section 02 example'
assert 'Секция 09' in content, 'missing section 09 example'
assert 'Секция 10' in content, 'missing section 10 example'
assert 'Cross-reference' in content or 'cross-reference' in content.lower(), 'missing cross-reference label'
assert '✅' in content, 'missing strength emoji in examples'
assert '📍' in content, 'missing growth emoji in examples'
assert 'Blockquote' in content or 'blockquote' in content.lower(), 'missing blockquote label'
# Regression
assert '16. NARRATIVE STYLE' in content, 'REGRESSION: item 16 lost'
assert '21. REFERENCE CALIBRATION' in content, 'REGRESSION: item 21 lost'
assert 'STRATEGY SECTION' in content, 'REGRESSION: Phase 4 item 7 lost'
assert '_PASS_FILL_TIMEOUT = 600' in content, 'timeout changed'
assert 'def _build_prompt(state: OrchestratorState) -> str:' in content, 'signature changed'
# Prompt length check
import re
match = re.search(r'return \((.+?)\n    \)', content, re.DOTALL)
if match:
    prompt_len = len(match.group(1))
    print(f'Prompt string length: {prompt_len} chars')
    assert prompt_len < 20000, f'prompt too long: {prompt_len} chars'
print('All Task 1 assertions PASS')
"</automated>
  </verify>
  <done>
    - pass_fill_assemble.py AST parses cleanly under Python 3.11
    - EXAMPLES BY SECTION calibration block present with header "ПРИМЕРЫ ИЗ РЕФЕРЕНСА"
    - At least 10 section-specific examples (Секция 01 through Секция 10)
    - At least 2 cross-reference examples labeled
    - At least 2 gap-block examples with ✅ and 📍 emoji
    - At least 2 blockquote examples labeled
    - Items 1-21 from Plan 05-01 + Phase 4 preserved (regression)
    - Function signature unchanged
    - _PASS_FILL_TIMEOUT = 600 unchanged
    - Prompt total length ≤ 20KB (~5000 tokens)
    - One atomic commit created
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reference HTML → Prompt | Static reference text embedded in prompt — no untrusted input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-T | Tampering | LLM could mimic examples too literally (copy specific numbers from reference instead of using clinic's data) | mitigate | Each example wrapped with "стиль и глубина — CANON" — explicitly frames them as STYLE examples, not DATA sources; existing ORC-04 honest principle (Phase 2) + Plan 05-01 item 16 "с цифрами из данных" both still enforced |
| T-05-03-I | Info disclosure | Reference examples contain real clinic data (ИПХиК revenue, doctor names) | accept | Reference is a PUBLIC document already shared with the client as a sales asset; embedding in LLM prompt doesn't expose new data — LLM has its own collected data for the actual client |
| T-05-03-D | DoS | More examples → longer prompt → more tokens | accept | Plan 05-03 adds ~4KB (~1000 tokens) — total prompt stays under 20KB (~5K tokens), sub-4% of DeepSeek 128K context; existing _PASS_FILL_TIMEOUT=600s sufficient |
| T-05-03-E | EoP | n/a | accept | Pure prompt text expansion |
| T-05-03-SC | Supply chain | No new packages | accept | Pure Python string changes |
</threat_model>

<verification>
## Plan-Level Checks

1. **AST parse under Python 3.11** — syntax valid
2. **Calibration block present** — header "ПРИМЕРЫ ИЗ РЕФЕРЕНСА" findable
3. **10 section examples** — Секция 01 through Секция 10 all present
4. **Cross-references** — at least 2 labeled
5. **Gap-block examples** — at least 1 strength (✅) + 1 growth (📍)
6. **Blockquote examples** — at least 2 labeled
7. **Regression** — items 1-21 (Plan 05-01 + Phase 4) preserved
8. **Function signature** — unchanged
9. **Timeout** — _PASS_FILL_TIMEOUT = 600 unchanged
10. **Prompt length** — ≤ 20KB total (~5000 tokens)
11. **One atomic commit** — in git log

## Requirement Coverage

| Requirement | How This Plan Reinforces It |
|-------------|---------------------------|
| INT-01 (narrative rewrite) | Calibration block makes narrative style concrete — LLM sees 10 examples of narrative vs metric-dump |
| INT-02 (cross-linked sections) | Cross-reference examples show HOW to weave links naturally |
| INT-03 (business language) | Examples demonstrate business language with embedded numbers |
| INT-04 (gap-blocks) | 2 concrete gap-block examples (strength + growth with competitor benchmark) |
| INT-05 (blockquote per section) | 2 blockquote examples showing 1-2 sentence strategic insight pattern |
</verification>

<success_criteria>
- EXAMPLES BY SECTION calibration block added to Pass 3 prompt
- 10+ narrative examples extracted from canonical reference HTML
- 2+ cross-reference examples
- 2+ gap-block examples
- 2+ blockquote examples
- D-11 FULLY satisfied (Plan 05-01 item 21 was short pointer; this plan is comprehensive)
- Regression — Plan 05-01 items + Phase 4 items all preserved
- Python 3.11 compatibility verified
- One atomic commit in git log
</success_criteria>

<output>
Create `.planning/phases/05-deep-interpretation/05-03-SUMMARY.md` when done.
</output>
