---
phase: 05-deep-interpretation
plan: 01
type: execute
name: pass3-prompt-narrative-rules
wave: 1
depends_on: []
requirements: [INT-01, INT-02, INT-03, INT-04, INT-05]
tags: [orchestrator, pass3, prompt, narrative, business-language, cross-references, gap-blocks, blockquote, reference-calibration, int-01, int-02, int-03, int-04, int-05, d-01, d-02, d-03, d-04, d-05, d-06, d-07, d-09, d-11]

files_modified:
  - AIM/hermes/app/orchestrator/pass_fill_assemble.py

autonomous: true

must_haves:
  truths:
    - "INT-01/D-01/D-02: Pass 3 prompt contains an explicit NARRATIVE STYLE rule (item 16) instructing the LLM to generate 2-3 coherent paragraphs per section with conclusions and numbers — NOT a metric dump"
    - "INT-03/D-05/D-06: Pass 3 prompt contains a BUSINESS LANGUAGE rule (item 17) with a 5-entry translation dictionary (LCP, Bounce rate, CLS, DA, Backlinks) — numbers stay accompanied by human interpretation"
    - "INT-02/D-03/D-04: Pass 3 prompt contains a CROSS-REFERENCES rule (item 18) requiring every section to reference data from at least one other section (e.g., strategy references fears from section 04)"
    - "INT-04/D-07: Pass 3 prompt contains a GAP-BLOCK FORMAT rule (item 19) specifying the ✅ strength + 📍 growth point structure with numbers and competitor benchmarks"
    - "INT-05/D-09/D-10: Pass 3 prompt contains a SECTION BLOCKQUOTE rule (item 20) requiring each section to end with `<blockquote class='section-insight'>` containing the main strategic insight (1-2 sentences)"
    - "D-11: Pass 3 prompt contains a REFERENCE CALIBRATION rule (item 21) pointing the LLM to `ИПХиК (2).html` style as the truth, with a concrete short example narrative paragraph embedded inline"
    - "Items 16-21 are appended AFTER existing items 1-15 (Phase 3+4) — preserves LLM attention hierarchy"
    - "Function signature `_build_prompt(state: OrchestratorState) -> str` unchanged"
    - "_PASS_FILL_TIMEOUT=600 unchanged (text-only prompt expansion doesn't affect runtime)"
    - "run_pass_fill_assemble body unchanged — only the prompt builder is modified"
  artifacts:
    - path: "AIM/hermes/app/orchestrator/pass_fill_assemble.py"
      provides: "Pass 3 _build_prompt extended with items 16-21 (narrative + business + cross-ref + gap-block + blockquote + reference calibration rules)"
      contains: "16. NARRATIVE STYLE"
      min_lines: 290
  key_links:
    - from: "pass_fill_assemble._build_prompt items 16-21"
      to: "LLM generates narrative section content + insight kwargs"
      via: "Pass 3 prompt instructions consumed by DeepSeek V4 Pro"
      pattern: "NARRATIVE STYLE|BUSINESS LANGUAGE|CROSS-REFERENCES|GAP-BLOCK|SECTION BLOCKQUOTE|REFERENCE CALIBRATION"
    - from: "Plan 05-02 (HTML helpers)"
      to: "items 19+20 kwarg contracts (insight, gap_blocks per section)"
      via: "LLM passes section_insights + section_gap_blocks kwargs to generate_html_report"
      pattern: "section_insights|section_gap_blocks"
---

<objective>
Extend the Pass 3 orchestrator prompt (`pass_fill_assemble.py:_build_prompt`) with 6 new numbered items (16-21) that transform section content from metric dumps into narrative business insights matching the reference `ИПХиК (2).html`.

This is the PROMPT LAYER of Phase 5. The LLM receives explicit rules for:
- **Narrative style** (item 16, D-02): 2-3 coherent paragraphs per section, not bullet lists of metrics
- **Business language** (item 17, D-05/D-06): translation dictionary for technical jargon → human meaning
- **Cross-references** (item 18, D-03/D-04): every section references data from at least one other section
- **Gap-block format** (item 19, D-07): ✅ strength (with number) + 📍 growth point (with competitor benchmark)
- **Section blockquote** (item 20, D-09/D-10): each section ends with main strategic insight in `<blockquote>`
- **Reference calibration** (item 21, D-11): `ИПХиК (2).html` as the canonical style truth

Purpose: Plan 04-05 added items 7-15 telling the LLM WHAT to generate per section. This plan adds the cross-cutting QUALITY rules for HOW to write each section. Plan 05-02 adds HTML helpers that render the new `insight` and `gap_blocks` kwargs.

Output: Modified `pass_fill_assemble.py` with 6 new prompt items (items 16-21) appended to the existing 15-item prompt string. ~120 lines added (text-only).
</objective>

## Phase Goal (MVP)

**As a** reader of the presale HTML report,
**I want to** see narrative text with business insights in every section (not metric dumps),
**so that** the report matches the reference `ИПХиК (2).html` quality and is useful for business decisions.

---

<context>
@.planning/PROJECT.md
@.planning/phases/05-deep-interpretation/05-CONTEXT.md
@.planning/phases/04-new-sections-data-depth/04-05-SUMMARY.md
@.planning/REQUIREMENTS.md

@AIM/hermes/app/orchestrator/pass_fill_assemble.py

<interfaces>
<!-- Current state of _build_prompt return string (after Phase 4 Plan 04-05) -->

From AIM/hermes/app/orchestrator/pass_fill_assemble.py:
```python
def _build_prompt(state: OrchestratorState) -> str:
    """Build the Pass 3 prompt — fill gaps then assemble HTML."""
    # ... setup code (lines 101-143) ...

    return (
        f"Вот твой gap report из Pass 2: summary=[{summary_line}].{missing_block}"
        f"{coverage_hint}\n\n"
        "ЗАДАЧА:\n"
        "1. Если есть fillable пробелы...\n"
        # ... items 1-6 (Phase 3 — fill, generate_html_report, ORC-04, coverage, niche, instagram) ...
        # ... items 7-11 (Phase 4 — Strategy, Offer, Whitefields, Experts, Content) ...
        # ... items 12-15 (Phase 4 — Revenue dynamics, Clinic metrics, Media URLs, Ratings) ...
        "15. RATINGS: Покажи рейтинги из run_review_platforms для "
        "минимум 2 платформ: ПроДокторов + Яндекс.Карты. Для каждой: "
        "рейтинг (звёзды), количество отзывов, главные темы положительных "
        "и отрицательных отзывов.\n"
        "   Передай ratings_data как kwarg в generate_html_report.\n\n"
        # ↑ Phase 5 / Plan 05-01 appends items 16-21 HERE ↑
    )
```

**Function signature (unchanged):**
```python
def _build_prompt(state: OrchestratorState) -> str:
```

**Module-level constant (unchanged):**
```python
_PASS_FILL_TIMEOUT = 600
```

**Existing items count:** 15 (after Phase 4 Plan 04-05). Phase 5 appends items 16-21 → total 21 items.

**Prompt length estimate:** Phase 4 ended at ~6480 chars. Phase 5 adds ~3000-4000 chars (items 16-21). Total ~10KB (~2500 tokens) — well under 1% of DeepSeek 128K context.
</interfaces>

<reference_style_canon>
<!-- Narrative style examples extracted from ИПХиК (2).html — the CANONICAL truth for Phase 5 -->

**Section 02 (Market) — narrative paragraph pattern:**
```
ИПХиК - безусловный лидер по выручке. 4.3 млрд выручки против 2.3 млрд у Seline (ближайший конкурент).
По числу хирургов (150+) отрыв от Seline (10+) в 15 раз. При этом суммарная аудитория топ-5 врачей
ИПХиК (~587K) больше, чем у всех конкурентов вместе взятых.
```

**Section 02 — gap-block pattern (strength):**
```
Сильная сторона: масштаб и академическая база.
150+ хирургов, 88 лет истории, 6 товарных знаков, собственный учебный центр.
ИПХиК не просто клиника - это институт с кафедрой, образовательной лицензией и научной школой.
Ни один конкурент не может предложить аналогичную глубину.
```

**Section 02 — gap-block pattern (growth point with competitor benchmark):**
```
Точка роста: цифровое присутствие института.
При 4.3 млрд выручки и 150+ хирургах - нет официального Telegram-канала, VK-страница на 1.6K
подписчиков (в 2 раза меньше, чем у конкурентов). Разрыв между масштабом бизнеса и
digital-присутствием.
```

**Section 02 — blockquote (main insight):**
```
Главный вывод: ИПХиК крупнее ближайшего конкурента (Seline, 2.3 млрд) почти в 2 раза.
По числу хирургов (150+) отрыв от Seline (10+) в 15 раз. Отрыв не в деньгах - отрыв в масштабе.
```

**Section 04 (Content Analysis) — cross-reference pattern:**
```
Только Авдошенко системно работает со страхами пациентов (3 из 5). Мельников имеет колоссальную
аудиторию (318K), но не использует её для закрытия страхов. Если Мельников запустит рубрику про
наркоз и реабилитацию - охват будет измеряться сотнями тысяч просмотров.
```
(References top fears from section 04 + top doctor from section 03 + strategy direction for section 09)

**Section 09 (Strategy) — business language with embedded numbers:**
```
У ИПХиК есть 150+ хирургов, 15 из которых уже имеют соцсети с аудиторией 587K. Не надо создавать
маркетинг с нуля - достаточно системно интегрировать то, что уже работает. Это даст рост
органического трафика в 3-5x при бюджете ниже, чем у конкурентов.
```

These examples MUST be embedded in item 21 (REFERENCE CALIBRATION) as few-shot pointers.
</reference_style_canon>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add prompt items 16-18 (narrative style + business language + cross-references)</name>
  <files>AIM/hermes/app/orchestrator/pass_fill_assemble.py</files>
  <behavior>
    - After commit: `python3 -c "import ast; ast.parse(open('AIM/hermes/app/orchestrator/pass_fill_assemble.py').read())"` returns exit 0 (syntax valid)
    - After commit: grep finds "16. NARRATIVE STYLE" in pass_fill_assemble.py
    - After commit: grep finds "17. BUSINESS LANGUAGE" in pass_fill_assemble.py
    - After commit: grep finds "18. CROSS-REFERENCES" in pass_fill_assemble.py
    - After commit: grep finds "LCP" AND "задержка" in close proximity (business language dict)
    - After commit: grep finds "Bounce rate" AND "уходят" in close proximity
    - After commit: grep finds "страхи пациентов" AND "секция 04" in cross-references rule
    - After commit: items 1-15 preserved unchanged (regression — grep for "STRATEGY SECTION", "OFFER SECTION", "WHITEFIELDS MATRIX", "RATINGS" still finds them)
    - After commit: `_PASS_FILL_TIMEOUT == 600` unchanged
    - After commit: function signature `_build_prompt(state: OrchestratorState) -> str` unchanged
  </behavior>
  <action>
MODIFY `AIM/hermes/app/orchestrator/pass_fill_assemble.py`:

1. **Update module docstring** (lines 1-31): Add a "Phase 5 / Plan 05-01" block after the existing "Phase 4 / Plan 04-05" block. The new block documents: "Pass 3 prompt extended with cross-cutting narrative quality rules (items 16-21). Items 16-18 added in Task 1 (narrative style, business language, cross-references). Items 19-21 added in Task 2 (gap-block format, section blockquote, reference calibration). INT-01..05 prompt-layer satisfied. HTML rendering layer in Plan 05-02."

2. **Append items 16-18 to `_build_prompt` return string** (after item 15 "RATINGS", before the closing paren). The text MUST be added inside the existing return tuple as new string literals concatenated with the previous item. Append exactly this text (in Russian, as the LLM prompt is Russian), keeping f-string safety in mind (NO backslash-escaped double quotes inside expression parts — Python 3.11 container runs Python 3.11 which rejects this; use single quotes for inner quotes or extract to local variables):

```
"16. NARRATIVE STYLE (D-02 — ОБЯЗАТЕЛЬНО для всех секций 7-15 выше): "
"КАЖДАЯ секция отчёта = 2-3 параграфа связного текста с выводами и цифрами, "
"НЕ маркированный список метрик. Образец — референс ИПХиК (2).html секция Market: "
"«Клиника ИПХиК стабильно удерживает топ-3 позицию по выручке. 4.3 млрд выручки "
"против 2.3 млрд у Seline (ближайший конкурент)» — это нарратив с цифрой внутри, "
"НЕ «Выручка: 4.3 млрд, Рост: +26%». "
"Правила: (a) первый параграф = главный вывод секции одной фразой с цифрой; "
"(b) второй параграф = раскрытие вывода (откуда цифра, сравнение с конкурентами); "
"(c) третий параграф (опционально) = что это значит для бизнеса клиента. "
"Запрещено: «Скорость загрузки: 7.3s, Bounce rate: 65%» без интерпретации. "
"Разрешено: «Сайт грузится 7.3 секунды — каждая секунда задержки теряет "
"пациентов; две трети посетителей уходят, не дочитав (bounce rate 65%)».\n\n"

"17. BUSINESS LANGUAGE (D-05, D-06): Технические метрики СОПРОВОЖДАЙ "
"человеческой интерпретацией. Цифры ОСТАЮТСЯ (LCP 7.3s, выручка 4.3 млрд), "
"но сопровождаются объяснением. Словарь замен (используй ВМЕСТЕ с цифрой, "
"не вместо): "
"(a) LCP / скорость загрузки → «каждая секунда задержки теряет пациентов»; "
"(b) Bounce rate 65% → «две трети посетителей уходят, не дочитав»; "
"(c) CLS 0.4 → «контент прыгает при загрузке — выглядит непрофессионально»; "
"(d) DA 25 → «поисковики слабо доверяют сайту»; "
"(e) Backlinks 45 → «45 сайтов ссылаются на клинику — мало для топ-3». "
"Образец: «LCP 7.3s — каждая секунда задержки теряет пациентов; при отраслевом "
"бенчмарке 2.5s клиника теряет ~5% лидов с каждого визита».\n\n"

"18. CROSS-REFERENCES (D-03, D-04): КАЖДАЯ секция должна ссылаться на данные "
"из других секций — НЕ изолированные блоки. Минимум 1 cross-reference на секцию. "
"Паттерны: "
"(a) Strategy (09) → упоминает конкретные страхи из секции 04 (Content Analysis), "
"конкретные пробелы врачей из секции 04, конкретные рейтинги из Reviews; "
"(b) Offer (10) → каждый пункт привязан к цифре или gap из других секций; "
"(c) Content Analysis (04) → ссылается на топ-врачей из секции 03 (Experts); "
"(d) Whitefields matrix (07) → ячейки заполнены из competitors (02), doctors (03), "
"instagram (04), financials (About). "
"Формулировка (пример для Strategy): «Видя, что {top_fear} из секции 04 — "
"главный страх пациентов, стратегия по направлению Контент должна закрыть "
"именно этот страх через формат {format}». LLM генерирует cross-references "
"САМ на основе собранных данных, никаких хардкод-линков.\n\n"
```

3. **Verify Python 3.11 f-string safety**: The existing return uses `f"..."` for the first 2 lines (gap_report interpolation) and plain `"..."` for items 1-15. Items 16-18 are pure string literals (no interpolation needed) — use plain `"..."` concatenated strings. NO backslash-escaped double quotes inside expression parts. If a quoted phrase is needed inside the prompt text, use single quotes `'«...»'` or escape via alternative phrasing. Run `python3 -c "import ast; ast.parse(open('AIM/hermes/app/orchestrator/pass_fill_assemble.py').read())"` to verify syntax.

4. **Run regression grep**: Verify items 1-15 markers still present (STRATEGY SECTION, OFFER SECTION, WHITEFIELDS MATRIX, EXPERTS SECTION, CONTENT ANALYSIS, REVENUE DYNAMICS, CLINIC METRICS, MEDIA URLS, RATINGS) — they MUST be unchanged.

5. **Commit atomically**: `git commit -m "feat(05-01): add Pass 3 prompt items 16-18 (narrative + business language + cross-references)"` — only pass_fill_assemble.py in this commit.
  </action>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && python3 -c "import ast; ast.parse(open('AIM/hermes/app/orchestrator/pass_fill_assemble.py').read()); print('AST OK')" && python3 -c "content = open('AIM/hermes/app/orchestrator/pass_fill_assemble.py').read(); assert '16. NARRATIVE STYLE' in content, 'missing item 16'; assert '17. BUSINESS LANGUAGE' in content, 'missing item 17'; assert '18. CROSS-REFERENCES' in content, 'missing item 18'; assert 'LCP' in content and 'задержка' in content, 'missing LCP business translation'; assert 'Bounce rate' in content, 'missing Bounce rate'; assert 'страхи пациентов' in content, 'missing cross-ref keyword'; assert 'STRATEGY SECTION' in content, 'REGRESSION: item 7 lost'; assert 'WHITEFIELDS MATRIX' in content, 'REGRESSION: item 9 lost'; assert 'RATINGS' in content, 'REGRESSION: item 15 lost'; assert '_PASS_FILL_TIMEOUT = 600' in content, 'timeout changed'; assert 'def _build_prompt(state: OrchestratorState) -> str:' in content, 'signature changed'; print('All Task 1 assertions PASS')"</automated>
  </verify>
  <done>
    - pass_fill_assemble.py AST parses cleanly under Python 3.11
    - Items 16, 17, 18 present in source with the exact headers "16. NARRATIVE STYLE", "17. BUSINESS LANGUAGE", "18. CROSS-REFERENCES"
    - Business language dictionary contains all 5 entries (LCP, Bounce rate, CLS, DA, Backlinks) with Russian translations
    - Cross-references rule references "страхи пациентов" and "секция 04"
    - Items 1-15 (Phase 3+4) preserved unchanged (regression check passes)
    - Function signature `_build_prompt(state: OrchestratorState) -> str` unchanged
    - `_PASS_FILL_TIMEOUT = 600` unchanged
    - One atomic commit created with message referencing Plan 05-01 Task 1
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add prompt items 19-21 (gap-block format + section blockquote + reference calibration)</name>
  <files>AIM/hermes/app/orchestrator/pass_fill_assemble.py</files>
  <behavior>
    - After commit: AST parse OK
    - After commit: grep finds "19. GAP-BLOCK FORMAT" in source
    - After commit: grep finds "20. SECTION BLOCKQUOTE" in source
    - After commit: grep finds "21. REFERENCE CALIBRATION" in source
    - After commit: grep finds "✅" AND "📍" emoji in gap-block format rule
    - After commit: grep finds "section-insight" CSS class reference in blockquote rule
    - After commit: grep finds "ИПХиК (2).html" reference + at least 1 embedded narrative example
    - After commit: items 16-18 from Task 1 preserved (regression)
    - After commit: items 1-15 from Phase 4 preserved (regression)
    - Total prompt items count = 21 (verified via grep count of numbered items)
  </behavior>
  <action>
MODIFY `AIM/hermes/app/orchestrator/pass_fill_assemble.py` (continuing from Task 1):

1. **Append items 19-21 to `_build_prompt` return string** (after item 18 "CROSS-REFERENCES", before the closing paren). Use plain `"..."` string literals (no f-string interpolation needed for these items). Append exactly this text:

```
"19. GAP-BLOCK FORMAT (D-07 — ОБЯЗАТЕЛЬНО для каждой секции 7-15): В каждой "
"секции после нарративных параграфов добавляй 2-4 gap-блока по единому формату: "
"✅ Сильная сторона: {что} ({цифра из данных}) — почему это сильная сторона. "
"📍 Точка роста: {что улучшить} — ориентир: {конкурент} ({цифра конкурента}). "
"Правила: (a) каждый gap-блок содержит ЦИФРУ из собранных данных (не общие "
"слова); (b) точка роста ОБЯЗАТЕЛЬНО содержит бенчмарк на конкретного конкурента "
"с цифрой; (c) если нет данных для gap-блока — НЕ выдумывай, пропусти секцию. "
"Передай gap_blocks как kwarg в generate_html_report в виде списка словарей: "
"[{'type': 'strength'|'growth', 'title': str, 'description': str}]. "
"Образец из референса: «✅ Сильная сторона: масштаб и академическая база (150+ "
"хирургов, 88 лет истории)» / «📍 Точка роста: цифровое присутствие — ориентир: "
"Олимп Клиник (Telegram-канал 15K, MedicalBusiness Schema внедрена)».\n\n"

"20. SECTION BLOCKQUOTE (D-09, D-10 — ОБЯЗАТЕЛЬНО для каждой секции 7-15): "
"КАЖДАЯ секция заканчивается blockquote с главным strategic insight (1-2 "
"предложения, бизнес-язык). Формат HTML: `<blockquote class=\"section-insight\">"
"{главный вывод секции}</blockquote>`. Передай insight как kwarg в "
"generate_html_report (string, не dict). Правила: (a) insight = ВЫВОД, не "
"пересказ данных (не «Выручка 4.3 млрд», а «ИПХиК — безусловный лидер рынка "
"по выручке, отрыв от ближайшего конкурента в 2 раза»); (b) 1-2 предложения "
"максимум; (c) если нет данных для вывода — честно «данные недоступны, "
"инсайт сформировать нельзя» (ORC-04 honest principle). "
"Образцы из референса: секция 02 — «Отрыв не в деньгах - отрыв в масштабе»; "
"секция 09 — «У ИПХиК есть 150+ хирургов, 15 из которых уже имеют соцсети с "
"аудиторией 587K. Не надо создавать маркетинг с нуля».\n\n"

"21. REFERENCE CALIBRATION (D-11): ИСТИНА для всех narrative правил — референс "
"`/Users/mikhaileliseev/Downloads/ИПХиК (2).html` (78KB, 965 строк, 10 секций). "
"ПОДРАЖАЙ стилю и глубине референса. Конкретные образцы стиля (few-shot): "
"Секция About: «Динамика выручки: +79% за 3 года (2.4 млрд в 2022, 3.4 млрд "
"в 2024, 4.3 млрд в 2025). Чистая прибыль 139 млн руб. в 2025. Рост "
"сотрудников: +74% за 4 года» — нарратив с цифрами, не метрик-дамп. "
"Секция Market: «ИПХиК — безусловный лидер по выручке» — вывод первой "
"фразой, затем раскрытие. "
"Секция Strategy: «У ИПХиК есть все активы для лидерства в digital: масштаб, "
"аудитория врачей, академический вес» — цепочка аргументов. "
"Секция Offer: «AIM не даёт одного человека — мы даём систему: AI-агент, "
"который делает работу 3 человек» — конкретное УТП. "
"КАЖДОЕ правило 16-20 выше — это формализация того, что уже есть в референсе. "
"Если сомневаешься в стиле — возвращайся к референсу как канону.\n\n"
```

2. **Verify Python 3.11 f-string safety**: Items 19-21 contain double quotes inside the string (e.g., `<blockquote class=\"section-insight\">`). Since the outer string literal is `"..."`, inner double quotes MUST be escaped as `\"`. This is valid in plain string literals (NOT inside f-string expression parts). Verify with AST parse. If AST parse fails, alternative: use single-quoted outer string `'...'` and unescaped double quotes inside, OR triple-quoted strings `"""..."""`. Choose the approach that keeps the file Python 3.11-compatible.

3. **Update module docstring** (if not already in Task 1): Append "Items 19-21 added in Task 2 (gap-block format, section blockquote, reference calibration with embedded few-shot examples). Total prompt items: 21."

4. **Regression check**: Items 16-18 from Task 1 still present. Items 1-15 from Phase 4 still present. Function signature unchanged.

5. **Commit atomically**: `git commit -m "feat(05-01): add Pass 3 prompt items 19-21 (gap-block format + section blockquote + reference calibration)"` — only pass_fill_assemble.py.
  </action>
  <verify>
    <automated>cd /Users/mikhaileliseev/Desktop/Dev/meAI && python3 -c "import ast; ast.parse(open('AIM/hermes/app/orchestrator/pass_fill_assemble.py').read()); print('AST OK')" && python3 -c "content = open('AIM/hermes/app/orchestrator/pass_fill_assemble.py').read(); assert '19. GAP-BLOCK FORMAT' in content, 'missing item 19'; assert '20. SECTION BLOCKQUOTE' in content, 'missing item 20'; assert '21. REFERENCE CALIBRATION' in content, 'missing item 21'; assert '✅' in content, 'missing strength emoji'; assert '📍' in content, 'missing growth emoji'; assert 'section-insight' in content, 'missing CSS class'; assert 'ИПХиК (2).html' in content, 'missing reference pointer'; assert '+79% за 3 года' in content or '79% за 3 года' in content, 'missing few-shot example'; assert '16. NARRATIVE STYLE' in content, 'REGRESSION: Task 1 item 16 lost'; assert '17. BUSINESS LANGUAGE' in content, 'REGRESSION: Task 1 item 17 lost'; assert '18. CROSS-REFERENCES' in content, 'REGRESSION: Task 1 item 18 lost'; assert 'STRATEGY SECTION' in content, 'REGRESSION: Phase 4 item 7 lost'; assert 'RATINGS' in content, 'REGRESSION: Phase 4 item 15 lost'; print('All Task 2 assertions PASS')"</automated>
  </verify>
  <done>
    - pass_fill_assemble.py AST parses cleanly under Python 3.11
    - Items 19, 20, 21 present with exact headers
    - Gap-block format rule contains ✅ and 📍 emoji
    - Section blockquote rule references `section-insight` CSS class
    - Reference calibration rule points to `ИПХиК (2).html` and contains at least 1 embedded few-shot example (the "+79% за 3 года" revenue dynamics phrase)
    - Items 1-18 (Phase 3+4+Task 1) preserved unchanged (regression check passes)
    - Total prompt items count = 21 (15 from Phase 4 + 6 from Phase 5)
    - One atomic commit created with message referencing Plan 05-01 Task 2
    - Prompt length increase < 5KB (sub-1% of DeepSeek 128K context budget)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → HTML output | LLM-generated text passes through prompt rules into HTML report — XSS surface if LLM injects `<script>` tags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-T | Tampering | LLM could fabricate narrative not based on collected data | mitigate | Item 16 explicitly says "с цифрами из данных"; item 21 (reference calibration) anchors to real reference style; ORC-04 honest principle (Phase 2) still enforced |
| T-05-01-I | Info disclosure | Prompt rules describe how to write about collected data | accept | No new data exposed — LLM already has Pass 1 history; prompt just instructs how to format |
| T-05-01-D | DoS | Longer prompt → more LLM tokens per Pass 3 invocation | accept | Prompt expansion ~4KB (~1000 tokens) — sub-1% of DeepSeek V4 Pro 128K context; existing `_PASS_FILL_TIMEOUT=600s` sufficient |
| T-05-01-E | EoP | n/a | accept | Pure prompt text expansion, no privilege change |
| T-05-01-X | XSS | Prompt rules describe HTML formatting (`<blockquote class="section-insight">`) | mitigate | Item 20 instructs LLM to generate `<blockquote>` tag — but the LLM output is NOT injected raw into final HTML; it flows through generate_html_report `_esc()` helper in Plan 05-02; this plan is prompt-only, no HTML rendering yet |
| T-05-01-SC | Supply chain | No new packages | accept | Pure Python string changes — no pip installs, no external dependencies |
</threat_model>

<verification>
## Plan-Level Checks

1. **AST parse under Python 3.11** — `python3 -c "import ast; ast.parse(open('AIM/hermes/app/orchestrator/pass_fill_assemble.py').read())"` exits 0
2. **All 6 new items present** — grep counts 6 occurrences of pattern `^\d+\. [A-Z]` in the new items range (16-21)
3. **Items 1-15 regression** — all Phase 3+4 markers still findable: STRATEGY SECTION, OFFER SECTION, WHITEFIELDS MATRIX, EXPERTS SECTION, CONTENT ANALYSIS, REVENUE DYNAMICS, CLINIC METRICS, MEDIA URLS, RATINGS
4. **Function signature preserved** — `def _build_prompt(state: OrchestratorState) -> str:` unchanged
5. **Timeout preserved** — `_PASS_FILL_TIMEOUT = 600` unchanged
6. **Commits** — 2 atomic commits, one per task, both in git log
7. **Container compatibility** — (deferred to Plan 05-08 deploy or Phase 8) local AST parse uses Python 3.14; container uses Python 3.11. The prompt string literal changes are Python 3.11-safe (no f-string backslash escapes in expression parts). Full container import test happens at deploy time.

## Requirement Coverage

| Requirement | How This Plan Addresses It |
|-------------|---------------------------|
| INT-01 (narrative rewrite) | Item 16 NARRATIVE STYLE rule — 2-3 paragraphs per section, not metric dumps |
| INT-02 (cross-linked sections) | Item 18 CROSS-REFERENCES rule — minimum 1 cross-ref per section |
| INT-03 (business language) | Item 17 BUSINESS LANGUAGE rule — 5-entry translation dictionary |
| INT-04 (gap-blocks) | Item 19 GAP-BLOCK FORMAT rule — ✅ strength + 📍 growth point with competitor benchmark |
| INT-05 (blockquote per section) | Item 20 SECTION BLOCKQUOTE rule — `<blockquote class="section-insight">` at end of every section |

Note: This plan satisfies INT-01..05 at the PROMPT LAYER. Plan 05-02 satisfies INT-04+05 at the HTML rendering layer (the `gap_blocks` and `insight` kwargs need HTML helpers to actually render).
</verification>

<success_criteria>
- Pass 3 prompt contains 6 new numbered items (16-21) with the exact headers specified in Task 1+2 behavior blocks
- All items reference the source decision IDs (D-02, D-03, D-05, D-07, D-09, D-11) per CONTEXT.md locked decisions
- Items 1-15 (Phase 3+4) preserved unchanged
- Python 3.11 AST parse succeeds
- 2 atomic commits in git log
- INT-01, INT-02, INT-03 fully covered (prompt-only requirements)
- INT-04, INT-05 partially covered (prompt rules added; HTML rendering deferred to Plan 05-02)
</success_criteria>

<output>
Create `.planning/phases/05-deep-interpretation/05-01-SUMMARY.md` when done.
</output>
