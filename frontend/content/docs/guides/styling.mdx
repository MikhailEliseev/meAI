---
title: Styling Guide
description: Styling with Tailwind CSS and CSS-in-JS
category: guides
order: 7
---

# Styling Guide

Learn how to style your Next.js application with Tailwind CSS and modern CSS techniques.

## Tailwind CSS

### Setup

Tailwind is pre-configured in this project:

```javascript
// tailwind.config.js
module.exports = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        border: 'hsl(var(--border))',
        input: 'hsl(var(--input))',
        ring: 'hsl(var(--ring))',
        background: 'hsl(var(--background))',
        foreground: 'hsl(var(--foreground))',
        primary: {
          DEFAULT: 'hsl(var(--primary))',
          foreground: 'hsl(var(--primary-foreground))',
        },
        secondary: {
          DEFAULT: 'hsl(var(--secondary))',
          foreground: 'hsl(var(--secondary-foreground))',
        },
        destructive: {
          DEFAULT: 'hsl(var(--destructive))',
          foreground: 'hsl(var(--destructive-foreground))',
        },
        muted: {
          DEFAULT: 'hsl(var(--muted))',
          foreground: 'hsl(var(--muted-foreground))',
        },
        accent: {
          DEFAULT: 'hsl(var(--accent))',
          foreground: 'hsl(var(--accent-foreground))',
        },
      },
      borderRadius: {
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)',
      },
    },
  },
  plugins: [require('tailwindcss-animate')],
}
```

### Basic Usage

```tsx
<div className="flex items-center justify-between p-4 bg-background">
  <h1 className="text-2xl font-bold text-foreground">Title</h1>
  <Button className="bg-primary text-primary-foreground">
    Click me
  </Button>
</div>
```

### Responsive Design

```tsx
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
  {items.map((item) => (
    <Card key={item.id}>{item.name}</Card>
  ))}
</div>
```

### Dark Mode

```tsx
<div className="bg-white dark:bg-gray-900">
  <p className="text-gray-900 dark:text-gray-100">
    This text adapts to dark mode
  </p>
</div>
```

## CSS Variables

### Theme Variables

```css
/* app/globals.css */
@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 222.2 84% 4.9%;
    --primary: 222.2 47.4% 11.2%;
    --primary-foreground: 210 40% 98%;
    --secondary: 210 40% 96.1%;
    --secondary-foreground: 222.2 47.4% 11.2%;
    --muted: 210 40% 96.1%;
    --muted-foreground: 215.4 16.3% 46.9%;
    --accent: 210 40% 96.1%;
    --accent-foreground: 222.2 47.4% 11.2%;
    --destructive: 0 84.2% 60.2%;
    --destructive-foreground: 210 40% 98%;
    --border: 214.3 31.8% 91.4%;
    --input: 214.3 31.8% 91.4%;
    --ring: 222.2 84% 4.9%;
    --radius: 0.5rem;
  }

  .dark {
    --background: 222.2 84% 4.9%;
    --foreground: 210 40% 98%;
    --primary: 210 40% 98%;
    --primary-foreground: 222.2 47.4% 11.2%;
    --secondary: 217.2 32.6% 17.5%;
    --secondary-foreground: 210 40% 98%;
    --muted: 217.2 32.6% 17.5%;
    --muted-foreground: 215 20.2% 65.1%;
    --accent: 217.2 32.6% 17.5%;
    --accent-foreground: 210 40% 98%;
    --destructive: 0 62.8% 30.6%;
    --destructive-foreground: 210 40% 98%;
    --border: 217.2 32.6% 17.5%;
    --input: 217.2 32.6% 17.5%;
    --ring: 212.7 26.8% 83.9%;
  }
}
```

### Custom Variables

```css
:root {
  --header-height: 64px;
  --sidebar-width: 256px;
  --content-max-width: 1200px;
}

.header {
  height: var(--header-height);
}

.sidebar {
  width: var(--sidebar-width);
}

.content {
  max-width: var(--content-max-width);
}
```

## Component Styling

### Using cn() Utility

```tsx
import { cn } from '@/lib/utils'

function Button({ className, variant, ...props }: ButtonProps) {
  return (
    <button
      className={cn(
        'inline-flex items-center justify-center rounded-md',
        'px-4 py-2 text-sm font-medium',
        'transition-colors focus-visible:outline-none',
        {
          'bg-primary text-primary-foreground hover:bg-primary/90':
            variant === 'default',
          'bg-destructive text-destructive-foreground hover:bg-destructive/90':
            variant === 'destructive',
          'border border-input bg-background hover:bg-accent':
            variant === 'outline',
        },
        className
      )}
      {...props}
    />
  )
}
```

### Conditional Classes

```tsx
<div
  className={cn(
    'rounded-lg border p-4',
    isActive && 'border-primary bg-primary/10',
    isDisabled && 'opacity-50 cursor-not-allowed',
    size === 'sm' && 'p-2 text-sm',
    size === 'lg' && 'p-6 text-lg'
  )}
>
  Content
</div>
```

## Animations

### Tailwind Animations

```tsx
// Spin
<Loader2 className="h-4 w-4 animate-spin" />

// Pulse
<div className="h-4 w-4 animate-pulse bg-primary rounded-full" />

// Bounce
<div className="animate-bounce">↓</div>

// Fade in
<div className="animate-in fade-in duration-300">
  Content
</div>

// Slide in
<div className="animate-in slide-in-from-bottom duration-500">
  Content
</div>
```

### Custom Animations

```css
/* app/globals.css */
@keyframes shimmer {
  0% {
    background-position: -1000px 0;
  }
  100% {
    background-position: 1000px 0;
  }
}

.animate-shimmer {
  animation: shimmer 2s infinite;
  background: linear-gradient(
    to right,
    transparent 0%,
    rgba(255, 255, 255, 0.5) 50%,
    transparent 100%
  );
  background-size: 1000px 100%;
}
```

```tsx
<div className="animate-shimmer">Loading...</div>
```

### Framer Motion

```tsx
import { motion } from 'framer-motion'

<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  exit={{ opacity: 0, y: -20 }}
  transition={{ duration: 0.3 }}
>
  Content
</motion.div>
```

## Layout Patterns

### Flexbox

```tsx
// Center content
<div className="flex items-center justify-center min-h-screen">
  <Card>Centered content</Card>
</div>

// Space between
<div className="flex items-center justify-between">
  <h1>Title</h1>
  <Button>Action</Button>
</div>

// Column layout
<div className="flex flex-col gap-4">
  <Card>Item 1</Card>
  <Card>Item 2</Card>
  <Card>Item 3</Card>
</div>
```

### Grid

```tsx
// Responsive grid
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
  {items.map((item) => (
    <Card key={item.id}>{item.name}</Card>
  ))}
</div>

// Auto-fit grid
<div className="grid grid-cols-[repeat(auto-fit,minmax(250px,1fr))] gap-4">
  {items.map((item) => (
    <Card key={item.id}>{item.name}</Card>
  ))}
</div>

// Dashboard layout
<div className="grid grid-cols-12 gap-4">
  <div className="col-span-12 lg:col-span-8">
    <MainContent />
  </div>
  <div className="col-span-12 lg:col-span-4">
    <Sidebar />
  </div>
</div>
```

### Container

```tsx
<div className="container mx-auto px-4 max-w-7xl">
  <Content />
</div>
```

## Typography

### Headings

```tsx
<h1 className="text-4xl font-bold tracking-tight">Heading 1</h1>
<h2 className="text-3xl font-semibold">Heading 2</h2>
<h3 className="text-2xl font-semibold">Heading 3</h3>
<h4 className="text-xl font-medium">Heading 4</h4>
```

### Body Text

```tsx
<p className="text-base text-foreground">
  Regular paragraph text
</p>

<p className="text-sm text-muted-foreground">
  Secondary text
</p>

<p className="text-xs text-muted-foreground">
  Small text
</p>
```

### Text Utilities

```tsx
// Truncate
<p className="truncate">Very long text that will be truncated...</p>

// Line clamp
<p className="line-clamp-3">
  Long text that will be clamped to 3 lines...
</p>

// Text alignment
<p className="text-left md:text-center lg:text-right">
  Responsive alignment
</p>
```

## Spacing

### Padding & Margin

```tsx
// Padding
<div className="p-4">Padding all sides</div>
<div className="px-4 py-2">Horizontal and vertical</div>
<div className="pt-4 pr-2 pb-4 pl-2">Individual sides</div>

// Margin
<div className="m-4">Margin all sides</div>
<div className="mx-auto">Center horizontally</div>
<div className="mt-4 mb-8">Top and bottom</div>
```

### Gap

```tsx
<div className="flex gap-4">
  <Button>Button 1</Button>
  <Button>Button 2</Button>
</div>

<div className="grid grid-cols-3 gap-x-4 gap-y-8">
  {/* Grid items */}
</div>
```

## Colors

### Background

```tsx
<div className="bg-background">Default background</div>
<div className="bg-primary">Primary background</div>
<div className="bg-secondary">Secondary background</div>
<div className="bg-muted">Muted background</div>
<div className="bg-accent">Accent background</div>
```

### Text

```tsx
<p className="text-foreground">Default text</p>
<p className="text-primary">Primary text</p>
<p className="text-muted-foreground">Muted text</p>
<p className="text-destructive">Destructive text</p>
```

### Borders

```tsx
<div className="border border-border">Default border</div>
<div className="border-2 border-primary">Primary border</div>
<div className="border-t border-b">Top and bottom borders</div>
```

## Hover & Focus States

```tsx
<Button className="hover:bg-primary/90 focus:ring-2 focus:ring-ring">
  Interactive button
</Button>

<Link
  href="/about"
  className="text-primary hover:underline focus:outline-none focus:ring-2"
>
  Link with states
</Link>
```

## Transitions

```tsx
<div className="transition-all duration-300 ease-in-out hover:scale-105">
  Smooth transition
</div>

<Button className="transition-colors duration-200">
  Color transition
</Button>
```

## Responsive Design

### Breakpoints

```tsx
<div className="text-sm md:text-base lg:text-lg xl:text-xl">
  Responsive text size
</div>

<div className="hidden md:block">
  Visible on medium screens and up
</div>

<div className="block md:hidden">
  Visible only on mobile
</div>
```

### Container Queries

```tsx
<div className="@container">
  <div className="@md:grid-cols-2 @lg:grid-cols-3">
    {/* Responsive to container size */}
  </div>
</div>
```

## Best Practices

1. **Use design tokens** - Stick to theme colors and spacing
2. **Mobile-first** - Start with mobile styles, enhance for larger screens
3. **Consistent spacing** - Use spacing scale (4, 8, 16, 24, 32, etc.)
4. **Semantic colors** - Use primary, secondary, destructive, not red, blue
5. **Avoid arbitrary values** - Use Tailwind's scale when possible
6. **Component composition** - Build reusable styled components
7. **Dark mode** - Always test both light and dark themes

## Performance

### Purging Unused CSS

Tailwind automatically purges unused styles in production:

```javascript
// tailwind.config.js
module.exports = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  // Tailwind will only include classes used in these files
}
```

### JIT Mode

Just-In-Time mode is enabled by default in Tailwind v3:

```tsx
// Any arbitrary value works
<div className="top-[117px]">
  Custom value
</div>
```

## Debugging

### Tailwind DevTools

Install the Tailwind CSS IntelliSense extension for VS Code.

### Debug Screens

```tsx
// Show current breakpoint in development
{process.env.NODE_ENV === 'development' && (
  <div className="fixed bottom-4 right-4 z-50 bg-black text-white p-2 text-xs">
    <div className="block sm:hidden">xs</div>
    <div className="hidden sm:block md:hidden">sm</div>
    <div className="hidden md:block lg:hidden">md</div>
    <div className="hidden lg:block xl:hidden">lg</div>
    <div className="hidden xl:block 2xl:hidden">xl</div>
    <div className="hidden 2xl:block">2xl</div>
  </div>
)}
```

## Related

- [Component Library](/docs/components/button)
- [Dark Mode Guide](/docs/guides/dark-mode)
- [Responsive Design](/docs/guides/responsive-design)
