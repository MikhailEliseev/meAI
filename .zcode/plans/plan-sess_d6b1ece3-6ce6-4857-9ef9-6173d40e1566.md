# План: Замена самописного Markdown на marked.js

## Что меняем
Файл: `/var/lib/docker/volumes/aim_wp_content/_data/themes/aim-theme/chat-inline.php` (на сервере `aim`)

## Шаги

### 1. Подключить marked.js + DOMPurify через CDN
После строки 12 (fingerprintjs) добавить:
```html
<script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/dompurify/dist/purify.min.js"></script>
```

### 2. Настроить marked.js
После загрузки — конфигурация (в начале `<script>`):
```javascript
marked.setOptions({
    breaks: true,        // одиночный \n → <br>
    gfm: true,           // GitHub Flavored Markdown (таблицы, чеклисты, etc.)
    headerIds: false,    // без якорных ссылок на заголовки
    mangle: false,
});
```

### 3. Заменить parseMarkdown() на обёртку над marked
Старый `parseMarkdown()` (строки 828-880) — **удалить полностью**. Новый:
```javascript
function parseMarkdown(text) {
    // 1. Извлечь [REPORT_READY]...[/REPORT_READY] блоки
    const reportPattern = /\[REPORT_READY\]\s*([\s\S]*?)\s*\[\/REPORT_READY\]/g;
    let reportCards = [];
    let cleaned = text.replace(reportPattern, (match, jsonStr) => {
        try {
            const data = JSON.parse(jsonStr);
            reportCards.push(renderReportCard(data));
            return `%%REPORT_CARD_${reportCards.length - 1}%%`;
        } catch (e) { return match; }
    });

    // 2. marked.parse() → HTML
    let html = marked.parse(cleaned);

    // 3. DOMPurify — sanitize (защита от XSS)
    html = DOMPurify.sanitize(html, {
        ALLOWED_TAGS: ['p','br','strong','em','del','s','a','ul','ol','li',
                       'h1','h2','h3','h4','h5','h6','blockquote','pre','code',
                       'table','thead','tbody','tr','th','td','hr','sup','sub',
                       'div','span','img'],
        ALLOWED_ATTR: ['href','target','rel','src','alt','class','id'],
    });

    // 4. Вернуть report cards (они уже в HTML, DOMPurify их пропустит)
    html = html.replace(/%%REPORT_CARD_(\d+)%%/g, (m, idx) => reportCards[parseInt(idx)] || '');

    return html;
}
```

### 4. Добавить CSS для Markdown-элементов
Добавить стили в `<style>` секцию для:
- `h1`-`h6` (размеры, отступы, цвет)
- `pre > code` (блоки кода — фон, border-radius, padding, overflow-x)
- `code` (inline code — фон, border-radius, padding)
- `blockquote` (левый border, курсив)
- `table`, `th`, `td` (border-collapse, padding, border)
- `hr` (разделитель)
- `img` (max-width: 100%)
- `input[type="checkbox"]` (чеклисты)
- Убрать margin у `p:last-child` чтобы не было лишнего отступа

### 5. Streaming — без изменений
Потоковый рендеринг продолжит использовать `textContent` (строка 1389) — это правильно, Markdown рендерится один раз финально через `renderMessages()` → `parseMarkdown()` после окончания потока.

### 6. Deploy
```bash
ssh aim  # файл уже bind-mounted, правки применятся сразу
# Только nginx restart может понадобиться если кэширует статику
```

## Нет изменений в:
- hermes-v2 (LLM как писал Markdown, так и продолжает)
- aim-app
- Стриминг (textContent остаётся)
- renderMessages() (только вызов parseMarkdown, который переписан)
- Report cards (renderReportCard и [REPORT_READY] логика сохраняется)

## Что получаем
| Элемент | Было | Стало |
|---------|------|-------|
| Заголовки `# ## ###` | ❌ (текст) | ✅ HTML |
| Таблицы | ❌ | ✅ (GFM) |
| Code blocks ` ``` ` | ❌ | ✅ |
| Inline code `` ` `` | ❌ | ✅ |
| Blockquotes `>` | ❌ | ✅ |
| Чеклисты `- [x]` | ❌ | ✅ |
| Strikethrough `~~` | ❌ | ✅ |
| Horizontal rules `---` | ❌ | ✅ |
| XSS-защита | ❌ | ✅ (DOMPurify) |
